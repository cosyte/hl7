# scripts/phi-allow-list.txt
#
# PHI-scan allow-list, the positive declaration that a fixture's identifiers
# are synthetic. `scripts/phi-scan.ts` reads this file; any realistic-PHI-shaped
# token NOT covered here is a hit. An HL7 v2 message is byte-strict at the front
# (the MSH / FHS / BHS segment must be the first thing in the file), so it cannot
# carry an inline `# synthetic: true` header, that would break every parser
# test. This allow-list is therefore the synthetic-fixture declaration mechanism,
# the same approach `@cosyte/dicom` uses for binary `.dcm` files and `@cosyte/x12`
# for byte-strict `.edi`.
#
# Format: `<TAG> <value>` per line. `#` comments and blank lines are ignored.
#   NAME <token>          synthetic person-name word (XPN / XCN name components +
#                         site-defined Z-segment names), matched case-insensitively
#   DOB <YYYYMMDD|YYYY>   synthetic date of birth (PID-7 / NK1-16), normalized
#   ADDR <street line>    synthetic street address (XAD component 1, e.g. PID-11),
#                         matched case-insensitively
#   ID <value>            synthetic id that legitimately matches an SSN / bare-MRN
#                         (6-9 digit) shape (PID-19, CX SS-typed, PID-3/-18)
#   EMAILDOMAIN <domain>  reserved / test email domain that is allowed to appear
#
# To add a new synthetic fixture: reuse the tokens below, or append new ones here
# and commit. Adding a token is a reviewed, intentional act, never silent.

# --- Synthetic patient / person surnames (XPN comp1 / XCN comp2) ---
NAME DOE
NAME ROE
NAME POE
NAME COE
NAME LEE
NAME SMITH
NAME JONES
NAME BROWN
NAME WELBY
NAME WILSON
NAME JOHNSON
NAME MÜLLER
NAME ZÖRB
NAME PUBLIC
# Classic placeholder / test surnames used across the corpus
NAME TEST
NAME CHILD
NAME MERGETEST
NAME LINKTEST
NAME MOVETEST
NAME YEARTEST
NAME MIDNIGHTTEST
NAME ZONETEST
# CJK synthetic names (charset / unicode fixtures)
NAME 山田
NAME 太郎
NAME 李
NAME 明

# --- Non-name literals in synthetic Z-segment fields (site-defined segment
#     backstop). These are fixed spec keywords, not person names:
#     the ZDS-1 RP composite's "Type of Data"/"Subtype" components carry the
#     literals "Application"/"DICOM" per the Philips Vue PACS 12.2.8 spec §5.11. ---
NAME APPLICATION
NAME DICOM

# --- Synthetic given / middle names (XPN comp2-3 / XCN comp3-4) ---
NAME JOHN
NAME JANE
NAME MARY
NAME SAM
NAME ANNA
NAME ALAN
NAME ALEX
NAME PAT
NAME ROBERT
NAME KAREN
NAME MARCUS
NAME SUE
NAME MAYA
NAME RENÉE
NAME JÜRGEN
NAME TESTER
NAME PATIENT
# Merge / link / move story-role given names (MRG-7 / PID-5 across the ADT set)
NAME SURVIVOR
NAME SURVIVORTWO
NAME SURVIVORTHREE
NAME PRIOR
NAME PRIORTWO
NAME PRIORTHREE
NAME TARGET
NAME SOURCE
NAME PARTYONE
NAME PARTYTWO

# --- Tokens the INLINE fixtures under test/ use as names -------------------
# These arrived with the walk root widening: `test/**` is swept now, so the
# HL7 literals embedded in the unit tests are read with the same field map a
# `.hl7` fixture gets. Everything below is a story-role or scaffolding token
# standing in for a surname or given name in a hand-written test message --
# the same species as the MERGETEST / LINKTEST / MOVETEST block above, and
# every one was surfaced by the gate rather than guessed at.
# The real given/family names among them (BRIEN, PATRICK, BOB, WANG, AL) are
# invented stand-ins, listed here for the same reason JOHN and JANE are.
NAME TRIGTEST
NAME EVNTEST
NAME GATETEST
NAME PHITEST
NAME ACCTTEST
NAME PERSONTEST
NAME PROPTEST
NAME CASE
NAME FALLBACK
NAME FAILSAFE
NAME NOMRG
NAME NOID
NAME WARNING
NAME ONLY
NAME MULTI
NAME GROUPONE
NAME GROUPTWO
NAME SUBJECT
NAME ONE
NAME TWO
NAME EXTRA
NAME TESTFIRSTNAME
NAME BRIEN
NAME PATRICK
NAME BOB
NAME WANG
NAME AL
# `\M2442\ABC\M2842\`: the payload of an ISO-2022 charset escape test, read as
# a PID-5 token once the escape sequences are stripped.
NAME ABC

# Batch per-message uniqueness tags placed in the PID-5 middle-name slot
# (test/fixtures/batch/*.hl7, `Test^Patient^MSGA0001`)
NAME MSGA
NAME MSGB
NAME MSGC
NAME MSGD
NAME MSGE

# --- Synthetic dates of birth (PID-7 / NK1-16), normalized YYYYMMDD or YYYY ---
DOB 1970
DOB 19601231
DOB 19620305
DOB 19630712
DOB 19650301
DOB 19651122
DOB 19700101
DOB 19700401
DOB 19720202
DOB 19720310
DOB 19750220
DOB 19750301
DOB 19750615
DOB 19750701
DOB 19771120
DOB 19800101
DOB 19800115
DOB 19850312
DOB 19850601
DOB 19850615
DOB 19851108
DOB 19880705
DOB 19881130
DOB 19900101
DOB 19901212
DOB 19910215
DOB 20000101
DOB 20180405
DOB 20190810
DOB 20200101

# --- Synthetic street addresses (XAD component 1, e.g. PID-11) ---
ADDR 123 Main St
ADDR 456 Elm St
ADDR 789 Oak St
# Inline fixture under test/ (NK1-4 next-of-kin address).
ADDR 456 Oak St

# --- Synthetic bare-numeric identifiers (PID-3 / PID-18 / PID-19) ---
# `123456`: the CX id in the field-coercion fixture's PID-3
# (`123456^1^M11^AUTH&1.2.3&UUID^MR`), an inline fixture under test/.
ID 123456

# --- Reserved / test email domains ---
EMAILDOMAIN example.com
EMAILDOMAIN example.org
EMAILDOMAIN example.net
EMAILDOMAIN example.test
