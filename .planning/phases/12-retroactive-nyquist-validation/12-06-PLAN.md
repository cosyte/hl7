---
phase: 12-retroactive-nyquist-validation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md
autonomous: true
requirements: []
requirements_addressed: []
threat_model_severity: none
gap_closure: true
tags: [retroactive-validation, nyquist, paper-trail, phase-09, gap-closure, rename-only, v2.1-audit]
must_haves:
  truths:
    - "A developer opens .planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md and sees YAML frontmatter with status: approved, nyquist_compliant: true, wave_0_complete: true, reconstructed_from: artifacts."
    - "A developer reading the Per-Task Verification Map sees exactly 4 rows (one per plan 09-01..09-04)."
    - "A developer reading the REQ Coverage section sees '0 new REQ-IDs' explicitly stated — Phase 9 is a rename-only phase with no functional requirements."
    - "A developer sees the two primary invariants documented: (1) `grep -r '@cosyte/hl7-parser'` returns only CHANGELOG breadcrumb hits; (2) `pnpm publish --dry-run` produces the expected tarball shape under the new name."
    - "A developer running `grep -E 'TODO|TBD|XXX|FIXME' .planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md` sees zero placeholder markers."
    - "A developer running `pnpm format:check .planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md` sees Prettier exit 0."
  artifacts:
    - path: ".planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md"
      provides: "Retroactive Nyquist validation report for Phase 9 — Rename Package to @cosyte/hl7. Smallest of the 6 (no new REQ-IDs; rename-only scope). Primary invariants are grep sweep + publish dry-run."
      contains: "YAML frontmatter; Test Infrastructure; Sampling Rate; Per-Task Verification Map (4 plan rows); REQ Coverage (explicitly 0 new REQs); Rename Invariants section (grep sweep + publish dry-run + full pipeline + CHANGELOG breadcrumb); Wave 0 Requirements; Manual-Only Verifications; Validation Sign-Off; Validation Audit with verdict"
  key_links:
    - from: ".planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md (Rename Invariants)"
      to: "grep sweep (`grep -rn '@cosyte/hl7-parser'` returns CHANGELOG breadcrumb only) + `pnpm publish --dry-run` (tarball shape under new name) + `pnpm install && pnpm build && pnpm test && pnpm examples` (pipeline under new name)"
      via: "each invariant has a runnable command captured in 09-VERIFICATION.md"
      pattern: "3 primary invariants all automated"
    - from: ".planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md (Per-Task Verification Map)"
      to: ".planning/phases/09-rename-package-to-cosyte-hl7/09-0N-SUMMARY.md (4 files)"
      via: "one row per plan"
      pattern: "4 plan rows: 09-01, 09-02, 09-03, 09-04"
---

<objective>
Produce `.planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md` — the missing Phase 9 Nyquist validation report. Phase 9 is the smallest/simplest of the 6 Phase 12 plans: **rename-only with 0 new REQ-IDs**. Validation surface reduces to three invariants: (1) grep sweep returns only the intentional CHANGELOG breadcrumb for `@cosyte/hl7-parser`; (2) `pnpm publish --dry-run` produces a tarball with the correct shape under `@cosyte/hl7`; (3) the full pipeline (`pnpm install && pnpm build && pnpm test && pnpm examples`) exits 0 under the new name.

Purpose: Close v2.1-MILESTONE-AUDIT tech-debt item 2 for Phase 9 (missing VALIDATION.md). State B mechanical reconstruction from 4 SUMMARYs + `09-VERIFICATION.md`. 

Output: One new file at `.planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md`, ~100-180 lines (smallest of the 6), committed in a single `docs(phase-09): add retroactive VALIDATION.md — Nyquist-compliant, rename-only (0 new REQs)` commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference VALIDATION.md templates (05 is closest in density — small/lean)
@.planning/phases/05-serialization-and-round-trip/05-VALIDATION.md
@.planning/phases/04-named-helpers/04-VALIDATION.md
@.planning/phases/06-profile-system-and-built-ins/06-VALIDATION.md

# Phase 9 evidence (4 SUMMARYs + VERIFICATION)
@.planning/phases/09-rename-package-to-cosyte-hl7/09-01-SUMMARY.md
@.planning/phases/09-rename-package-to-cosyte-hl7/09-02-SUMMARY.md
@.planning/phases/09-rename-package-to-cosyte-hl7/09-03-SUMMARY.md
@.planning/phases/09-rename-package-to-cosyte-hl7/09-04-SUMMARY.md
@.planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md

# REQ-ID traceability (confirm 0 new REQs)
@.planning/REQUIREMENTS.md

# Project conventions
@CLAUDE.md
</context>

<phase_scope>
Phase 9 REQ-IDs: **0 new REQ-IDs** (rename-only phase).
Phase 9 plans: 4 (09-01 identity-files rename, 09-02 source+tests sweep, 09-03 examples+starter-kit sweep, 09-04 verification + publish dry-run).
Phase 9 Success Criteria (from ROADMAP.md lines 232-236): 5 SCs — all verified by 09-VERIFICATION.md.

Three primary invariants:
1. **Grep sweep invariant**: `grep -rn '@cosyte/hl7-parser' --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=dist` returns ONLY CHANGELOG rename-history breadcrumb hits (SC-1).
2. **Publish dry-run invariant**: `pnpm publish --dry-run` produces tarball under `@cosyte/hl7` with expected shape (10 files, 346.2kB per 08-05-SUMMARY for the pre-rename baseline; 09-04 verified the post-rename equivalent).
3. **Full pipeline invariant**: `pnpm install && pnpm build && pnpm test && pnpm examples` exits 0 with zero warnings under the new name (SC-2).

Secondary: CHANGELOG.md has rename breadcrumb + migration note (SC-4). `.github/workflows/publish.yml` publishes under `@cosyte/hl7` (SC-5).
</phase_scope>

<tasks>

<task type="auto">
  <name>Task 1: Read Phase 9 source artifacts + extract per-plan evidence + confirm 0 new REQ-IDs</name>
  <read_first>
    - .planning/phases/09-rename-package-to-cosyte-hl7/09-01-SUMMARY.md (Plan 01: package.json rename + CHANGELOG rewrite + README/CONTRIBUTING/CLAUDE/tsup/vitest sweep — Wave 1)
    - .planning/phases/09-rename-package-to-cosyte-hl7/09-02-SUMMARY.md (Plan 02: 51 src/**/*.ts JSDoc sweep + test/model-public-exports.test.ts + typecheck — Wave 2)
    - .planning/phases/09-rename-package-to-cosyte-hl7/09-03-SUMMARY.md (Plan 03: examples/ top-level + examples/profile-starter-kit/ — Wave 3)
    - .planning/phases/09-rename-package-to-cosyte-hl7/09-04-SUMMARY.md (Plan 04: grep sweep + full pipeline + publish dry-run — Wave 4 capstone)
    - .planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md (ratified 5/5 SC PASS with grep sweep + publish dry-run evidence)
    - .planning/phases/05-serialization-and-round-trip/05-VALIDATION.md (small/lean template reference)
    - .planning/REQUIREMENTS.md (confirm NO new REQ-IDs added in Phase 9)
  </read_first>
  <files>(none — prepares draft for Task 2)</files>
  <action>
    Extract per-plan rows (4 total). Columns: `Task ID | Plan | Wave | Sweep Target | Automated Check | Artifact / Evidence | File Exists | Status`.

    - `09-01` / `01 identity-files` / Wave 1 / package.json (name/description/keywords/URLs), CHANGELOG.md (rewrite + breadcrumb), README.md, CONTRIBUTING.md, CLAUDE.md, tsup.config.ts, vitest.config.ts / `grep -rn "@cosyte/hl7-parser" package.json README.md CONTRIBUTING.md CLAUDE.md tsup.config.ts vitest.config.ts` returns 0 / all top-level identity files renamed / ✅ / ✅ green.
    - `09-02` / `02 source-and-tests` / Wave 2 / 51 src/**/*.ts JSDoc docblocks + test/model-public-exports.test.ts / `pnpm typecheck` exits 0 + `grep -rn "@cosyte/hl7-parser" src/ test/` returns 0 / source tree clean under new name / ✅ / ✅ green.
    - `09-03` / `03 examples-and-starter-kit` / Wave 3 / examples/ top-level (3 runnables + README) + examples/profile-starter-kit/ (6 files + lockfile) / `pnpm examples` exits 0 + in-kit pipeline green + `grep -rn "@cosyte/hl7-parser" examples/` returns 0 / examples + kit sweep clean / ✅ / ✅ green.
    - `09-04` / `04 verification-and-publish-dry-run` / Wave 4 (capstone) / repo-wide grep sweep + full pipeline + publish dry-run / authoritative `grep -rn "@cosyte/hl7-parser" --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=dist` returns ONLY CHANGELOG breadcrumb hits + `pnpm install && pnpm build && pnpm test && pnpm examples` all exit 0 + `pnpm publish --dry-run` tarball shape correct under `@cosyte/hl7` / all 5 SCs ratified in 09-VERIFICATION.md / ✅ / ✅ green.

    Extract the 5 Phase 9 Success Criteria + their invariants (these replace the per-REQ table since Phase 9 has 0 REQ-IDs):
    - SC-1: Zero `@cosyte/hl7-parser` occurrences outside CHANGELOG rename-history — invariant: grep sweep returns only CHANGELOG hits.
    - SC-2: `pnpm install && pnpm build && pnpm test && pnpm examples` all exit 0 with zero warnings under new name — invariant: full pipeline.
    - SC-3: Fresh-project install of `@cosyte/hl7` + `import { parseHL7, buildMessage }` runs a canonical example end-to-end — invariant: `pnpm publish --dry-run` tarball shape (proxy; full fresh-install test requires npm registry publish which is post-phase).
    - SC-4: README/CHANGELOG/examples/starter-kit use `@cosyte/hl7` consistently; CHANGELOG calls out rename with migration note — invariant: grep + CHANGELOG section presence.
    - SC-5: `.github/workflows/publish.yml` publishes under new name — invariant: grep workflow file for `@cosyte/hl7` + absence of legacy `@cosyte/hl7-parser` publish paths.

    Confirm 0 new REQ-IDs: Search REQUIREMENTS.md for any REQ-ID explicitly tagged as Phase 9. Expected result: NONE. ROADMAP.md Phase 9 entry explicitly states "(none — rename-only phase; no new functional REQ-IDs)". Record this in the journal.

    Test Infrastructure facts: Framework (same Phase 1 Vitest + pipeline; no new test framework), Config file (vitest.config.ts renamed in 09-01), Quick run (n/a — rename sweep has no unit-test surface), Full suite (`pnpm test`), Coverage (`pnpm test:coverage` — unchanged from Phase 7 gate), Estimated runtime (~14s full + ~20s for publish dry-run).

    Verify at HEAD: run `grep -rn "@cosyte/hl7-parser" --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=dist --exclude-dir=.planning -l` — expected output: only CHANGELOG.md (the intentional rename-history breadcrumb). If any other file shows up, STOP and escalate — that is a regression, not a Phase 12 gap.
  </action>
  <verify>
    <automated>for f in .planning/phases/09-rename-package-to-cosyte-hl7/09-0{1,2,3,4}-SUMMARY.md .planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done && test "$(grep -rln '@cosyte/hl7-parser' --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=dist --exclude-dir=.planning --exclude-dir=coverage 2>/dev/null | grep -v '^CHANGELOG.md$' | wc -l)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - 4 per-plan rows drafted with all 8 columns populated.
    - 5 SC rows drafted with invariant commands.
    - 0-new-REQ-ID confirmation recorded.
    - Test Infrastructure block captured.
    - All 4 SUMMARYs + 09-VERIFICATION.md present.
    - Repo-wide grep sweep returns only CHANGELOG.md (the intentional breadcrumb) — any other hit is a regression and must STOP this plan.
  </acceptance_criteria>
  <done>
    Draft ready: 4 per-plan rows, 5 SC rows, 0-REQ confirmation, infra block, invariant evidence.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write .planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md</name>
  <read_first>
    - Task 1 journal drafts
    - .planning/phases/05-serialization-and-round-trip/05-VALIDATION.md (small/lean template — closest in size to what this file should be)
    - .planning/phases/04-named-helpers/04-VALIDATION.md (section order reference)
  </read_first>
  <files>.planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md</files>
  <action>
    Write top-to-bottom. Section order (smaller than other Phase 12 plans because there are 0 REQ-IDs to cross-reference):

    Frontmatter:
    ```yaml
    ---
    phase: 09
    slug: rename-package-to-cosyte-hl7
    status: approved
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-04-21
    reconstructed_from: artifacts (State B — Phase 12 retroactive Nyquist validation; no prior VALIDATION.md existed)
    rename_only: true
    new_requirements: 0
    ---
    ```

    Header:
    ```
    # Phase 9 — Validation Strategy

    > Retroactive Nyquist validation audit. **Phase 9 is a rename-only phase with 0 new REQ-IDs** — the package was renamed from `@cosyte/hl7-parser` to `@cosyte/hl7` across all source/configs/docs/examples/starter-kit/CI. Validation reduces to three primary invariants: grep sweep (no residual old-name hits except the intentional CHANGELOG breadcrumb), full pipeline exits 0 under new name, and `pnpm publish --dry-run` produces a correctly-shaped tarball. All 5 ROADMAP Success Criteria ratified by 09-VERIFICATION.md 2026-04-20.
    ```

    1. `## Test Infrastructure` — table: Framework (Vitest 1.2.x — unchanged from Phase 1), Config (vitest.config.ts — renamed only), Quick run (n/a — rename phase has no unit-test surface; regression guarded by full suite), Full suite (`pnpm test`), Coverage (`pnpm test:coverage` — Phase 7 gate unchanged), Publish check (`pnpm publish --dry-run`), Estimated runtime (~14s full suite; ~20s publish dry-run).

    2. `## Sampling Rate`:
       - After every task commit: `pnpm typecheck` (confirms no rename-adjacent regressions)
       - After every plan wave: full pipeline (`pnpm install && pnpm build && pnpm test && pnpm examples`)
       - Before `/gsd-verify-work 9`: full pipeline + `pnpm publish --dry-run` + authoritative grep sweep
       - Max feedback latency: ~14s full suite; ~30s for full rename-invariant check

    3. `## Per-Task Verification Map` — 4-row table from Task 1 (columns: `Task ID | Plan | Wave | Sweep Target | Automated Check | Artifact / Evidence | File Exists | Status`). Legend below.

    4. `## Requirement Coverage` (specialized for Phase 9) — paragraph: "Phase 9 adds **zero new functional REQ-IDs**. ROADMAP.md Phase 9 entry explicitly states `Requirements: (none — rename-only phase; no new functional REQ-IDs)`. There is no REQ → Test Cross-Reference table in this report because there are no REQs to cross-reference. Validation instead covers the 5 ROADMAP Success Criteria below."

    5. `## Rename Invariants` (Phase-9-specific section, replaces the per-REQ-ID table) — 5-row table from Task 1. Columns: `SC # | Success Criterion (summary) | Primary Invariant | Command | Status`.

       | SC # | Success Criterion | Primary Invariant | Command | Status |
       |------|-------------------|-------------------|---------|--------|
       | SC-1 | Zero `@cosyte/hl7-parser` occurrences outside CHANGELOG rename-history | Repo-wide grep sweep returns only CHANGELOG hits | `grep -rn '@cosyte/hl7-parser' --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=dist --exclude-dir=.planning --exclude-dir=coverage \| grep -v '^CHANGELOG.md:'` returns 0 hits | ✅ verified |
       | SC-2 | Full pipeline exits 0 under new name | Pipeline exit codes | `pnpm install --frozen-lockfile && pnpm build && pnpm test && pnpm examples` all exit 0 | ✅ verified |
       | SC-3 | Fresh install of `@cosyte/hl7` + import runs canonical example | Publish dry-run tarball shape proxy | `pnpm publish --dry-run` produces tarball with `name: @cosyte/hl7` and expected file count | ✅ verified |
       | SC-4 | README/CHANGELOG/examples consistent + CHANGELOG has migration note | Grep for new name in key files + CHANGELOG section presence | `grep -q '@cosyte/hl7' README.md CHANGELOG.md examples/README.md` + `grep -q 'Renamed' CHANGELOG.md` | ✅ verified |
       | SC-5 | publish.yml publishes under new name | Workflow grep | `grep -q '@cosyte/hl7' .github/workflows/publish.yml && ! grep -q '@cosyte/hl7-parser' .github/workflows/publish.yml` | ✅ verified |

       Below the table: "All 5 invariants have corresponding evidence in `09-VERIFICATION.md` (commit `da2a686` per STATE.md)."

    6. `## Wave 0 Requirements` — "*None.* Phase 9 reuses all Phase 1-8 infrastructure (Vitest, tsup, ESLint, Prettier, pnpm, CI workflows). The rename is a mechanical mass-edit, not a framework-level change. `wave_0_complete: true` because no new infrastructure was needed."

    7. `## Manual-Only Verifications` — "*None with gating authority.* All 5 SC invariants have automated commands. A human spot-check after publish (verifying `npm view @cosyte/hl7` shows the expected version + shape) is a post-phase activity and not a Phase 9 gating step — `pnpm publish --dry-run` proxies the tarball shape without touching the npm registry."

    8. `## Validation Sign-Off` — all `[x]`:
       - [x] All tasks have automated verify (typecheck, pipeline, grep sweep, publish dry-run)
       - [x] Sampling continuity: 4 plans × at least 1 automated check each
       - [x] Wave 0 covers all MISSING references (none — rename reuses Phase 1-8 infrastructure)
       - [x] No watch-mode flags
       - [x] Feedback latency < 30s (full rename-invariant check)
       - [x] `nyquist_compliant: true` set; `rename_only: true` and `new_requirements: 0` flags set
       - [x] Verifier PASS 2026-04-20 per `09-VERIFICATION.md` (5/5 SCs — commit `da2a686`)

       `**Approval:** approved 2026-04-21 (Nyquist audit — State B reconstruction from 4 SUMMARYs + 09-VERIFICATION.md).`

    9. `## Validation Audit 2026-04-21`:
       | Input state | B (4 SUMMARYs + 09-VERIFICATION.md) |
       | REQ-IDs audited | 0 (rename-only phase) |
       | Success Criteria audited | 5 (SC-1..SC-5) |
       | Plans mapped | 4 (09-01..09-04) |
       | Primary invariants | 3 (grep sweep + pipeline + publish dry-run) |
       | Gaps found | 0 |
       | Resolved | 0 |
       | Escalated | 0 |
       | Rename-only | true |

       Verdict paragraph: "Phase 9 is Nyquist-compliant. Zero new REQ-IDs to audit; 5/5 ROADMAP Success Criteria have automated invariants (grep sweep + pipeline + publish dry-run) all ratified by 09-VERIFICATION.md. The repo-wide grep for `@cosyte/hl7-parser` still returns only the intentional CHANGELOG rename-history breadcrumb, confirming no rename regression. Closes v2.1-MILESTONE-AUDIT tech-debt item 2 for Phase 9 — smallest and simplest of the 6 Phase 12 deliverables."

    Target length: 100-180 lines (smallest of the 6 Phase-12 plans). No placeholder markers.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md && grep -q '^nyquist_compliant: true$' .planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md && grep -q '^rename_only: true$' .planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md && grep -q '^new_requirements: 0$' .planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md && grep -q '## Rename Invariants' .planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md && test "$(grep -cE '^\| SC-[1-5] \|' .planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md)" -eq 5 && test "$(grep -cE '^\| 09-0[1-4] \|' .planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md)" -eq 4 && ! grep -qE '\b(TODO|TBD|XXX|FIXME)\b' .planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md`.
    - Frontmatter has `phase: 09`, `slug: rename-package-to-cosyte-hl7`, `status: approved`, `nyquist_compliant: true`, `wave_0_complete: true`, `rename_only: true`, `new_requirements: 0`.
    - Contains `## Requirement Coverage` explicitly stating 0 new REQ-IDs.
    - Contains `## Rename Invariants` section with 5 SC rows.
    - Per-Task Verification Map has exactly 4 rows (09-01..09-04).
    - Zero placeholder markers.
    - All 9 sections present in order.
  </acceptance_criteria>
  <done>
    File written with all 9 sections, 4 plan rows, 5 SC-invariant rows, explicit 0-REQ callout, zero placeholders.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify format, path integrity, and commit</name>
  <read_first>
    - The file written in Task 2
    - CLAUDE.md
  </read_first>
  <files>(no new files; `git add` of Task-2 output)</files>
  <action>
    1. `pnpm format:check .planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md` — expect exit 0.
    2. Grep for placeholders — expect zero matches.
    3. Re-run the repo-wide grep sweep to confirm it still holds: `grep -rln '@cosyte/hl7-parser' --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=dist --exclude-dir=.planning --exclude-dir=coverage | grep -v '^CHANGELOG.md$'` returns 0 hits. (If this fails here despite Task 1 passing, it means the VALIDATION.md draft we just wrote accidentally introduced a spurious match — check and fix.)
    4. `git add .planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md`.
    5. Confirm single staged path.
    6. Commit via HEREDOC:
       ```
       git commit -m "$(cat <<'EOF'
       docs(phase-09): add retroactive VALIDATION.md — Nyquist-compliant, rename-only (0 new REQs)

       Closes v2.1-MILESTONE-AUDIT tech-debt item 2 for Phase 9 (missing VALIDATION.md).
       State B reconstruction from 4 plan SUMMARYs + 09-VERIFICATION.md. Phase 9 added
       zero new REQ-IDs (rename-only); validation covers 5 ROADMAP Success Criteria via
       3 primary invariants: (1) repo-wide grep sweep for @cosyte/hl7-parser returns
       only CHANGELOG breadcrumb; (2) `pnpm install && pnpm build && pnpm test && pnpm
       examples` exit 0 under new name; (3) `pnpm publish --dry-run` tarball shape under
       @cosyte/hl7. Frontmatter flags `rename_only: true` and `new_requirements: 0` set.

       Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
       EOF
       )"
       ```
    7. Confirm single commit.
  </action>
  <verify>
    <automated>pnpm format:check .planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md && ! grep -qE 'TODO|TBD|XXX|FIXME|placeholder' .planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md && test "$(grep -rln '@cosyte/hl7-parser' --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=dist --exclude-dir=.planning --exclude-dir=coverage 2>/dev/null | grep -v '^CHANGELOG.md$' | wc -l)" -eq 0 && test "$(git log --oneline -1 --format='%s' -- .planning/phases/09-rename-package-to-cosyte-hl7/09-VALIDATION.md | grep -c 'docs(phase-09): add retroactive VALIDATION.md')" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm format:check` passes.
    - Zero placeholder markers.
    - Repo-wide `@cosyte/hl7-parser` grep still returns only CHANGELOG.md (SC-1 invariant still holds after this plan writes).
    - Exactly one commit touches the new file.
    - Commit message begins with `docs(phase-09):` and contains `add retroactive VALIDATION.md`.
    - `git status` clean afterward.
  </acceptance_criteria>
  <done>
    File committed; working tree clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced. Writes a single Markdown file under `.planning/phases/09-rename-package-to-cosyte-hl7/`.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06-01 | Tampering | `09-VALIDATION.md` content | mitigate | Transcribed from 4 SUMMARYs + VERIFICATION.md (authoritative sources); git commit preserves audit trail. |
| T-12-06-02 | Information Disclosure | citations | accept | Public repo; no secrets. |
| T-12-06-03 | Integrity (accidental rename-regression introduced by this plan) | VALIDATION.md prose accidentally contains `@cosyte/hl7-parser` outside CHANGELOG scope | mitigate | Task 3 re-runs the repo-wide grep sweep AFTER writing this file; if it detects a new spurious match caused by the draft's prose, halts before commit. |
</threat_model>

<verification>
After Task 3:
1. File exists at the expected path.
2. `grep -c '^nyquist_compliant: true$'` = 1.
3. `grep -c '^rename_only: true$'` = 1.
4. `grep -c '^new_requirements: 0$'` = 1.
5. `grep -c '## Rename Invariants'` ≥ 1.
6. `grep -cE '^\| SC-[1-5] \|'` = 5.
7. `grep -cE '^\| 09-0[1-4] \|'` = 4.
8. Repo-wide `@cosyte/hl7-parser` grep still returns only CHANGELOG.md.
9. `git log` records a `docs(phase-09): add retroactive VALIDATION.md` commit.
</verification>

<success_criteria>
- All 3 tasks complete.
- File exists with all 9 sections including Rename Invariants + explicit 0-REQ callout.
- One commit with `docs(phase-09):` prefix touching only the new file.
- Zero format failures, zero placeholders.
- Rename-regression check passes (grep invariant still holds after this plan).
- Wave 1 parallel-safe.
</success_criteria>

<output>
After completion, execute-plan creates `.planning/phases/12-retroactive-nyquist-validation/12-06-SUMMARY.md` citing:
- 0 REQ-IDs audited (rename-only phase).
- 5 Success Criteria with 3 primary invariants (grep + pipeline + publish dry-run).
- 4 plan rows transcribed (09-01..09-04).
- New file path + commit hash.
- Plan-level REQ-IDs closed: 0 (paper-trail).
- Additional post-commit re-check of grep-sweep invariant confirms SC-1 still holds (no regression introduced by this plan).
</output>
