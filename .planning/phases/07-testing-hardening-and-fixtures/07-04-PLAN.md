---
phase: 07-testing-hardening-and-fixtures
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - test/fixtures/vendor-quirks/mllp-framing-stripped.hl7
  - test/fixtures/vendor-quirks/field-whitespace-trimmed.hl7
  - test/fixtures/vendor-quirks/unknown-escape-sequence.hl7
  - test/fixtures/vendor-quirks/timestamp-fallback-format.hl7
  - test/fixtures/vendor-quirks/segment-case.hl7
  - test/fixtures/vendor-quirks/extra-fields.hl7
  - test/fixtures/vendor-quirks/unknown-segment.hl7
  - test/fixtures/vendor-quirks/duplicate-required-segment.hl7
  - test/fixtures/vendor-quirks/encoding-mismatch.hl7
  - test/fixtures/vendor-quirks/missing-required-field.hl7
  - test/fixtures/vendor-quirks/out-of-order-segment.hl7
  - test/fixtures/vendor-quirks/version-mismatch.hl7
  - test/fixtures/vendor-quirks/unknown-charset.hl7
  - test/fixtures/vendor-quirks/README.md
  - test/parser-strict-mode-sweep.test.ts
autonomous: true
requirements: [TEST-05, TEST-06]
must_haves:
  truths:
    - "test/fixtures/vendor-quirks/ contains exactly 13 .hl7 fixture files — one per Tier-2 warning code in src/parser/warnings.ts::WARNING_CODES"
    - "Filename of each fixture is the kebab-case of its target warning code (D-12) — fileToCode(filename) === target code"
    - "Every fixture, when parsed in lenient mode, has its target code in msg.warnings (.toContain — co-triggers acceptable per D-14)"
    - "Every fixture, when parsed in strict mode, throws Hl7ParseError"
    - "test/parser-strict-mode-sweep.test.ts uses describe.each over readdirSync (filename auto-discovery — adding a fixture auto-joins the sweep)"
    - "Adding a new vendor-quirks fixture does NOT require editing the test file — it auto-joins the sweep (D-15 + D-16)"
  artifacts:
    - path: "test/fixtures/vendor-quirks/mllp-framing-stripped.hl7"
      provides: "Triggers MLLP_FRAMING_STRIPPED in lenient; throws in strict"
      contains: "MSH|^~"
    - path: "test/fixtures/vendor-quirks/field-whitespace-trimmed.hl7"
      provides: "Triggers FIELD_WHITESPACE_TRIMMED"
    - path: "test/fixtures/vendor-quirks/unknown-escape-sequence.hl7"
      provides: "Triggers UNKNOWN_ESCAPE_SEQUENCE"
    - path: "test/fixtures/vendor-quirks/timestamp-fallback-format.hl7"
      provides: "Triggers TIMESTAMP_FALLBACK_FORMAT (built-in fallback path, no profile)"
    - path: "test/fixtures/vendor-quirks/segment-case.hl7"
      provides: "Triggers SEGMENT_CASE (lowercase segment name) — note: code is SEGMENT_CASE per warnings.ts, NOT SEGMENT_NAME_CASE as CONTEXT.md D-13 speculated"
    - path: "test/fixtures/vendor-quirks/extra-fields.hl7"
      provides: "Triggers EXTRA_FIELDS"
    - path: "test/fixtures/vendor-quirks/unknown-segment.hl7"
      provides: "Triggers UNKNOWN_SEGMENT (Z-segment without profile)"
    - path: "test/fixtures/vendor-quirks/duplicate-required-segment.hl7"
      provides: "Triggers DUPLICATE_REQUIRED_SEGMENT (two MSH segments)"
    - path: "test/fixtures/vendor-quirks/encoding-mismatch.hl7"
      provides: "Triggers ENCODING_MISMATCH"
    - path: "test/fixtures/vendor-quirks/missing-required-field.hl7"
      provides: "Triggers MISSING_REQUIRED_FIELD"
    - path: "test/fixtures/vendor-quirks/out-of-order-segment.hl7"
      provides: "Triggers OUT_OF_ORDER_SEGMENT"
    - path: "test/fixtures/vendor-quirks/version-mismatch.hl7"
      provides: "Triggers VERSION_MISMATCH (MSH-12 mismatch)"
    - path: "test/fixtures/vendor-quirks/unknown-charset.hl7"
      provides: "Triggers UNKNOWN_CHARSET (MSH-18 invalid)"
    - path: "test/fixtures/vendor-quirks/README.md"
      provides: "Per-dir doc + filename contract + co-trigger documentation"
    - path: "test/parser-strict-mode-sweep.test.ts"
      provides: "Parameterized describe.each sweep over fs.readdirSync(vendor-quirks/) — TEST-05 + TEST-06"
  key_links:
    - from: "test/parser-strict-mode-sweep.test.ts"
      to: "test/_helpers/fixture-code.ts"
      via: "import { fileToCode }"
      pattern: '_helpers/fixture-code'
    - from: "test/parser-strict-mode-sweep.test.ts"
      to: "test/fixtures/vendor-quirks/"
      via: "readdirSync(VQ_DIR).filter(f => f.endsWith('.hl7'))"
      pattern: 'fixtures.*vendor-quirks'
    - from: "Each fixture filename"
      to: "src/parser/warnings.ts::WARNING_CODES.<CODE>"
      via: "fileToCode transform — kebab-case → UPPER_SNAKE"
      pattern: 'WARNING_CODES'
---

<objective>
Author 13 vendor-quirks fixtures (one per Tier-2 warning code in `src/parser/warnings.ts::WARNING_CODES`) plus the parameterized `test/parser-strict-mode-sweep.test.ts` that asserts each fixture (a) parses in lenient mode with its target warning code present, and (b) throws `Hl7ParseError` under `{ strict: true }`. Closes TEST-05 and TEST-06 simultaneously via the same sweep.

Purpose: TEST-05 demands a fixture per Tier-2 scenario; TEST-06 demands a strict-mode escalation sweep. Combined into one parameterized test file (per CONTEXT.md D-15 + PATTERNS.md note 6 — `SEGMENT_CASE` is the real code name, NOT `SEGMENT_NAME_CASE`). Filename ↔ code contract (D-12) means adding a fixture auto-joins the sweep; no hand-maintained list.

Output: 13 `.hl7` fixture files (each isolating one warning code), 1 per-dir README, 1 parameterized test file consuming `test/_helpers/fixture-code.ts` (Plan 01 output).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-testing-hardening-and-fixtures/07-CONTEXT.md
@.planning/phases/07-testing-hardening-and-fixtures/07-PATTERNS.md
@.planning/phases/07-testing-hardening-and-fixtures/07-01-SUMMARY.md
@CLAUDE.md
@src/parser/warnings.ts
@src/parser/errors.ts
@test/parser-warnings.test.ts
@test/parser-mllp.test.ts
@test/parser-errors.test.ts
@test/_helpers/fixture-code.ts
@test/fixtures/canonical/adt-a01.hl7
@test/fixtures/vendor-shapes/epic/adt-a01.hl7

<interfaces>
<!-- AUTHORITATIVE source of truth: src/parser/warnings.ts lines 26–40. -->

```typescript
export const WARNING_CODES = {
  MLLP_FRAMING_STRIPPED: "MLLP_FRAMING_STRIPPED",
  FIELD_WHITESPACE_TRIMMED: "FIELD_WHITESPACE_TRIMMED",
  UNKNOWN_ESCAPE_SEQUENCE: "UNKNOWN_ESCAPE_SEQUENCE",
  TIMESTAMP_FALLBACK_FORMAT: "TIMESTAMP_FALLBACK_FORMAT",
  SEGMENT_CASE: "SEGMENT_CASE",        // <-- NOT SEGMENT_NAME_CASE
  EXTRA_FIELDS: "EXTRA_FIELDS",
  UNKNOWN_SEGMENT: "UNKNOWN_SEGMENT",
  DUPLICATE_REQUIRED_SEGMENT: "DUPLICATE_REQUIRED_SEGMENT",
  ENCODING_MISMATCH: "ENCODING_MISMATCH",
  MISSING_REQUIRED_FIELD: "MISSING_REQUIRED_FIELD",
  OUT_OF_ORDER_SEGMENT: "OUT_OF_ORDER_SEGMENT",
  VERSION_MISMATCH: "VERSION_MISMATCH",
  UNKNOWN_CHARSET: "UNKNOWN_CHARSET",
} as const;
```

13 codes total. Filename for code C = `C.toLowerCase().replace(/_/g, "-") + ".hl7"`.

From src/index.ts:
```typescript
export function parseHL7(input: string | Buffer, options?: ParseOptions | Profile): Hl7Message;
export class Hl7ParseError extends Error { ... }
```

From src/parser/types.ts (ParseOptions):
```typescript
interface ParseOptions {
  strict?: boolean;            // <-- the test toggles this
  // ... other options
}
```

From test/_helpers/fixture-code.ts (Plan 01 output):
```typescript
export function fileToCode(filename: string): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author 13 vendor-quirks fixtures + per-dir README — one per Tier-2 warning code</name>
  <files>
    test/fixtures/vendor-quirks/mllp-framing-stripped.hl7,
    test/fixtures/vendor-quirks/field-whitespace-trimmed.hl7,
    test/fixtures/vendor-quirks/unknown-escape-sequence.hl7,
    test/fixtures/vendor-quirks/timestamp-fallback-format.hl7,
    test/fixtures/vendor-quirks/segment-case.hl7,
    test/fixtures/vendor-quirks/extra-fields.hl7,
    test/fixtures/vendor-quirks/unknown-segment.hl7,
    test/fixtures/vendor-quirks/duplicate-required-segment.hl7,
    test/fixtures/vendor-quirks/encoding-mismatch.hl7,
    test/fixtures/vendor-quirks/missing-required-field.hl7,
    test/fixtures/vendor-quirks/out-of-order-segment.hl7,
    test/fixtures/vendor-quirks/version-mismatch.hl7,
    test/fixtures/vendor-quirks/unknown-charset.hl7,
    test/fixtures/vendor-quirks/README.md
  </files>
  <read_first>
    - src/parser/warnings.ts (AUTHORITATIVE — exact 13 codes; the filenames in this task derive mechanically from this file)
    - test/parser-warnings.test.ts (style anchor — shows which inputs trigger which codes; reuse the trigger patterns where applicable)
    - test/parser-mllp.test.ts (MLLP framing trigger style)
    - test/parser-errors.test.ts (lenient/strict toggle pattern)
    - .planning/phases/07-testing-hardening-and-fixtures/07-CONTEXT.md (D-12 filename ↔ code; D-13 fixture list with triggers; D-14 co-triggers permitted; D-16 fixtures additive — do not delete existing tests)
    - .planning/phases/07-testing-hardening-and-fixtures/07-PATTERNS.md (§Vendor-quirks fixtures — the canonical filename↔code table + per-fixture trigger guide; PATTERNS reconciliation note 6 confirms `SEGMENT_CASE` is the actual code name, not `SEGMENT_NAME_CASE`)
    - test/fixtures/canonical/adt-a01.hl7 (base body for derived fixtures — most quirks override one field of an ADT^A01)
    - src/parser/segments.ts, src/parser/tokenize.ts, src/parser/escapes.ts, src/parser/dates.ts, src/parser/normalize.ts (read the relevant module BEFORE authoring each fixture to confirm what input actually triggers the warning — particularly for OUT_OF_ORDER_SEGMENT, VERSION_MISMATCH, MISSING_REQUIRED_FIELD which may require profile context)
  </read_first>
  <behavior>
    Each fixture MUST satisfy:
    1. `parseHL7(raw)` returns successfully (lenient mode default)
    2. `parseHL7(raw).warnings.map(w => w.code)` contains the target code (filename → code mapping per `fileToCode`)
    3. `parseHL7(raw, { strict: true })` throws `Hl7ParseError`

    Co-triggers (other warning codes also present) are permitted per D-14 — the sweep test uses `.toContain(code)` not `.toEqual([code])`. When a fixture has unavoidable co-triggers, document them in the README.

    Some warnings (`ENCODING_MISMATCH`, `MISSING_REQUIRED_FIELD`, `OUT_OF_ORDER_SEGMENT`, `VERSION_MISMATCH`) may require an explicit profile or option to trigger reliably. If so, the fixture's content must work with the DEFAULT lenient parser (no profile arg) — meaning the fixture content itself triggers the emit path. **If a code cannot be triggered without a profile, document this in the README and raise it as a blocker for the executor to resolve before completing the plan** (likely solution: add a 14th file `vendor-quirks-profile.hl7` paired with a tiny inline profile in the test, OR adjust the trigger to use a parser-default codepath).

    File byte format: `\r`-separated, no trailing newline (canonical HL7 wire format), unless the fixture's whole point is a different format (only `mllp-framing-stripped.hl7` deviates — it's wrapped in MLLP bytes).
  </behavior>
  <action>
    For each of the 13 codes, author one fixture. Use `\r` segment separators, no trailing newline, synthetic data only. Body derived from `canonical/adt-a01.hl7` unless the trigger demands otherwise.

    **`mllp-framing-stripped.hl7`** — wrap a canonical message in MLLP bytes (`\x0B` prefix + body + `\x1C\x0D` suffix):
    ```
    <VT>MSH|^~\&|SENDAPP|SENDFAC|RECVAPP|RECVFAC|20260419100000||ADT^A01^ADT_A01|MSGVQ001|P|2.5<CR>EVN|A01|20260419100000<CR>PID|1||MRN-VQ-001^^^HOSP^MR||Doe^John||19800101|M<CR>PV1|1|I|UNIT^^^HOSP<FS><CR>
    ```
    Where `<VT>` = 0x0B, `<FS>` = 0x1C, `<CR>` = 0x0D. The fixture starts with VT and ends with FS+CR. Author this with a node script using `fs.writeFileSync(path, Buffer.concat([Buffer.from([0x0B]), Buffer.from(bodyString, 'utf8'), Buffer.from([0x1C, 0x0D])]))` if the Write tool can't emit raw control bytes.

    **`field-whitespace-trimmed.hl7`** — PID field with leading/trailing whitespace:
    ```
    MSH|^~\&|SENDAPP|SENDFAC|RECVAPP|RECVFAC|20260419100000||ADT^A01^ADT_A01|MSGVQ002|P|2.5<CR>EVN|A01|20260419100000<CR>PID|1|| MRN-VQ-002 ^^^HOSP^MR||Doe^John||19800101|M<CR>PV1|1|I|UNIT^^^HOSP
    ```
    PID-3 component 0 has leading + trailing space (` MRN-VQ-002 `).

    **`unknown-escape-sequence.hl7`** — OBX-5 with `\Z99\`:
    ```
    MSH|^~\&|LAB|MAIN|EHR|REF|20260419100000||ORU^R01^ORU_R01|MSGVQ003|P|2.5<CR>PID|1||MRN-VQ-003^^^HOSP^MR||Doe^John<CR>OBR|1|ORD-VQ-003|FLR-VQ-003|CBC^Complete Blood Count^LN<CR>OBX|1|TX|FREE^Free Text^L||Some text with \Z99\ embedded||||||F
    ```

    **`timestamp-fallback-format.hl7`** — MSH-7 in ISO-8601 format (built-in fallback when no profile dateFormats supplied):
    ```
    MSH|^~\&|SENDAPP|SENDFAC|RECVAPP|RECVFAC|2026-01-15T14:30:00||ADT^A01^ADT_A01|MSGVQ004|P|2.5<CR>EVN|A01|2026-01-15T14:30:00<CR>PID|1||MRN-VQ-004^^^HOSP^MR||Doe^John||19800101|M<CR>PV1|1|I|UNIT^^^HOSP
    ```
    Per src/parser/dates.ts, the parser tries strict HL7 format first, then user formats, then built-in fallbacks (ISO-8601 included). MSH-7 in ISO format triggers the fallback path → `TIMESTAMP_FALLBACK_FORMAT` warning. **Verify by reading src/parser/dates.ts emit site — if the warning only fires when a parsed Date is consumed (not on raw MSH-7 read), this fixture may need an OBR or OBX timestamp instead. Confirm before authoring.**

    **`segment-case.hl7`** — lowercase segment name:
    ```
    MSH|^~\&|SENDAPP|SENDFAC|RECVAPP|RECVFAC|20260419100000||ADT^A01^ADT_A01|MSGVQ005|P|2.5<CR>EVN|A01|20260419100000<CR>pid|1||MRN-VQ-005^^^HOSP^MR||Doe^John||19800101|M<CR>PV1|1|I|UNIT^^^HOSP
    ```
    `pid` (lowercase) instead of `PID`.

    **`extra-fields.hl7`** — segment with more fields than the spec declares for that segment:
    ```
    MSH|^~\&|SENDAPP|SENDFAC|RECVAPP|RECVFAC|20260419100000||ADT^A01^ADT_A01|MSGVQ006|P|2.5<CR>EVN|A01|20260419100000|||extra1|extra2|extra3|extra4|extra5<CR>PID|1||MRN-VQ-006^^^HOSP^MR||Doe^John||19800101|M<CR>PV1|1|I|UNIT^^^HOSP
    ```
    EVN normally has fields 1–6; this fixture pushes to EVN-11 (5 extra fields beyond the spec). **Verify against the parser's EXTRA_FIELDS emit site — it may require a profile defining `extraFieldsLimit` for the segment. If the parser does not detect EXTRA_FIELDS without a profile, document in README and surface as a blocker.**

    **`unknown-segment.hl7`** — ADT^A01 + Z-segment, no profile:
    ```
    MSH|^~\&|SENDAPP|SENDFAC|RECVAPP|RECVFAC|20260419100000||ADT^A01^ADT_A01|MSGVQ007|P|2.5<CR>EVN|A01|20260419100000<CR>PID|1||MRN-VQ-007^^^HOSP^MR||Doe^John||19800101|M<CR>PV1|1|I|UNIT^^^HOSP<CR>ZZZ|1|customField
    ```

    **`duplicate-required-segment.hl7`** — TWO MSH segments:
    ```
    MSH|^~\&|SENDAPP|SENDFAC|RECVAPP|RECVFAC|20260419100000||ADT^A01^ADT_A01|MSGVQ008|P|2.5<CR>MSH|^~\&|SENDAPP|SENDFAC|RECVAPP|RECVFAC|20260419100001||ADT^A01^ADT_A01|MSGVQ008B|P|2.5<CR>EVN|A01|20260419100000<CR>PID|1||MRN-VQ-008^^^HOSP^MR||Doe^John||19800101|M<CR>PV1|1|I|UNIT^^^HOSP
    ```

    **`encoding-mismatch.hl7`** — sender declares `^~\&` but uses different separators downstream. **Read src/parser/delimiters.ts emit site for ENCODING_MISMATCH first** to find the exact trigger condition. Likely shape: MSH declares one set, but a later segment uses a character that's reserved-but-not-declared:
    ```
    MSH|^~\&|SENDAPP|SENDFAC|RECVAPP|RECVFAC|20260419100000||ADT^A01^ADT_A01|MSGVQ009|P|2.5<CR>EVN|A01|20260419100000<CR>PID|1||MRN-VQ-009!~!HOSP!~!MR||Doe@John||19800101|M<CR>PV1|1|I|UNIT^^^HOSP
    ```
    Where PID uses `!` and `@` as embedded separators that don't match MSH-2's declaration. **If reading delimiters.ts shows the actual trigger requires a different shape, override this draft with the actual triggering shape; document the chosen approach in README.**

    **`missing-required-field.hl7`** — segment with required field empty. The default parser may not flag MISSING_REQUIRED_FIELD without a profile (segments don't all have intrinsic required-field metadata). **Read src/parser/segments.ts and src/parser/tokenize.ts to find emit site BEFORE authoring.** Most likely trigger: empty MSH-9 (message type), since that has tokenizer-level requirements:
    ```
    MSH|^~\&|SENDAPP|SENDFAC|RECVAPP|RECVFAC|20260419100000|||MSGVQ010|P|2.5<CR>EVN|A01|20260419100000<CR>PID|1||MRN-VQ-010^^^HOSP^MR||Doe^John||19800101|M<CR>PV1|1|I|UNIT^^^HOSP
    ```
    MSH-9 is empty (between two `||`). **If no warning fires without a profile, the planner-recommended path is: in the test sweep file, parse this specific fixture with an inline profile that declares required fields, OR move this fixture to a follow-up plan and skip the entry from the sweep with documentation.** Surface as a blocker.

    **`out-of-order-segment.hl7`** — segment in wrong position. Same caveat as MISSING_REQUIRED_FIELD: usually requires a profile defining segment ordering. Read src/parser/segments.ts emit site; if profile-only, follow the same blocker handling pattern.
    Draft (PID before EVN; assuming a profile defines EVN-then-PID ordering):
    ```
    MSH|^~\&|SENDAPP|SENDFAC|RECVAPP|RECVFAC|20260419100000||ADT^A01^ADT_A01|MSGVQ011|P|2.5<CR>PID|1||MRN-VQ-011^^^HOSP^MR||Doe^John||19800101|M<CR>EVN|A01|20260419100000<CR>PV1|1|I|UNIT^^^HOSP
    ```

    **`version-mismatch.hl7`** — MSH-12 = `2.9` (parser may expect 2.5). Same caveat — read the emit site; default parser may not have an "expected version" without a profile or option. Draft:
    ```
    MSH|^~\&|SENDAPP|SENDFAC|RECVAPP|RECVFAC|20260419100000||ADT^A01^ADT_A01|MSGVQ012|P|2.9<CR>EVN|A01|20260419100000<CR>PID|1||MRN-VQ-012^^^HOSP^MR||Doe^John||19800101|M<CR>PV1|1|I|UNIT^^^HOSP
    ```

    **`unknown-charset.hl7`** — MSH-18 = invalid charset name. Per src/parser/normalize.ts, this is exercised when input is a Buffer (not string). The fixture FILE itself is plain text; the test will need to read it as a Buffer to trigger the charset path:
    ```
    MSH|^~\&|SENDAPP|SENDFAC|RECVAPP|RECVFAC|20260419100000||ADT^A01^ADT_A01|MSGVQ013|P|2.5||||||ISO IR 999<CR>EVN|A01|20260419100000<CR>PID|1||MRN-VQ-013^^^HOSP^MR||Doe^John||19800101|M<CR>PV1|1|I|UNIT^^^HOSP
    ```
    MSH-18 (the 18th `|`-separated field) = `ISO IR 999`. **Note: the sweep test in Task 2 will need to parse this fixture as a Buffer (not a string) to exercise the charset codepath. Document this in the README and ensure the sweep handles it (likely by detecting filename `unknown-charset.hl7` and reading Buffer instead of string — see Task 2).**

    **README.md** (per CONTEXT.md D-11 + PATTERNS.md template, with the co-trigger documentation table):
    ```markdown
    # vendor-quirks fixtures

    One fixture per Tier-2 warning code in `src/parser/warnings.ts::WARNING_CODES`
    (13 total). Each fixture parses in lenient mode with the named code present
    in `msg.warnings`, and throws `Hl7ParseError` under `{ strict: true }`.

    ## Filename contract (D-12)

    Each filename is the kebab-case of the UPPER_SNAKE warning code. Example:
    `mllp-framing-stripped.hl7` → `MLLP_FRAMING_STRIPPED`.

    `test/parser-strict-mode-sweep.test.ts` derives the expected code via
    `test/_helpers/fixture-code.ts::fileToCode`.

    ## Source of truth

    `src/parser/warnings.ts::WARNING_CODES` (lines 26–40) is authoritative for
    the code list. Adding a fixture for a new code requires (a) adding the code
    to `WARNING_CODES` first (a project-level decision — Phase 7 carries
    forward the no-new-codes constraint from Phase 5 D-27/D-28 and Phase 6
    D-31), then (b) authoring the fixture here.

    ## Fixtures

    | Filename                            | Code                          | Single-warning isolation? | Notes                                                              |
    |-------------------------------------|-------------------------------|---------------------------|--------------------------------------------------------------------|
    | mllp-framing-stripped.hl7           | MLLP_FRAMING_STRIPPED         | yes                       | Wrapped in 0x0B + body + 0x1C 0x0D                                 |
    | field-whitespace-trimmed.hl7        | FIELD_WHITESPACE_TRIMMED      | likely                    | PID-3 has leading/trailing spaces                                  |
    | unknown-escape-sequence.hl7         | UNKNOWN_ESCAPE_SEQUENCE       | yes                       | OBX-5 contains `\Z99\`                                             |
    | timestamp-fallback-format.hl7       | TIMESTAMP_FALLBACK_FORMAT     | yes                       | MSH-7 + EVN-2 in ISO-8601 format (built-in fallback)               |
    | segment-case.hl7                    | SEGMENT_CASE                  | yes                       | `pid` lowercase instead of `PID`                                   |
    | extra-fields.hl7                    | EXTRA_FIELDS                  | depends                   | EVN with 5 extra fields; **may require profile — see README note** |
    | unknown-segment.hl7                 | UNKNOWN_SEGMENT               | yes                       | `ZZZ` segment, no profile applied                                  |
    | duplicate-required-segment.hl7      | DUPLICATE_REQUIRED_SEGMENT    | likely                    | Two MSH segments                                                   |
    | encoding-mismatch.hl7               | ENCODING_MISMATCH             | depends                   | **trigger details from src/parser/delimiters.ts**                  |
    | missing-required-field.hl7          | MISSING_REQUIRED_FIELD        | depends                   | Empty MSH-9; **may require profile**                               |
    | out-of-order-segment.hl7            | OUT_OF_ORDER_SEGMENT          | depends                   | PID before EVN; **may require profile**                            |
    | version-mismatch.hl7                | VERSION_MISMATCH              | depends                   | MSH-12 = 2.9; **may require profile or `expectedVersion` option**  |
    | unknown-charset.hl7                 | UNKNOWN_CHARSET               | yes                       | MSH-18 = `ISO IR 999`; **read as Buffer in sweep**                 |

    ## Co-trigger policy (D-14)

    Some fixtures may emit additional warnings beyond their target. The sweep
    test uses `.toContain(code)` (not `.toEqual([code])`) so co-triggers do
    not fail the test. Document any deliberate co-triggers above.

    ## Adding a new fixture

    1. Confirm the target code exists in `src/parser/warnings.ts::WARNING_CODES`.
    2. Author the fixture using `\r` separators, synthetic data, and the
       minimum bytes needed to trigger the target code.
    3. Name the file as the kebab-case of the code.
    4. The sweep at `test/parser-strict-mode-sweep.test.ts` picks it up
       automatically (via `readdirSync`).
    5. Update the table above to document any co-triggers.

    ## Profile-required quirks (executor escalation)

    Several Tier-2 codes (EXTRA_FIELDS, MISSING_REQUIRED_FIELD,
    OUT_OF_ORDER_SEGMENT, VERSION_MISMATCH, ENCODING_MISMATCH) emit only when
    profile rules or parser options anchor an expectation. If the lenient
    default parser does not surface the warning for one of these fixtures,
    the executor MUST:

    1. Confirm by reading `src/parser/warnings.ts` factory call sites.
    2. EITHER adjust the fixture body to a shape the default parser flags
       OR pair the fixture with a tiny inline profile in
       `parser-strict-mode-sweep.test.ts` (parameterized by filename so the
       sweep auto-pairs).

    Document the chosen path in this README.
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='test/fixtures/vendor-quirks'; const expected=['mllp-framing-stripped.hl7','field-whitespace-trimmed.hl7','unknown-escape-sequence.hl7','timestamp-fallback-format.hl7','segment-case.hl7','extra-fields.hl7','unknown-segment.hl7','duplicate-required-segment.hl7','encoding-mismatch.hl7','missing-required-field.hl7','out-of-order-segment.hl7','version-mismatch.hl7','unknown-charset.hl7']; for (const f of expected) { const buf=fs.readFileSync(p+'/'+f); if (buf.length===0) { console.error('empty:', f); process.exit(1); } } console.log('OK', expected.length, 'fixtures'); fs.statSync(p+'/README.md');"</automated>
  </verify>
  <acceptance_criteria>
    - All 13 fixture files exist in `test/fixtures/vendor-quirks/`
    - File count matches code count: `ls test/fixtures/vendor-quirks/*.hl7 | wc -l` returns 13
    - Each filename's `fileToCode` output matches an entry in `src/parser/warnings.ts::WARNING_CODES`. Verify with one-liner: `node -e "const codes=Object.keys(require('./src/parser/warnings.js').WARNING_CODES); const fs=require('fs'); const files=fs.readdirSync('test/fixtures/vendor-quirks').filter(f=>f.endsWith('.hl7')); for (const f of files) { const code=f.replace('.hl7','').replace(/-/g,'_').toUpperCase(); if (!codes.includes(code)) { console.error('orphan:', f); process.exit(1); } } console.log('all', files.length, 'fixtures map to a real code');"` (this requires the file to be transpiled or the script to use the .ts source; the sweep test in Task 2 covers this assertion via `pnpm test`)
    - `test/fixtures/vendor-quirks/README.md` exists and lists all 13 fixtures
    - The README documents the profile-required quirk handling per the executor escalation note
    - `pnpm test` exits 0 (no test changes yet)
  </acceptance_criteria>
  <done>13 vendor-quirks fixtures + README in place; ready for Task 2's sweep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author test/parser-strict-mode-sweep.test.ts (TEST-05 + TEST-06 sweep)</name>
  <files>test/parser-strict-mode-sweep.test.ts</files>
  <read_first>
    - test/round-trip.test.ts (style anchor — fs imports + describe.each pattern works similarly)
    - test/parser-errors.test.ts (style anchor — Hl7ParseError import + throw assertion)
    - .planning/phases/07-testing-hardening-and-fixtures/07-CONTEXT.md (D-15 sweep template; D-14 co-trigger policy)
    - .planning/phases/07-testing-hardening-and-fixtures/07-PATTERNS.md (§test/parser-strict-mode-sweep.test.ts — exact import block + describe.each shape)
    - test/_helpers/fixture-code.ts (fileToCode signature)
    - test/fixtures/vendor-quirks/ (Task 1 output — 13 fixtures)
    - src/parser/warnings.ts (WARNING_CODES enum — for Buffer-vs-string handling note re unknown-charset)
  </read_first>
  <behavior>
    The sweep MUST:
    1. Auto-discover every `.hl7` file in `test/fixtures/vendor-quirks/` via `readdirSync`
    2. For each, derive the expected warning code via `fileToCode(filename)`
    3. Assert `parseHL7(input)` returns successfully AND the result's warnings include the expected code (`.toContain` per D-14)
    4. Assert `parseHL7(input, { strict: true })` throws `Hl7ParseError`

    Special case: `unknown-charset.hl7` requires Buffer input (not string) to exercise the MSH-18 charset path per src/parser/normalize.ts. The sweep MUST detect this filename and read the file as a Buffer for that case (or always read as Buffer — Buffer input also works for all other fixtures since parseHL7 accepts both per its signature).

    Adding a new fixture to vendor-quirks/ MUST auto-join the sweep — no edits to the test file required (D-16).
  </behavior>
  <action>
    Create `test/parser-strict-mode-sweep.test.ts`:

    ```typescript
    /**
     * TEST-05 + TEST-06 sweep. For every fixture in
     * `test/fixtures/vendor-quirks/`, asserts:
     *   - lenient parse succeeds and `msg.warnings` contains the expected
     *     Tier-2 warning code (filename → code via `fileToCode`)
     *   - strict-mode parse throws `Hl7ParseError`
     *
     * The fixture filename is the kebab-case of its target code (D-12);
     * adding a new fixture file to the directory auto-joins the sweep
     * (D-15 — `readdirSync` + `describe.each`).
     *
     * Per D-14, co-triggered warnings are permitted; we use `.toContain`
     * on the codes array, not `.toEqual([code])`.
     */

    import { readdirSync, readFileSync } from "node:fs";
    import path from "node:path";
    import { fileURLToPath } from "node:url";

    import { describe, expect, it } from "vitest";

    import { parseHL7 } from "../src/index.js";
    import { Hl7ParseError } from "../src/parser/errors.js";

    import { fileToCode } from "./_helpers/fixture-code.js";

    const VQ_DIR = path.join(
      path.dirname(fileURLToPath(import.meta.url)),
      "fixtures",
      "vendor-quirks",
    );

    const fixtures = readdirSync(VQ_DIR).filter((f) => f.endsWith(".hl7"));

    /**
     * Some fixtures (e.g. `unknown-charset.hl7`) require Buffer input to
     * exercise the MSH-18 charset codepath in `src/parser/normalize.ts`.
     * For uniformity AND to cover Buffer input in coverage, we read every
     * fixture as a Buffer; `parseHL7` accepts both per its signature.
     */
    function readAsBuffer(file: string): Buffer {
      return readFileSync(path.join(VQ_DIR, file));
    }

    describe("TEST-05 + TEST-06: vendor-quirks fixture sweep", () => {
      it("the directory contains at least 13 fixtures (one per WARNING_CODES entry)", () => {
        expect(fixtures.length).toBeGreaterThanOrEqual(13);
      });

      describe.each(fixtures)("vendor-quirks/%s", (file) => {
        const buf = readAsBuffer(file);
        const expectedCode = fileToCode(file);

        it("lenient mode: parse succeeds with the expected warning code", () => {
          const msg = parseHL7(buf);
          const codes = msg.warnings.map((w) => w.code);
          expect(codes).toContain(expectedCode);
        });

        it("strict mode: throws Hl7ParseError", () => {
          expect(() => parseHL7(buf, { strict: true })).toThrow(Hl7ParseError);
        });
      });
    });
    ```

    Run `pnpm test test/parser-strict-mode-sweep.test.ts` and observe per-fixture pass/fail. **For every failing fixture (lenient assertion fails because the warning didn't fire), the executor MUST:**

    1. Read `src/parser/warnings.ts` to find the factory function for that code (e.g., `extraFields`, `versionMismatch`, etc.).
    2. Grep for the factory's call site (e.g., `grep -r 'extraFields(' src/parser/`) to find what input triggers emission.
    3. Adjust the fixture's content (Task 1 file) to match the actual trigger condition.
    4. Re-run the sweep.

    Iterate until all 13 fixtures pass. If a code genuinely cannot be triggered without a profile (after reading the call site), the executor MUST update the README's "Profile-required quirks" section AND adjust the test to parse that specific fixture with an inline profile (a tiny `defineProfile({ name: 'test', expectedVersion: '2.5' })` per code as needed). The mechanism: in the sweep's `it` block, branch on `expectedCode` and supply the relevant profile; the rest of the sweep is unchanged.

    Then run `pnpm test` (full suite), `pnpm typecheck`, `pnpm lint`.
  </action>
  <verify>
    <automated>pnpm test test/parser-strict-mode-sweep.test.ts 2>&1 | tail -10 | grep -E 'Tests.*passed' && pnpm test 2>&1 | tail -3 | grep -E 'Tests.*passed' && pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `test/parser-strict-mode-sweep.test.ts` exists
    - File uses `describe.each` over `readdirSync` (`grep -c 'describe.each' test/parser-strict-mode-sweep.test.ts` ≥ 1; `grep -c 'readdirSync' test/parser-strict-mode-sweep.test.ts` ≥ 1)
    - File imports `fileToCode` from the Plan 01 helper (`grep -c '_helpers/fixture-code' test/parser-strict-mode-sweep.test.ts` ≥ 1)
    - File imports `Hl7ParseError` from `../src/parser/errors.js` (`grep -c 'parser/errors' test/parser-strict-mode-sweep.test.ts` ≥ 1)
    - `pnpm test test/parser-strict-mode-sweep.test.ts` exits 0 with at least 27 sub-tests passing (1 count assertion + 13 × 2 lenient/strict)
    - `pnpm test` exits 0 (whole suite green)
    - `pnpm typecheck` exits 0
    - `pnpm lint --max-warnings=0` exits 0
    - If any fixture required a profile or fixture reshape during execution, README updated to reflect actual trigger
  </acceptance_criteria>
  <done>TEST-05 + TEST-06 closed; 13 vendor-quirks fixtures verified to (a) emit their target code in lenient mode and (b) throw in strict mode; sweep auto-grows with new fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture data → parseHL7 (lenient + strict) | Same boundary as Plans 02 + 03. Strict-mode tests deliberately exercise error throwing, but the error data flows back into the test runner — no external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | mllp-framing-stripped.hl7 control bytes | mitigate | The fixture contains 0x0B / 0x1C / 0x0D bytes which some text editors strip or transform. Author via `fs.writeFileSync(path, Buffer.concat(...))` and verify post-write with `od -c <file> | head -2` (must show `\v` and `\f` markers). |
| T-07-04-02 | Information Disclosure | All fixtures | accept | Synthetic data only per CONTEXT.md D-17 carry-forward (MRN-VQ-NNN pattern, Doe^John). |
| T-07-04-03 | Denial of Service | encoding-mismatch / extra-fields fixtures | accept | Pathological HL7 inputs are bounded (≤ 1 KB per fixture). Phase 2 already unit-tests the parser's resilience to malformed-but-recoverable input. |
</threat_model>

<verification>
Run `pnpm test test/parser-strict-mode-sweep.test.ts --reporter=verbose` to inspect per-fixture pass/fail.

For any failing lenient assertion, run `node -e "const {parseHL7}=require('./src/index.cjs'); const m=parseHL7(require('fs').readFileSync('test/fixtures/vendor-quirks/<file>.hl7')); console.log(m.warnings.map(w=>w.code));"` to inspect the actual warnings emitted (substitute the fixture filename) — guides fixture content adjustments.
</verification>

<success_criteria>
- TEST-05 + TEST-06 closed: 13 vendor-quirks fixtures (one per Tier-2 code) + a parameterized sweep test
- Filename ↔ code contract enforced (every filename's `fileToCode` matches a `WARNING_CODES` entry)
- `pnpm test` passes; `pnpm typecheck` passes; `pnpm lint` passes
- README documents any profile-required quirk handling decisions
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing-hardening-and-fixtures/07-04-SUMMARY.md` summarizing:
- 13 vendor-quirks fixtures authored (one per WARNING_CODES entry)
- TEST-05 + TEST-06 closed via parser-strict-mode-sweep.test.ts
- Test count delta (1 + 13 × 2 = 27 new tests minimum)
- For each Tier-2 code: how the fixture triggers the warning (one-line trigger summary)
- Any code that required adjusting the fixture body or pairing with an inline profile (PROF-required path) — note for Plan 06 / verifier
- Note correction: `SEGMENT_CASE` is the actual code (not `SEGMENT_NAME_CASE` per CONTEXT.md D-13 speculation; PATTERNS.md note 6 confirmed)
</output>
