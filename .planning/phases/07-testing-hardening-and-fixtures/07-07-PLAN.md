---
phase: 07-testing-hardening-and-fixtures
plan: 07
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .planning/phases/07-testing-hardening-and-fixtures/TEST-08-AUDIT.md
  - test/profiles-builtins.test.ts
  - test/profiles-define.test.ts
  - test/profiles-extends.test.ts
  - test/profiles-custom-segments.test.ts
  - test/profiles-default.test.ts
  - test/profiles-onwarning-chain.test.ts
autonomous: true
requirements: [TEST-07, TEST-08]
must_haves:
  truths:
    - ".planning/phases/07-testing-hardening-and-fixtures/TEST-08-AUDIT.md exists and maps every TEST-08 enumerated case to the existing Phase 6 test file(s) covering it"
    - "For each TEST-08 case marked as a gap in the audit, a targeted test was added to the relevant existing test/profiles-*.test.ts file (no new profile test files unless forced)"
    - "All 6 existing test/profiles-*.test.ts files still pass under pnpm test"
    - "TEST-07 confirmed satisfied by Phase 6 BIP-06: test/profiles-builtins.test.ts contains an explicit per-vendor `with profile vs without profile` warning-count comparison; if not, a smoke assertion is added"
    - "All 5 built-in profiles (epic, cerner, meditech, athena, genericLab) have at least one fixture under test/fixtures/vendor-shapes/<vendor>/ that demonstrates fewer warnings with the profile than without (TEST-07)"
  artifacts:
    - path: ".planning/phases/07-testing-hardening-and-fixtures/TEST-08-AUDIT.md"
      provides: "TEMPORARY planning artifact mapping each TEST-08 enumerated case to its covering test file(s); summarized into the verifier report at phase close (D-26)"
      contains: "TEST-08"
    - path: "test/profiles-builtins.test.ts (modify if gap)"
      provides: "Existing Phase 6 file; may gain smoke assertions for TEST-07 warning-count comparison if audit reveals gap"
    - path: "test/profiles-*.test.ts (modify if gap)"
      provides: "Existing Phase 6 files; gain targeted tests ONLY if audit reveals a TEST-08 gap"
  key_links:
    - from: "TEST-08-AUDIT.md"
      to: "test/profiles-define.test.ts, profiles-extends, profiles-custom-segments, profiles-default, profiles-builtins, profiles-onwarning-chain"
      via: "audit table mapping TEST-08 cases to test files"
      pattern: 'profiles-.*\\.test\\.ts'
---

<objective>
Audit the existing Phase 6 profile test suite (6 files) against the TEST-08 enumerated cases and fill any gaps with targeted additions to the existing files (per CONTEXT.md D-25 — NO new profile test files unless forced). Confirm TEST-07 is already satisfied by Phase 6 BIP-06; if `test/profiles-builtins.test.ts` lacks an explicit per-vendor warning-count comparison, add a smoke assertion. Closes TEST-07 + TEST-08.

Purpose: Phase 6 already shipped 6 profile test files covering defineProfile, extends, custom segments, default profile, built-ins, and onWarning chains (753 tests green at Phase 6 close, including profile coverage). Phase 7 must not duplicate that work — it audits the surface against TEST-08's enumeration, surfaces gaps in a temporary planning doc (`TEST-08-AUDIT.md`), and patches gaps in-place. TEST-07 was closed by Phase 6 Plan 06's per-vendor fixture-parity test; Phase 7 confirms.

Output: 1 new planning doc (TEST-08-AUDIT.md, temporary) + zero or more targeted test additions to existing test/profiles-*.test.ts files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-testing-hardening-and-fixtures/07-CONTEXT.md
@.planning/phases/07-testing-hardening-and-fixtures/07-PATTERNS.md
@.planning/phases/07-testing-hardening-and-fixtures/07-01-SUMMARY.md
@CLAUDE.md
@test/profiles-define.test.ts
@test/profiles-extends.test.ts
@test/profiles-custom-segments.test.ts
@test/profiles-default.test.ts
@test/profiles-builtins.test.ts
@test/profiles-onwarning-chain.test.ts

<interfaces>
<!-- TEST-08 enumerated cases (from REQUIREMENTS.md line 109): -->

```
TEST-08 — Profile-authoring test suite covers:
  (1) valid defineProfile output
  (2) ProfileDefinitionError cases
  (3) extends single + array
  (4) merge semantics per option category
  (5) default-profile set/get/opt-out
  (6) profile.describe()
  (7) msg.profile attribution
  (8) round-trip with custom profile
```

<!-- TEST-07 (from REQUIREMENTS.md line 108): -->

```
TEST-07 — At least one fixture per built-in profile (epic, cerner, meditech,
athena, genericLab) demonstrates fewer warnings with the profile than without.
```

<!-- BIP-06 closure note (REQUIREMENTS.md line 98): -->

```
BIP-06 closed Phase 6 Plan 06 — test/profiles-builtins.test.ts asserts
UNKNOWN_SEGMENT absent with profile for declared Z-segments across all 5
vendors + cross-profile smoke `withP.warnings.length <= without.warnings.length`.
```

So TEST-07 is closed BY DESIGN at Phase 6. Phase 7 confirms via audit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author TEST-08-AUDIT.md mapping each enumerated case to its covering test file(s)</name>
  <files>.planning/phases/07-testing-hardening-and-fixtures/TEST-08-AUDIT.md</files>
  <read_first>
    - test/profiles-define.test.ts (whole file — contains defineProfile validation + describe() + frozen-output tests)
    - test/profiles-extends.test.ts (whole file — single-parent + array-parent + merge semantics tests)
    - test/profiles-custom-segments.test.ts (whole file — Z-segment registration + msg.profile attribution + PROF-09 round-trip)
    - test/profiles-default.test.ts (whole file — set/get/null + opt-out for one call)
    - test/profiles-builtins.test.ts (whole file — TEST-07 candidate + BIP-06 fixture parity)
    - test/profiles-onwarning-chain.test.ts (whole file — onWarning composition)
    - .planning/phases/07-testing-hardening-and-fixtures/07-CONTEXT.md (D-25 audit-not-rewrite mandate; D-26 audit doc location)
    - .planning/phases/07-testing-hardening-and-fixtures/07-PATTERNS.md (§Audit doc — template provided)
    - .planning/REQUIREMENTS.md (TEST-08 line 109 — exact enumeration)
  </read_first>
  <action>
    Scan the 6 profile test files. For each TEST-08 enumerated case, identify:
    1. WHICH test file(s) cover it (by `describe`/`it` text matching)
    2. The COVERAGE QUALITY (smoke / partial / comprehensive)
    3. Any GAP (case enumerated but not actually tested)

    Use grep to scan:
    - `grep -in 'defineProfile\|describe(' test/profiles-define.test.ts` — for case 1, 6
    - `grep -in 'ProfileDefinitionError\|throws' test/profiles-define.test.ts` — for case 2
    - `grep -in 'extends' test/profiles-extends.test.ts` — for cases 3, 4
    - `grep -in 'mergeLineage\|mergeDateFormats\|mergeCustomSegments\|mergeScalar\|composeOnWarning' test/profiles-extends.test.ts` and `test/profiles-onwarning-chain.test.ts` — for case 4 detail
    - `grep -in 'setDefaultProfile\|getDefaultProfile\|profile: null' test/profiles-default.test.ts` — for case 5
    - `grep -in 'msg.profile\|.profile?' test/profiles-custom-segments.test.ts test/profiles-builtins.test.ts` — for case 7
    - `grep -in 'toString\|round-trip\|round_trip\|PROF-09' test/profiles-custom-segments.test.ts test/profiles-builtins.test.ts` — for case 8

    Author the audit doc:

    ```markdown
    # TEST-08 — Profile-authoring Test Suite Audit

    **Generated:** Phase 7 Plan 07
    **Purpose:** Map each TEST-08 enumerated case to the existing Phase 6
    test file(s) that cover it; surface any gaps for in-place patching.
    **Disposition:** Per CONTEXT.md D-26, this doc is summarized into the
    verifier report at phase close and discarded once gaps are closed.

    ## Coverage matrix

    Each row maps a TEST-08 case (REQUIREMENTS.md line 109) to the existing
    Phase 6 test file(s) and notes coverage quality.

    | TEST-08 case                                      | Covered by                                                                 | Quality       | Gap? |
    |---------------------------------------------------|----------------------------------------------------------------------------|---------------|------|
    | (1) valid defineProfile output                    | test/profiles-define.test.ts (`defineProfile returns frozen Profile`...)   | comprehensive | no   |
    | (2) ProfileDefinitionError cases                  | test/profiles-define.test.ts (`throws ProfileDefinitionError when...`)     | comprehensive | no   |
    | (3) extends single + array                        | test/profiles-extends.test.ts (single-parent + array-parent describe blks) | comprehensive | no   |
    | (4) merge semantics per option category           | test/profiles-extends.test.ts (mergeScalar/mergeDateFormats/mergeCustom..) | comprehensive | no   |
    |                                                   | test/profiles-onwarning-chain.test.ts (composeOnWarning chain)             |               |      |
    | (5) default-profile set/get/opt-out               | test/profiles-default.test.ts (set/get/null + `profile: null` opt-out)     | comprehensive | no   |
    | (6) profile.describe()                            | test/profiles-define.test.ts (`describe() returns "Profile '<name>'"`...)  | comprehensive | no   |
    | (7) msg.profile attribution                       | test/profiles-custom-segments.test.ts (msg.profile.name + .lineage)        | comprehensive | no   |
    | (8) round-trip with custom profile                | test/profiles-custom-segments.test.ts (`PROF-09 round-trip` describe)      | comprehensive | no   |

    [Adjust the table above based on the actual grep results. If a row's
    Quality is "missing" or "smoke", mark Gap = yes and add to the
    "Gaps to close" section below.]

    ## Gaps to close

    [If audit reveals any gap, list here as a checklist:]

    - [ ] Gap 1 (case N): proposed test in file X — describe/it text
    - [ ] Gap 2 (case N): proposed test in file Y

    [If no gaps found, replace the checklist with:]

    No gaps. All 8 TEST-08 enumerated cases are covered by the existing
    Phase 6 test suite. Task 2 is a no-op for TEST-08; only TEST-07
    confirmation in Task 3 remains.

    ## TEST-07 confirmation status

    TEST-07 — `At least one fixture per built-in profile demonstrates fewer
    warnings with the profile than without.`

    Source: test/profiles-builtins.test.ts (Phase 6 Plan 06 BIP-06).

    [After reading test/profiles-builtins.test.ts, document the exact
    `it(...)` text + assertion shape that confirms TEST-07. Example:
    `it("each built-in's parse-serialize round-trip byte-matches the
    no-profile version") {...}` — not the right shape; check that there's
    a `withP.warnings.length <= without.warnings.length` or stronger
    assertion. If not, mark TEST-07 as gap and Task 3 will add it.]

    ## Disposition

    - Summarized into the verifier report at phase close (D-26)
    - This raw audit doc is discarded after gaps are closed (it does NOT
      ship; it lives only in `.planning/phases/...`)
    ```

    Populate the table from grep results — do NOT just copy the template above. The audit's value comes from accurately mapping each case to actual test file content. If a case is split across multiple files, list both.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-testing-hardening-and-fixtures/TEST-08-AUDIT.md && grep -c '| (' .planning/phases/07-testing-hardening-and-fixtures/TEST-08-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07-testing-hardening-and-fixtures/TEST-08-AUDIT.md` exists
    - File contains a coverage matrix with at least 8 rows (one per TEST-08 case)
    - Each row references at least one specific test file path under `test/profiles-*.test.ts`
    - File contains a "Gaps to close" section (either with a populated checklist or an explicit "No gaps" note)
    - File contains a "TEST-07 confirmation status" section with the actual test it() text from profiles-builtins.test.ts
    - File mentions D-26 disposition (audit summarized into verifier report)
  </acceptance_criteria>
  <done>Audit doc captures the current state of TEST-08 + TEST-07 coverage; Tasks 2 + 3 act on the gap list (or note no-op if empty).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Patch TEST-08 gaps in-place (if audit found any)</name>
  <files>varies — only test/profiles-*.test.ts files identified in TEST-08-AUDIT.md "Gaps to close"</files>
  <read_first>
    - .planning/phases/07-testing-hardening-and-fixtures/TEST-08-AUDIT.md (Task 1 output — drives this task's scope)
    - For each gap: the test file named in the gap row (existing style anchor)
    - For each gap: the source file(s) under src/profiles/ that the gap test would exercise
  </read_first>
  <action>
    **If TEST-08-AUDIT.md "Gaps to close" is empty:** This task is a no-op. Skip to Task 3. Note in the commit message that no TEST-08 gaps were found.

    **If TEST-08-AUDIT.md lists one or more gaps:**

    For each gap:
    1. Open the existing test file named in the gap row.
    2. Add a NEW `it(...)` block (or a NEW `describe(...)` if the existing structure has no natural home) that asserts the missing case.
    3. The new block MUST follow the existing file's style (imports, fixture-load helpers, vitest patterns).
    4. Per CONTEXT.md D-25, do NOT create new test files unless ABSOLUTELY forced (e.g., the gap belongs to a case orthogonal to all 6 existing files — extremely unlikely for TEST-08's 8 enumerated cases, all of which map cleanly to one of: define / extends / custom-segments / default / builtins / onwarning-chain).
    5. Run `pnpm test <changed-file>` to verify the new block passes.

    After all gaps closed, run full `pnpm test`, `pnpm typecheck`, `pnpm lint --max-warnings=0`.

    Update TEST-08-AUDIT.md's gap checklist to mark each closed item as `[x]` and reference the new it() text.
  </action>
  <verify>
    <automated>pnpm test 2>&1 | tail -3 | grep -E 'Tests.*passed' && pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - If audit found no gaps: no test files modified; the audit doc's gap section says "No gaps" explicitly
    - If audit found gaps: each gap has a corresponding new it()/describe() block in the named existing test file
    - For each gap-closing addition: the new block passes under `pnpm test <file>`
    - No new test files created under `test/profiles-*.test.ts` unless explicitly justified in TEST-08-AUDIT.md
    - `pnpm test` (full suite) exits 0
    - `pnpm typecheck` exits 0
    - `pnpm lint --max-warnings=0` exits 0
    - TEST-08-AUDIT.md's gap checklist is fully `[x]`-marked (or stays "No gaps" if Task 1 found none)
  </acceptance_criteria>
  <done>TEST-08 closed; all 8 enumerated cases covered by the existing 6 test files (with any audit gaps patched in-place).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Confirm TEST-07 — add per-vendor warning-count smoke assertion if missing</name>
  <files>test/profiles-builtins.test.ts (modify only if TEST-07 confirmation gap detected)</files>
  <read_first>
    - test/profiles-builtins.test.ts (whole file — confirm whether the BIP-06 cross-profile assertion `withP.warnings.length <= without.warnings.length` exists per-vendor or only as a sweep)
    - .planning/phases/07-testing-hardening-and-fixtures/TEST-08-AUDIT.md (Task 1 output — TEST-07 confirmation status section)
    - test/fixtures/vendor-shapes/<vendor>/adt-a01.hl7 (for each of the 5 vendors — the fixture used by the comparison)
    - REQUIREMENTS.md line 108 (TEST-07 exact wording)
  </read_first>
  <action>
    Read `test/profiles-builtins.test.ts`. Look for an existing assertion of the shape:
    ```ts
    expect(withProfile.warnings.length).toBeLessThanOrEqual(withoutProfile.warnings.length);
    ```
    OR a stricter form like:
    ```ts
    expect(withProfile.warnings.length).toBeLessThan(withoutProfile.warnings.length);
    ```
    OR an UNKNOWN_SEGMENT absence assertion equivalent.

    **If such an assertion exists per-vendor (5 separate it/describe blocks, one per built-in):** TEST-07 is already closed. No file edit. Note in the commit message: "TEST-07 confirmed — profiles-builtins.test.ts already has per-vendor warning-count comparisons; no patch required."

    **If only a sweep-style assertion exists (one block iterating all 5 vendors):** TEST-07 is satisfied for the contract but a per-vendor breakdown improves verifier clarity. Optionally split the existing sweep into 5 explicit blocks — but this is OPTIONAL; do NOT touch a passing test for cosmetic reasons unless the audit doc flagged it.

    **If NO warning-count comparison assertion exists:** TEST-07 is a real gap. Add a per-vendor describe block to `test/profiles-builtins.test.ts`:

    ```ts
    describe("TEST-07 confirmation: per-vendor warning reduction with profile applied", () => {
      // For each of epic, cerner, meditech, athena, genericLab:
      it("epic profile reduces warnings on test/fixtures/vendor-shapes/epic/adt-a01.hl7", () => {
        const raw = readFileSync(
          path.join(VENDOR_SHAPES_DIR, "epic", "adt-a01.hl7"),
          "utf8",
        );
        const without = parseHL7(raw);
        const withP = parseHL7(raw, profiles.epic);
        expect(withP.warnings.length).toBeLessThanOrEqual(without.warnings.length);
      });
      // Repeat 4 more times for cerner, meditech, athena, genericLab.
    });
    ```

    Adjust the import path (`profiles` is the barrel object exporting all 5 — see test/profiles-builtins.test.ts existing imports for the actual import shape; mirror it).

    Run `pnpm test test/profiles-builtins.test.ts` to verify. Then full `pnpm test`, `pnpm typecheck`, `pnpm lint`.

    Update TEST-08-AUDIT.md's "TEST-07 confirmation status" section to reflect the final state.
  </action>
  <verify>
    <automated>pnpm test test/profiles-builtins.test.ts 2>&1 | tail -3 | grep -E 'Tests.*passed' && pnpm test 2>&1 | tail -3 | grep -E 'Tests.*passed' && pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `test/profiles-builtins.test.ts` contains at least one assertion of the shape `warnings.length).toBeLessThan` or `toBeLessThanOrEqual` comparing with-profile vs without-profile (`grep -E 'warnings.*length.*toBeLessThan' test/profiles-builtins.test.ts | wc -l` ≥ 1; this is satisfied by the BIP-06 work or by Task 3's addition)
    - All 5 built-in profiles (epic, cerner, meditech, athena, genericLab) appear in the file (`grep -cE 'profiles\\.(epic|cerner|meditech|athena|genericLab)' test/profiles-builtins.test.ts` ≥ 5)
    - `pnpm test` exits 0 (whole suite green)
    - `pnpm typecheck` exits 0
    - `pnpm lint --max-warnings=0` exits 0
    - TEST-08-AUDIT.md updated to confirm TEST-07 status (closed by Phase 6 OR closed by Task 3 addition)
  </acceptance_criteria>
  <done>TEST-07 confirmed satisfied; per-vendor warning-count assertion present in profiles-builtins.test.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Audit + targeted test additions to existing files. No new fixtures, no new helpers, no production code changes. The TEST-08-AUDIT.md is a planning artifact (not shipped). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Tampering | Existing Phase 6 test files | mitigate | Edits restricted to ADDING blocks; no existing it()/describe() removed or altered. Verifier confirms 753 baseline tests still all green. |
| T-07-07-02 | Information Disclosure | TEST-08-AUDIT.md content | accept | Audit doc lives in `.planning/`; no code or PHI; documents test-file structure only. Discarded after gaps closed per D-26. |
</threat_model>

<verification>
- Run `pnpm test test/profiles-*.test.ts` to scope-test all profile files together.
- Read TEST-08-AUDIT.md and confirm every TEST-08 case has a "no" in the Gap column (or every "yes" has a corresponding `[x]`-marked checkbox below).
- Confirm test count delta: should be small (zero if no gaps found; small positive if TEST-07 needed augmentation or audit found a gap).
</verification>

<success_criteria>
- TEST-08-AUDIT.md exists with a populated coverage matrix
- All TEST-08 enumerated cases mapped to existing test file(s); any gaps closed in-place via additions to test/profiles-*.test.ts
- TEST-07 confirmed via per-vendor warning-count comparison in profiles-builtins.test.ts
- pnpm test passes; pnpm typecheck passes; pnpm lint --max-warnings=0 passes
- No new test files created; no production source modifications
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing-hardening-and-fixtures/07-07-SUMMARY.md` summarizing:
- TEST-08 audit results: which cases were already covered, which (if any) needed patches
- TEST-07 confirmation: how it's satisfied (Phase 6 BIP-06 already did it, or Task 3 added the assertion)
- Number of test additions (should be small — typically 0–3)
- TEST-08-AUDIT.md path noted for verifier consumption (will be summarized + discarded at phase close per D-26)
</output>
