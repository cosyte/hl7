---
phase: 09-rename-package-to-cosyte-hl7
plan: 03
status: complete
date: 2026-04-20
---

# Plan 09-03: Examples + Starter Kit Rename — SUMMARY

## What was built

Bulk sed sweep of `@cosyte/hl7-parser` → `@cosyte/hl7` across the examples tree and the profile starter kit.

## Files edited

| File | Before | After |
|------|------:|------:|
| examples/README.md | 1 | 0 |
| examples/extract-patient-info.ts | 1 | 0 |
| examples/read-lab-results.ts | 1 | 0 |
| examples/modify-and-resend.ts | 1 | 0 |
| examples/profile-starter-kit/package.json | 4 | 0 |
| examples/profile-starter-kit/src/index.ts | 2 | 0 |
| examples/profile-starter-kit/test/profile.test.ts | 1 | 0 |
| examples/profile-starter-kit/tsup.config.ts | 1 | 0 |
| examples/profile-starter-kit/README.md | 6 | 0 |
| examples/profile-starter-kit/CUSTOMIZING.md | 4 | 0 |
| examples/profile-starter-kit/pnpm-lock.yaml | 3 | 0 |
| **Total source** | **25** | **0** |

## Starter kit package.json dep keys (verified post-sweep)

```
peerDependencies:     ['@cosyte/hl7']     ✓
peerDependenciesMeta: ['@cosyte/hl7']     ✓
devDependencies:      ['@cosyte/hl7']     ✓
```

`devDependencies["@cosyte/hl7"]` value is `file:../..` (unchanged — pnpm resolves by path, and the parent dir's package.json now declares name `@cosyte/hl7`).

## Starter kit own-name field

Left alone — it's a placeholder: `@{{YOUR_ORG}}/hl7-profile-{{PROFILE_NAME}}`. Users copy the kit and fill placeholders at publish time. No rename needed (matches CONTEXT.md "Claude's Discretion" guidance).

## Lockfile handling (Task 3)

**Option A (text-sweep) used.** The starter kit's `pnpm-lock.yaml` is listed in `.gitignore`, so the local sweep stands but there is nothing to commit for the lockfile. Wave 4's `pnpm install` at the root will regenerate any needed lockfile consistency.

## Remaining old-name occurrences under examples/

All in non-committed build/install artifacts (excluded from the final sweep per CONTEXT.md):
- `examples/profile-starter-kit/node_modules/**` (pnpm install artifact — regenerated by Wave 4)
- `examples/profile-starter-kit/dist/**` (build output — regenerated by Wave 4)

`grep -rln "@cosyte/hl7-parser" examples/ --exclude-dir=node_modules --exclude-dir=dist` returns empty (exit 1 from grep = no matches).

## Commit

- `45b9653` — docs(09-03): sweep examples and profile-starter-kit to @cosyte/hl7

## Deviations

`pnpm-lock.yaml` edit isn't committed because the file is gitignored (consistent with the starter-kit-as-template pattern — users regenerate the lockfile themselves). Called out in the commit message and here.

## Self-Check: PASSED
