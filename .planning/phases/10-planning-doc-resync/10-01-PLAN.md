---
phase: 10-planning-doc-resync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - "A developer reading REQUIREMENTS.md sees every shipped REQ-ID with `[x]` in the categorized checklist (no stale `[ ]` on PARSE-01..09, TOL-01..09, EX-01..03, DOC-01..15, KIT-01..07)."
    - "A developer scanning the Traceability table sees every shipped REQ-ID with Status `Closed` or `Complete (XX-YY)` — zero `Pending` rows remain."
    - "A developer grep'ing for `- \\[ \\]` in REQUIREMENTS.md finds 0 matches."
    - "A developer grep'ing for `| Pending` in the Traceability table finds 0 matches."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Fully synced v1 traceability — all 97 REQ-IDs marked Closed/Complete; checklist checkboxes match ground-truth shipped state"
      contains: "Coverage: 97 / 97 v1 REQ-IDs mapped; 0 `- [ ]` lines; 0 `Pending` rows"
  key_links:
    - from: ".planning/REQUIREMENTS.md (checklist section)"
      to: ".planning/REQUIREMENTS.md (Traceability table)"
      via: "matching per-REQ-ID state across both surfaces"
      pattern: "Every REQ-ID `[x]` in checklist has a non-`Pending` row in Traceability"
---

<objective>
Flip every stale `- [ ]` checkbox and every `| Pending` traceability row in `.planning/REQUIREMENTS.md` to reflect the v2.1 ground-truth from `.planning/v2.1-MILESTONE-AUDIT.md`: all 97 v1 REQ-IDs are implemented, tested, and shipped. No other file is touched.

Purpose: Close v2.1-MILESTONE-AUDIT.md tech-debt item 3 (REQUIREMENTS.md traceability stale — 42 stale checkboxes + 35 "Pending" rows). An outside reader of `.planning/` currently sees an inaccurate picture of which REQ-IDs are complete; this plan fixes that.

Output: `.planning/REQUIREMENTS.md` with (a) zero `- [ ]` checkboxes in the categorized checklist, (b) zero `| Pending |` cells in the Traceability table, (c) updated "Last updated" footer timestamp and note reflecting the resync.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/v2.1-MILESTONE-AUDIT.md
@.planning/ROADMAP.md

<ground_truth_source>
The authoritative source of what shipped is `.planning/v2.1-MILESTONE-AUDIT.md`. Relevant findings:

- Frontmatter line 6: `requirements: 97/97` — all 97 REQ-IDs satisfied.
- Lines 72-78 (Requirements Coverage table): every category marked **satisfied**.
- Line 78 (Phase 8 row): "EX-01..03 + KIT-01..07 + DOC-01..15 — satisfied — SUMMARYs + artifacts on disk + `pnpm examples` green in 09-04 pipeline".
- Line 72 (Phase 2 row): "PARSE-01..09 + TOL-01..10 — satisfied — traceability checkboxes need update".
- Line 146-148 (Tech Debt item 3): enumerates the 33 stale checkboxes (PARSE-01..09 + TOL-01..07/09 from Phase 2 = 17; EX-01..03 + DOC-01..15 from Phase 8 = 18; DOC-14/15 may already be checked per audit note — but the current file shows them as `[ ]` so they DO need flipping. KIT-01..07 in the top checklist also show `[ ]` and need flipping despite being Closed in the Traceability table).
- Line 149 (Tech Debt item 4-note): "DOC-14/15 and KIT-01..07 are correctly checked" — this refers to the TRACEABILITY table rows (which DO show KIT-01..07 as Closed and DOC-14/15 as Pending — audit is slightly off on DOC-14/15 but the grep of the live file is the source of truth).

Empirical grep against the live file (2026-04-20):
- `grep -c '^- \[ \]' .planning/REQUIREMENTS.md` → **42** (PARSE: 8 × [PARSE-03 already x]; TOL: 9 × [TOL-10 already x]; EX: 3; KIT: 7; DOC: 15)
- `grep -c '| Pending' .planning/REQUIREMENTS.md` → **35** (PARSE: 8 [PARSE-03 already Complete]; TOL: 9 [TOL-10 already Complete]; EX: 3; KIT: 0 [KIT-01..07 already Closed in table]; DOC: 15)

Target after plan: both grep counts → **0**.

Per-phase evidence trail for the "(XX-YY)" closure-plan references:
- PARSE-01..09 closed by Phase 2 plans 02-01/02/03/06; PARSE-09 specifically closed by Plan 02-07 charset gap-closure (commit 04a180b).
- TOL-01..09 closed by Phase 2 plans (TOL-01 by 02-06 strict-mode; TOL-02/03/04/05 by 02-01 warnings/errors/shell; TOL-06 by 02-02 MLLP; TOL-07 by 02-03 tokenize whitespace; TOL-08/09 plumbing by 02-05 dates, OBSERVABLE SLICE by Phase 3 TYPES-04 + Phase 4 HELPERS-01).
- EX-01/02/03 closed by Phase 8 Plan 08-01 (runnable scripts + smoke runner, 8 files, evidence: 09-04 pipeline `pnpm examples` exit 0).
- DOC-01..13 closed by Phase 8 Plan 08-03 (README 13 sections).
- DOC-14/15 closed by Phase 8 Plan 08-04 (CHANGELOG + CONTRIBUTING + LICENSE verify).
- KIT-01..07 already show Closed in Traceability table (lines 246-252); only the top checklist checkboxes (lines 119-125) need flipping.
</ground_truth_source>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip categorized checklist — PARSE + TOL stale checkboxes (Phase 2)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Edit the categorized-checklist section of `.planning/REQUIREMENTS.md` (the part between "## v1 Requirements" on line 7 and the "---" separator before "## v2 Requirements (Deferred)" around line 145).

Flip the following specific REQ-ID checkboxes from `- [ ]` to `- [x]`. Each flip must preserve the surrounding text verbatim — only the three-character `[ ]` → `[x]` substitution, nothing else. Append a brief `_(Closed by …)_` italicized suffix to each, mirroring the format already used on SETUP-01..06 and TOL-10 in the same file.

**Phase 2 — Core Parsing (PARSE):**
1. Line 20 — `- [ ] **PARSE-01**` → `- [x] **PARSE-01**` + append ` _(Closed by Plan 02-06 — public parseHL7 entry + strict-mode escalation.)_`
2. Line 21 — `- [ ] **PARSE-02**` → `- [x] **PARSE-02**` + append ` _(Closed by Plan 02-03 — readDelimiters MSH-1/MSH-2 reader + tokenize custom-enc path.)_`
3. (Line 22 PARSE-03 is ALREADY `[x]` — skip.)
4. Line 23 — `- [ ] **PARSE-04**` → `- [x] **PARSE-04**` + append ` _(Closed by Plan 02-03 — splitSegments preserves original order including Z-segments.)_`
5. Line 24 — `- [ ] **PARSE-05**` → `- [x] **PARSE-05**` + append ` _(Closed by Plan 02-03 — tokenize decomposes reps/comps/subs.)_`
6. Line 25 — `- [ ] **PARSE-06**` → `- [x] **PARSE-06**` + append ` _(Closed by Plan 02-03 — RawField.isNull flag distinguishes `""` from empty.)_`
7. Line 26 — `- [ ] **PARSE-07**` → `- [x] **PARSE-07**` + append ` _(Closed by Plan 02-02 — BOM stripped silently in normalizeBuffer.)_`
8. Line 27 — `- [ ] **PARSE-08**` → `- [x] **PARSE-08**` + append ` _(Closed by Plan 02-02 — line-ending normalize.)_`
9. Line 28 — `- [ ] **PARSE-09**` → `- [x] **PARSE-09**` + append ` _(Closed by Plan 02-07 gap-closure — MSH-18 charset auto-discovery + options.charset override; commit 04a180b.)_`

**Phase 2 — Tolerance (TOL):**
1. Line 59 — `- [ ] **TOL-01**` → `- [x] **TOL-01**` + append ` _(Closed by Plan 02-06 — strict-mode escalation chokepoint in makeEmitter.)_`
2. Line 60 — `- [ ] **TOL-02**` → `- [x] **TOL-02**` + append ` _(Closed by Plan 02-01 — Hl7ParseError 4 fatal codes with position + snippet.)_`
3. Line 61 — `- [ ] **TOL-03**` → `- [x] **TOL-03**` + append ` _(Closed by Plan 02-01 — 13-code WARNING_CODES registry with position-bearing factories.)_`
4. Line 62 — `- [ ] **TOL-04**` → `- [x] **TOL-04**` + append ` _(Closed by Plan 02-01 — Hl7Message.warnings always readonly array, frozen on construction.)_`
5. Line 63 — `- [ ] **TOL-05**` → `- [x] **TOL-05**` + append ` _(Closed by Plan 02-06 — onWarning invoked inside makeEmitter chokepoint.)_`
6. Line 64 — `- [ ] **TOL-06**` → `- [x] **TOL-06**` + append ` _(Closed by Plan 02-02 — stripMllp + MLLP_FRAMING_STRIPPED emit.)_`
7. Line 65 — `- [ ] **TOL-07**` → `- [x] **TOL-07**` + append ` _(Closed by Plan 02-03 — tokenize whitespace trim with non-whitespace-guard.)_`
8. Line 66 — `- [ ] **TOL-08**` → `- [x] **TOL-08**` + append ` _(Plumbing closed by Plan 02-05; observable slice closed by Phase 3 TYPES-04 (TS/DTM composite) + Phase 4 HELPERS-01 (`msg.meta.timestamp`). See `.planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md` Resolution Note §TOL-08.)_`
9. Line 67 — `- [ ] **TOL-09**` → `- [x] **TOL-09**` + append ` _(Closed by Plan 02-05 — BUILTIN_DATE_FALLBACKS always-tried cascade.)_`
10. (Line 68 TOL-10 is ALREADY `[x]` — skip.)

Do NOT modify any other line in the file in this task.
  </action>
  <verify>
    <automated>grep -cE '^- \[ \] \*\*(PARSE|TOL)-' .planning/REQUIREMENTS.md — expect `0` (zero stale PARSE/TOL checklist checkboxes remain).</automated>
  </verify>
  <done>
All 17 PARSE + TOL checklist rows flipped to `[x]` with closure-plan citations. Zero stale `- [ ] **PARSE-…` or `- [ ] **TOL-…` lines remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip categorized checklist — EX + KIT + DOC stale checkboxes (Phase 8)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Continue editing the categorized-checklist section of `.planning/REQUIREMENTS.md`. Flip the following specific REQ-ID checkboxes from `- [ ]` to `- [x]`. Same substitution rule as Task 1 — preserve surrounding text verbatim; append an italicized `_(Closed by …)_` suffix mirroring SETUP style.

**Phase 8 — Examples (EX):**
1. Line 113 — `- [ ] **EX-01**` → `- [x] **EX-01**` + append ` _(Closed by Plan 08-01 — examples/extract-patient-info.ts runs end-to-end via `pnpm examples`; verified green in 09-04 pipeline.)_`
2. Line 114 — `- [ ] **EX-02**` → `- [x] **EX-02**` + append ` _(Closed by Plan 08-01 — examples/read-lab-results.ts iterates msg.observations() / msg.orders().)_`
3. Line 115 — `- [ ] **EX-03**` → `- [x] **EX-03**` + append ` _(Closed by Plan 08-01 — examples/modify-and-resend.ts mutates + round-trip serializes.)_`

**Phase 8 — Profile Starter Kit (KIT):**
1. Line 119 — `- [ ] **KIT-01**` → `- [x] **KIT-01**` + append ` _(Closed by Plan 08-02 — examples/profile-starter-kit/ subtree with all 15 files.)_`
2. Line 120 — `- [ ] **KIT-02**` → `- [x] **KIT-02**` + append ` _(Closed by Plan 08-02 — in-kit `pnpm install && pnpm test` green; 4/4 tests pass against ZAL sample fixture.)_`
3. Line 121 — `- [ ] **KIT-03**` → `- [x] **KIT-03**` + append ` _(Closed by Plan 08-02 — `pnpm build` produces dist/index.mjs + index.cjs + index.d.ts matching exports map.)_`
4. Line 122 — `- [ ] **KIT-04**` → `- [x] **KIT-04**` + append ` _(Closed by Plan 08-02 — ci.yml + publish.yml actionlint-clean.)_`
5. Line 123 — `- [ ] **KIT-05**` → `- [x] **KIT-05**` + append ` _(Closed by Plan 08-02 — manifest has peerDependencies.@cosyte/hl7 + publishConfig.access:public + files allow-list + no dependencies block + prepublishOnly chain.)_`
6. Line 124 — `- [ ] **KIT-06**` → `- [x] **KIT-06**` + append ` _(Closed by Plan 08-02 — CUSTOMIZING.md 5-step walk-through with runnable Verify blocks.)_`
7. Line 125 — `- [ ] **KIT-07**` → `- [x] **KIT-07**` + append ` _(Closed by Plan 08-02 — `{{YOUR_ORG}}` / `{{PROFILE_NAME}}` / `MyProfile` placeholder triple used consistently; kit pipeline green with placeholders intact.)_`

**Phase 8 — Documentation (DOC):**
1. Line 129 — `- [ ] **DOC-01**` → `- [x] **DOC-01**` + append ` _(Closed by Plan 08-03 — README one-sentence value prop + badges at top.)_`
2. Line 130 — `- [ ] **DOC-02**` → `- [x] **DOC-02**` + append ` _(Closed by Plan 08-03 — 30-second copy-pasteable quickstart block.)_`
3. Line 131 — `- [ ] **DOC-03**` → `- [x] **DOC-03**` + append ` _(Closed by Plan 08-03 — 6-8 feature bullets.)_`
4. Line 132 — `- [ ] **DOC-04**` → `- [x] **DOC-04**` + append ` _(Closed by Plan 08-03 — "HL7 in 90 seconds" core-concepts section.)_`
5. Line 133 — `- [ ] **DOC-05**` → `- [x] **DOC-05**` + append ` _(Closed by Plan 08-03 — three access patterns (helpers / paths / structural) with runnable examples.)_`
6. Line 134 — `- [ ] **DOC-06**` → `- [x] **DOC-06**` + append ` _(Closed by Plan 08-03 — Cookbook section with every recipe from the spec.)_`
7. Line 135 — `- [ ] **DOC-07**` → `- [x] **DOC-07**` + append ` _(Closed by Plan 08-03 — top-level Profiles section covering authoring, extending, merging, publishing.)_`
8. Line 136 — `- [ ] **DOC-08**` → `- [x] **DOC-08**` + append ` _(Closed by Plan 08-03 — 4-tier Real-World Tolerance section with table + runnable warnings iteration.)_`
9. Line 137 — `- [ ] **DOC-09**` → `- [x] **DOC-09**` + append ` _(Closed by Plan 08-03 — Error Handling section covering Hl7ParseError / Hl7ParseWarning / ProfileDefinitionError.)_`
10. Line 138 — `- [ ] **DOC-10**` → `- [x] **DOC-10**` + append ` _(Closed by Plan 08-03 — Contributing section with CONTRIBUTING.md pointer.)_`
11. Line 139 — `- [ ] **DOC-11**` → `- [x] **DOC-11**` + append ` _(Closed by Plan 08-03 — "Built by Cosyte" footer + license link.)_`
12. Line 140 — `- [ ] **DOC-12**` → `- [x] **DOC-12**` + append ` _(Closed by Plan 08-03 — Roadmap / stretch-goals section documents all 6 v2 deferrals.)_`
13. Line 141 — `- [ ] **DOC-13**` → `- [x] **DOC-13**` + append ` _(Closed by Plan 08-03 — "Publishing Your Profile" recipe links to examples/profile-starter-kit/ + CUSTOMIZING.md.)_`
14. Line 142 — `- [ ] **DOC-14**` → `- [x] **DOC-14**` + append ` _(Closed by Plan 08-04 — CHANGELOG.md in Keep-a-Changelog format with [Unreleased] section.)_`
15. Line 143 — `- [ ] **DOC-15**` → `- [x] **DOC-15**` + append ` _(Closed by Plan 08-04 — LICENSE (MIT) at repo root.)_`

Do NOT modify the Traceability table rows yet — that is Task 3.
  </action>
  <verify>
    <automated>grep -cE '^- \[ \]' .planning/REQUIREMENTS.md — expect `0` (zero stale checkboxes remain anywhere in the file after Tasks 1 + 2).</automated>
  </verify>
  <done>
All 25 EX + KIT + DOC checklist rows flipped to `[x]` with closure-plan citations. Combined with Task 1, zero `- [ ]` lines remain in `.planning/REQUIREMENTS.md`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip Traceability table — PARSE + TOL + EX + DOC "Pending" rows to Closed</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Edit the Traceability table section of `.planning/REQUIREMENTS.md` (between the `| REQ-ID | Phase | Status |` header on line 169 and `**Coverage:**` around line 269).

Flip the following 35 `| Pending |` cells to explicit closure citations. Match the existing format used for already-Closed rows in the same table (e.g. `Complete (02-04)`, `Closed (Plan 05 — …)`). Each substitution replaces the single token `| Pending |` at end-of-row with `| Closed (Plan <plan-id> — <short-closure-descriptor>) |`. Preserve the REQ-ID and Phase columns verbatim.

**PARSE rows (lines 177-185, skipping line 179 PARSE-03 which is already `Complete (02-04)`):**
- Line 177 (PARSE-01): `| Pending |` → `| Closed (Plan 02-06 — parseHL7 public entry + strict-mode) |`
- Line 178 (PARSE-02): `| Pending |` → `| Closed (Plan 02-03 — readDelimiters + tokenize custom-enc) |`
- Line 180 (PARSE-04): `| Pending |` → `| Closed (Plan 02-03 — splitSegments preserves order incl. Z-segments) |`
- Line 181 (PARSE-05): `| Pending |` → `| Closed (Plan 02-03 — tokenize reps/comps/subs) |`
- Line 182 (PARSE-06): `| Pending |` → `| Closed (Plan 02-03 — RawField.isNull flag) |`
- Line 183 (PARSE-07): `| Pending |` → `| Closed (Plan 02-02 — normalizeBuffer silent BOM strip) |`
- Line 184 (PARSE-08): `| Pending |` → `| Closed (Plan 02-02 — line-ending normalize to \r) |`
- Line 185 (PARSE-09): `| Pending |` → `| Closed (Plan 02-07 gap-closure — MSH-18 charset + options.charset override, commit 04a180b) |`

**TOL rows (lines 186-194, skipping line 195 TOL-10 which is already `Complete (02-04)`):**
- Line 186 (TOL-01): `| Pending |` → `| Closed (Plan 02-06 — strict-mode escalation chokepoint) |`
- Line 187 (TOL-02): `| Pending |` → `| Closed (Plan 02-01 — 4 fatal codes in Hl7ParseError) |`
- Line 188 (TOL-03): `| Pending |` → `| Closed (Plan 02-01 — 13-code WARNING_CODES registry) |`
- Line 189 (TOL-04): `| Pending |` → `| Closed (Plan 02-01 — msg.warnings frozen readonly array) |`
- Line 190 (TOL-05): `| Pending |` → `| Closed (Plan 02-06 — onWarning invoked in emitter chokepoint) |`
- Line 191 (TOL-06): `| Pending |` → `| Closed (Plan 02-02 — stripMllp + MLLP_FRAMING_STRIPPED emit) |`
- Line 192 (TOL-07): `| Pending |` → `| Closed (Plan 02-03 — tokenize whitespace trim + warning) |`
- Line 193 (TOL-08): `| Pending |` → `| Closed (plumbing Plan 02-05; observable slice Phase 3 TYPES-04 + Phase 4 HELPERS-01 — see 02-VERIFICATION.md §TOL-08) |`
- Line 194 (TOL-09): `| Pending |` → `| Closed (Plan 02-05 — BUILTIN_DATE_FALLBACKS always-tried cascade) |`

**EX rows (lines 243-245):**
- Line 243 (EX-01): `| Pending |` → `| Closed (Plan 08-01 — examples/extract-patient-info.ts; pnpm examples green in 09-04 pipeline) |`
- Line 244 (EX-02): `| Pending |` → `| Closed (Plan 08-01 — examples/read-lab-results.ts observations+orders iteration) |`
- Line 245 (EX-03): `| Pending |` → `| Closed (Plan 08-01 — examples/modify-and-resend.ts mutation + round-trip) |`

**DOC rows (lines 253-267) — all 15:**
- Line 253 (DOC-01): `| Pending |` → `| Closed (Plan 08-03 — README value prop + badges) |`
- Line 254 (DOC-02): `| Pending |` → `| Closed (Plan 08-03 — 30-second quickstart block) |`
- Line 255 (DOC-03): `| Pending |` → `| Closed (Plan 08-03 — feature list 6-8 bullets) |`
- Line 256 (DOC-04): `| Pending |` → `| Closed (Plan 08-03 — HL7 in 90 seconds section) |`
- Line 257 (DOC-05): `| Pending |` → `| Closed (Plan 08-03 — three access patterns with examples) |`
- Line 258 (DOC-06): `| Pending |` → `| Closed (Plan 08-03 — Cookbook with every spec recipe) |`
- Line 259 (DOC-07): `| Pending |` → `| Closed (Plan 08-03 — top-level Profiles section) |`
- Line 260 (DOC-08): `| Pending |` → `| Closed (Plan 08-03 — 4-tier Real-World Tolerance table + example) |`
- Line 261 (DOC-09): `| Pending |` → `| Closed (Plan 08-03 — Error Handling section) |`
- Line 262 (DOC-10): `| Pending |` → `| Closed (Plan 08-03 — Contributing section) |`
- Line 263 (DOC-11): `| Pending |` → `| Closed (Plan 08-03 — "Built by Cosyte" footer + license link) |`
- Line 264 (DOC-12): `| Pending |` → `| Closed (Plan 08-03 — Roadmap / stretch-goals section with 6 v2 deferrals) |`
- Line 265 (DOC-13): `| Pending |` → `| Closed (Plan 08-03 — Publishing Your Profile recipe links to starter kit + CUSTOMIZING.md) |`
- Line 266 (DOC-14): `| Pending |` → `| Closed (Plan 08-04 — CHANGELOG.md Keep-a-Changelog + [Unreleased]) |`
- Line 267 (DOC-15): `| Pending |` → `| Closed (Plan 08-04 — LICENSE MIT at repo root) |`

KIT-01..07 Traceability rows (lines 246-252) already carry `Closed (Plan 08-02 — …)` — DO NOT modify them.

Line numbers above are based on the current HEAD snapshot (commit a73392f). If any line numbers have drifted, resolve by REQ-ID match — the substitution target is always the specific `| REQ-ID | Phase … | Pending |` row.
  </action>
  <verify>
    <automated>grep -c '| Pending' .planning/REQUIREMENTS.md — expect `0` (zero Pending rows remain in the Traceability table).</automated>
  </verify>
  <done>
All 35 `| Pending |` rows flipped to explicit `| Closed (Plan XX-YY — ...) |` citations. Zero `| Pending` rows remain in `.planning/REQUIREMENTS.md`. Combined with Tasks 1+2, the REQUIREMENTS.md ground-truth matches the v2.1 audit entirely.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update footer timestamp and "Last updated" note</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Edit the very last line of `.planning/REQUIREMENTS.md` (currently line 271):

Replace:
```
*Last updated: 2026-04-18 (traceability populated by roadmapper).*
```

With:
```
*Last updated: 2026-04-20 (v2.1 resync: all 97 REQ-IDs flipped from stale `[ ]` / "Pending" to `[x]` / "Closed" matching the v2.1-MILESTONE-AUDIT.md ground-truth — Phase 10 gap-closure Plan 10-01).*
```

Also update the Coverage line (currently line 269):
```
**Coverage:** 97 / 97 v1 REQ-IDs mapped (no orphans, no duplicates).
```

Leave the Coverage line unchanged — it is still correct (97/97 mapped is the mapping count, not a checkbox count). The implicit ground-truth assertion is now reinforced by the flipped checkboxes and table rows above.

Do NOT modify the body of the file — only the footer.
  </action>
  <verify>
    <automated>grep -c '2026-04-20.*v2.1 resync' .planning/REQUIREMENTS.md — expect at least `1`.</automated>
  </verify>
  <done>
Footer updated with 2026-04-20 resync note citing Phase 10 Plan 10-01.
  </done>
</task>

</tasks>

<verification>
After all 4 tasks land, verify the three load-bearing grep invariants:

1. `grep -c '^- \[ \]' .planning/REQUIREMENTS.md` → must return `0`
2. `grep -c '| Pending' .planning/REQUIREMENTS.md` → must return `0`
3. `grep -c '2026-04-20.*v2.1 resync' .planning/REQUIREMENTS.md` → must return `≥ 1`

Additionally:
4. `grep -c '^- \[x\]' .planning/REQUIREMENTS.md` → must return `≥ 91` (all 91 checked-in-checklist REQ-IDs: 6 SETUP + 9 PARSE + 9 TOL + 3 EX + 7 KIT + 15 DOC + 7 HELPERS + 7 MODEL + 4 TYPES + 6 SER + 9 PROF + 6 BIP + 8 TEST = 96 if all flip; SETUP=6 were already [x]; total should match category lists).
</verification>

<success_criteria>
- All 42 stale `- [ ]` lines in the categorized checklist flipped to `- [x]` with closure-plan citations.
- All 35 `| Pending |` cells in the Traceability table flipped to `| Closed (Plan XX-YY — …) |`.
- Footer timestamp updated to 2026-04-20 with Plan 10-01 attribution.
- No other file modified (plan scope is disjoint from Plans 10-02/03/04).
- Commit atomic: `docs(10-01): resync REQUIREMENTS.md to v2.1 ground-truth`.
</success_criteria>

<output>
After completion, create `.planning/phases/10-planning-doc-resync/10-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md` template. Include:
- Final grep counts (expected 0/0/1) as evidence.
- Per-section change count (17 PARSE/TOL + 25 EX/KIT/DOC + 35 Pending-row flips + 1 footer = 78 line edits).
- Confirmation that Task 3 left KIT-01..07 Traceability rows untouched (they were already Closed).
</output>
