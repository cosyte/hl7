---
phase: 10-planning-doc-resync
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - "A developer opening STATE.md sees the frontmatter `status:` field reflecting v2.1 post-rename complete (not mid-Phase-7-EXECUTION-COMPLETE)."
    - "A developer reading STATE.md `## Current Position` sees Phase 9 complete, package renamed to @cosyte/hl7, all 9 phases shipped — NOT 'Phase 7 EXECUTION COMPLETE pending /gsd-verify-work 7'."
    - "A developer reading STATE.md `progress:` frontmatter block sees 9/9 phases complete, not 3/8."
    - "A developer opening PROJECT.md sees all 11 `Top-level capabilities` bullets checked `[x]`."
    - "A developer reading PROJECT.md's `## What This Is` opening line sees the `@cosyte/hl7` package name (renamed per Phase 9), NOT `@cosyte/hl7-parser`."
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Frontmatter + Current Position + Performance Metrics reflecting v2.1 post-rename state"
      contains: "status: reflects v2.1 post-rename (9/9 phases); progress.completed_phases: 9; progress.total_phases: 9; progress.percent: 100"
    - path: ".planning/PROJECT.md"
      provides: "Capabilities checklist reflecting shipped v1 surface"
      contains: "11 `- [x]` top-level capability bullets; 0 `- [ ]` bullets in that list"
  key_links:
    - from: ".planning/STATE.md frontmatter"
      to: ".planning/STATE.md Current Position body"
      via: "matching phase-complete narrative on both surfaces"
      pattern: "frontmatter status field and Current Position body must both cite Phase 9 post-rename complete"
    - from: ".planning/PROJECT.md top-level capabilities checklist"
      to: "shipped code surface"
      via: "each capability maps to a closed REQ-ID in REQUIREMENTS.md"
      pattern: "every `[x]` capability is backed by a Closed REQ-ID per REQUIREMENTS.md"
---

<objective>
Bring `.planning/STATE.md` and `.planning/PROJECT.md` into sync with the v2.1 ground-truth:
1. STATE.md frontmatter `status`/`progress` + body `Current Position` + `Progress` + progress bar reflect v2.1 post-rename state (9/9 phases shipped, package renamed to `@cosyte/hl7`, 824 tests pass + 14 todo, tarball green).
2. PROJECT.md top-level capabilities checklist flips all 11 `- [ ]` to `- [x]`.

Purpose: Close v2.1-MILESTONE-AUDIT.md tech-debt items 5 (STATE.md current-position stale — reflects Phase 7 pending) and 6 (PROJECT.md capabilities checklist — all 11 `[ ]` despite v1 completion).

Output: Two files touched, each an atomic commit. No other file modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/v2.1-MILESTONE-AUDIT.md
@.planning/ROADMAP.md

<scope_split_note>
**Important scope-split convention (matched with Plan 10-02):** STATE.md frontmatter `total_phases: 9` scopes the completion rollup to the v1-milestone span (Phases 1-9). The `status:` narrative may reference Phases 10-12 as active follow-on work under the v2.1 audit — this is intentional: the frontmatter tracks the core-milestone rollup (9/9 shipped), the narrative tracks current work (Phase 10 in progress; 11 + 12 planned). ROADMAP.md (Plan 10-02) is the authoritative project-wide phase list and keeps all 12 rows in its Progress table; its rollup caption mirrors this split with the line `**v1 milestone:** 9/9 phases complete. **v2.1 gap-closure:** Phases 10-12 in progress (this phase = 10).` — STATE.md's `status:` narrative carries the same split convention so both surfaces read consistently.
</scope_split_note>

<ground_truth_source>
The authoritative source is `.planning/v2.1-MILESTONE-AUDIT.md`:

- Line 59 (Executive Summary): "All nine phases have shipped their code. The live pipeline is green across every gate the project claims: `pnpm install/typecheck/lint/test/build/examples`, CI on Node 18/20/22, kit subtree install+test+build, `pnpm publish --dry-run` (346.2 kB / 10-file tarball), and all 824 tests pass (+14 documented `it.todo`)."
- Line 59: "The package is renamed to `@cosyte/hl7` at 0.1.0 with zero legacy references outside the CHANGELOG breadcrumb."
- Tech Debt item 5 (frontmatter line 45): "STATE.md status field reflects Phase 7 EXECUTION COMPLETE pending verify — not updated after Phases 7, 8, 9 landed".
- Tech Debt item 6 (frontmatter line 46): "PROJECT.md top-level capabilities checklist still shows all items `[ ]` despite v1 completion".

Current STATE.md state (observed at 2026-04-20):
- Frontmatter `status:` (line 5) is a 2,000-char narrative describing Phase 8 Plan 05 completion — this is stale and needs replacement with a v2.1-milestone-complete narrative.
- Frontmatter `progress:` block (lines 7-12): `total_phases: 8`, `completed_phases: 3`, `completed_plans: 5`, `percent: 56`. ALL WRONG. Correct values per ROADMAP: `total_phases: 9` (v1 milestone spans Phases 1-9; Phases 10-12 are v2.1-tech-debt follow-on gap-closure phases, NOT part of the core milestone), `completed_phases: 9`, `completed_plans: 46` (4+7+4+4+5+6+7+5+4 from Progress table in ROADMAP 10-02 Task 2), `percent: 100`.
- Body `Current Position` (lines 28-41): cites Phase 7 EXECUTION COMPLETE, next step `/gsd-verify-work 7`. Stale.
- Body `Performance Metrics` (lines 43-47): "Phases completed: 2", "Plans completed: 37", "REQ-IDs validated: 69 / 97". All stale — after v2.1 milestone, Phases completed: 9, Plans completed: 46, REQ-IDs validated: 97/97.
- Progress bar (line 40): "[████████████░░░░░░░░] 40%". Stale.
- Session Continuity bullets and commit/test-count rows later in the file are historical breadcrumbs — they are append-only logs and should NOT be rewritten. Only the snapshot-like fields (frontmatter, Current Position, Performance Metrics rollup, progress bar) need flipping.

Current PROJECT.md state (observed at 2026-04-20):
- Line 1: `# @cosyte/hl7-parser` — stale (package renamed to `@cosyte/hl7` in Phase 9).
- Line 5: `An open-source, developer-focused HL7 v2 parser…` — opening sentence mentions "parser" multiple times but is about the library's DX; this intentional narrative phrasing is fine, but any reference to the package NAME `@cosyte/hl7-parser` needs flipping.
- Lines 23-33 (Top-level capabilities): all 11 bullets show `- [ ]`. Per audit, all 11 are shipped:
  1. Parse any well-formed HL7 v2.1–v2.8 message into a typed object model — shipped (PARSE-01..09)
  2. Named helpers for common extractions — shipped (HELPERS-01..07)
  3. Dot-path accessors — shipped (MODEL-01)
  4. Structural access to every segment, field, component, subcomponent, repetition — shipped (MODEL-03..04)
  5. Round-trip serialization — shipped (SER-01..06)
  6. Lenient default parsing with 4-tier deviation model — shipped (TOL-01..09, PARSE-03 escapes)
  7. First-class `defineProfile()` API — shipped (PROF-01..09)
  8. 5 built-in vendor profiles — shipped (BIP-01..05)
  9. Profile starter kit — shipped (KIT-01..07)
  10. Zero runtime dependencies; dual ESM + CJS build; strict TypeScript — shipped (SETUP-03, SETUP-02, SETUP-05)
  11. Three runnable examples + comprehensive README with cookbook — shipped (EX-01..03, DOC-01..13)

The "Key Decisions" table (lines 66-77) shows every decision with `— Pending` as Outcome. Per v1 completion, every decision's outcome can be summarised "Confirmed in v1 — shipped behaviour matches intent". HOWEVER, this plan's scope is limited to the 11 capability bullets (per ROADMAP Phase 10 SC-4 which specifically names the 11 `[ ]` bullets). The Key Decisions Outcome column is NOT in scope for this plan. If future work wants to refresh it, a later phase can do so. Do NOT modify the Outcome column.

Project-naming policy: The package name was renamed in Phase 9 from `@cosyte/hl7-parser` to `@cosyte/hl7`. Where PROJECT.md mentions the package NAME (not the word "parser" as a feature descriptor), flip it. Specifically: the `# @cosyte/hl7-parser` H1 title (line 1) and `**Package manager:** pnpm. Package name: `@cosyte/hl7-parser`…` line in Constraints (line 60). Leave the word "parser" alone where it describes the library's purpose, not its published name.
</ground_truth_source>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resync STATE.md frontmatter (status + progress block + last_updated)</name>
  <files>.planning/STATE.md</files>
  <action>
Edit the frontmatter of `.planning/STATE.md` (lines 1-13).

**Replace the entire current frontmatter (lines 1-13) with this exact content:**

```
---
gsd_state_version: 1.0
milestone: v2.1
milestone_name: milestone
status: "v2.1 MILESTONE COMPLETE 2026-04-20 — all 9 phases shipped (Phases 1-9). Package renamed to @cosyte/hl7 at 0.1.0 (Phase 9); 97/97 v1 REQ-IDs closed; 824 tests pass + 14 documented it.todo across 59 files; full pipeline green (pnpm install/typecheck/lint/test/build/examples), kit subtree install+test+build green, starter-kit ci.yml + publish.yml actionlint-clean, pnpm publish --dry-run produces clean 10-file/346.2kB tarball; coverage gate (per-dir branches ≥ 90% on parser/model/helpers/serialize/builder) enforced on every PR across Node 18/20/22 matrix. Phases 10-12 are v2.1-audit-follow-on tech-debt gap-closure phases (Phase 10: planning-doc resync — IN PROGRESS; Phase 11: retroactive VERIFICATION.md for Phases 01/08/09 — PLANNED; Phase 12: retroactive Nyquist VALIDATION.md for Phases 01/02/03/07/08/09 — PLANNED) and are tracked separately from the v1 milestone. Next: finish Phase 10 gap-closure plans (10-01/10-02/10-03/10-04) → /gsd-execute-phase 11 → /gsd-execute-phase 12."
last_updated: "2026-04-20T21:00:00Z"
progress:
  total_phases: 9
  completed_phases: 9
  total_plans: 46
  completed_plans: 46
  percent: 100
---
```

Plan counts sum per ROADMAP Progress table (Plan 10-02 Task 2): 4 + 7 + 4 + 4 + 5 + 6 + 7 + 5 + 4 = 46.

`total_phases: 9` (core milestone span only — Phases 10/11/12 are follow-on tech-debt closure, tracked in body text not the summary rollup).

Do NOT edit the body of the file (lines 14+) in this task — that is Task 2.
  </action>
  <verify>
    <automated>grep -c 'v2.1 MILESTONE COMPLETE' .planning/STATE.md — expect `≥ 1`. Also: grep -c 'total_phases: 9' .planning/STATE.md — expect `1`. Also: grep -c 'completed_phases: 9' .planning/STATE.md — expect `1`.</automated>
  </verify>
  <done>
Frontmatter replaced. `status:` field reflects v2.1 milestone complete; `progress:` block shows 9/9/46/46/100%. `last_updated` bumped to 2026-04-20T21:00:00Z.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resync STATE.md body — Current Position + Performance Metrics + progress bar</name>
  <files>.planning/STATE.md</files>
  <action>
Edit three blocks in the body of `.planning/STATE.md`:

**Block A — `## Project Reference` (lines 20-26):** Update "Current focus" bullet (line 25) only. Leave the other bullets (name, core value, workflow config) verbatim.

Replace the line that currently begins:
```
- **Current focus:** Phase 7 — Testing Hardening & Fixtures (EXECUTION COMPLETE 2026-04-19 — all 7 plans done + all 8 TEST-01..08 REQ-IDs closed)…
```
(Multi-line — the whole bullet, starting at `- **Current focus:**` and ending before `- **Workflow config:**`.)

With this single bullet:
```
- **Current focus:** v2.1 MILESTONE COMPLETE (2026-04-20). All 9 phases shipped (Phases 1-9), 97/97 v1 REQ-IDs closed, package renamed to @cosyte/hl7 at 0.1.0, 824 tests pass + 14 it.todo, full pipeline green including pnpm publish --dry-run (10-file/346.2kB tarball). Gap-closure Phase 10 (planning-doc resync) IN PROGRESS; Phases 11 (retroactive VERIFICATION.md × 3) + 12 (retroactive Nyquist VALIDATION.md × 6) queued — both are v2.1-audit-follow-on tech-debt phases, not part of the v1 milestone span.
```

**Block B — `## Current Position` (lines 28-41):** Replace ENTIRELY. Current content describes Phase 7 pending. Replace with:

```
## Current Position

Phase: v2.1 MILESTONE COMPLETE — all 9 core-milestone phases shipped (Phases 1-9). Gap-closure Phase 10 (planning-doc resync) in progress; Phases 11-12 (retroactive VERIFICATION.md + Nyquist VALIDATION.md) planned.
Next Step: complete Phase 10 gap-closure plans (10-01 REQUIREMENTS.md / 10-02 ROADMAP.md / 10-03 STATE+PROJECT / 10-04 Phase-2 VERIFICATION TOL-08 retirement), then `/gsd-execute-phase 11` → `/gsd-execute-phase 12` → `/gsd-complete-milestone v2.1`.

- **Milestone:** v2.1 (v1 code ships under this label; paper-trail gaps closed in Phases 10-12 before milestone close)
- **Phase:** 9 (rename to @cosyte/hl7) — COMPLETE 2026-04-20. All 4 plans (09-01 identity files / 09-02 source-and-tests / 09-03 examples-and-starter-kit / 09-04 verification-and-publish-dry-run) done. Plan 09-04 SUMMARY confirms: grep for `@cosyte/hl7-parser` outside CHANGELOG breadcrumb = 0 matches; full pipeline green; pnpm publish --dry-run clean 10-file/346.2kB tarball under the new name.
- **Plans (milestone total):** 46 complete — 4 Phase 1 + 7 Phase 2 (incl. 02-07 gap-closure) + 4 Phase 3 + 4 Phase 4 + 5 Phase 5 + 6 Phase 6 + 7 Phase 7 + 5 Phase 8 + 4 Phase 9.
- **Status:** v2.1 core milestone COMPLETE; Phase 10 (planning-doc resync) IN PROGRESS; Phases 11 + 12 PLANNED (both v2.1-audit tech-debt follow-on).
- **Progress:** 9/9 core-milestone phases complete; 6/9 with VERIFICATION.md on disk (Phases 2, 3, 4, 5, 6; Phase 2 via 02-VERIFICATION.md with TOL-08 deferred-now-satisfied note pending retirement in Plan 10-04); 3/9 missing VERIFICATION.md (Phases 01, 08, 09 — closed in Phase 11). 3/9 with VALIDATION.md (Phases 04, 05, 06); 6/9 missing VALIDATION.md (Phases 01, 02, 03, 07, 08, 09 — closed in Phase 12).

```
[████████████████████] 100%   (9 / 9 core-milestone phases shipped; v2.1 audit tech-debt closure tracked in Phases 10-12)
```
```

**Block C — `## Performance Metrics` section's four lead-off bullets (lines 43-48):** Replace the four bullets that begin with `- **Phases completed:**`, `- **Plans completed:**`, `- **REQ-IDs validated:**`, and `- **Known coverage:**` with:

```
- **Phases completed:** 9 / 9 core-milestone (v1 span). Phase 1 completed 2026-04-18 (evidence-only, VERIFICATION.md pending Phase 11); Phase 2 verified 2026-04-18 (02-VERIFICATION.md with TOL-08 deferred-now-satisfied note pending retirement in Plan 10-04); Phase 3 verified 2026-04-18; Phase 4 verified 2026-04-19; Phase 5 verified 2026-04-19; Phase 6 verified 2026-04-19; Phase 7 completed 2026-04-19 (evidence-only); Phase 8 completed 2026-04-20 (evidence-only); Phase 9 completed 2026-04-20 (evidence-only).
- **Plans completed:** 46 / 46 — 4 Phase 1 + 7 Phase 2 (incl. 02-07 gap-closure) + 4 Phase 3 + 4 Phase 4 + 5 Phase 5 + 6 Phase 6 + 7 Phase 7 + 5 Phase 8 + 4 Phase 9.
- **REQ-IDs validated:** 97 / 97. All v1 categories closed: 6 SETUP + 9 PARSE + 9 TOL + 1 TOL-10 + 7 MODEL + 4 TYPES + 7 HELPERS + 6 SER + 9 PROF + 6 BIP + 8 TEST + 3 EX + 7 KIT + 15 DOC = 97 (no orphans; see `.planning/REQUIREMENTS.md` Traceability table after Plan 10-01 resync).
- **Known coverage:** 824 tests pass + 14 documented `it.todo` across 59 files (final v2.1 tally from 09-04 pipeline). Coverage gate ≥ 90% per-dir branches on parser/model/helpers/serialize/builder enforced in CI via `pnpm test:coverage` on every PR/push across Node 18/20/22 matrix. Starter-kit subtree: 4/4 tests green against ZAL sample fixture. Examples: 3/3 runnable via `pnpm examples`. Tarball dry-run: 10 files / 346.2 kB.
```

Leave the per-plan stats table (lines 50-88) UNTOUCHED — it is an append-only historical log.

Leave `## Accumulated Context` (line 89+) and subsequent bullets UNTOUCHED — they are append-only historical decision logs.

Leave `## Session Continuity` (line 212+) UNTOUCHED — it is an append-only breadcrumb log. (The last `*Last updated:` line is covered in Task 3.)

Do NOT touch PROJECT.md in this task — that is Task 4.
  </action>
  <verify>
    <automated>grep -c 'v2.1 MILESTONE COMPLETE' .planning/STATE.md — expect `≥ 2` (frontmatter + body). Also: grep -c 'Phase 7 — Testing Hardening & Fixtures (EXECUTION COMPLETE' .planning/STATE.md — expect `0` (stale Current-Position phrase gone).</automated>
  </verify>
  <done>
Current Position rewritten to reflect v2.1 milestone complete; Performance Metrics rollup bullets updated to 9/9 phases + 46/46 plans + 97/97 REQ-IDs + 824 tests. Progress bar flipped to 100%. Historical breadcrumb logs (per-plan table, Accumulated Context, Session Continuity) untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md footer "Last updated" stamp</name>
  <files>.planning/STATE.md</files>
  <action>
Edit the final footer line of `.planning/STATE.md` (currently line 231).

Replace the current `*Last updated: 2026-04-20 (Phase 8 Plan 02 EXECUTED …` line with:

```
*Last updated: 2026-04-20 (Phase 10 gap-closure Plan 10-03 — STATE.md resynced to v2.1 post-rename complete: frontmatter `status` + `progress` block reflect 9/9 phases shipped + 46/46 plans + 97/97 REQ-IDs closed + 824 tests pass; Current Position + Performance Metrics rollup updated; historical breadcrumb logs preserved unchanged. PROJECT.md capabilities checklist flipped in Task 4 of same plan.)*
```

Do NOT modify any other line.
  </action>
  <verify>
    <automated>grep -c 'Phase 10 gap-closure Plan 10-03' .planning/STATE.md — expect `≥ 1`.</automated>
  </verify>
  <done>
STATE.md footer cites Plan 10-03.
  </done>
</task>

<task type="auto">
  <name>Task 4: Flip PROJECT.md top-level capabilities checklist + fix package-name references</name>
  <files>.planning/PROJECT.md</files>
  <action>
Edit `.planning/PROJECT.md`.

**Block A — Flip the 11 capability checkboxes (lines 23-33):**

Each bullet flips `- [ ]` → `- [x]` with the same surrounding text. Append a short italic REQ-ID citation in parentheses so the checkbox state is justified by the Traceability source. Format: `- [x] <text> _(REQ-IDs: …)_`.

Exact replacements (line numbers based on current HEAD):
1. Line 23: `- [ ] Parse any well-formed HL7 v2.1–v2.8 message into a typed object model` → `- [x] Parse any well-formed HL7 v2.1–v2.8 message into a typed object model _(PARSE-01..09, MODEL-01..07, TYPES-01..04; shipped Phase 2-3)_`
2. Line 24: `- [ ] Named helpers for common extractions (`msg.patient.mrn`, `msg.meta.type`, etc.)` → `- [x] Named helpers for common extractions (`msg.patient.mrn`, `msg.meta.type`, etc.) _(HELPERS-01..07; shipped Phase 4)_`
3. Line 25: `- [ ] Dot-path accessors (`msg.get('PID.5.1')`)` → `- [x] Dot-path accessors (`msg.get('PID.5.1')`) _(MODEL-01/02/05; shipped Phase 3 Plan 01)_`
4. Line 26: `- [ ] Structural access to every segment, field, component, subcomponent, repetition` → `- [x] Structural access to every segment, field, component, subcomponent, repetition _(MODEL-03/04, TYPES-01..04; shipped Phase 3 Plans 01-03)_`
5. Line 27: `- [ ] Round-trip serialization (parse → modify → `toString()`) produces valid HL7` → `- [x] Round-trip serialization (parse → modify → `toString()`) produces valid HL7 _(SER-01..06; shipped Phase 5)_`
6. Line 28: `- [ ] Lenient default parsing with a 4-tier deviation model (silent/warn/fatal/strict-only)` → `- [x] Lenient default parsing with a 4-tier deviation model (silent/warn/fatal/strict-only) _(TOL-01..09, PARSE-03 escapes; shipped Phase 2)_`
7. Line 29: `- [ ] First-class `defineProfile()` API for vendor- and integration-specific quirks` → `- [x] First-class `defineProfile()` API for vendor- and integration-specific quirks _(PROF-01..09; shipped Phase 6)_`
8. Line 30: `- [ ] 5 built-in vendor profiles (Epic, Cerner, Meditech, athenahealth, generic lab)` → `- [x] 5 built-in vendor profiles (Epic, Cerner, Meditech, athenahealth, generic lab) _(BIP-01..06; shipped Phase 6 Plan 05-06)_`
9. Line 31: `- [ ] Profile starter kit (`examples/profile-starter-kit/`) that ships publishable as-is` → `- [x] Profile starter kit (`examples/profile-starter-kit/`) that ships publishable as-is _(KIT-01..07; shipped Phase 8 Plan 02)_`
10. Line 32: `- [ ] Zero runtime dependencies; dual ESM + CJS build; strict TypeScript` → `- [x] Zero runtime dependencies; dual ESM + CJS build; strict TypeScript _(SETUP-02/03/04/05/06; shipped Phase 1)_`
11. Line 33: `- [ ] Three runnable examples + comprehensive README with cookbook` → `- [x] Three runnable examples + comprehensive README with cookbook _(EX-01..03, DOC-01..15; shipped Phase 8 Plans 01 + 03-04)_`

**Block B — Fix package-name references post-Phase-9 rename:**

Edit line 1 (H1 title):
Old: `# @cosyte/hl7-parser`
New: `# @cosyte/hl7`

Edit line 60 (Constraints bullet — Package manager / name):
Old: `- **Package manager:** pnpm. Package name: `@cosyte/hl7-parser`. License: MIT.`
New: `- **Package manager:** pnpm. Package name: `@cosyte/hl7` (renamed from `@cosyte/hl7-parser` in Phase 9, 2026-04-20). License: MIT.`

DO NOT modify:
- Line 5 narrative ("HL7 v2 parser and utility library") — "parser" here is a feature descriptor, not the package name.
- Lines 7-9 (Core Value) — same.
- The Requirements → Active pointer (line 19) — `REQUIREMENTS.md` filename is unchanged; no rename needed.
- The Key Decisions table Outcome column (lines 66-77) — out of scope per ground-truth note; a later phase can refresh.

**Block C — Update PROJECT.md footer timestamp (line 97):**

Old: `*Last updated: 2026-04-18 after initialization*`
New: `*Last updated: 2026-04-20 (Phase 10 gap-closure Plan 10-03 Task 4 — capabilities checklist 11/11 flipped to `[x]` with REQ-ID traceability; package-name references updated post-Phase-9 rename to `@cosyte/hl7`.)*`
  </action>
  <verify>
    <automated>grep -cE '^- \[ \]' .planning/PROJECT.md — expect `0` (no stale capability checkboxes). Also: grep -c '^# @cosyte/hl7$' .planning/PROJECT.md — expect `1` (H1 matches new package name). Also: grep -c 'Phase 10 gap-closure Plan 10-03 Task 4' .planning/PROJECT.md — expect `1`.</automated>
  </verify>
  <done>
PROJECT.md capabilities checklist fully flipped (11/11 `[x]` with REQ-ID traceability); H1 + Constraints package-name reference flipped to `@cosyte/hl7`; footer stamps Plan 10-03 Task 4.
  </done>
</task>

</tasks>

<verification>
After all 4 tasks land, verify these grep invariants:

**STATE.md:**
1. `grep -c 'v2.1 MILESTONE COMPLETE' .planning/STATE.md` → `≥ 2` (frontmatter + body)
2. `grep -c 'total_phases: 9' .planning/STATE.md` → `1`
3. `grep -c 'completed_phases: 9' .planning/STATE.md` → `1`
4. `grep -c 'percent: 100' .planning/STATE.md` → `1`
5. `grep -c 'Phase 7 — Testing Hardening & Fixtures (EXECUTION COMPLETE' .planning/STATE.md` → `0` (stale phrase gone)
6. `grep -c 'Phase 10 gap-closure Plan 10-03' .planning/STATE.md` → `≥ 1`

**PROJECT.md:**
7. `grep -cE '^- \[ \]' .planning/PROJECT.md` → `0` (no stale capability checkboxes)
8. `grep -cE '^- \[x\]' .planning/PROJECT.md` → `≥ 11` (all 11 capability checkboxes flipped)
9. `grep -c '^# @cosyte/hl7$' .planning/PROJECT.md` → `1` (H1 matches new name)
10. `grep -c '^# @cosyte/hl7-parser$' .planning/PROJECT.md` → `0` (old H1 gone)
11. `grep -c 'Phase 10 gap-closure Plan 10-03 Task 4' .planning/PROJECT.md` → `1`
</verification>

<success_criteria>
- STATE.md frontmatter fully replaced with v2.1-milestone-complete snapshot (status + progress + last_updated).
- STATE.md body: `Current Position` + `Performance Metrics` rollup rewritten; historical breadcrumb logs (per-plan table, Accumulated Context, Session Continuity) preserved verbatim.
- STATE.md footer cites Plan 10-03.
- PROJECT.md all 11 capability bullets flipped to `[x]` with REQ-ID citations.
- PROJECT.md H1 + Constraints package-name reference flipped to `@cosyte/hl7`.
- PROJECT.md footer cites Plan 10-03 Task 4.
- Two atomic commits: `docs(10-03): resync STATE.md to v2.1 post-rename complete` and `docs(10-03): flip PROJECT.md capabilities checklist + package-name references`.
</success_criteria>

<output>
After completion, create `.planning/phases/10-planning-doc-resync/10-03-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md` template. Include:
- Final grep counts for all 11 verification gates.
- Confirmation that historical breadcrumb logs in STATE.md (per-plan stats table, Accumulated Context, Session Continuity) were preserved verbatim.
- Confirmation that the Key Decisions table Outcome column in PROJECT.md was intentionally left unchanged (out of scope; later phase may refresh).
</output>
