---
gsd_state_version: 1.0
milestone: v2.1
milestone_name: milestone
status: "v2.1 MILESTONE COMPLETE 2026-04-20 — all 9 phases shipped (Phases 1-9). Package renamed to @cosyte/hl7 at 0.1.0 (Phase 9); 97/97 v1 REQ-IDs closed; 824 tests pass + 14 documented it.todo across 59 files; full pipeline now includes format:check (added during Phase 11 — install/typecheck/lint/format:check/test/build/examples), kit subtree install+test+build green, starter-kit ci.yml + publish.yml actionlint-clean, pnpm publish --dry-run produces clean 10-file/346.2kB tarball; coverage gate (per-dir branches ≥ 90% on parser/model/helpers/serialize/builder) enforced on every PR across Node 18/20/22 matrix. Phases 10-12 are v2.1-audit-follow-on tech-debt gap-closure phases (Phase 10: planning-doc resync — COMPLETE 2026-04-21 (4/4 plans shipped, verification passed 14/14); Phase 11: retroactive VERIFICATION.md for Phases 01/08/09 — COMPLETE 2026-04-21 (3/3 plans shipped, verifier passed; orchestrator landed prerequisite prettier sweep e1c9ee4 to clear 47-file pre-existing drift on main); Phase 12: retroactive Nyquist VALIDATION.md for Phases 01/02/03/07/08/09 — COMPLETE 2026-04-21 (6/6 plans shipped, verifier passed 7/7 SCs via commit `063f004`; paper trail now 9/9 VALIDATION.md on disk)) and are tracked separately from the v1 milestone. Next: /gsd-complete-milestone v2.1."
last_updated: "2026-04-21T12:00:00Z"
progress:
  total_phases: 9
  completed_phases: 9
  total_plans: 46
  completed_plans: 46
  percent: 100
---

# @cosyte/hl7-parser — STATE

Project memory for session-to-session continuity. Updated at phase/plan boundaries.

---

## Project Reference

- **Name:** `@cosyte/hl7-parser`
- **Core value:** A developer can parse a real-world, vendor-quirky HL7 v2 message and pull useful fields out of it in one line — without having read the HL7 spec.
- **Current focus:** v2.1 MILESTONE COMPLETE (2026-04-20). All 9 phases shipped (Phases 1-9), 97/97 v1 REQ-IDs closed, package renamed to @cosyte/hl7 at 0.1.0, 824 tests pass + 14 it.todo, full pipeline green including pnpm publish --dry-run (10-file/346.2kB tarball). Gap-closure Phase 10 (planning-doc resync) COMPLETE 2026-04-21 (4/4 plans, verification 14/14); Phase 11 (retroactive VERIFICATION.md × 3) COMPLETE 2026-04-21 (3/3 plans + verifier passed; prereq commit e1c9ee4 prettier sweep cleared 47-file pre-existing drift); Phase 12 (retroactive Nyquist VALIDATION.md × 6) COMPLETE 2026-04-21 (6/6 plans + verifier passed 7/7 SCs, commit `063f004`; paper trail now 9/9 VALIDATION.md on disk) — all three are v2.1-audit-follow-on tech-debt phases, not part of the v1 milestone span.
- **Workflow config:** standard granularity, yolo mode, parallelization enabled, plan-check + verifier + Nyquist validation on, auto-advance on.

## Current Position

Phase: v2.1 MILESTONE COMPLETE — all 9 core-milestone phases shipped (Phases 1-9). Gap-closure Phase 10 (planning-doc resync) COMPLETE 2026-04-21; Phase 11 (retroactive VERIFICATION.md × 3) COMPLETE 2026-04-21 (3/3 plans shipped + verifier passed; orchestrator landed prereq commit e1c9ee4 prettier sweep — 47 files of pre-existing drift cleared, milestone-close paper trail now self-consistent); Phase 12 (retroactive Nyquist VALIDATION.md × 6) COMPLETE 2026-04-21 (6/6 plans shipped + verifier passed 7/7 SCs, commit `063f004`; v2.1 paper trail now at 9/9 VALIDATION.md + 9/9 VERIFICATION.md on disk).
Next Step: v2.1 milestone ready to close — all three gap-closure phases (10/11/12) done. Run: `/clear` → `/gsd-complete-milestone v2.1`.

- **Milestone:** v2.1 (v1 code ships under this label; paper-trail gaps closed in Phases 10-12 before milestone close)
- **Phase:** 9 (rename to @cosyte/hl7) — COMPLETE 2026-04-20. All 4 plans (09-01 identity files / 09-02 source-and-tests / 09-03 examples-and-starter-kit / 09-04 verification-and-publish-dry-run) done. Plan 09-04 SUMMARY confirms: grep for `@cosyte/hl7-parser` outside CHANGELOG breadcrumb = 0 matches; full pipeline green; pnpm publish --dry-run clean 10-file/346.2kB tarball under the new name.
- **Plans (milestone total):** 46 complete — 4 Phase 1 + 7 Phase 2 (incl. 02-07 gap-closure) + 4 Phase 3 + 4 Phase 4 + 5 Phase 5 + 6 Phase 6 + 7 Phase 7 + 5 Phase 8 + 4 Phase 9.
- **Status:** v2.1 core milestone COMPLETE; Phase 10 (planning-doc resync) COMPLETE 2026-04-21; Phase 11 (retroactive VERIFICATION.md × 3) COMPLETE 2026-04-21; Phase 12 (retroactive Nyquist VALIDATION.md × 6) COMPLETE 2026-04-21 (all three v2.1-audit tech-debt follow-on phases now shipped).
- **Progress:** 9/9 core-milestone phases complete; 9/9 with VERIFICATION.md on disk after Phase 11; **9/9 with VALIDATION.md on disk after Phase 12** (Phases 04/05/06 pre-existed; Phases 01/02/03/07/08/09 added 2026-04-21 via State-B reconstruction from SUMMARY evidence). Phase 10 gap-closure: 4/4 plans complete 2026-04-21. Phase 11 gap-closure: 3/3 plans complete + verifier passed 2026-04-21; orchestrator landed prereq commit `e1c9ee4` (`style(v2.1-close): prettier --write sweep` — 47 files of pre-existing drift cleared so SC-1's `pnpm format:check` gate passes for Phase 1's retroactive verification). Phase 12 gap-closure: 6/6 plans complete + verifier passed 7/7 SCs 2026-04-21 (commit `063f004`); all 6 retroactive VALIDATION.md files Prettier-clean, placeholder-free, with full per-REQ-ID test-coverage classification.

```
[████████████████████] 100%   (9 / 9 core-milestone phases shipped; v2.1 audit tech-debt closure tracked in Phases 10-12)
```

## Performance Metrics

- **Phases completed:** 9 / 9 core-milestone (v1 span). Phase 1 completed 2026-04-18 (evidence-only; retroactive 01-VERIFICATION.md ratified 2026-04-21 by Plan 11-01); Phase 2 verified 2026-04-18 (02-VERIFICATION.md TOL-08 deferral retired 2026-04-20 by Plan 10-04); Phase 3 verified 2026-04-18; Phase 4 verified 2026-04-19; Phase 5 verified 2026-04-19; Phase 6 verified 2026-04-19; Phase 7 completed 2026-04-19 (07-VERIFICATION.md on disk per audit 2026-04-21); Phase 8 completed 2026-04-20 (evidence-only; retroactive 08-VERIFICATION.md ratified 2026-04-21 by Plan 11-02); Phase 9 completed 2026-04-20 (evidence-only; retroactive 09-VERIFICATION.md ratified 2026-04-21 by Plan 11-03). All 9 v1 phases now have VERIFICATION.md on disk.
- **Plans completed:** 46 / 46 — 4 Phase 1 + 7 Phase 2 (incl. 02-07 gap-closure) + 4 Phase 3 + 4 Phase 4 + 5 Phase 5 + 6 Phase 6 + 7 Phase 7 + 5 Phase 8 + 4 Phase 9.
- **REQ-IDs validated:** 97 / 97. All v1 categories closed: 6 SETUP + 9 PARSE + 9 TOL + 1 TOL-10 + 7 MODEL + 4 TYPES + 7 HELPERS + 6 SER + 9 PROF + 6 BIP + 8 TEST + 3 EX + 7 KIT + 15 DOC = 97 (no orphans; see `.planning/REQUIREMENTS.md` Traceability table after Plan 10-01 resync).
- **Known coverage:** 824 tests pass + 14 documented `it.todo` across 59 files (final v2.1 tally from 09-04 pipeline). Coverage gate ≥ 90% per-dir branches on parser/model/helpers/serialize/builder enforced in CI via `pnpm test:coverage` on every PR/push across Node 18/20/22 matrix. Starter-kit subtree: 4/4 tests green against ZAL sample fixture. Examples: 3/3 runnable via `pnpm examples`. Tarball dry-run: 10 files / 346.2 kB.

| Phase | Plan | Duration | Tasks | Files |
|-------|------|----------|-------|-------|
| 1 | 01 package-scaffold | 2 min | 3 | 9 |
| 1 | 02 build-system | 1 min | 2 | 1 |
| 1 | 03 lint-and-test | 2 min | 4 | 6 |
| 1 | 04 smoke-verification | 4 min | 2 (+ 2 auto-fix commits) | 2 created, 4 modified |
| 2 | 01 warnings/errors/message-shell | 8 min | 3 (2 TDD cycles) | 8 created |
| 2 | 04 escape-sequences | 6 min | 1 (1 TDD cycle) | 2 created |
| 2 | 05 dateFormats-plumbing | 3 min | 1 (1 TDD cycle) | 2 created |
| 2 | 06 parseHL7-public-and-strict-mode | 4 min | 1 (1 TDD cycle) | 2 created, 1 modified |
| 3 | 01 read-path-foundation | 11 min | 3 (3 TDD cycles) | 7 created, 4 modified |
| 3 | 02 composites-person-address-identifier | 5 min | 3 (3 TDD cycles) | 14 created (7 src + 7 test) |
| 3 | 03 composites-telecom-location-timestamp-numeric | 4 min | 3 (handshake + 2 TDD cycles) | 8 created (4 src + 4 test) |
| 3 | 04 mutation-and-barrel | 8 min | 3 (2 TDD + 1 barrel) | 5 created, 4 modified |
| 4 | 01 scaffold-xcn-and-cache | 12 min | 3 (2 TDD + 1 wiring) | 15 created, 5 modified |
| 4 | 02 meta-and-patient | 10 min | 3 (3 TDD cycles) | 3 created, 2 modified |
| 4 | 03 visit-and-observations | 9 min | 3 (2 TDD + 1 cache-test + Rule 3 regex fix) | 3 created, 4 modified |
| 4 | 04 orders-and-collections | 8 min | 2 TDD cycles | 2 created, 5 modified |
| 5 | 01 scaffold-emit-field-and-method-wiring | 5 min | 4 (1 TDD + 3 scaffold) | 8 created, 3 modified |
| 5 | 02 to-string-and-round-trip | 15 min | 2 (1 TDD + 1 fixture sweep + 1 Rule-3 deviation) | 7 created, 2 modified |
| 5 | 03 to-json | 8 min | 1 (1 TDD cycle — RED + GREEN, no refactor needed) | 1 created, 1 modified |
| 5 | 04 pretty-print | 8 min | 1 (1 TDD cycle — RED + GREEN, no refactor needed) | 1 created, 1 modified |
| 5 | 05 build-message | 7 min | 3 (3 TDD cycles + 1 Rule-1 test fix + 1 Rule-3 lint fix) | 3 created, 3 modified (bodies only) |
| 6 | 01 defineProfile-core-and-validation | 10 min | 3 + 1 Rule-3 auto-fix (control-id.ts pre-existing lint blocker) + 1 Rule-1 test fix (YYY/MM contradiction) | 5 created (3 src/profiles + 1 test + 1 SUMMARY), 3 modified (types.ts, dates.ts, control-id.ts) |
| 6 | 02 extends-merge-semantics | 5 min | 2 + 3 Rule-1 inline fixes (onWarning reference-identity test updated for composed chain, `as Hl7ParseWarning` → typed declaration, Array.isArray cast) | 3 created (merge.ts + extends test + SUMMARY), 2 modified src (define.ts, validate.ts) + 1 modified test (profiles-define.test.ts) |
| 6 | 03 segment-get-and-unknown-segment-and-D22-hoist | 10 min | 4 + 2 Rule-1 inline fixes (discriminateOptionsOrProfile narrowed to truly-options-only keys + test-scope adjustment for TIMESTAMP_FALLBACK_FORMAT warning which requires eager MSH-7 parse outside Plan 06-03 scope) | 4 created (known-segments.ts + 2 test files + SUMMARY), 4 modified src (segment.ts, message.ts, parser/index.ts, helpers/meta.ts) |
| 6 | 04 default-profile-management | 3 min | 2 (both TDD) — ZERO deviations | 3 created (src/profiles/default.ts, test/profiles-default.test.ts, SUMMARY), 1 modified (src/parser/index.ts Step 6.5 + 1 new import) |
| 6 | 05 built-in-vendor-profiles-and-fixtures | 4 min | 2 + 1 Rule-3 auto-fix (jsdoc/require-jsdoc on `export const <vendor>` — added export-adjacent JSDoc block alongside file-header block) | 11 created (5 src/profiles/<vendor>.ts + 5 test/fixtures/vendor-shapes/**/*.hl7 + 1 SUMMARY), 0 modified |
| 6 | 06 barrel-and-fixture-tests | 3 min | 2 + 1 Rule-1 test-assertion fix (athena ZCA providerName — fixture stops at field 5 so assertion updated to exercise D-14 undefined-for-out-of-range contract instead) | 3 created (src/profiles/index.ts + test/profiles-builtins.test.ts + SUMMARY), 1 modified (src/index.ts Phase 6 block appended) |
| 7 | 01 fixture-migration-and-helpers | 5 min | 2 (Task 1 measurement, Task 2 migration+helpers) + 1 Rule-3 prettier --write on fixture-code.ts | 7 created (5 fixture renames via git mv + 2 helpers in test/_helpers/), 1 modified (test/round-trip.test.ts) |
| 7 | 02 canonical-fixtures-and-canonical-messages-test | 4 min | 2 (Task 1 fixtures+README, Task 2 test file) + 1 Rule-1 fixture-shape fix (trailing empty subcomponents) + 1 Rule-3 raw-tree subcomponent access | 9 created (7 canonical fixtures + canonical README + canonical-messages.test.ts), 0 modified |
| 7 | 03 edge-case-fixtures-and-parser-edge-cases-test | 10 min | 2 (Task 1 fixtures+README, Task 2 test file) + 1 Rule-1 encodingCharacters property-name correction (.field/.component not .fieldSeparator/.componentSeparator) | 13 created (11 edge-case fixtures + edge-cases README + parser-edge-cases.test.ts), 0 modified |
| 7 | 04 vendor-quirks-fixtures-and-strict-mode-sweep | 12 min | 2 (Task 1 13 fixtures+README+generator, Task 2 sweep) — zero deviations | 16 created (13 fixtures + README + scripts/write-vendor-quirks.mjs + parser-strict-mode-sweep.test.ts), 0 modified |
| 7 | 05 malformed-fixtures-and-sweep | 8 min | 2 (Task 1 4 fixtures+README, Task 2 sweep) — zero deviations | 6 created (4 malformed fixtures + README + parser-malformed-sweep.test.ts), 0 modified |
| 7 | 07 test-08-audit-and-test-07-confirmation | 3 min | 1 executed (Task 1 audit doc); Tasks 2+3 no-op per plan's conditional branches (0 TEST-08 gaps found; TEST-07 already closed by Phase 6 BIP-06) — zero deviations | 2 created (TEST-08-AUDIT.md planning artifact + 07-07-SUMMARY.md), 0 modified (test suite untouched) |
| 7 | 06 coverage-gate-tighten-and-ci-wiring (Wave 3 capstone) | 3 min | 2 executed (Task 1 vitest branches 85→90 + Task 4 CI coverage step); Task 2 no-op (coverage passed first-run post-bump); Task 3 checkpoint auto-approved per --auto mode + 1 Rule-1 inline fix (initial global-threshold bump broke profiles/** ungated-by-design — reverted global to 85, kept per-dir at 90; fix rolled into Task 1 commit) | 0 created, 2 modified (vitest.config.ts threshold block + .github/workflows/ci.yml new step) |
| 8 | 01 examples-starter-kit-ex-01-02-03 (Wave 1) | — (completed 2026-04-19) | 3 runnable example scripts + smoke-runner + 3 synthetic fixtures + README | 8 created |
| 8 | 02 profile-starter-kit-subtree (Wave 1) | 7 min | 3 (Task 1 configs+source+fixture+LICENSE, Task 2 docs+workflows, Task 3 smoke-verify — zero code changes) + 3 deviations (Rule-3 devDep `file:../..` vs plan's `workspace:*`, Rule-2 README/CUSTOMIZING devDep replacement note, Rule-2 pnpm-lock.yaml in kit .gitignore) | 15 created (examples/profile-starter-kit/**: package.json + 4 toolchain configs + .prettierrc.json + .gitignore + src/index.ts + test/profile.test.ts + test/fixtures/sample.hl7 + 2 .github/workflows/*.yml + README.md + CUSTOMIZING.md + LICENSE), 0 modified |
| 10 | 01 requirements-traceability-resync | 8 min | 4 (Task 1 PARSE+TOL checkboxes, Task 2 EX+KIT+DOC checkboxes, Task 3 35 Pending traceability rows, Task 4 footer timestamp) — zero deviations | 0 created, 1 modified (.planning/REQUIREMENTS.md — 42 checkbox flips + 35 Pending→Closed row flips + 1 footer) |
| 10 | 04 phase-2-tol08-deferral-retirement | 12 min | 5 (Task 1 frontmatter, Task 2 Resolution Note, Task 3 body headers, Task 4 table rows x7+1 deviation, Task 5 Gaps+Verdict+footer) — 1 Rule-1 deviation (unescape key-link row ⚠️ glyph removed) | 0 created, 1 modified (.planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md — frontmatter retired_overrides: + Resolution Note rewrite + 9 table-row flips + Verdict rewrite + footer append) |

## Accumulated Context

### Roadmap Evolution

- Phase 9 added: Rename package to `@cosyte/hl7` (full HL7 toolkit — parser, builder, mutator, serializer, helpers — not just a parser).

### Key Decisions (carry-forward from PROJECT.md)

- Lenient parsing is the default; strict is opt-in.
- Warnings carry stable string codes + positional context.
- Profiles are plain data produced by `defineProfile()`; built-ins use the public API.
- Serializer always emits spec-clean HL7 (Postel's Law: conservative emitter).
- Profile starter kit is a first-class deliverable.
- Zero runtime dependencies.
- Fatal errors limited to 4 Tier-3 codes: `NO_MSH_SEGMENT`, `MSH_TOO_SHORT`, `INVALID_ENCODING_CHARACTERS`, `EMPTY_INPUT`.
- `setDefaultProfile()` exists but is discouraged; process-scoped, not shared across workers.
- Zero runtime dependencies enforced via explicit empty `dependencies: {}` block in package.json (Plan 01-01).
- Strict TypeScript goes beyond `strict: true` — also `exactOptionalPropertyTypes`, `useUnknownInCatchVariables`, `noPropertyAccessFromIndexSignature` (Plan 01-01).
- `packageManager: pnpm@9.0.0` pinned for reproducibility (Plan 01-01).
- Wave 2 plans (02, 03) must NOT modify package.json — all devDeps and scripts are already wired in Plan 01 (Plan 01-01).
- tsup outExtension override forces `.mjs`/`.cjs` suffixes because tsup's defaults would not match the `exports` map in package.json (Plan 01-02).
- `skipNodeModulesBundle: true` in tsup.config keeps zero-runtime-deps posture honest — any future runtime dep would require an explicit architectural decision (Plan 01-02).
- ESLint flat config (eslint.config.js) — no legacy .eslintrc.*; eslint-config-prettier MUST be the last element in the default-exported array so Prettier-compat rules win (Plan 01-03).
- Type-aware lint rules (no-unsafe-*, no-floating-promises) enabled via `parserOptions.project: "./tsconfig.json"` — catches bugs strict tsc alone wouldn't flag at author time (Plan 01-03).
- CLAUDE.md guardrails (no any, no unjustified as, JSDoc+@example on public exports, no console) enforced as ESLint errors — not just typecheck (Plan 01-03).
- Vitest config declares per-directory coverage thresholds (src/parser/**, src/model/**, src/helpers/** at ≥ 90% lines/functions/statements, ≥ 85% branches) now for shape stability; Phase 7 is the enforcement gate via `pnpm test:coverage` (Plan 01-03).
- Prettier config (`printWidth:100`, `tabWidth:2`, `semi:true`, `singleQuote:false`, `trailingComma:all`, `endOfLine:lf`) aligned with `.editorconfig` exactly; `.planning/` excluded to avoid markdown-code-fence reflow churn (Plan 01-03).
- Removed `rootDir: ./src` from base `tsconfig.json`: it forbade `tsc --noEmit` from typechecking test files and root-level config files that the `include` glob also matched. Emit scoping is handled by tsup's `entry` + `tsconfig.build.json`'s narrower `include` (Plan 01-04, Rule 1 integration bug fix).
- `src/index.ts` file-level JSDoc cannot start with `@{package-name}` pattern because `eslint-plugin-jsdoc`'s `check-tag-names` rule parses leading `@tokens` as tag names. Put the package name in backticks mid-sentence instead (Plan 01-04, Rule 1 fix).
- CI workflow at `.github/workflows/ci.yml` runs Node 18/20/22 matrix with `permissions: contents: read` + `concurrency` + `cancel-in-progress`. All actions pinned to `@v4` major tag. `pnpm install --frozen-lockfile` enforces lockfile consistency; final step re-runs ESM/CJS dual-module smoke to guard SETUP-02 (Plan 01-04).
- `pnpm-lock.yaml` is committed (not in `.gitignore`) for supply-chain reproducibility (T-01-01/T-01-04-01 mitigation) (Plan 01-04).
- Public `Hl7Message.segments: readonly RawSegment[]` field renamed to `rawSegments` in Phase 3 Plan 01 to free the name for the typed `segments(type): readonly Segment[]` method. Phase 2 D-05 constructor init key `segments` stays unchanged for backward compat with parser/index.ts (Plan 03-01).
- Wrapper caches use a master-cache-filtering pattern: `allSegments()` builds a document-order cache of Segment wrappers; `segments(type)` filters from it. This gives cross-cache Segment identity for free (`msg.segments('OBX')[0] === msg.allSegments().find(s => s.type === 'OBX')`) and lets Plan 04 mutation invalidate everything by dropping `_allSegments` in one line (Plan 03-01).
- MSH.N → fields[N-1] offset in dot-path resolver: Phase 2 tokenize placed the field separator at MSH `fields[0]` and encoding-chars at `fields[1]`, so MSH-1 through MSH-18 user-facing indices map to `fields[N-1]`. Non-MSH segments use a straight 1:1 mapping because `fields[0]` is the segment-name placeholder. Encoded as a one-line special case in `resolvePath` (Plan 03-01).
- Depth-collapse (D-04) needs no extra branch in the resolver — when a component has a single subcomponent, the normal walk already returns it for subcomponentIndex === 1. A comment documents the reasoning (Plan 03-01).
- `Field.empty(enc)` accepts `enc` for API symmetry but returns a module-scoped sentinel built with DEFAULT_ENCODING_CHARACTERS. The synthetic field has no content so the active encoding characters are irrelevant on reads. Stable sentinel satisfies MODEL-05 never-throws contract while preserving referential stability (Plan 03-01).
- Segment-name validation regex `^(?:[A-Z]{3}|Z[A-Z0-9]{2})$/u` in `parsePath` mirrors D-19 (addSegment) — avoids drift between path parsing and mutation validation (Plan 03-01).
- Composite parsers are silent (D-09). The shared `readSubcomponent` helper in `src/model/types/_shared.ts` passes a `NOOP_EMITTER` to `unescape` so any `UNKNOWN_ESCAPE_SEQUENCE` discovered at composite-read time is dropped. Best-effort position `{ segmentIndex: 0 }` — composites don't know their own tree position (Plan 03-02).
- Empty-string → undefined mapping centralised in `readSubcomponent`. HL7 parses `^^Middle^` with explicit `""` subcomponents; composites need to OMIT rather than record empty strings for `exactOptionalPropertyTypes` compliance. Pushing the mapping into the shared helper gives every composite uniform behaviour for free (Plan 03-02).
- Nested-composite synthesis pattern: when component N of composite A is shaped like composite B (e.g. CX.4 = HD), `parseA` synthesises a `RawRepetition` from `comp.subcomponents` and delegates to `parseB`. CX.assigningAuthority is the canonical example — no logic duplication, no special-case parser. Plan 03's XTN/PL may reuse the pattern (Plan 03-02).
- CX.assigningFacility flattened to `string` (v1 simplification — HL7 spec is HD-shaped). Precedent set by XPN.nameContext (CWE-shaped in spec, string in v1). Documented in CX interface JSDoc (Plan 03-02).
- CWE trimmed to 9 components for v1 (full v2.6+ has 22). The 9 core fields (identifier, text, coding systems, version ids, originalText) cover common HL7 v2.5 use cases; v2 may restore the full shape (Plan 03-02).
- `exactOptionalPropertyTypes`-compliant composite construction via `Mutable<T>` local + conditional assignment. `type Mutable<T> = { -readonly [K in keyof T]?: T[K] }` + per-field `if (x !== undefined) out.x = x` + implicit return. Passes `consistent-type-assertions: objectLiteralTypeAssertions: "never"` without any cast (Plan 03-02).
- Plan 02 and Plan 03 file ownership stays truly disjoint: Plan 02 ships 6 composites (XPN, XAD, CX, CWE, CE, HD) + `_shared.ts`; Plan 03 adds 4 (XTN, PL, TS, NM) and consumes `_shared.ts` + `hd.ts`. Zero shared-edit risk. Plan 04 handles `src/index.ts` barrel + `HL7` namespace + `Field.asXxx()` wiring (Plan 03-02).
- TS composite shape locked to `{ raw: string; date: Date | undefined }` per D-14 — BOTH keys ALWAYS present. `date` is typed as `Date | undefined` (not optional), so callers can destructure uniformly whether the parse succeeded or failed. NM composite parallels this with `{ raw: string; value: number | undefined }`. Distinguishes scalar composites from structured composites (XPN/XAD/etc all-optional) (Plan 03-03).
- `parseTs` delegates to `parseHl7Timestamp({})` — empty options, silent (no emit/position). Phase 4's `msg.meta.timestamp` helper may call `parseHl7Timestamp` DIRECTLY with user `dateFormats`/`emit`/`position`; the composite layer stays stateless so `Field.asTs()` works identically whether the user supplied dateFormats or not. D-10 zero duplicate date logic preserved (Plan 03-03).
- NM uses strict `Number(raw)` over `parseFloat(raw)` — trailing non-numeric characters invalidate the parse (`Number("12abc") === NaN` vs `parseFloat("12abc") === 12`). Matches developer expectation for HL7 NM fields that should be strictly numeric. Documented in NM JSDoc so maintainers don't "optimise" it back to parseFloat. Explicit empty-string pre-check prevents JS's `Number("") === 0` trap (Plan 03-03).
- D-24 NaN-gate layered on parseTs delegate output: `parsed !== undefined && !Number.isNaN(parsed.getTime())` prevents `new Date("20251345")` (month 13 → NaN time) from leaking as an Invalid Date. This is how TYPES-04 (no-throw) becomes observable at the composite layer — callers get undefined, never an Invalid Date (Plan 03-03).
- XTN trimmed from 14 → 12 for v1 (dropped 2 rarely-used legacy slots 13/14); PL trimmed from 12 → 11 for v1 (dropped entityIdentifier slot 12). Both trimming decisions interface-level only — parsers silently ignore additional trailing components from the raw tree. v2 may restore full shapes if vendor-quirk fixtures require them (Plan 03-03).
- PL.facility uses inline `parseFacility` nested-HD synthesis helper — mirrors `parseCx::parseAssigningAuthority` from Plan 02. Helper kept inline in pl.ts, NOT promoted to `_shared.ts`. Pattern now exists in 2 places (CX, PL); DRY threshold for promoting to `_shared` is 3 occurrences. Keeps `_shared.ts` focused on truly-universal helpers (readComponent, readSubcomponent) used by every composite (Plan 03-03).
- Field gains 10 `.asXxx()` composite coercions (asXpn, asXad, asCx, asCwe, asCe, asXtn, asPl, asTs, asNm, asHd). Each delegates to its Plan 02/03 parser with a shared frozen `EMPTY_REP` fallback so empty Fields return empty typed objects (`{}` for optional-field composites, `{raw:"", date:undefined}` / `{raw:"", value:undefined}` for TS/NM) without throwing. D-09 compliant — not memoized; two calls return distinct objects (Plan 03-04).
- `Hl7Message` gains 3 mutation methods: `setField(path, value)` (leaf-to-root rebuild with auto-create of missing rep/comp/sub within existing field; TypeError on missing segment), `addSegment(name, fields)` (D-19 segment-name regex), `removeSegment(type, occurrence | { all })` (MSH-protected, idempotent on unknowns). All three return `this` (D-15 chainable), invalidate both wrapper caches wholesale (D-17), and never touch the frozen warnings array (D-16). D-18: mutation value accepted verbatim — re-escape deferred to Phase 5 serializer (Plan 03-04).
- Segment.field() now applies a user-facing `n-1` MSH offset so `msg.segments('MSH')[0].field(3)` returns MSH-3 consistently with `msg.get('MSH.3')`. Plan 01 shipped dot-path with the offset but the wrapper without it — the asymmetry surfaced as a Rule 1 bug when coercion tests targeted MSH-3 (APP^1.2.3^UUID) and received MSH-4 (FAC) instead. Fixed in Plan 04 (Plan 03-04).
- Wholesale cache invalidation (drop both `_segmentsByType` and `_allSegments`) on every mutation, rather than per-type invalidation. D-17 letter said "invalidate the Segment/Field wrapper cache for the affected segment type"; wholesale is equally correct, simpler, and preserves Plan 01's cross-cache identity guarantee since `segments(type)` filters from `allSegments()` (Plan 03-04).
- HL7 namespace is types-only; parsers exported as named values alongside (D-13 "named exports AND re-exported under an HL7 namespace"). `src/model/types/namespace.ts` holds nothing but `export type { ... }`; `export * as HL7 from "./namespace.js"` in `src/index.ts` surfaces it. Consumers import types as either `import type { XPN }` or `HL7.XPN`; parsers as `import { parseXpn }` (Plan 03-04).
- `setField` auto-creates missing repetitions / components / subcomponents within an existing field, but does NOT auto-create missing segments. Throws `TypeError` with an actionable message (`Add it first with addSegment(...)`). Matches CONTEXT.md Claude's Discretion recommendation (Plan 03-04).
- `setField` rebuilds the affected path leaf-to-root (new RawComponent → new RawRepetition → new RawField → new RawSegment), keeping declared `readonly Raw*` shapes structurally immutable. Only readonly bypass: reassigning `this.rawSegments` (documented inline as D-16). ~125 object allocations per mutation — negligible (Plan 03-04).
- `emitField` is the SOLE D-04 reescape chokepoint — every other emitter (`emitMessage`, `emitPrettyPrint`, future Phase 6 profile hooks) composes on it and never calls `reescape` directly. Strips trailing empty components AND trailing empty subcomponents per D-02; preserves `RawField.isNull === true` as the two-character literal `""` (quote-quote). Zero module-level state, no caching (D-30) (Plan 05-01).
- `emitSegment` throws a loud `Error` on MSH input — the one deliberate deviation from D-07 "never throws" purity. Justified because silent mis-emission on MSH would re-escape the encoding chars in MSH-2 and produce garbage output downstream. Guards the D-06 routing contract: the only correct caller for MSH is `to-string.ts::emitMessage`, which inlines MSH-1/MSH-2 before handing MSH-3..N to `emitField` directly. Error message explicitly tells the caller to route through `to-string.ts`'s D-06 path (Plan 05-01).
- `Hl7Message.toString` / `toJSON` / `prettyPrint` are class-method-delegates-to-module-level-emitter (mirrors Phase 4 `observations()` delegating to `walkObservations`). Method body is a single-line `return emit*(this);`. Naming `toString` overrides `Object.prototype.toString` with matching return type (`(): string`) — TS + ESLint accept it without the `override` keyword (class extends nothing) (Plan 05-01).
- Stubs that throw a plan-identified `NOT IMPLEMENTED — Phase 5 Plan 0N` marker are the disjoint-file contract gate. Plans 02/03/04/05 each replace ONE function body (`emitMessage`, `emitJson`, `emitPrettyPrint`, `buildMessage` + its two helpers respectively); NOBODY touches `src/model/message.ts`, `src/index.ts`, `src/serialize/emit-field.ts`, `vitest.config.ts`, or the LIVE `SerializedMessage` / `BuildMessageInit` interface declarations (Plan 05-01).
- `SerializedMessage` is boundary-frozen (top-level `Object.freeze` only); inner arrays are `readonly` at the TypeScript level but mutable at runtime. Deep-freeze explicitly rejected per D-30 (emit is hot-path; type-level readonly contract is sufficient). Documented inline on the interface JSDoc so Plan 03's `emitJson` implementer has a locked semantic to conform to (Plan 05-01).
- `BuildMessageInit` empty-vs-null semantics documented on the interface JSDoc: omitting a field and passing an empty string produce IDENTICAL wire output (both emit as absent). To emit HL7 explicit null (`""`) at a specific position, `buildMessage({...}).setField(path, '""')` — the Phase 3 `setField` mutation sets `isNull=true` which the emitter preserves per D-02. No separate null-marker input shape. Documented at the interface level so Plan 05's `buildMessage` implementer doesn't need to re-invent it (Plan 05-01).
- Vitest per-directory coverage thresholds extended to `src/serialize/**` and `src/builder/**` at the same `lines: 90, branches: 85, functions: 90, statements: 90` bar as Phase 1/2/3/4 dirs. Load-bearing for Phase 7's `pnpm test:coverage` gate — without it, a low-coverage new dir could hide behind the 90% top-level average (Plan 05-01).
- **Phase 2 tokenize now unescapes subcomponents on parse (Plan 05-02 Rule-3 deviation).** `tokenizeComponent` runs every subcomponent through `unescape(sub, enc, emit, position)`, so the raw tree stores DECODED strings (e.g. `Smith|Jones` not `Smith\F\Jones`). This is the exact inverse of Phase 5 `reescape` on emit — the pair makes SER-02 first-pass structural equivalence hold. Without this fix, emit would double-escape literal backslashes in the raw tree (`Smith\F\Jones` → `Smith\E\F\E\Jones`), and SER-02 structural round-trip would only hold on the second pass via D-03 idempotency normalization. MSH-1 / MSH-2 placeholders intentionally NOT unescaped (they flow through the D-06 emission path). `UNKNOWN_ESCAPE_SEQUENCE` warnings from `unescape` propagate with full positional context (segment, field, rep, component, subcomponent all 1-indexed). `src/model/field.ts::value` getter's unescape-on-access left in place — double-unescape on a decoded subcomponent is identity; removing would break non-parser callers (Plan 05-02).
- **SER-02 first-pass structural equivalence confirmed.** 5 canonical fixtures under `test/fixtures/round-trip/` (canonical-adt-a01, oru-r01-repetitions, null-fields, embedded-delimiters, decoded-br) round-trip with `rawSegments` deep equality on the first pass — not just byte-identical idempotency from the second pass. `encodingCharacters` also preserved structurally. Fixtures use literal CR byte terminators (written via Node script because the Write tool normalises to LF) (Plan 05-02).
- **`emitJson` is a direct-map raw-tree mirror with exactOptionalPropertyTypes-safe conditional `profile` assignment.** Uses `.map().map().map()` projection from `msg.rawSegments`, `subcomponents.slice()` to decouple from parser-internal arrays, and the `type Mutable<T> = { -readonly [K in keyof T]?: T[K] }` + `if (msg.profile !== undefined) out.profile = ...` pattern (mirror of `src/helpers/meta.ts::buildMeta`) to satisfy `exactOptionalPropertyTypes` — an absent key is valid for the optional `profile?`, an explicit `profile: undefined` is not. Top-level `Object.freeze(out)` at return for W5 boundary freeze (Plan 05-03).
- **B3 (Plan 05-03): `Hl7Message.profile`'s TYPE constraints eliminated two test cases and one code fallback.** The public field is `{readonly name: string; readonly lineage: readonly string[]} | undefined` — `lineage` is REQUIRED when profile is defined, not optional. The upstream parser constructor (src/parser/index.ts ~line 385) already strips non-serializable `Profile`-descriptor fields (onWarning, customSegments, dateFormats, description) before assignment, so those fields never reach emitJson. Original tests "profile does NOT include non-serializable fields" (#14) and "profile.lineage defaults to [] when not set" (#15) were TYPE-UNREACHABLE. Replaced with a single structural `Object.keys(snap.profile).sort() === ["lineage", "name"]` assertion at the output boundary. The `?? []` fallback on `msg.profile.lineage` was also removed — dead code given the type guarantee (Plan 05-03).
- **W5 boundary-freeze semantics codified at runtime (Plan 05-03).** Two tests anchor the trade-off: `Object.isFrozen(msg.toJSON()) === true` (top-level frozen); `Object.isFrozen(snap.segments) === false` (inner arrays intentionally mutable at runtime — TS readonly contract + boundary freeze is sufficient; deep-freeze rejected per D-30 cost doctrine). Plan 01 already documented this on the `SerializedMessage` interface JSDoc; Plan 03 confirms runtime matches declared contract (Plan 05-03).
- **D-30 no-caching observable — `emitJson(msg) !== emitJson(msg)` (Plan 05-03).** Each call returns a new top-level reference (referential inequality) while content is deep-equal (`toEqual`). The structural equality holds because `.map()` produces fresh arrays and plain objects each call; no identity stability is expected or desired at this boundary. If Phase 6 or Phase 7 ever adds caching, the D-30 commitment is observable to break — the `not.toBe` assertion is the guard (Plan 05-03).
- **`buildMessage` constructs via `new Hl7Message({...})`, NOT via parse → mutate (Plan 05-05).** The `import type { Hl7Message }` landed in Plan 01 became a value `import { Hl7Message }` because Plan 05 actually constructs one. Init shape: `{ segments: [msh], encodingCharacters: DEFAULT_ENCODING_CHARACTERS, version, warnings: [] }` — no `profile` key (exactOptionalPropertyTypes-compliant omission for the "no profile" case). `Hl7MessageInit.segments` is still the parser-side key name (Phase 2 D-05 lock); the public field is `rawSegments` but construction-time uses `segments` (Plan 05-05).
- **MSH synthesis via 3 private helpers — `scalarField`, `absentField`, `compositeField` (Plan 05-05).** `scalarField("")` collapses to an absent field `{ repetitions: [], isNull: false }` — identical wire output to omitting the field (W1 wire semantic). `scalarField(value)` builds a single-rep / single-comp / single-sub RawField. `absentField()` is MSH-8's unused slot. `compositeField(typeString)` splits `typeString.split("^")` into components for MSH-9 — the ONE field that may have multiple components at MSH synthesis time. Empty parts suppressed by D-02 trailing-strip at emit time (Plan 05-05).
- **D-12 control-id — 17-char UTC timestamp `YYYYMMDDHHmmssSSS` + 6 random alnum = 23 chars, stdlib only (Plan 05-05).** Private module-level `ALNUM_ALPHABET` (62 chars, `[A-Za-z0-9]`, plain — readability doesn't matter) + private `pad(n, width)`. Loop of 6 `charAt(Math.floor(Math.random() * 62))` picks. 62^6 ≈ 5.68e10 distinct suffixes per ms → strong-enough uniqueness for outbound test messages and small tools; stricter callers pass their own `controlId`. Zero deps (Date + Math.random only per D-31). 100-call uniqueness sweep in the test suite is the guard (Plan 05-05).
- **D-13 timestamp resolver — 3-way switch on `init.timestamp` (Plan 05-05).** Private `resolveTimestamp(ts)`: `undefined` → `formatHl7Timestamp(new Date())`; string → passed through verbatim (user-supplied pre-formatted HL7 TS like `"20260419101530.1234+0500"` lands unchanged on the wire); Date → `formatHl7Timestamp(ts)` producing 14-char UTC second-precision. This is the ONLY place the Date branch is invoked — `formatHl7Timestamp` stays `@internal` (Plan 05-05).
- **D-16 type validation — 4-way guard (Plan 05-05).** `buildMessage` throws `TypeError` when `init` is null/undefined, `init.type` is not a string, empty `""`, or whitespace-only `"   "` (trimmed check). Message includes `JSON.stringify(received)` for actionable debugging. No validation against a known-structures list per Claude's Discretion ("probably no — keep open for Z-event-codes"). Any non-empty, non-whitespace string is accepted (e.g. `"ZPA^001"`) (Plan 05-05).
- **W1 wire semantics now LANDED at both doc surfaces AND runtime (Plan 05-05).** Interface-level `BuildMessageInit` JSDoc (Plan 01) AND function-level `buildMessage` JSDoc (Plan 05) both carry the paragraph: "omitting a field and passing an empty string produce IDENTICAL wire output; to emit HL7 explicit null at a specific position, use `.setField(path, '\"\"')` after construction — the Phase 3 mutation method sets `isNull=true` which the emitter preserves as the literal two-char output per D-02." Two dedicated runtime tests: (1) `sendingApp: ""` and `sendingApp` omitted produce identical MSH line (string equality); (2) `setField("MSH.3", '""')` renders `|""|` on the wire and round-trips to `mshRound.fields[2].isNull === true` (Plan 05-05).
- **D-15 addSegment scalar-string input: delimiter chars in the string get reescape'd (Plan 05-05 Rule-1 test fix).** A scalar string like `"Doe^John"` passed to `addSegment("PID", [..., "Doe^John", ...])` is stored as a single subcomponent; `emitField`'s `reescape` encodes `^` as `\S\` on emit. Round-trip yields a single-component PID-5, NOT a composite XPN — `parseHL7` unescapes `\S\` back to `^` but the resulting subcomponent is `"Doe^John"` (single component). Callers who need composite structure use `.setField("PID.5.1", "Doe").setField("PID.5.2", "John")` after `addSegment`, OR pass a full `RawField` object to `addSegment`. This is consistent with the Phase 2 unescape-on-parse / Phase 5 reescape-on-emit invariant. Plan 05's original test had an assertion contradicting this; corrected inline (Plan 05-05).
- **CustomSegmentDefinition canonically declared in src/parser/types.ts, NOT in src/profiles/define.ts (Plan 06-01).** The type lives alongside `Profile` in the parser's types module and is re-exported (as a type) from `src/profiles/define.ts` via `export type { CustomSegmentDefinition } from "../parser/types.js"`. This avoids the circular type edge that would have formed (parser/types.ts → `Profile.customSegments` referencing a type in profiles/ while profiles/define.ts also imports Profile from parser/types.ts). Single source of truth; TS structural equivalence makes both import paths carry the same identity. Plan 06-06 barrel-sweep will add `CustomSegmentDefinition` + `DefineProfileOptions` + `defineProfile` to `src/index.ts` (Plan 06-01).
- **SUPPORTED_DATE_TOKENS is VALIDATOR-ONLY — format matcher's internal TOKENS stays 6 tokens (Plan 06-01).** Public export in `src/parser/dates.ts` includes `SSSS` (fractional seconds) for completeness so the Plan 06-01 `validateDateFormats` accepts `"YYYYMMDDHHmmss.SSSS"`, but the internal `matchTokenFormat` still recognises only 6 tokens. Milliseconds already flow through the HL7 TS/DTM strict path (`parseHl7TsDtm`), so adding SSSS to the matcher is unnecessary for Phase 6. If a profile author declares an SSSS-only format and the matcher can't match it at runtime, the cascade falls through to the built-in fallbacks — no crash, just no match. Format-matcher expansion explicitly NOT required for Phase 6 (Plan 06-01).
- **Wave-1 defineProfile lineage is a STUB: `lineage === [opts.name]` regardless of opts.extends (Plan 06-01).** The Plan 06-01 factory accepts `opts.extends` (type-level) but literally ignores it at runtime, freezing a single-element lineage array. Plan 06-02 replaces line 112 of `src/profiles/define.ts` with `mergeLineage(parents, opts.name)` — flatten parents' `lineage ?? [p.name]`, append `opts.name`, dedupe preserving first occurrence per D-03. The Wave-1 test `"extends option accepted but lineage stays [name]"` will have its assertion flipped to `["parent", "child"]` in Plan 06-02 (Plan 06-01).
- **Inlined Levenshtein distance helper (iterative DP, ≤ 15 LoC excl. signature) powers the D-07 did-you-mean hint (Plan 06-01).** Zero new runtime deps per D-33; `src/profiles/validate.ts::levenshtein(a, b)` runs in O(m·n) with bounded input (known option keys are ≤ 14 chars, user-provided keys typically < 30). Used only in `validateOptionKeys` when an unknown key is discovered; returns `undefined` from the hint search when every known key is Levenshtein > 2 from the offender, and the thrown message omits the "Did you mean" clause in that case (Plan 06-01).
- **ProfileDefinitionError throw pattern: name-first fail-fast + profileName as 2nd ctor arg (Plan 06-01).** `defineProfile` validates in order: `validateProfileName(opts)` (handles null/undefined opts, non-string name, empty/whitespace-only name — NO profileName for the first two since opts.name isn't trustable yet) → `validateOptionKeys(opts)` → `validateCustomSegments(map, name)` → `validateDateFormats(formats, name)`. Every throw site passes `opts.name` (or the explicit `profileName` arg for helpers) as the second `ProfileDefinitionError` ctor arg wherever name is known, so callers can narrow on `err.profileName` for routing in multi-profile loading contexts (Plan 06-01).
- **boundary-freeze-only for Profile return value (Plan 06-01).** `defineProfile` returns `Object.freeze(profile)` at the top level. Inner `customSegments`, `lineage`, `dateFormats` stay `readonly` at the type level but mutable at runtime per D-30. Matches Phase 5's `src/serialize/to-json.ts:139` W5 boundary-freeze posture. Deep-freeze rejected — profile authoring is not a hot path but the cost/benefit is still tilted toward TS readonly contracts carrying the immutability guarantee (Plan 06-01).
- **Pre-existing Phase 5 WR-03 lint regression (control-id.ts `bytes[i]!` non-null assertion) fixed as Rule-3 auto-fix (Plan 06-01).** The `!` operator was introduced by commit `4d5e281` and blocked Plan 06-01's `pnpm lint --max-warnings=0` gate. Replaced with `bytes[i] ?? 0` — loop bound is a literal 6 matching `randomBytes(6)` so the `?? 0` fallback is a correctness-preserving no-op that satisfies both `noUncheckedIndexedAccess` and `no-non-null-assertion`. Committed standalone (`509d7c9`) to keep the Phase 5 regression fix independently revertable from Plan 06-01's own commits (Plan 06-01).
- **composeOnWarning wraps EVERY handler in a try/catch closure — uniform D-12 behavior regardless of chain length (Plan 06-02).** Even a single-handler profile gets wrapped for consistent exception-swallowing behavior; reference identity `p.onWarning === opts.onWarning` is NOT preserved and was never a documented contract. The Plan 01 test that asserted reference identity was pinned to the Wave-1 stub's passthrough implementation; Plan 02 replaces the assertion with a behavior check (`called = true` side-effect verification). The composed chain's body: `for (const h of concrete) { try { h(w); } catch { /* swallow */ } }` — per-handler try/catch so one noisy handler can't break downstream handlers (Plan 06-02).
- **validateUniqueFieldNames (D-06) installed as DEFENSE-IN-DEPTH — unreachable under the current merge strategy but kept as code-level defense against future changes (Plan 06-02).** Under mergeCustomSegments' position-indexed accumulator, a field name cannot survive at two positions after merge: `{a:3}` ⊕ `{a:5}` → accumulator `{3:"a", 5:"a"}` → final Record `{a:5}` (second assignment wins in the final lowered `Record<string, number>` because JS object literal keys dedupe by key, not by value). The validator therefore never fires via defineProfile today. Kept because (a) O(N) cost is negligible and (b) if a future contributor changes mergeCustomSegments to a name-keyed accumulator (e.g. to preserve "last write per name" semantics that COULD allow a name to survive at two positions), the validator immediately becomes reachable. JSDoc carries the full runtime observation + rationale (Plan 06-02).
- **Pre-merge vs post-merge validation split in defineProfile body (Plan 06-02).** `validateCustomSegments(selfCustomSegments, opts.name)` + `validateDateFormats(selfDateFormats, opts.name)` run BEFORE merge so D-05/D-08 errors surface with the offending profile's OWN name (not the composed lineage — which would be confusing for authors). `validateCustomSegments(merged, opts.name)` runs AGAIN post-merge to catch rogue parents (hand-crafted Profile objects bypassing defineProfile whose customSegments contain a non-Z key), joined by `validateUniqueFieldNames(merged, opts.name)` as defense-in-depth. `validateDateFormats` runs pre-merge ONLY because merging doesn't re-introduce D-08 violations (dedupe preserves already-validated strings from parents, which themselves went through validateDateFormats at their own defineProfile call) (Plan 06-02).
- **mergeCustomSegments uses a position-indexed accumulator (Map<segName, Map<position, fieldName>>) with later layers overwriting same-position entries (Plan 06-02).** Iteration order: every parent left-to-right, then self. The final lowered map `{fieldName: position}` naturally collapses collisions — if position 3 was written by parent as `a` and then overwritten by child at position 3 as `b`, the accumulator stores `{3:"b"}` and the final map has `{b:3}` (no `a` entry). Distinct positions survive additively: parent `{a:3}` + child `{b:5}` → accumulator `{3:"a", 5:"b"}` → final `{a:3, b:5}`. Child wins on position conflict: parent `{a:3}` + child `{a:5}` → accumulator `{3:"a", 5:"a"}` → final `{a:5}` (the final object literal key dedupe picks the last assignment). This strategy is why `validateUniqueFieldNames` is unreachable today — same name at two positions cannot survive the final record lowering (Plan 06-02).
- **mergeScalar uses a descending-index scan (Plan 06-02).** Signature: `mergeScalar<K extends keyof Profile>(parents, selfValue, key)`. Child explicit value wins unconditionally; absent child falls back to the LAST parent with a non-undefined value (descending scan from `parents.length - 1` to 0). Matches D-09 semantics (`p1 → p2 → child` evaluation order). Empty-undefined child does NOT clear a defined parent value. Generic-keyed signature means future scalars (beyond `description`) reuse the same helper (Plan 06-02).
- **Option A hoist: effectiveProfile resolved at new Step 6.5 BEFORE makeEmitter construction (Plan 06-03).** Resolves the D-22 "profile.onWarning must fire before options.onWarning" contract for the FULL parse — including Buffer-decode warnings replayed at Step 7, MLLP_FRAMING_STRIPPED at Step 8, tokenize warnings (FIELD_WHITESPACE_TRIMMED / UNKNOWN_ESCAPE_SEQUENCE), UNKNOWN_SEGMENT at Step 11.5, and any future early-pipeline warnings. Option B (lazy getter) would skip Steps 1-10 warnings; Option C (replay after Step 13) would break the "live as warnings arise" emit contract. Plan 04 extends ONLY Step 6.5 with default-profile fallback (trivial 3-branch discrimination) — every other Plan 06-03 edit stays untouched (Plan 06-03).
- **makeEmitter 4th param effectiveProfile + per-handler try/catch on BOTH profile.onWarning and options.onWarning (Plan 06-03).** Lenient-mode body invokes `effectiveProfile.onWarning` FIRST inside its own try/catch, then `options.onWarning` inside its own try/catch. Strict mode unchanged (neither fires; throws Hl7ParseError). The symmetric silent-swallow on options.onWarning is a mild extension of the pre-Plan-03 contract (where options.onWarning exceptions WOULD have surfaced) — sanctioned by the D-12 silent-swallow rule extended uniformly to both handler slots for consistency. Test 4 in profiles-onwarning-chain.test.ts anchors the contract (Plan 06-03).
- **discriminateOptionsOrProfile Rule-1 fix — OPTIONS_ONLY_KEYS narrowed to truly-options-only keys; discrimination flipped to prefer Profile when arg has string `name` (Plan 06-03).** Pre-Plan-03 OPTIONS_ONLY_KEYS included `onWarning` and `dateFormats`, both of which ALSO appear on Profile. Since defineProfile() outputs ALWAYS have `dateFormats` as own-key (default `[]`), passing `parseHL7(raw, profile)` was routing profile into ParseOptions branch (profile silently dropped — msg.profile undefined). Fix: (a) tighten OPTIONS_ONLY_KEYS to `["strict", "stripMllpFraming", "trimFields", "profile", "charset"]` (keys exclusive to ParseOptions); (b) flip discrimination — Profile requires `name: string`, so absence of string `name` = unambiguously ParseOptions (preserves bare `{ onWarning }` / `{ dateFormats: [...] }` as options); string `name` + truly-options-only key = ParseOptions (preserves `{ name: "x", strict: true }` contract). Parser-public.test.ts continues to pass (`{ onWarning }` → options unambiguously) (Plan 06-03).
- **KNOWN_SEGMENTS ~70-entry ReadonlySet covering HL7 v2.5.1 chapters 3-7 + common pharmacy/scheduling/master-file extensions (Plan 06-03).** Deliberately broad list to minimise false-positive UNKNOWN_SEGMENT emissions on real-world vendor-quirky messages. Z-segments deliberately excluded — they MUST be profile-declared to suppress the warning (the exact DX lever the library sells). Storage shape is `ReadonlySet<string>` (O(1) Set.has), declared at module top for single allocation at load time (Plan 06-03).
- **UNKNOWN_SEGMENT emit site at Step 11.5 with prototype-pollution guard (Plan 06-03).** After tokenize / before version extract. Iterates rawSegments, checks `KNOWN_SEGMENTS.has(name)` first, then `Object.prototype.hasOwnProperty.call(profileCustomSegments, name)` as suppression (matches T-02-06-01 mitigation pattern in discriminateOptionsOrProfile). If both checks fail, emits `unknownSegment({segmentIndex}, name)`. Keeps tokenize.ts profile-agnostic per PATTERNS.md §Integration Points (Plan 06-03).
- **Segment.get(name) D-14 narrow: undefined for missing name AND out-of-range position (Plan 06-03).** `field(n)` returns `Field.empty(enc)` for out-of-range `n` (sentinel with `repetitions.length === 0 && isNull === false`). `get(name)` checks that shape and returns `undefined` so "name not declared" and "name declared but position missing in raw tree" collapse identically (callers cannot distinguish). Explicit HL7 null (`""` field with `isNull === true`) DOES surface as a present Field — declared-and-populated content is real, even if its content is a null marker. Matches D-14's typos-surface-as-undefined rule verbatim (Plan 06-03).
- **msg.dateFormats is always a concrete `readonly string[]`, not `readonly string[] | undefined` (Plan 06-03).** Empty array when neither options.dateFormats nor profile.dateFormats supplied. Simpler consumer contract — no narrowing required at use site. The Hl7MessageInit key remains optional (conditional-assign) so construction-time semantics under exactOptionalPropertyTypes stay honest; the public field is always defined (Plan 06-03).
- **src/profiles/default.ts is the FIRST mutable module-scoped file in the codebase (Plan 06-04).** A single `let _defaultProfile: Profile | undefined = undefined` at module top; `setDefaultProfile(profile: Profile | null): void` stores via `profile ?? undefined` (defensive JS-caller undefined handling — TypeScript's `Profile | null` signature rules out undefined at compile time but a JS caller may pass it, and collapsing null/undefined to "clear" is the least-surprising semantic); `getDefaultProfile(): Profile | undefined` returns the stored value. NO class wrapper, NO globalThis symbol registry, NO worker-safe locking — the implementation matches PROJECT.md Key Decision posture ("setDefaultProfile EXISTS but is DISCOURAGED"). The afterEach test-isolation discipline is the ONLY mitigation for T-06-04-02 cross-test bleed; documented in 3 places (src JSDoc, test-file top-level afterEach hook, dedicated isolation-contract test case) (Plan 06-04).
- **parseHL7 Step 6.5 3-branch D-19 discrimination extends Plan 03's hoist (Plan 06-04).** Plan 03 shipped a single-line `const effectiveProfile = profileOpt !== undefined && profileOpt !== null ? profileOpt : undefined` which collapsed "opt-out" and "no-default" into one branch. Plan 04 splits them into explicit `if/else if/else`: `options.profile === null` → `effectiveProfile = undefined` (opt-out ignores any registered default); `options.profile !== undefined` → explicit Profile wins; `options.profile === undefined` → `effectiveProfile = getDefaultProfile()` (may itself be undefined, giving zero semantic change for unregistered consumers). `const` changed to `let` with explicit type annotation `Profile | undefined` to support 3-branch assignment. Every downstream consumer in Step 11.5 (UNKNOWN_SEGMENT customSegments suppression), Step 12 (version extract), and Step 13 (Hl7MessageInit profileInit/mergedDateFormats/mergedCustomSegments assembly) reads from `effectiveProfile` verbatim — Plan 04 makes NO other parser-code change (Plan 06-04).
- **D-22 onWarning chain coverage for default profiles is INHERITED FREE from Plan 03 (Plan 06-04).** Plan 03's makeEmitter captures `effectiveProfile` in a closure at Step 7; a default-profile onWarning handler fires during parse EXACTLY like an explicit-profile handler — same per-handler try/catch (both profile.onWarning AND options.onWarning wrapped), same BEFORE-options ordering, same swallow-on-throw. Plan 04 needs NO additional D-22 wiring and ships NO dedicated D-22 tests for defaults. Plan 04's 4 D-20 effects-equivalence tests (customSegments + dateFormats + lineage + UNKNOWN_SEGMENT suppression) EACH exercise the full emit path, implicitly covering the D-22 chain for the default-profile path (Plan 06-04).
- **Plan 06-04 is the first Phase 6 plan with ZERO deviations (Plan 06-04).** Plan 06-01 had 2 (Rule-3 control-id pre-existing lint blocker + Rule-1 test-contradiction fix); Plan 06-02 had 3 (Rule-1 onWarning reference-identity update + 2 Rule-1 lint fixes); Plan 06-03 had 2 (Rule-1 discriminateOptionsOrProfile pre-existing bug + Rule-1 test-scope adjustment). Plan 04's narrower scope (2 small files + single-line-to-3-branch edit) + benefit from Plan 03's integration fixes already landed = plan author's discretion carried through verbatim (Plan 06-04).
- **buildMeta's MSH-7 fallback warning deferred to a larger scope change (Plan 06-03).** `parseHl7Timestamp` emits `TIMESTAMP_FALLBACK_FORMAT` only when `emit + position` are supplied; `buildMeta` runs lazily on `.meta` access AFTER `msg.warnings` is frozen (Phase 2 D-07). Eager MSH-7 parsing during `parseHL7` to surface the warning would lift Phase 4's meta-helper into the parse pipeline — separate scope change. The Date VALUE itself IS the observable D-21 contract from Plan 06-03's perspective; the warning-emission contract can land in a later phase / v2 (Plan 06-03).
- **Profile starter kit uses peer-dep-first manifest shape (Plan 08-02).** `examples/profile-starter-kit/package.json` has `peerDependencies.@cosyte/hl7-parser: ">=0.1.0"` + NO top-level `dependencies` block (zero runtime deps posture preserved for downstream profile packages). `peerDependenciesMeta.@cosyte/hl7-parser.optional: false` makes the peer relationship explicit. `publishConfig.access: "public"` + `files: ["dist","README.md","LICENSE","CUSTOMIZING.md"]` (allow-list — no tests/configs/sources leak into the published tarball). `prepublishOnly` script chains typecheck → lint → test → build as the publish-time safety net. No `provenance: true` in publishConfig (kit users may not have provenance configured; kit's `publish.yml` declares `id-token: write` so users can opt-in by adding `--provenance` to the publish step) (Plan 08-02).
- **Starter kit uses canonical CustomSegmentDefinition RECORD shape, not array-of-names (Plan 08-02).** Sample profile's `customSegments` is `{ ZAL: { fields: { allergyId: 1, severity: 2, verifiedAt: 3 } } }` — matches `src/parser/types.ts` line 122 (`fields: Readonly<Record<string, number>>`) and the shipping built-ins. CONTEXT D-07's draft shape `{ fields: ['allergyId', 'severity', 'verifiedAt'] }` was superseded by PATTERNS.md + confirmed wrong against the type module. Plan 08-02's `<interfaces>` block made this explicit; SUMMARY carries the cross-reference (Plan 08-02).
- **Starter kit's devDependency on `@cosyte/hl7-parser` uses `file:../..` specifier while kit lives in parent repo (Plan 08-02 deviation from plan's `workspace:*` default).** Rationale: repo root has no `pnpm-workspace.yaml` (that's Plan 08-05's scope), and the parser package isn't published to npm yet, so `workspace:*` would fail to resolve at install time. `file:../..` resolves directly to the parent tree — parent's `dist/` satisfies the exports map — enabling the in-kit smoke test (`pnpm install --no-frozen-lockfile && pnpm test && pnpm build`) to run standalone today with no parent-side wiring. README's Develop section + CUSTOMIZING.md step 1 instruct downstream users to replace `"file:../.."` with a real published version (e.g. `"^0.1.0"`) before running install. When Plan 08-05 lands `pnpm-workspace.yaml` the kit's devDep can optionally flip back to `"workspace:*"` (Plan 08-02).
- **Publish workflow is `workflow_dispatch`-only with `NODE_AUTH_TOKEN` scoped to the single publish step (Plan 08-02 — T-08-05 + T-08-06 mitigations).** `examples/profile-starter-kit/.github/workflows/publish.yml` has `on: { workflow_dispatch: }` with NO `push`, NO `tag`, NO `pull_request` triggers (T-08-05 — no accidental publish from commits). The secret injection `env.NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}` is set on the SINGLE `pnpm publish --access public --no-git-checks` step's `env` block (not job-level env) — GitHub auto-masks secret values in logs, and single-step scoping minimises T-08-06 surface. `permissions: { contents: read, id-token: write }` grants only what's needed; `id-token: write` enables future `--provenance` opt-in without a workflow edit (Plan 08-02).
- **Starter kit ships NO pnpm-lock.yaml (Plan 08-02).** Template packages shouldn't ship lockfiles — downstream users generate their own post-customization when their real scope/name/version land. Kit's `.gitignore` appends `pnpm-lock.yaml` to the root ignore set inherited from the parent's `.gitignore`. Kit's ci.yml uses `pnpm install --no-frozen-lockfile` (NOT the parent's `--frozen-lockfile`) to match this posture. pnpm 9.0.0 verified to tolerate `{{YOUR_ORG}}` / `{{PROFILE_NAME}}` in the `name` field during install — D-06 `scripts.prepare` fallback NOT needed (Plan 08-02).
- **Starter kit placeholder contract is a triple: `{{YOUR_ORG}}` / `{{PROFILE_NAME}}` / `MyProfile` (Plan 08-02 — KIT-07).** The first two are the templated tokens (`{{YOUR_ORG}}` → npm scope, `{{PROFILE_NAME}}` → kebab-case package name). The third is the JS-identifier placeholder for `export const MyProfile = defineProfile({...})` — `{{PROFILE_NAME}}` can't live in identifier position because braces are invalid JS syntax. CUSTOMIZING.md step 1 instructs find-replace of all three. Kit's ci.yml runs green with all three placeholders intact (`pnpm install --no-frozen-lockfile` + typecheck + lint + test + build all exit 0 inside the kit — no rename required for verification). This is the KIT-07 "green-with-placeholders-intact" contract (Plan 08-02).

### Active Todos

(none — pending Phase 1 planning)

### Blockers

(none)

### Notes

- Roadmap derived from 97 v1 REQ-IDs across 13 categories (SETUP, PARSE, MODEL, HELPERS, TYPES, TOL, SER, PROF, BIP, TEST, EX, KIT, DOC).
- Tolerance (TOL) folded into Phase 2 (Core Parser) because warnings are deeply coupled to parsing and standard granularity discourages splitting cross-cutting concerns into their own phase when they can't run independently.
- Testing (TEST) is its own late phase (Phase 7) to centralize coverage enforcement, vendor-quirks fixtures, strict-mode sweep, and profile-authoring tests — but earlier phases still ship with enough tests to verify their success criteria; Phase 7 is hardening, not initial testing.
- Library has no UI; every phase is `UI hint: no`.

## Session Continuity

- **Last action (2026-04-20):** `/gsd-execute-phase 8` Plan 02 — profile starter kit subtree (Wave 1, KIT-01..07 closed). 3 tasks executed; 2 atomic commits + 0 code changes for Task 3: **43fccae** Task 1 authored 11 files (examples/profile-starter-kit/package.json with peerDependencies + publishConfig.access:public + files allow-list + no dependencies block + devDependencies.@cosyte/hl7-parser=`"file:../.."`; tsconfig.json / tsup.config.ts / vitest.config.ts no-coverage / eslint.config.js slim-subset / .prettierrc.json / .gitignore-with-pnpm-lock; src/index.ts sample profile extends profiles.genericLab + declares ZAL Z-segment with canonical `{allergyId:1, severity:2, verifiedAt:3}` record shape + adds `YYYY-MM-DD` dateFormat; test/profile.test.ts 4 assertions name-description / UNKNOWN_SEGMENT-delta / ZAL-aliases / toString-spec-clean; test/fixtures/sample.hl7 ADT^A01 synthetic MRN-KIT-001 CR-only separators 243 bytes; LICENSE MIT with `{{YOUR_ORG}}` copyright); **3c303c0** Task 2 authored 4 files (.github/workflows/ci.yml push/PR Node-20 install-typecheck-lint-test-build; .github/workflows/publish.yml workflow_dispatch-only NODE_AUTH_TOKEN single-step-scoped permissions contents:read+id-token:write; README.md placeholder-aware quickstart + devDep-replacement note; CUSTOMIZING.md 5 numbered steps each closing with Verify block). Task 3 smoke-verify: Option A path — pre-built parent dist/ + kit's `file:../..` devDep resolved on first try. pnpm install --no-frozen-lockfile 3.3s; typecheck 0 errors; lint 0 warnings --max-warnings=0; 4/4 tests pass in 422ms; build produces dist/index.mjs (488B) + index.cjs (517B) + index.d.ts (900B). actionlint clean on both workflows. Zero D-06 `scripts.prepare` fallback needed — pnpm 9.0.0 tolerates `{{...}}` in name field during install. **3 deviations documented:** (1) Rule-3 devDep=`file:../..` vs plan's `workspace:*` (no pnpm-workspace.yaml yet, package unpublished — orchestrator Option A); (2) Rule-2 README + CUSTOMIZING.md document `file:../..` default and downstream replacement path; (3) Rule-2 pnpm-lock.yaml appended to kit .gitignore (templates shouldn't ship lockfiles). KIT-01 configs + KIT-02 test green + KIT-03 dist 3-artifact + KIT-04 both workflows + KIT-05 manifest shape + KIT-06 5-step CUSTOMIZING + KIT-07 placeholder consistency all mapped in SUMMARY.md. Next: /gsd-execute-phase 8 Plan 03.

- **Previous action (2026-04-19):** `/gsd-execute-phase 7` Plan 06 (Wave 3 capstone — TEST-01 closed, Phase 7 execution complete). Two tasks committed atomically: d263222 Task 1 (vitest.config.ts — branch threshold 85 → 90 on five per-dir entries parser/model/helpers/serialize/builder per Plan 01 baseline showing all five clear 90 with margin; global threshold KEPT at 85 as the backstop for ungated dirs — first-pass edit bumped global to 90 too, which broke the gate because src/profiles/** sits at exactly 85.00% branches and is explicitly ungated per CONTEXT.md D-02/D-06; Rule-1 inline fix rolled into the same Task 1 commit, documented via inline comment block in vitest.config.ts); c66af76 Task 4 (.github/workflows/ci.yml — new `Test (with coverage)` step inserted between Test and Build, runs pnpm test:coverage across Node 18/20/22 matrix, no third-party actions, no matrix change; actionlint clean). Task 2 no-op (coverage passed first-run post-bump — no gap-closure tests needed). Task 3 human-verify checkpoint auto-approved per --auto mode (user_response="approved" without waiting; coverage gate active + passing + all sanity checks green). 824 passing + 14 todo (unchanged from Plan 07-05 baseline — no test deltas). pnpm test:coverage exit 0; pnpm test / typecheck / lint --max-warnings=0 all exit 0. Scenario A chosen per plan (keep broader per-dir gate because all five dirs already pass at 90 — narrowing would be regression). TEST-01 closed; all 8 Phase 7 REQ-IDs (TEST-01..08) now closed. Phase 7 execution COMPLETE. Next: /gsd-verify-work 7 (verifier) → /gsd-validate-phase 7 (Nyquist) → Phase 8.

- **Previous action (2026-04-19):** `/gsd-execute-phase 7` Plan 05 — 4 malformed fixtures (one per FATAL_CODES entry) + test/parser-malformed-sweep.test.ts. 2 tasks committed atomically + zero deviations: 7110694 Task 1 (4 .hl7 fixtures under test/fixtures/malformed/ authored via Node one-liners so byte content is exact — empty-input.hl7 is 0 bytes, no-msh-segment.hl7 starts with EVN + CR-separated PID, msh-too-short.hl7 is 6 bytes MSH|^~, invalid-encoding-characters.hl7 has MSH-2 overlapping field separator so readDelimiters throws on the 'field separator must not appear among MSH-2 chars' branch — plus README with filename↔FATAL_CODES contract + trigger table + alternative-trigger notes for INVALID_ENCODING_CHARACTERS); 0490069 Task 2 (test/parser-malformed-sweep.test.ts — describe.each over readdirSync + per-fixture lenient-mode assertion that parseHL7 throws Hl7ParseError with err.code matching fileToCode(filename) + err.position + err.snippet defined per TOL-02, PLUS strict-mode assertion proving Tier-3 mode-independence). 824/824 passing + 14 todo (was 815 passing, +9 passing); typecheck clean, lint --max-warnings=0 clean. Closes TEST-04. Zero deviations — parser throw-site behaviour matched PLAN byte predictions on first try; manual dist/index.cjs probe confirmed all 4 fixtures throw correct code + position + snippet in both modes. Next: /gsd-execute-phase 7 Plan 06 (coverage gate tighten + CI wiring).
- **Previous action (2026-04-20):** `/gsd-execute-phase 7` Plan 04 — 13 vendor-quirks fixtures (one per WARNING_CODES entry) + test/parser-strict-mode-sweep.test.ts. 2 tasks committed atomically + zero deviations: f718c10 Task 1 (13 .hl7 fixtures under test/fixtures/vendor-quirks/ + README documenting filename-as-kebab-code contract and per-fixture emission-status matrix, authored via committed scripts/write-vendor-quirks.mjs generator that lands MLLP 0x0B/0x1C/0x0D control bytes via Buffer.concat); 7f714e6 Task 2 (test/parser-strict-mode-sweep.test.ts — describe.each over readdirSync + per-fixture lenient-parse-succeeds + .toContain(code) + .toThrow(Hl7ParseError), with EMITTING_CODES set for the 6 codes that have active parser emit sites today and it.todo blocks for the 7 that have factories only). 815/815 passing + 14 todo (was 789 passing, +26 passing +14 todo); typecheck clean, lint --max-warnings=0 clean. Closes TEST-05 + TEST-06. **Emission gap surfaced empirically:** only MLLP_FRAMING_STRIPPED, FIELD_WHITESPACE_TRIMMED, UNKNOWN_ESCAPE_SEQUENCE, UNKNOWN_SEGMENT, ENCODING_MISMATCH (with options.charset override), and UNKNOWN_CHARSET emit from the lenient default parser; SEGMENT_CASE, EXTRA_FIELDS, DUPLICATE_REQUIRED_SEGMENT, MISSING_REQUIRED_FIELD, OUT_OF_ORDER_SEGMENT, VERSION_MISMATCH, TIMESTAMP_FALLBACK_FORMAT have factory functions in src/parser/warnings.ts but no parser call site. Fixtures authored in anticipated trigger shapes; sweep self-documents the gap via it.todo. A future plan that wires an emit site needs only to add its code to EMITTING_CODES in the sweep — zero fixture/sweep shape changes required. Next: /gsd-execute-phase 7 Plan 05 (malformed fixtures + parser-malformed-sweep.test.ts for TEST-04).
- **Previous action (2026-04-19):** `/gsd-execute-phase 6` Plan 05 — 5 built-in vendor profiles (epic, cerner, meditech, athena, genericLab) + handcrafted vendor-shape fixtures. 2 tasks committed atomically + 1 Rule-3 inline auto-fix: 2ed522f Task 1 5 src/profiles/<vendor>.ts files — each authored via public defineProfile() API with vendor-typical non-HL7 dateFormats + Z-segments (epic: MM/DD/YYYY HH:mm:ss + MM/DD/YYYY + ZDP/ZRS; cerner: ISO-8601-T + ISO-date-only + ZDS/ZCO; meditech: YYYYMMDDHHmm + ZVI; athena: MM/DD/YYYY + ZCA with AM/PM variant DROPPED per SUPPORTED_DATE_TOKENS; genericLab: YYYYMMDD HHmm + YYYY-MM-DD + ZLB/ZNT); a7e243e Task 2 5 synthetic CR-terminated vendor-shape fixtures under test/fixtures/vendor-shapes/{epic/cerner/meditech/athena/genericLab}/ (203-368 bytes each, synthetic patient data only Doe/Smith/Lee/Brown/Doe-Jones + MRN prefixes EPIC-00001/CERN-00042/MT-00099/ATH-00007/LAB-00123) written via temporary Node .mjs helper script (deleted after commit). Rule-3 auto-fix: jsdoc/require-jsdoc required JSDoc IMMEDIATELY above each `export const <vendor>` declaration, so an export-adjacent JSDoc block (with @example) was added alongside the file-header block. 724/724 tests still green (plan scope adds no tests — BIP-06 fixture-parity tests land in Plan 06-06); typecheck clean, lint --max-warnings=0 clean, build green. Closes BIP-01..05. Plan 06-06 is the final Phase 6 plan (barrel + public exports + BIP-06 fixture-parity tests). Next: /gsd-execute-phase 6 Plan 06.
- **Previous action:** `/gsd-execute-phase 6` Plan 01 — defineProfile core + validation + describe + types. 3 tasks + 1 Rule-3 auto-fix + 1 Rule-1 test fix committed atomically (509d7c9 control-id lint fix, 996e7be Task 1 types+dates, 446687c Task 2 define/validate/describe, bb27b31 Task 3 tests). 35 new tests pass across 9 describe blocks (happy path + Wave-1 extends-ignored + D-05/D-07/D-08 validation + name validation + describe() + frozen); 658/658 total suite passing (+35 from 623 baseline); typecheck clean, lint --max-warnings=0 clean, build green (dual ESM 93.58 KB + CJS 94.55 KB + DTS 116.60 KB). Wave-1 lineage stub flagged in SUMMARY.md "Known Stubs" section — Plan 06-02 replaces with real mergeLineage. Closes PROF-01 (happy path), PROF-02 (4 validation throws), PROF-04 (readonly fields), PROF-05 (describe), PROF-07-types (CustomSegmentDefinition).
- **Previous action:** `/gsd-plan-phase 6` — 6 plans created across 5 waves covering all 15 Phase 6 REQ-IDs. Pattern mapper surfaced 5 load-bearing discoveries (UNKNOWN_SEGMENT factory exists but never emitted; parseHl7Timestamp called with empty options at composite layer; Hl7MessageInit.profile is `{name, lineage}` — merged customSegments goes as SIBLING init key; Segment ctor takes 3 positional args — add 4th optional; profile attribution lands at parser/index.ts:383-411). Planner iterated through checker: iteration 1 surfaced 1 blocker (D-22 profile.onWarning never wired into makeEmitter — dead handler chain) + 4 warnings + 1 info; iteration 2 VERIFICATION PASSED after revisions. D-22 resolved via Option A hoist: profile resolution moved before makeEmitter construction (new Step 6.5 in parseHL7), makeEmitter extended to 4 args, profile.onWarning invoked in per-handler try/catch BEFORE options.onWarning. CustomSegmentDefinition moved canonically to src/parser/types.ts (no cyclic forward import).
- **Previous action:** `/gsd-discuss-phase 6` — Phase 6 context gathered. All 4 selected gray areas resolved to recommended defaults across 34 decisions in `.planning/phases/06-profile-system-and-built-ins/06-CONTEXT.md` (defineProfile API + validation, extends merge semantics, customSegments access API, built-in catalog + fixture strategy).
- **Phase 5 execution summary:** Phase 5 Plan 05 (build-message) executed 2026-04-19. Shipped the three builder function bodies — `formatHl7Timestamp` (14-char UTC `YYYYMMDDHHmmss` via `getUTC*` + `padStart`), `generateControlId` (17-char UTC `YYYYMMDDHHmmssSSS` + 6-char `[A-Za-z0-9]` random suffix = 23 chars total, zero deps), and `buildMessage` (D-16 4-way type validation → 3 private synthesis helpers `scalarField` / `absentField` / `compositeField` → new `Hl7Message({segments:[msh], encodingCharacters, version, warnings:[]})`). Closed SER-06 (last SER requirement; all 6 SER-01..06 now closed). 40-test delta across 3 new files (9 format-timestamp + 8 control-id + 23 builder integration including 2 dedicated W1 empty-vs-null runtime tests — empty/omitted wire equivalence + explicit null via `.setField("MSH.3", '""')` rendering as `|""|`). Function-level JSDoc on `buildMessage` carries the full W1 reinforcement paragraph (complementing the Plan 01 interface-level note on `BuildMessageInit`). **Two deviations:** (1) Rule-1 test fix — the plan's `"chained result round-trips through parseHL7"` test asserted `familyName === "Doe"` after passing `"Doe^John"` as a scalar string to `addSegment`; per Phase 3 D-15 scalar-string input is a single subcomponent and `^` reescape's to `\S\` on emit (round-trip yields single-component PID-5, not composite XPN). Fix: rebuilt PID-5 via `.setField("PID.5.1", "Doe").setField("PID.5.2", "John")` post-`addSegment` with an inline comment documenting the semantics + added `givenName === "John"` assertion. (2) Rule-3 lint fix — `{} as BuildMessageInit` tripped `consistent-type-assertions`; extracted to `const missingType` with scoped `eslint-disable-next-line` matching Plan 02-06 precedent. **Plan scope honored exactly** — only the 3 src/builder bodies (module JSDoc + BuildMessageInit interface declaration + function signatures preserved from Plan 01; `import type { Hl7Message }` → value `import` for the `new Hl7Message(...)` call) + 3 new test files touched. 618/618 tests passing across 49 files (+40 from Plan 05). Typecheck + lint (max-warnings=0) + build + bundle-smoke (byte-identical match against plan's expected `MSH|^~\\&|CLINIC||LAB||20260419101500||ADT^A01|MSG001|P|2.5\rPID|||MRN123||Doe^John||19800115|M\r`) all green.
- **Next action:** `/gsd-verify-work 7` — verifier on Phase 7 now that all 7 plans are complete + all 8 TEST-01..08 REQs closed. Then `/gsd-validate-phase 7` (Nyquist) → `/gsd-transition` → Phase 8 planning/execution. (Phase 5 verifier/Nyquist gates + Phases 1-4 outstanding audits still open but not blocking under yolo mode.)
- **Open questions:** Surfacing `TIMESTAMP_FALLBACK_FORMAT` warnings in `msg.warnings` for non-HL7 MSH-7 formats requires eager MSH-7 parsing during `parseHL7` (so `parseHl7Timestamp`'s emit+position can land the warning before `msg.warnings` freezes at Hl7Message construction). Plan 06-03 honours D-21 by populating `msg.meta.timestamp` via the merged cascade but leaves the warning-emission surface for a later phase. Other carry-overs: (a) all 3 emitters + `buildMessage` fully implemented — profile hooks consume the complete emit pipeline; (b) `addSegment` scalar-string input reescape's delimiter chars per D-15 — Phase 7 / v2 may add composite-object inputs; (c) `formatHl7Timestamp` silently accepts Invalid Dates per D-07 (`@internal` only). Phase 7 may lift selected IN2 / IN3 fields into Insurance based on vendor-quirk fixture evidence (carry-over). Phase 8's README Error Handling section should document the strict-mode `err.code` widening (carry-over from Phase 2).
- **Resume file:** .planning/phases/08-examples-starter-kit-and-documentation/08-02-SUMMARY.md (Phase 8 Plan 02 EXECUTION COMPLETE — profile starter kit subtree shipped; KIT-01..07 all closed; next /gsd-execute-phase 8 Plan 03)

---

*Last updated: 2026-04-20 (Phase 10 gap-closure Plan 10-03 — STATE.md resynced to v2.1 post-rename complete: frontmatter `status` + `progress` block reflect 9/9 phases shipped + 46/46 plans + 97/97 REQ-IDs closed + 824 tests pass; Current Position + Performance Metrics rollup updated; historical breadcrumb logs preserved unchanged. PROJECT.md capabilities checklist flipped in Task 4 of same plan.)*
