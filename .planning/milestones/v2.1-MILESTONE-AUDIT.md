---
milestone: v2.1
audited: 2026-04-20T00:00:00Z
status: tech_debt
scores:
  requirements: 97/97
  phases: 6/9 (fully verified) — 3/9 evidence-only (no VERIFICATION.md)
  integration: passing (pipeline + examples + starter-kit + tarball green per 09-04-SUMMARY)
  flows: 3/3 examples pass; full parse → mutate → serialize → parse round-trip green across 9 canonical + 14 edge + 13 vendor-quirk + 4 malformed fixtures
  nyquist:
    compliant_phases: [04, 05, 06]
    partial_phases: []
    missing_phases: [01, 02, 03, 07, 08, 09]
    overall: partial
gaps: []  # No critical blockers — all 97 REQ-IDs have implementation + evidence
tech_debt:
  - phase: 01-project-foundation
    items:
      - "Missing VERIFICATION.md (evidence in 01-04-SUMMARY + green CI pipeline — formal doc never produced)"
      - "Missing VALIDATION.md (no Nyquist sweep)"
  - phase: 02-core-parser-and-tolerance
    items:
      - "Missing VALIDATION.md (no Nyquist sweep — VERIFICATION.md exists and passed)"
      - "REQUIREMENTS.md traceability checkboxes PARSE-01..09 + TOL-01..07/09 still show [ ] despite VERIFICATION closure — stale doc"
      - "TOL-08 observable slice deferred to Phase 3/4 via recorded override — carry-forward item; should be re-verified now that TYPES-04 + HELPERS-01 have landed"
  - phase: 03-structural-model-and-types
    items:
      - "Missing VALIDATION.md (no Nyquist sweep — VERIFICATION passed 4/4 SC)"
  - phase: 07-testing-hardening-and-fixtures
    items:
      - "14 `it.todo` entries in parser-strict-mode-sweep.test.ts (7 WARNING_CODES have factories but no parser emit site — intentional fixture-ahead-of-emit posture, documented in fixture README; auto-promotes when emit sites land)"
      - "Missing VALIDATION.md (no Nyquist sweep)"
  - phase: 08-examples-starter-kit-and-documentation
    items:
      - "Missing VERIFICATION.md (all 5 plans have SUMMARYs + artifacts on disk + CI pipeline green — formal verifier never invoked)"
      - "Missing VALIDATION.md (no Nyquist sweep)"
      - "REQUIREMENTS.md traceability checkboxes EX-01..03 + DOC-01..13 still show Pending despite plan closure + artifacts present on disk — stale doc"
  - phase: 09-rename-package-to-cosyte-hl7
    items:
      - "Missing VERIFICATION.md (rename-only phase; 09-04-SUMMARY demonstrates full pipeline green + grep sweep confirms 1 legacy ref in CHANGELOG Notes: breadcrumb only — formal verifier never invoked)"
      - "Missing VALIDATION.md (no Nyquist sweep; rename-only so coverage is thin-by-design)"
  - phase: cross-cutting
    items:
      - "ROADMAP.md Phase checkboxes stale — Phases 1, 2, 7, 8, 9 show `[ ]` despite being complete; Progress table shows 3/8 phases verified but init-milestone reports all 9/9 complete"
      - "STATE.md status field reflects Phase 7 EXECUTION COMPLETE pending verify — not updated after Phases 7, 8, 9 landed"
      - "PROJECT.md top-level capabilities checklist still shows all items `[ ]` despite v1 completion"
---

# v2.1 Milestone Audit — @cosyte/hl7

**Audited:** 2026-04-20
**Scope:** All 9 phases (01–09), 97 v1 REQ-IDs
**Verdict:** `tech_debt` — no functional blockers; process debt in verification paper trail

---

## Executive Summary

All nine phases have shipped their code. The live pipeline is green across every gate the project claims: `pnpm install/typecheck/lint/test/build/examples`, CI on Node 18/20/22, kit subtree install+test+build, `pnpm publish --dry-run` (346.2 kB / 10-file tarball), and all 824 tests pass (+14 documented `it.todo`). The package is renamed to `@cosyte/hl7` at 0.1.0 with zero legacy references outside the CHANGELOG breadcrumb.

The milestone does **not** have critical gaps. It **does** have process debt: three phases (01, 08, 09) never produced a formal VERIFICATION.md, six phases never produced a Nyquist VALIDATION.md, and three top-level planning docs (ROADMAP, STATE, PROJECT) are stale relative to the code ground-truth. These are documentation-trail gaps, not product gaps.

---

## Requirements Coverage (3-source cross-reference)

**97 / 97 REQ-IDs satisfied.** Status derived from REQUIREMENTS.md traceability, SUMMARY.md `requirements-completed` frontmatter, and VERIFICATION.md evidence (or SUMMARY + on-disk artifact proof where VERIFICATION.md is missing).

| Category | Count | VERIFICATION.md status | REQUIREMENTS.md freshness | Verdict |
|---|---|---|---|---|
| SETUP-01..06 (Phase 1) | 6 | missing | fresh ✓ | **satisfied** (evidence: 01-04-SUMMARY + green CI) |
| PARSE-01..09 + TOL-01..10 (Phase 2) | 19 | passed (1 override TOL-08) | stale (shows `[ ]`) | **satisfied** — traceability checkboxes need update |
| MODEL-01..07 + TYPES-01..04 (Phase 3) | 11 | passed 4/4 SC | fresh ✓ | **satisfied** |
| HELPERS-01..07 (Phase 4) | 7 | passed 4/4 SC | fresh ✓ | **satisfied** |
| SER-01..06 (Phase 5) | 6 | passed 7/7 must-haves | fresh ✓ | **satisfied** |
| PROF-01..09 + BIP-01..06 (Phase 6) | 15 | passed 5/5 SC | fresh ✓ | **satisfied** |
| TEST-01..08 (Phase 7) | 8 | passed 5/5 SC | fresh ✓ | **satisfied** |
| EX-01..03 + KIT-01..07 + DOC-01..15 (Phase 8) | 25 | missing | partly stale (EX + DOC-01..13 show Pending) | **satisfied** — SUMMARYs + artifacts on disk + `pnpm examples` green in 09-04 pipeline |
| (rename-only, Phase 9) | 0 | missing | n/a | **satisfied** — 09-04-SUMMARY confirms: grep yields 1 legacy ref (CHANGELOG breadcrumb per D-07), full pipeline green, examples green, tarball clean |

**Orphans:** None. Every REQ-ID in REQUIREMENTS.md has at least one phase SUMMARY claiming completion, with observable artifact or test evidence.

**Override carry-forward:** TOL-08's observable slice was explicitly deferred from Phase 2 to Phase 3 (TYPES-04 TS/DTM composite) and Phase 4 (HELPERS-01 `msg.meta.timestamp`). Both phases verified `passed`, so the deferral has been satisfied — but the Phase 2 VERIFICATION.md deferred block was never retired. Minor doc-trail item; not a blocker.

---

## Cross-Phase Integration

Integration checker spawn was bypassed in favor of in-line verification because:

1. **The library has no services or RPC boundaries.** Cross-phase integration is module-boundary only (exports, types, instance methods).
2. **The 09-04 plan summary already provides E2E evidence:**
   - `pnpm install --frozen-lockfile=false` → exit 0
   - `pnpm typecheck` → exit 0
   - `pnpm lint --max-warnings=0` → exit 0
   - `pnpm test` → 824 passed + 14 todo across 59 files
   - `pnpm build` → ESM 110 KB + CJS 111 KB + DTS 132 KB
   - `pnpm examples` → 3/3 examples exit 0
   - Starter kit subtree → install + test + build exit 0 (4/4 tests green)
   - `pnpm publish --dry-run` → 10-file / 346.2 kB tarball
3. **`actionlint` clean** on 4 workflow files (parent ci + publish + kit ci + kit publish).
4. **Round-trip integration** (the riskiest cross-phase flow: Phase 2 parse → Phase 3 model → Phase 4 helpers → Phase 5 serialize → Phase 6 profiles) is pinned by 9 canonical fixtures × `assertStructuralRoundTrip` and 13 vendor-quirks × strict-mode escalation sweep.

No integration gaps detected.

---

## End-to-End Flows

| Flow | Evidence | Status |
|---|---|---|
| Parse any v2.1–v2.8 message → extract patient in one line | `examples/extract-patient-info.ts` runs end-to-end | ✓ |
| Iterate observations + orders | `examples/read-lab-results.ts` runs end-to-end | ✓ |
| Parse → mutate → serialize (round-trip) | `examples/modify-and-resend.ts` + SER-02 sweep over 9 canonical fixtures | ✓ |
| Profile-applied parse with Z-segment access | 5 vendor-shape fixtures under `test/fixtures/vendor-shapes/` + `profiles-builtins.test.ts` | ✓ |
| Publish a custom profile | `examples/profile-starter-kit/` installs + tests + builds standalone against parent via `file:../..` | ✓ |
| Malformed input throws with position + snippet | 4 fatal fixtures × dual-mode assertion in `parser-malformed-sweep.test.ts` | ✓ |

All user-observable flows functional.

---

## Nyquist Compliance Discovery

| Phase | VALIDATION.md | Compliant | Action |
|---|---|---|---|
| 01-project-foundation | missing | — | `/gsd-validate-phase 1` (optional — setup phase has thin logic surface) |
| 02-core-parser-and-tolerance | missing | — | `/gsd-validate-phase 2` (large surface; worth running) |
| 03-structural-model-and-types | missing | — | `/gsd-validate-phase 3` |
| 04-named-helpers | exists | true | none |
| 05-serialization-and-round-trip | exists | true | none |
| 06-profile-system-and-built-ins | exists | true | none |
| 07-testing-hardening-and-fixtures | missing | — | `/gsd-validate-phase 7` (self-referential — the phase IS the test suite; likely trivial) |
| 08-examples-starter-kit-and-documentation | missing | — | `/gsd-validate-phase 8` (docs-heavy; likely thin) |
| 09-rename-package-to-cosyte-hl7 | missing | — | `/gsd-validate-phase 9` (rename-only; likely trivial) |

**Overall:** partial (3/9 compliant). The coverage gate itself (≥ 90% per-dir branches on parser/model/helpers/serialize/builder) is enforced in CI via `pnpm test:coverage` on every PR — this is stronger than ad-hoc Nyquist in practice, but does not substitute for the formal per-phase audits.

---

## Tech Debt (Non-Blocking)

### Process / paper trail
1. **Missing VERIFICATION.md for Phases 01, 08, 09** — evidence is abundant (SUMMARYs + artifacts + green pipeline), but no formal verifier run produced a report.
2. **Missing VALIDATION.md for Phases 01, 02, 03, 07, 08, 09** — six phases never had Nyquist sweep.
3. **REQUIREMENTS.md traceability stale** — 33 checkboxes still show `[ ]` or "Pending" despite being implemented:
   - PARSE-01..09 + TOL-01..07/09 (17 from Phase 2)
   - EX-01..03 + DOC-01..15 (18 from Phase 8) — DOC-14/15 and KIT-01..07 are correctly checked
4. **ROADMAP.md checkboxes + Progress table stale** — Phases 1/2/7/8/9 still show `[ ]` and "Not started" / "Planned" in Progress table despite all 9 phases complete.
5. **STATE.md current-position stale** — reflects "Phase 7 EXECUTION COMPLETE, pending /gsd-verify-work 7" though Phases 7, 8, 9 have all landed since.
6. **PROJECT.md capabilities checklist** — all 11 top-level capability bullets still show `[ ]` despite all being implemented and shipped.

### Code-level (genuine debt, flagged not fixed)
7. **14 `it.todo` in `parser-strict-mode-sweep.test.ts`** — 7 Tier-2 WARNING_CODES have factories in `src/parser/warnings.ts` but no parser call site. Fixtures exist and auto-join the sweep when emit sites land. Documented in `test/fixtures/vendor-quirks/README.md` and accepted as "fixture-ahead-of-emit-site" posture per Plan 07-04 D-14. Codes pending wiring:
   - `SEGMENT_CASE_MISMATCH`
   - `EXTRA_FIELDS`
   - `DUPLICATE_REQUIRED_SEGMENT`
   - `MISSING_REQUIRED_FIELD`
   - `OUT_OF_ORDER_SEGMENT`
   - `VERSION_MISMATCH`
   - `UNKNOWN_CHARSET` (factory exists; partial wiring landed in Phase 2 Plan 07 charset path)
8. **Phase 2 TOL-08 "deferred" frontmatter never retired** — observable slice did land in Phase 3/4; the Phase 2 VERIFICATION.md still carries the deferred block. Doc-trail clean-up only.

### Scope note
9. **Milestone version label inconsistency** — init-milestone reports `v2.1`; REQUIREMENTS.md + ROADMAP reference "v1 Requirements"; package.json is 0.1.0. The tracker's milestone version and the semver trajectory are decoupled here. Not a bug, but worth clarifying before the next milestone opens.

---

## Verdict

**Status: `tech_debt`.** All 97 v1 REQ-IDs are implemented, exercised by tests, and shipped behind a green pipeline. There are no functional gaps, no broken flows, and no integration regressions. The outstanding work is entirely paper-trail: six missing VALIDATION.md files, three missing VERIFICATION.md files, and three stale planning docs (ROADMAP/STATE/PROJECT). None of these block the code from being released or the milestone from being closed; they just leave the project artifact trail out of sync with the code.

Recommended path: either run `/gsd-plan-milestone-gaps` to close the documentation debt before archiving, or accept the debt, run `/gsd-complete-milestone v2.1`, and track the missing VERIFICATION/VALIDATION sweeps as backlog items for the next cycle.
