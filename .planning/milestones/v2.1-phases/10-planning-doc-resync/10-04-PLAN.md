---
phase: 10-planning-doc-resync
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - "A developer reading .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md sees NO frontmatter `deferred:` block — the TOL-08 deferral has been satisfied and the block is retired with a pointer to its closure."
    - "A developer reading the `Resolution Note §TOL-08` body section sees an explicit closure stamp citing Phase 3 TYPES-04 + Phase 4 HELPERS-01 as the consumers that satisfied the observable slice — NOT a future-tense 'will land in Phase 3/4' narrative."
    - "A developer grep'ing for `⚠️ PLUMBING-COMPLETE` or `⚠️ DEFERRED` in 02-VERIFICATION.md finds zero ambiguity — every TOL-08 reference is updated to explicit CLOSED status with closure evidence."
    - "The overall score line in the frontmatter reflects the satisfied deferral — e.g. `19/19 must-haves verified (0 deferrals open, 0 gaps open)`."
  artifacts:
    - path: ".planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md"
      provides: "Phase 2 VERIFICATION.md with TOL-08 deferral retired and replaced by explicit Phase 3/4 closure pointer"
      contains: "no `deferred:` frontmatter block; `Resolution Note §TOL-08` body section stamps the closure with file-path pointers to src/model/composites/timestamp.ts (Phase 3 TYPES-04) and src/helpers/meta.ts (Phase 4 HELPERS-01); score line updated to 0 deferrals open"
  key_links:
    - from: ".planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md §Resolution Note §TOL-08"
      to: "src/model/types/ts.ts (or equivalent composite path) + src/helpers/meta.ts"
      via: "explicit closure stamp with line-path citations"
      pattern: "Resolution Note cites the actual code paths that satisfy the observable slice, not phrases like 'will land in Phase 3/4'"
---

<objective>
Retire the TOL-08 "deferred" block in `.planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md` now that its satisfaction has landed: Phase 3 TYPES-04 (TS/DTM typed composite) and Phase 4 HELPERS-01 (`msg.meta.timestamp`) — the two phases the original override named as the natural consumers — both shipped verified 2026-04-18 (Phase 3) and 2026-04-19 (Phase 4). The deferred block is no longer accurate: nothing is pending.

Replace the frontmatter `deferred:` block and the body `Resolution Note §TOL-08` with a pointer to the Phase 3/4 closure. Update the overall score line, SC-5 status cell, and per-artifact rows to reflect the closure.

Purpose: Close v2.1-MILESTONE-AUDIT.md tech-debt item 8 ("Phase 2 TOL-08 'deferred' frontmatter never retired — observable slice did land in Phase 3/4; the Phase 2 VERIFICATION.md still carries the deferred block. Doc-trail clean-up only.").

Output: `.planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md` with (a) no `deferred:` frontmatter block, (b) frontmatter `score` + `overrides_applied` + `status` fields updated, (c) body `Resolution Note §TOL-08` rewritten as a closure stamp, (d) SC-5 row in Success Criteria table flipped from DEFERRED → VERIFIED-VIA-PHASE-3/4, (e) TOL-08 row in REQ-ID coverage table flipped from PLUMBING-COMPLETE → SATISFIED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md
@.planning/v2.1-MILESTONE-AUDIT.md

<ground_truth_source>
The authoritative source is `.planning/v2.1-MILESTONE-AUDIT.md`:

- Line 83 (override carry-forward): "TOL-08's observable slice was explicitly deferred from Phase 2 to Phase 3 (TYPES-04 TS/DTM composite) and Phase 4 (HELPERS-01 `msg.meta.timestamp`). Both phases verified `passed`, so the deferral has been satisfied — but the Phase 2 VERIFICATION.md deferred block was never retired. Minor doc-trail item; not a blocker."
- Tech Debt §8 (lines 161-162): "Phase 2 TOL-08 'deferred' frontmatter never retired — observable slice did land in Phase 3/4; the Phase 2 VERIFICATION.md still carries the deferred block. Doc-trail clean-up only."
- Phase 3 summary row (line 74): "MODEL-01..07 + TYPES-01..04 (Phase 3) | 11 | passed 4/4 SC | fresh ✓ | satisfied".
- Phase 4 summary row (line 75): "HELPERS-01..07 (Phase 4) | 7 | passed 4/4 SC | fresh ✓ | satisfied".

Code-path verification (per STATE.md Accumulated Context lines 133-135):
- **Phase 3 TYPES-04 consumer site:** `src/model/types/ts.ts` ships the `parseTs` composite which delegates to `parseHl7Timestamp({})` — the handshake point where `ParseOptions.dateFormats` can flow into the cascade. Line 133 of STATE.md: "TS composite shape locked to `{ raw: string; date: Date | undefined }` per D-14 — BOTH keys ALWAYS present." Line 134: "`parseTs` delegates to `parseHl7Timestamp({})` — empty options, silent (no emit/position). Phase 4's `msg.meta.timestamp` helper may call `parseHl7Timestamp` DIRECTLY with user `dateFormats`/`emit`/`position`; the composite layer stays stateless so `Field.asTs()` works identically whether the user supplied dateFormats or not. D-10 zero duplicate date logic preserved."
- **Phase 4 HELPERS-01 consumer site:** `src/helpers/meta.ts` ships `buildMeta` which produces `msg.meta.timestamp: Date | undefined` — the documented user-facing surface. Per REQUIREMENTS.md line 42 (HELPERS-01 row): "Phase 4 Plan 02 — buildMeta with D-03 always-present, D-18 flat Date."

Both consumer sites are in production since 2026-04-19. The observable-slice deferral is therefore satisfied.

Note on warning emission: per STATE.md line 189 ("buildMeta's MSH-7 fallback warning deferred to a larger scope change"), the TIMESTAMP_FALLBACK_FORMAT warning-emission for MSH-7 through `buildMeta` was deliberately not wired — it would require lifting meta-helper parsing into the parseHL7 pipeline before warnings are frozen. This is a DISTINCT issue from the TOL-08 observable-Date slice, which IS satisfied. The warning-emission gap is already documented as a known open question in STATE.md's "Open questions" bullet; we do NOT need to close it in this plan. The TOL-08 deferral retirement narrative must explicitly note this distinction: the observable DATE contract IS satisfied; the warning-emission observable-through-msg.warnings remains a carry-over for v2 and is NOT part of TOL-08's contract.

Current 02-VERIFICATION.md state (2026-04-20):
- Frontmatter lines 4-16: `status: verified`, `score: 19/19 must-haves verified (1 deferred via override, 0 gaps open)`, `overrides_applied: 1`.
- Frontmatter lines 19-83: the entire `deferred:` block for TOL-08 — multi-paragraph rationale, traceability block, author/accepted_by line.
- Body line 92: "Overrides Recorded: 2026-04-18T20:45:00Z (TOL-08 deferred to Phase 3/4 — see frontmatter `deferred:` block and Resolution Note below)".
- Body line 93: "Status: verified (0 gaps open; PARSE-09 closed by Plan 07; TOL-08 deferred via override)".
- Body line 114 (SC-5 row): "⚠️ DEFERRED at observable slice (plumbing-complete; observable slice → Phase 3/4 per override)".
- Body line 116: "Score: 4/5 success criteria VERIFIED end-to-end; SC-5 DEFERRED at observable slice…".
- Body line 129 (dates.ts artifact row): "⚠️ ORPHANED BY DESIGN (helper exists, consumed in Phase 3/4 per override)" — update to "CONSUMED by Phase 3/4 (no longer orphaned)".
- Body line 144 (parseHl7Timestamp link row): "⚠️ DEFERRED — observable wiring lands in Phase 3 (TS/DTM) and Phase 4 (`msg.meta.timestamp`) per override. See Resolution Note §TOL-08" — update to "SATISFIED by Phase 3 TYPES-04 (src/model/types/ts.ts) + Phase 4 HELPERS-01 (src/helpers/meta.ts)".
- Body line 161 (data-flow trace row): "dateFormats → parseHl7Timestamp | `options.dateFormats` accepted + plumbed; observable call site deferred to Phase 3/4 per override | ⚠️ DEFERRED" — update to "✓ FLOWING — observable call site landed in Phase 3/4: Field.asTs() (ts.ts) + buildMeta.timestamp (meta.ts)".
- Body line 193 (TOL-08 REQ-ID row): "⚠️ PLUMBING-COMPLETE (observable slice deferred via override — see frontmatter + Resolution Note §TOL-08)" — update to "✓ SATISFIED (plumbing Plan 02-05; observable slice landed Phase 3 TYPES-04 + Phase 4 HELPERS-01)".
- Body line 216 (Gaps Summary TOL-08 entry): "TOL-08 DEFERRED via override (unchanged from previous verification) — see frontmatter `deferred:` block and Resolution Note §TOL-08 below. Observable slice lands in Phase 3 (TYPES-04 TS/DTM typed composite) and Phase 4 (HELPERS-01 `msg.meta.timestamp`)" — update to closure: "TOL-08 CLOSED (deferral satisfied 2026-04-19). Observable slice landed in Phase 3 (TYPES-04 via `src/model/types/ts.ts::parseTs` + `Field.asTs()`) and Phase 4 (HELPERS-01 via `src/helpers/meta.ts::buildMeta` populating `msg.meta.timestamp: Date | undefined`). Phase 2 deferral retired 2026-04-20 via Plan 10-04."
- Body lines 220-246 (the entire `Resolution Note §TOL-08` section) — rewrite as a closure stamp.
- Body line 251 (Verdict): "Phase 2 status: verified — 19/19 must-haves closed (PARSE-09 gap closed by Plan 07; TOL-08 deferred via override)" — update to "verified — 19/19 must-haves closed (PARSE-09 closed by Plan 07; TOL-08 deferral satisfied by Phase 3 TYPES-04 + Phase 4 HELPERS-01, retired 2026-04-20 by Plan 10-04)."
- Body line 254: "19/19 REQ-IDs closed: 18 fully end-to-end verified (PARSE-01..09, TOL-01..07, TOL-09, TOL-10); 1 plumbing-complete with observable slice deferred via override (TOL-08)" — update to "19/19 REQ-IDs closed: 19 fully end-to-end verified; TOL-08 observable slice satisfied by Phase 3/4 consumers per Plan 10-04 retirement."
- Body footer (lines 263-267): add a new footer line for 2026-04-20 Plan 10-04 retirement.

The exact consumer file path for the TS/DTM composite is `src/model/types/ts.ts` (per STATE.md line 134 which references `parseTs` + `Field.asTs()`; confirmed by STATE.md's Phase 3 Plan 03 entry `03 composites-telecom-location-timestamp-numeric` which created the file). The Phase 4 consumer is `src/helpers/meta.ts::buildMeta` (per REQUIREMENTS.md line 42). These two file paths are the ones to cite in the retirement note.

If the executor wants to double-check the exact filenames, this is allowed — run `ls src/model/types/` and `ls src/helpers/` to confirm. But the plan is authoritative on the two paths above.
</ground_truth_source>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire the frontmatter `deferred:` block + update score/overrides_applied/status</name>
  <files>.planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md</files>
  <action>
Edit the frontmatter of `.planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md` (lines 1-84).

**Replace the entire current frontmatter (lines 1-84) with this exact content:**

```
---
phase: 02-core-parser-and-tolerance
verified: 2026-04-18T21:00:00Z
overrides_recorded: 2026-04-18T20:45:00Z
overrides_retired: 2026-04-20T21:00:00Z
status: verified
score: 19/19 must-haves verified (0 deferrals open, 0 gaps open — TOL-08 deferral satisfied by Phase 3 TYPES-04 + Phase 4 HELPERS-01, retired 2026-04-20 by Plan 10-04)
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 17/19
  gaps_closed:
    - "PARSE-09 — Parser respects MSH-18 character set when set (Buffer input)"
  gaps_remaining: []
  regressions: []
  closure_plan: "02-PLAN-07-gap-closure-charset.md (wave 4, TDD, approach (c) — both options.charset override AND two-pass MSH-18 auto-discovery)"
  closure_commits:
    - "32d7ebe (RED): test(02-07): add failing tests for MSH-18 charset wiring"
    - "04a180b (GREEN): feat(02-07): wire MSH-18 charset resolution into parseHL7"
retired_overrides:
  - truth: "TOL-08 / Success Criterion #5 — `dateFormats: [...]` passed to `parseHL7` is honored end-to-end"
    original_deferral_date: 2026-04-18T20:45:00Z
    satisfied_by:
      - phase: 03-structural-model-and-types
        req_id: TYPES-04
        code_path: "src/model/types/ts.ts — parseTs delegates to parseHl7Timestamp; Field.asTs() is the typed composite access surface that exercises the cascade end-to-end"
        verified_date: 2026-04-18T00:00:00Z
      - phase: 04-named-helpers
        req_id: HELPERS-01
        code_path: "src/helpers/meta.ts — buildMeta populates msg.meta.timestamp: Date | undefined from MSH-7 through the composite layer"
        verified_date: 2026-04-19T00:00:00Z
    observable_date_slice: satisfied
    warning_emission_slice: "deferred separately — msg.warnings cannot receive TIMESTAMP_FALLBACK_FORMAT from buildMeta lazily because msg.warnings is frozen at parseHL7 construction (Phase 2 D-07). Eager MSH-7 parsing during parseHL7 would lift meta-helper parsing into the parse pipeline — separate scope change tracked in STATE.md Open Questions. NOT part of TOL-08's observable-Date contract; documented carry-over for v2."
    retired_by: "Phase 10 gap-closure Plan 10-04 (2026-04-20) — v2.1-MILESTONE-AUDIT.md tech-debt §8 closure."
---
```

Rationale for the specific edits:
- `overrides_applied: 0` because the override was retired; no override carries forward.
- `overrides_retired: 2026-04-20T21:00:00Z` is the new stamp.
- `retired_overrides:` is the new structured record for the retirement — preserves the historical audit trail (what was deferred, by which phases it was satisfied, where the code lives) in a structured, grep-able shape instead of the original multi-paragraph rationale. The body `Resolution Note §TOL-08` carries the human-readable version in Task 2.
- The warning_emission_slice sub-field makes the scope boundary explicit: Date-value observability is satisfied; the warning-emission-via-msg.warnings is a separately-tracked distinct issue that is NOT part of TOL-08.

Do NOT edit the body of the file in this task — that is Tasks 2-5.
  </action>
  <verify>
    <automated>grep -c 'overrides_applied: 0' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md — expect `1`. Also: grep -c '^deferred:$' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md — expect `0` (no `deferred:` top-level frontmatter key). Also: grep -c 'retired_overrides:' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md — expect `1`.</automated>
  </verify>
  <done>
Frontmatter `deferred:` block retired and replaced with structured `retired_overrides:` record. Score/status/overrides_applied updated. Historical re_verification block preserved verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite body `Resolution Note §TOL-08` as a closure stamp</name>
  <files>.planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md</files>
  <action>
Edit the body section `## Resolution Note §TOL-08 — `dateFormats` Deferred to Phase 3/4` (currently approximately lines 220-246 — the section between the `---` after Gaps Summary and the next `---` before `## Verdict`).

**Replace the entire section (from the `## Resolution Note §TOL-08 — ` heading through the closing `---` separator before `## Verdict`) with this exact content:**

```
## Resolution Note §TOL-08 — Deferral Satisfied by Phase 3/4 (Retired 2026-04-20)

**Status:** RETIRED 2026-04-20 via Phase 10 gap-closure Plan 10-04. Deferral satisfied by Phase 3 TYPES-04 (verified 2026-04-18) + Phase 4 HELPERS-01 (verified 2026-04-19). No action pending.

**Historical summary:** On 2026-04-18 Phase 2's TOL-08 observable slice was explicitly deferred via user-approved override to Phase 3 (typed composite TS/DTM via `msg.get('MSH.7')` / `msg.get('OBX.14')`) and Phase 4 (`msg.meta.timestamp` named helper). The deferral was recorded in the frontmatter `deferred:` block and the previous version of this Resolution Note. Rationale at the time: CONTEXT.md line 22 scoped TOL-08 for Phase 2 as "plumbing"; D-08 (CONTEXT.md:54) locked the Phase 2 Hl7Message surface to `{segments, encodingCharacters, version, warnings, profile?}` — no Phase-2-visible surface on which `dateFormats` could produce a user-observable effect; the helper `parseHl7Timestamp` + `BUILTIN_DATE_FALLBACKS` + `ParseHl7TimestampOptions` + `TIMESTAMP_FALLBACK_FORMAT` warning factory were all shipped + unit-tested in isolation (Plan 02-05) and exported through `src/index.ts`. Full rationale preserved in git history at commits 7fc2318 (original verification) + the frontmatter `retired_overrides:` block of this file.

**How the deferral was satisfied:**

1. **Phase 3 TYPES-04** (verified 2026-04-18, Plan 03-03): shipped the TS/DTM typed composite at `src/model/types/ts.ts`. `parseTs` delegates to `parseHl7Timestamp({})` (empty options, silent) at the composite layer; `Field.asTs()` — wired via Plan 03-04 into `src/model/field.ts` — is the typed access surface that exercises the cascade end-to-end from user code. The composite layer stays stateless so `Field.asTs()` works identically whether the user supplied `dateFormats` or not. TS composite shape `{ raw: string; date: Date | undefined }` per D-14 — BOTH keys ALWAYS present. See Phase 3 03-VERIFICATION.md for 4/4 SC pass.

2. **Phase 4 HELPERS-01** (verified 2026-04-19, Plan 04-02): shipped `msg.meta.timestamp: Date | undefined` at `src/helpers/meta.ts::buildMeta`. `buildMeta` is the documented user-facing path for MSH-7 specifically — the observable slice the original ROADMAP SC-5 targeted. Per REQUIREMENTS.md line 42: "HELPERS-01 — `msg.meta` exposes: `type`, `messageCode`, `triggerEvent`, `messageStructure`, `controlId`, `timestamp` (Date)… Phase 4 Plan 02 — buildMeta with D-03 always-present, D-18 flat Date." See Phase 4 04-VERIFICATION.md for 4/4 SC pass.

**What IS satisfied (observable Date slice):** A developer supplying `dateFormats: [...]` to `parseHL7(raw, { dateFormats })`, then calling `msg.meta.timestamp` or `msg.get('MSH.7', HL7.TS).date`, receives a parsed `Date` value when a user-supplied format matches — matching the ROADMAP SC-5 user-facing contract.

**What is NOT in TOL-08's scope (and remains a separately-tracked carry-over):** The `TIMESTAMP_FALLBACK_FORMAT` warning emission through `msg.warnings` when `buildMeta`'s lazy parse hits a non-HL7 MSH-7 format. This warning CAN'T land in `msg.warnings` from `buildMeta` because `msg.warnings` is frozen at `parseHL7` construction time (Phase 2 D-07). Surfacing the warning requires eager MSH-7 parsing during `parseHL7` — a separate scope change that lifts meta-helper parsing into the parse pipeline. This is documented as an Open Question in STATE.md and is explicitly NOT part of TOL-08's observable-Date contract per the original Plan 02-05 scope boundary (CONTEXT.md line 22 — "plumbing and built-in fallbacks"). Tracked for v2 or a dedicated follow-on plan.

**Retirement authority:** Phase 10 gap-closure Plan 10-04 (2026-04-20). v2.1-MILESTONE-AUDIT.md tech-debt §8 explicitly calls out: "Phase 2 TOL-08 'deferred' frontmatter never retired — observable slice did land in Phase 3/4; the Phase 2 VERIFICATION.md still carries the deferred block. Doc-trail clean-up only." This plan closes that gap.

**Cross-references:**
- `src/model/types/ts.ts` (Phase 3 TYPES-04 composite)
- `src/model/field.ts` (Phase 3 Plan 04 `Field.asTs()` wiring)
- `src/helpers/meta.ts` (Phase 4 HELPERS-01 `buildMeta`)
- `.planning/phases/03-structural-model-and-types/03-VERIFICATION.md` (Phase 3 verified 4/4 SC)
- `.planning/phases/04-named-helpers/04-VERIFICATION.md` (Phase 4 verified 4/4 SC)

---
```

Keep the following `## Verdict` section that immediately follows this separator intact — Task 5 updates it.
  </action>
  <verify>
    <automated>grep -c 'RETIRED 2026-04-20 via Phase 10 gap-closure Plan 10-04' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md — expect `≥ 1`. Also: grep -c 'src/model/types/ts.ts' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md — expect `≥ 2` (Task 2 body reference + Task 1 frontmatter reference). Also: grep -c 'src/helpers/meta.ts' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md — expect `≥ 2`.</automated>
  </verify>
  <done>
`Resolution Note §TOL-08` body section rewritten as a closure stamp with explicit Phase 3/4 consumer citations + file-path pointers + scope-boundary carve-out for the separately-tracked warning-emission issue.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update in-body headers — line 92 "Overrides Recorded" + line 93 "Status"</name>
  <files>.planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md</files>
  <action>
Edit two specific lines in the body header area (approximately lines 91-93 of the original file; use the exact-string match).

**Line ~92 — Replace:**
```
**Overrides Recorded:** 2026-04-18T20:45:00Z (TOL-08 deferred to Phase 3/4 — see frontmatter `deferred:` block and Resolution Note below)
```
With:
```
**Overrides Recorded:** 2026-04-18T20:45:00Z (TOL-08 deferred to Phase 3/4 — RETIRED 2026-04-20 per frontmatter `retired_overrides:` block and Resolution Note §TOL-08 below)
```

**Line ~93 — Replace:**
```
**Status:** verified (0 gaps open; PARSE-09 closed by Plan 07; TOL-08 deferred via override)
```
With:
```
**Status:** verified (0 gaps open; 0 deferrals open; PARSE-09 closed by Plan 07; TOL-08 deferral satisfied by Phase 3 TYPES-04 + Phase 4 HELPERS-01, retired 2026-04-20 by Plan 10-04)
```
  </action>
  <verify>
    <automated>grep -c 'TOL-08 deferral satisfied by Phase 3 TYPES-04' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md — expect `≥ 1`. Also: grep -c 'TOL-08 deferred via override)' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md — expect `0` (stale closing clause gone).</automated>
  </verify>
  <done>
Body headers updated to reflect retirement.
  </done>
</task>

<task type="auto">
  <name>Task 4: Flip per-table TOL-08 / SC-5 rows from DEFERRED to SATISFIED</name>
  <files>.planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md</files>
  <action>
Five specific table-row edits. Each is an exact-string substitution.

**Edit A — SC-5 row in Success Criteria table (line ~114):**

Old (3rd-column cell after `SC-5`):
```
| SC-5 | A developer supplying `dateFormats: [...]` sees non-HL7 timestamp formats accepted in order with a `TIMESTAMP_FALLBACK_FORMAT` warning, falling back to built-in ISO/date/US formats when no user format matches. | ⚠️ DEFERRED at observable slice (plumbing-complete; observable slice → Phase 3/4 per override) | Helper `parseHl7Timestamp` fully implemented and unit-tested (`src/parser/dates.ts:93-122`; `test/parser-dates.test.ts` full cascade coverage). `ParseOptions.dateFormats` declared, discriminated via `OPTIONS_ONLY_KEYS`, barrel-exported. Observable MSH-7 call site is out of Phase 2 scope per D-08 (blocks Phase 2 from typing MSH-derived timestamps). Plumbing satisfies Phase 2's bounded TOL-08 responsibility; observable satisfaction tracks to Phase 3 TYPES-04 and Phase 4 HELPERS-01. See frontmatter `deferred:` block and Resolution Note §TOL-08 below. |
```

New:
```
| SC-5 | A developer supplying `dateFormats: [...]` sees non-HL7 timestamp formats accepted in order with a `TIMESTAMP_FALLBACK_FORMAT` warning, falling back to built-in ISO/date/US formats when no user format matches. | ✓ VERIFIED (observable Date slice satisfied by Phase 3 TYPES-04 + Phase 4 HELPERS-01; plumbing shipped in Plan 02-05) | Plumbing (Plan 02-05): `parseHl7Timestamp` (`src/parser/dates.ts:93-122`) + `ParseOptions.dateFormats` + `OPTIONS_ONLY_KEYS` discrimination + barrel export + unit tests (`test/parser-dates.test.ts`). Observable Date slice landed Phase 3 via TS/DTM composite (`src/model/types/ts.ts::parseTs`) + `Field.asTs()` (`src/model/field.ts`), and Phase 4 via `msg.meta.timestamp` (`src/helpers/meta.ts::buildMeta`). Deferral retired 2026-04-20 by Plan 10-04 — see Resolution Note §TOL-08 below. (Warning-emission-through-msg.warnings via buildMeta is a separately-tracked carry-over distinct from TOL-08; noted under scope boundary.) |
```

**Edit B — Score line (line ~116):**

Old:
```
**Score:** 4/5 success criteria VERIFIED end-to-end; SC-5 DEFERRED at observable slice (plumbing layer complete; observable slice owned by Phase 3's TS/DTM composite and Phase 4's `msg.meta.timestamp`). After override and gap-closure, all 5 SCs are closed (4 verified, 1 deferred).
```

New:
```
**Score:** 5/5 success criteria VERIFIED end-to-end. Plumbing layer complete in Phase 2 (Plan 02-05); observable slice satisfied by Phase 3 TYPES-04 (TS/DTM composite) + Phase 4 HELPERS-01 (`msg.meta.timestamp`). TOL-08 deferral retired 2026-04-20 by Plan 10-04 — all 5 SCs verified, 0 deferred.
```

**Edit C — dates.ts artifact row (line ~129):**

Old (3rd column):
```
| `src/parser/dates.ts` | `parseHl7Timestamp` cascade + `BUILTIN_DATE_FALLBACKS` | ⚠️ ORPHANED BY DESIGN (helper exists, consumed in Phase 3/4 per override) | Symbols exported and tested in isolation; no Phase-2-visible integration call site (D-08). Phase 3/4 are the natural consumers. |
```

New:
```
| `src/parser/dates.ts` | `parseHl7Timestamp` cascade + `BUILTIN_DATE_FALLBACKS` | ✓ CONSUMED (symbols consumed by Phase 3 `src/model/types/ts.ts::parseTs` + Phase 4 `src/helpers/meta.ts::buildMeta`; no longer orphaned post-2026-04-19) | Symbols exported + unit-tested in isolation (Phase 2); consumed end-to-end by Phase 3 TS/DTM composite (`Field.asTs()`) and Phase 4 `msg.meta.timestamp` helper. Phase 2 scope boundary (D-08) held correctly: no Phase-2-visible MSH-7 integration call site was needed. |
```

**Edit D — Key Link row "parseHL7 | parseHl7Timestamp" (line ~144):**

Old (3rd + 4th columns):
```
| `parseHL7` | `parseHl7Timestamp` (`dateFormats` honoured) | none in Phase 2 | ⚠️ DEFERRED | `options.dateFormats` is declared + discriminated; observable wiring lands in Phase 3 (TS/DTM) and Phase 4 (`msg.meta.timestamp`) per override. See Resolution Note §TOL-08. |
```

New:
```
| `parseHL7` | `parseHl7Timestamp` (`dateFormats` honoured) | none in Phase 2 (intentional per D-08); landed in Phase 3 via `Field.asTs()` + Phase 4 via `buildMeta` | ✓ WIRED (end-to-end) | `options.dateFormats` declared + discriminated in Phase 2; end-to-end observable wiring landed Phase 3 TYPES-04 (`src/model/types/ts.ts`) + Phase 4 HELPERS-01 (`src/helpers/meta.ts`). Deferral retired 2026-04-20 by Plan 10-04. See Resolution Note §TOL-08. |
```

**Edit E — Data-Flow Trace row (line ~161):**

Old:
```
| `parseHL7` → `dateFormats` → `parseHl7Timestamp` | `options.dateFormats` accepted + plumbed; observable call site deferred to Phase 3/4 per override | ⚠️ DEFERRED |
```

New:
```
| `parseHL7` → `dateFormats` → `parseHl7Timestamp` | `options.dateFormats` accepted + plumbed in Phase 2; observable call sites live in Phase 3 (`src/model/types/ts.ts::parseTs` via `Field.asTs()`) + Phase 4 (`src/helpers/meta.ts::buildMeta` populating `msg.meta.timestamp`) | ✓ FLOWING (end-to-end) |
```

**Edit F — TOL-08 row in REQ-ID coverage table (line ~193):**

Old (3rd + 4th columns):
```
| **TOL-08** | `dateFormats: [...]` order-sensitive + emits `TIMESTAMP_FALLBACK_FORMAT` on non-HL7 match | ⚠️ PLUMBING-COMPLETE (observable slice deferred via override — see frontmatter + Resolution Note §TOL-08) | Helper `src/parser/dates.ts:93-122` implements full cascade with order sensitivity. Tests: `parser-dates.test.ts:46-67` (user format + order). Observable end-to-end slice deferred to Phase 3 (TS/DTM typed composite) and Phase 4 (`msg.meta.timestamp`) — D-08 bars Phase 2 from exposing typed MSH-7. |
```

New:
```
| **TOL-08** | `dateFormats: [...]` order-sensitive + emits `TIMESTAMP_FALLBACK_FORMAT` on non-HL7 match | ✓ SATISFIED (plumbing Plan 02-05; observable Date slice Phase 3 TYPES-04 + Phase 4 HELPERS-01, deferral retired 2026-04-20 by Plan 10-04) | Helper `src/parser/dates.ts:93-122` implements full cascade with order sensitivity (Phase 2 Plan 02-05; tests: `parser-dates.test.ts:46-67`). Observable end-to-end Date slice shipped Phase 3 (`src/model/types/ts.ts::parseTs` + `Field.asTs()` at `src/model/field.ts`) + Phase 4 (`src/helpers/meta.ts::buildMeta` → `msg.meta.timestamp: Date \| undefined`). Note: `TIMESTAMP_FALLBACK_FORMAT` warning-emission through `msg.warnings` from `buildMeta` is a separately-tracked scope boundary, not part of TOL-08's Date-value contract — see Resolution Note §TOL-08 below. |
```

**Edit G — TOL-09 row NOTE (line ~194) — minor update to remove the stale "observable slice follows TOL-08's override" clause:**

Old:
```
| **TOL-09** | Built-in fallbacks (ISO, `YYYY-MM-DD`, `MM/DD/YYYY`, `MM/DD/YYYY HH:mm:ss`) always tried | ✓ SATISFIED (helper) | `src/parser/dates.ts:34-39` + cascade at `:113-119`. Tests: `parser-dates.test.ts:70-110` (all 4 fallbacks). Observable slice follows TOL-08's override — inert by design in Phase 2, live in Phase 3/4. |
```

New:
```
| **TOL-09** | Built-in fallbacks (ISO, `YYYY-MM-DD`, `MM/DD/YYYY`, `MM/DD/YYYY HH:mm:ss`) always tried | ✓ SATISFIED | `src/parser/dates.ts:34-39` + cascade at `:113-119`. Tests: `parser-dates.test.ts:70-110` (all 4 fallbacks). Observable slice live via TOL-08's Phase 3/4 consumers (see §TOL-08). |
```
  </action>
  <verify>
    <automated>grep -c '⚠️ DEFERRED' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md — expect `0` (no warning-glyph DEFERRED markers remain). Also: grep -c '⚠️ PLUMBING-COMPLETE' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md — expect `0`. Also: grep -c '⚠️ ORPHANED BY DESIGN' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md — expect `0`. Also: grep -c '5/5 success criteria VERIFIED end-to-end' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md — expect `≥ 1`.</automated>
  </verify>
  <done>
All 7 table-row edits landed. No `⚠️ DEFERRED` / `⚠️ PLUMBING-COMPLETE` / `⚠️ ORPHANED BY DESIGN` ambiguity markers remain. SC-5 flipped from 4/5 → 5/5.
  </done>
</task>

<task type="auto">
  <name>Task 5: Update Gaps Summary + Verdict + footer</name>
  <files>.planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md</files>
  <action>
Three specific edits.

**Edit A — Gaps Summary TOL-08 bullet (line ~216):**

Old (the entire `- **TOL-08 DEFERRED** via override …` bullet):
```
- **TOL-08 DEFERRED** via override (unchanged from previous verification) — see frontmatter `deferred:` block and Resolution Note §TOL-08 below. Observable slice lands in Phase 3 (TYPES-04 TS/DTM typed composite) and Phase 4 (HELPERS-01 `msg.meta.timestamp`).
```

New:
```
- **TOL-08 CLOSED** — deferral satisfied 2026-04-19 by Phase 3 TYPES-04 (TS/DTM composite at `src/model/types/ts.ts`; wired via `Field.asTs()`) + Phase 4 HELPERS-01 (`msg.meta.timestamp` at `src/helpers/meta.ts::buildMeta`). Phase 2 frontmatter `deferred:` block retired 2026-04-20 via Plan 10-04 and replaced with structured `retired_overrides:` audit record; see Resolution Note §TOL-08 below for full closure narrative.
```

**Edit B — Verdict section (lines ~249-258):** Replace the entire `## Verdict` section with the following. This is the section from `## Verdict` through the closing `---` before the footer.

```
## Verdict

**Phase 2 status: verified — 19/19 must-haves closed. 0 deferrals open; 0 gaps open.**

- **5/5 success criteria VERIFIED end-to-end.** TOL-08 observable Date slice satisfied by Phase 3 TYPES-04 + Phase 4 HELPERS-01; plumbing-complete in Phase 2 Plan 02-05 as originally scoped (CONTEXT.md line 22).
- **19/19 REQ-IDs closed end-to-end:** PARSE-01..09 (PARSE-09 via Plan 02-07 gap-closure), TOL-01..10 (TOL-08 observable slice closed by Phase 3/4 consumers; Plan 10-04 retired the deferral record 2026-04-20).
- **All four pipeline gates pass cleanly**: typecheck=0, lint=0 warnings, **132/132 tests** (+9 PARSE-09 cases vs previous 123), build success (ESM 23.25 KB, CJS 23.90 KB, DTS 34.21 KB) at time of last verification. Current v2.1-milestone tally: 824 tests + 14 it.todo (per 09-04-SUMMARY).
- **No regressions:** every previously-passing test still green; pre-existing artefact sizes grew within expected O(10%) for ~60 lines of new source at Plan 02-07 closure.
- **Code quality remains high**: strict TS, no `any`, JSDoc + `@example` on every public export, no `console.*`, no TODOs, single-responsibility modules. The two new helpers (`extractMsh18FromTentativeDecode`, `resolveBufferCharset`) are `@internal`-tagged and carry comprehensive JSDoc including their Postel's-Law-motivated defensiveness and the four-branch precedence rule.
- **Zero open items.** The TOL-08 deferral — the last outstanding Phase 2 carry-forward — was retired 2026-04-20 by Plan 10-04 with explicit Phase 3/4 closure evidence.

Ready for v2.1 milestone close after Phases 10 (this phase) + 11 + 12 complete the retroactive paper-trail sweeps.

---
```

**Edit C — Footer (lines ~263-267):** Append a new line to the footer block.

Current footer block:
```
*Verified initially: 2026-04-18T20:14:00Z*
*Override recorded: 2026-04-18T20:45:00Z (TOL-08 deferred to Phase 3/4)*
*Re-verified after Plan 07 gap closure: 2026-04-18T21:00:00Z (PARSE-09 promoted PARTIAL → VERIFIED)*
*Verifier: Claude (gsd-verifier) — Override author: Claude (gsd-planner)*
```

Update to:
```
*Verified initially: 2026-04-18T20:14:00Z*
*Override recorded: 2026-04-18T20:45:00Z (TOL-08 deferred to Phase 3/4)*
*Re-verified after Plan 07 gap closure: 2026-04-18T21:00:00Z (PARSE-09 promoted PARTIAL → VERIFIED)*
*Override retired: 2026-04-20T21:00:00Z (TOL-08 deferral satisfied by Phase 3 TYPES-04 + Phase 4 HELPERS-01; Phase 10 Plan 10-04 rewrote frontmatter + Resolution Note §TOL-08 + updated all per-table status cells)*
*Verifier: Claude (gsd-verifier) — Override author: Claude (gsd-planner) — Retirement author: Claude (gsd-planner, Phase 10 gap closure)*
```
  </action>
  <verify>
    <automated>grep -c 'TOL-08 CLOSED' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md — expect `≥ 1`. Also: grep -c 'Override retired: 2026-04-20T21:00:00Z' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md — expect `1`. Also: grep -c '0 deferrals open; 0 gaps open' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md — expect `≥ 1`.</automated>
  </verify>
  <done>
Gaps Summary, Verdict, and footer all reflect the retirement. Zero open-item markers remain in the file.
  </done>
</task>

</tasks>

<verification>
After all 5 tasks land, verify the full set of grep invariants:

**Retirement landed (positive):**
1. `grep -c 'overrides_applied: 0' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md` → `1`
2. `grep -c 'retired_overrides:' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md` → `1`
3. `grep -c 'RETIRED 2026-04-20 via Phase 10 gap-closure Plan 10-04' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md` → `≥ 1`
4. `grep -c 'TOL-08 CLOSED' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md` → `≥ 1`
5. `grep -c '5/5 success criteria VERIFIED end-to-end' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md` → `≥ 1`
6. `grep -c 'Override retired: 2026-04-20T21:00:00Z' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md` → `1`
7. `grep -c 'src/model/types/ts.ts' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md` → `≥ 3` (frontmatter + Resolution Note + at least one table row)
8. `grep -c 'src/helpers/meta.ts' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md` → `≥ 3`

**Stale markers gone (negative):**
9. `grep -c '⚠️ DEFERRED' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md` → `0`
10. `grep -c '⚠️ PLUMBING-COMPLETE' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md` → `0`
11. `grep -c '⚠️ ORPHANED BY DESIGN' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md` → `0`
12. `grep -cE '^deferred:$' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md` → `0` (no top-level `deferred:` key in frontmatter)
13. `grep -c 'TOL-08 deferred via override)' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md` → `0`
14. `grep -c '4/5 success criteria VERIFIED end-to-end' .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md` → `0`

The file after this plan should read as unambiguously closed — no "will land in Phase 3/4" future-tense narrative, no `⚠️` warning glyphs on TOL-08 / SC-5 / dates.ts rows.
</verification>

<success_criteria>
- Frontmatter `deferred:` block removed; replaced with structured `retired_overrides:` audit record.
- Frontmatter `overrides_applied: 1` → `0`; `overrides_retired:` added; `score` string updated to "0 deferrals open".
- Body `## Resolution Note §TOL-08` section rewritten as closure stamp with explicit Phase 3/4 code-path pointers + scope-boundary carve-out for the separately-tracked warning-emission issue.
- Per-table rows (SC-5, dates.ts artifact, parseHl7Timestamp key link, data-flow trace, TOL-08 REQ-ID, TOL-09 note) flipped from DEFERRED/PLUMBING-COMPLETE/ORPHANED markers to SATISFIED/WIRED/FLOWING/CONSUMED.
- Gaps Summary TOL-08 bullet flipped from "DEFERRED via override" to "CLOSED — deferral satisfied".
- Verdict score flipped from 4/5 → 5/5.
- Footer append records the retirement with Plan 10-04 attribution.
- No other file modified (plan scope disjoint from Plans 10-01/02/03).
- Commit atomic: `docs(10-04): retire Phase 2 TOL-08 deferral (satisfied by Phase 3 TYPES-04 + Phase 4 HELPERS-01)`.
</success_criteria>

<output>
After completion, create `.planning/phases/10-planning-doc-resync/10-04-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md` template. Include:
- Final grep counts for all 14 verification gates.
- Confirmation that the historical re_verification block (Plan 02-07 PARSE-09 gap closure record) in the frontmatter was preserved verbatim.
- Confirmation that the scope-boundary carve-out (TIMESTAMP_FALLBACK_FORMAT warning-emission through msg.warnings being a separately-tracked carry-over, NOT part of TOL-08) is explicitly captured in three places: frontmatter `retired_overrides.warning_emission_slice`, Resolution Note §TOL-08 body paragraph, TOL-08 REQ-ID row.
- Note that if the executor encounters the exact consumer paths `src/model/types/ts.ts` or `src/helpers/meta.ts` having been renamed or restructured since the plan was written, they should resolve by inspecting the current code tree (`ls src/model/types/ && ls src/helpers/`) and substituting the live paths in all three Resolution-Note pointers.
</output>
