---
phase: 10-planning-doc-resync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - "A developer scanning `## Phases` in ROADMAP.md sees Phases 1, 2, 7, 8, 9 checked `[x]` with completion dates matching the v2.1 ground-truth."
    - "A developer reading the `## Progress` table sees all 9 shipped phases marked `Complete (verified)` or `Complete (evidence-only)` with explicit completion dates; counts per row accurately reflect shipped plan counts (e.g., 7/7 for Phase 7, 5/5 for Phase 8, 4/4 for Phase 9)."
    - "A developer grep'ing for stale phase status in ROADMAP.md finds none (no `Planned` for Phase 1/2/9; no `In Progress` for Phase 7; no `Not started` for Phase 8)."
    - "Completion markers for Phases 1-7 use a consistent italicized `*(completed YYYY-MM-DD[, verified YYYY-MM-DD])*` style — no plain-parens markers remain on verified phases."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Fully synced phase-status trail — Phases 1-9 all complete; Phases 10-12 still listed (gap-closure in progress/planned)"
      contains: "All 12 phase rows present in Progress table (ROADMAP is authoritative list of every project phase). Section heading / rollup language frames the 9-complete figure as 'v1 milestone 9/9 complete; v2.1 gap-closure Phases 10-12 in progress'."
  key_links:
    - from: ".planning/ROADMAP.md `## Phases` section"
      to: ".planning/ROADMAP.md `## Progress` table"
      via: "matching per-phase status across both surfaces"
      pattern: "Phase N checked `[x]` in `## Phases` must have matching Complete row in `## Progress`"
---

<objective>
Flip the stale Phase 1/2/7/8/9 checkboxes and rewrite the stale `## Progress` table rows in `.planning/ROADMAP.md` to reflect the v2.1 ground-truth: all 9 v1-milestone phases are complete with explicit completion dates. Normalize Phase 3's completion marker to the italicized `*(completed ..., verified ...)*` style used by Phases 4/5. Gap-closure Phases 10-12 remain listed in both surfaces (ROADMAP is the authoritative project-wide phase list) — the Progress-table rollup caption frames the 9-complete figure as "v1 milestone 9/9" with Phases 10-12 explicitly noted as v2.1-audit gap-closure work (in progress / planned). No other file is touched.

Purpose: Close v2.1-MILESTONE-AUDIT.md tech-debt item 4 (ROADMAP.md checkboxes + Progress table stale). Item 4 of the audit explicitly calls out Phases 1, 2, 7, 8, 9 still showing `[ ]` + "Not started" / "Planned" despite all 9 phases complete.

Output: `.planning/ROADMAP.md` with (a) 9/12 phase checkboxes in `## Phases` marked `[x]` with completion dates (Phases 10-12 stay `[ ]`), (b) Phase 3 marker normalized to the italicized verified-pattern, (c) `## Progress` table retains all 12 rows with 9 Complete-with-date rows + Phase 10 In Progress + Phases 11-12 Planned, with a rollup caption explicitly framing the split between v1-milestone complete and v2.1-audit gap-closure, (d) updated "Last updated" footer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v2.1-MILESTONE-AUDIT.md

<scope_split_note>
**Important scope-split convention (matched with Plan 10-03):** ROADMAP.md is the authoritative project-wide list of *every* phase, including the v2.1-audit gap-closure Phases 10-12 added in commit a73392f. The `## Progress` table therefore keeps all 12 rows. STATE.md (Plan 10-03) scopes its frontmatter `total_phases: 9` to the v1-milestone span only — the `status:` narrative still references Phases 10-12 as active follow-on work.

To keep both surfaces internally consistent, the Progress-table rollup caption (and/or a note line immediately after the table) must frame the 9-complete figure as:

> **v1 milestone:** 9/9 phases complete. **v2.1 gap-closure:** Phases 10-12 in progress (this phase = 10).

That exact split language anchors the "9 complete" claim to the v1 milestone scope, matches STATE.md's `total_phases: 9` scope choice, and keeps Phases 10-12 correctly listed as Planned / In Progress in the same table (since they ARE the gap-closure work being planned right now). Plan 10-03's STATE.md Task 1 carries the reciprocal scope note.
</scope_split_note>

<ground_truth_source>
The authoritative source is `.planning/v2.1-MILESTONE-AUDIT.md`:

- Executive Summary line 59: "All nine phases have shipped their code. The live pipeline is green…"
- Frontmatter line 44 (cross-cutting tech debt): "ROADMAP.md Phase checkboxes stale — Phases 1, 2, 7, 8, 9 show `[ ]` despite being complete; Progress table shows 3/8 phases verified but init-milestone reports all 9/9 complete".

Completion dates (extracted from ROADMAP.md inline `*(completed YYYY-MM-DD)*` markers + SUMMARY.md dates):
- **Phase 1**: 2026-04-18 (per STATE.md frontmatter: Plan 01-04 smoke-verification pipeline completed 2026-04-18).
- **Phase 2**: 2026-04-18 (per 02-VERIFICATION.md frontmatter: verified 2026-04-18T21:00:00Z; Plan 02-07 gap-closure landed same-day).
- **Phase 3**: 2026-04-18 (currently marked `(completed 2026-04-18)` on line 16 — NEEDS NORMALIZATION to `*(completed 2026-04-18, verified 2026-04-18)*` to match Phase 4/5's style; Phase 3 has 03-VERIFICATION.md on disk and passed 4/4 SC per audit).
- **Phase 4**: 2026-04-19 (already marked `*(completed 2026-04-19, verified 2026-04-19)*` on line 17 — style reference).
- **Phase 5**: 2026-04-19 (already marked `*(completed 2026-04-19, verified 2026-04-19)*` on line 18).
- **Phase 6**: 2026-04-19 (already marked `*(completed 2026-04-19)*` on line 19).
- **Phase 7**: 2026-04-19 (already marked `*(completed 2026-04-19)*` on line 20 — execution complete; Plan 07-06 capstone landed 2026-04-19 closing TEST-01).
- **Phase 8**: 2026-04-20 (per Plan 08-05 SUMMARY + inline marker on lines 174/177 `✅ 2026-04-20`; Plans 01/03/04 executed earlier same day).
- **Phase 9**: 2026-04-20 (per 09-04-SUMMARY + `docs(09-04)` commits in recent git log; package rename landed + tarball dry-run clean same day).

Status vocabulary choice (per roadmap house style):
- Phases with VERIFICATION.md on disk → `Complete (verified)`.
- Phases shipped with evidence only (SUMMARY + pipeline green, no VERIFICATION.md) → `Complete (evidence-only)`.
- Per audit frontmatter: Phase 1 (evidence-only; VERIFICATION.md missing per tech debt item 1); Phase 2 (verified — VERIFICATION.md exists); Phase 3 (verified); Phase 4 (verified); Phase 5 (verified); Phase 6 (verified); Phase 7 (evidence-only — VERIFICATION.md missing per audit); Phase 8 (evidence-only); Phase 9 (evidence-only).

(Note: ROADMAP line 17/18 already use `Complete (verified)` for Phase 4/5 — that vocabulary matches the audit's "VERIFICATION.md exists" phases. Phase 2's VERIFICATION.md does exist on disk; Phase 3 was marked `(completed 2026-04-18)` only but VERIFICATION does exist. For consistency, use the `verified` label when VERIFICATION.md exists and `evidence-only` otherwise, and normalize Phase 3's inline `## Phases` marker to the italicized-with-verified pattern.)
</ground_truth_source>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip Phase 1/2/7/8/9 checkboxes + normalize Phase 3 marker in `## Phases` section</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Edit the `## Phases` section (lines 12-25).

Flip the 5 stale checkboxes from `- [ ]` to `- [x]`, append completion dates, AND normalize Phase 3's marker to the italicized verified-pattern. Preserve each phase's existing goal description verbatim — only the `[ ]` → `[x]` substitution, the appended `*(completed YYYY-MM-DD[, verified YYYY-MM-DD])*` suffix, and the one Phase 3 marker normalization below.

**Line 14 (Phase 1):**
Old: `- [ ] **Phase 1: Project Foundation** — Scaffold the repo, build, lint, and TypeScript toolchain so any subsequent phase can iterate.`
New: `- [x] **Phase 1: Project Foundation** — Scaffold the repo, build, lint, and TypeScript toolchain so any subsequent phase can iterate. *(completed 2026-04-18)*`

(Phase 1 stays plain `*(completed …)*` — no verified date because Phase 11 will produce the retroactive VERIFICATION.md for Phase 1; marker will be re-normalized to include `verified` at that time.)

**Line 15 (Phase 2):**
Old: `- [ ] **Phase 2: Core Parser & Tolerance** — Tokenize HL7 input into segments/fields/components/subcomponents with a lenient default, warnings system, and strict-mode escalation.`
New: `- [x] **Phase 2: Core Parser & Tolerance** — Tokenize HL7 input into segments/fields/components/subcomponents with a lenient default, warnings system, and strict-mode escalation. *(completed 2026-04-18, verified 2026-04-18)*`

**Line 16 (Phase 3) — NORMALIZATION (new):**
Phase 3 currently carries a plain-parens marker; has 03-VERIFICATION.md on disk (passed 4/4 SC per audit). Normalize its marker to the italicized verified-pattern used by Phases 4/5.

Old (line 16 tail — exact substring): `(completed 2026-04-18)`
New: `*(completed 2026-04-18, verified 2026-04-18)*`

(This substring substitution affects ONLY line 16 — the other lines do not contain the exact `(completed 2026-04-18)` plain-parens substring. After the Phase 2 flip above, Phase 2 already uses the italicized pattern with both dates.)

(Lines 17-19 for Phases 4, 5, 6 are ALREADY `[x]` with italicized markers — skip. Phase 6 uses `*(completed 2026-04-19)*` without `verified` — leave unchanged; Phase 6 has 06-VERIFICATION.md, but marker normalization for Phase 6 is out of scope since its marker is already italicized.)

**Line 20 (Phase 7)** already has `[x]` and an italicized date — verify but DO NOT modify if correct:
Expected: `- [x] **Phase 7: Testing Hardening & Fixtures** — Canonical, edge-case, vendor-quirk, strict-mode, and profile-authoring test suites that verify ≥ 90% coverage on core modules. *(completed 2026-04-19)*`

If line 20 shows `[x]` and `*(completed 2026-04-19)*` — leave it verbatim.

**Line 21 (Phase 8):**
Old: `- [ ] **Phase 8: Examples, Starter Kit & Documentation** — Three runnable examples, publishable profile starter kit, and the complete README + ancillary docs.`
New: `- [x] **Phase 8: Examples, Starter Kit & Documentation** — Three runnable examples, publishable profile starter kit, and the complete README + ancillary docs. *(completed 2026-04-20)*`

**Line 22 (Phase 9):**
Old: `- [ ] **Phase 9: Rename Package to @cosyte/hl7** — Rename the package from `@cosyte/hl7-parser` to `@cosyte/hl7` (full HL7 toolkit — parser, builder, mutator, serializer, helpers — not just a parser).`
New: `- [x] **Phase 9: Rename Package to @cosyte/hl7** — Rename the package from `@cosyte/hl7-parser` to `@cosyte/hl7` (full HL7 toolkit — parser, builder, mutator, serializer, helpers — not just a parser). *(completed 2026-04-20)*`

(Lines 23, 24, 25 for Phases 10, 11, 12 stay `[ ]` — they are gap-closure phases that have not shipped yet. Phase 10 IS this phase — it flips to `[x]` only when this entire phase (all 4 plans) lands.)
  </action>
  <verify>
    <automated>grep -cE '^- \[ \] \*\*Phase [12789]:' .planning/ROADMAP.md — expect `0` (zero stale Phase 1/2/7/8/9 checkboxes). Also: grep -cE '^- \[x\] \*\*Phase [1-9]:' .planning/ROADMAP.md — expect `9` (all 9 shipped phases checked). Also: grep -c '\*(completed 2026' .planning/ROADMAP.md — expect `>= 7` (Phases 1-7 inline markers use the italicized `*(completed 2026-…)*` pattern; Phases 8-9 also use italicized markers making the total 9). Also: grep -c '(completed 2026-04-18)' .planning/ROADMAP.md | awk '$1 <= 0 { exit 0 } { exit 1 }' — expect `0` plain-parens `(completed 2026-04-18)` substrings remaining outside italicized wrappers (check manually: `grep -n '(completed 2026-04-18)' .planning/ROADMAP.md` should only match lines where the substring is wrapped inside `*...*`).</automated>
  </verify>
  <done>
Phases 1, 2, 7, 8, 9 checkboxes flipped to `[x]` with completion dates; Phase 3 marker normalized to italicized `*(completed 2026-04-18, verified 2026-04-18)*`. Combined with Phases 4, 5, 6 (already `[x]` + italicized), all 9 shipped phases now show `[x]` with italicized markers in `## Phases`. Phases 10, 11, 12 remain `[ ]` (still planned / gap-closure).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite `## Progress` table with accurate per-phase status + v1/v2.1 rollup split caption</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Edit the `## Progress` section (lines 259-274). Rewrite the entire table body so every row reflects the v2.1 ground-truth. Keep the header row verbatim. Add a rollup caption line immediately after the table that frames the 9-complete figure against the v1-milestone / v2.1-gap-closure scope split (see `<scope_split_note>` in `<context>` above — this caption keeps ROADMAP internally consistent with STATE.md's `total_phases: 9` scope choice in Plan 10-03).

The table structure:
```
| Phase | Plans Complete | Status | Completed |
|-------|----------------|--------|-----------|
```

**Replace the current 12-row body (lines 263-274) with this exact content (all 12 rows preserved — ROADMAP is the authoritative project-wide phase list):**

```
| 1. Project Foundation | 4/4 | Complete (evidence-only) | 2026-04-18 |
| 2. Core Parser & Tolerance | 7/7 | Complete (verified) | 2026-04-18 |
| 3. Structural Model & Types | 4/4 | Complete (verified) | 2026-04-18 |
| 4. Named Helpers | 4/4 | Complete (verified) | 2026-04-19 |
| 5. Serialization & Round-Trip | 5/5 | Complete (verified) | 2026-04-19 |
| 6. Profile System & Built-ins | 6/6 | Complete (verified) | 2026-04-19 |
| 7. Testing Hardening & Fixtures | 7/7 | Complete (evidence-only) | 2026-04-19 |
| 8. Examples, Starter Kit & Documentation | 5/5 | Complete (evidence-only) | 2026-04-20 |
| 9. Rename Package to @cosyte/hl7 | 4/4 | Complete (evidence-only) | 2026-04-20 |
| 10. Planning-Doc Resync | 0/4 | In Progress (gap closure) | — |
| 11. Retroactive Verification | 0/3 | Planned (gap closure) | — |
| 12. Retroactive Nyquist Validation | 0/6 | Planned (gap closure) | — |
```

**Immediately after the table (new line, followed by one blank line separator before any subsequent existing content), insert this rollup-split caption:**

```
**v1 milestone:** 9/9 phases complete. **v2.1 gap-closure:** Phases 10-12 in progress (this phase = 10). The 9-complete figure scopes to the v1 milestone span; Phases 10-12 are v2.1-audit-follow-on tech-debt gap-closure phases (ROADMAP lists every phase for project-wide traceability; STATE.md's `total_phases: 9` frontmatter value reflects the same v1-scope rollup).
```

Notes on the values:
- **Plans Complete counts** match the ROADMAP body: Phase 1 = 4 plans (lines 42-45); Phase 2 = 7 plans (6 original + Plan 02-07 gap-closure per VERIFICATION.md frontmatter line 15); Phase 3 = 4 plans; Phase 4 = 4 plans; Phase 5 = 5 plans; Phase 6 = 6 plans; Phase 7 = 7 plans (lines 152-158); Phase 8 = 5 plans (lines 173-177); Phase 9 = 4 plans (lines 235-238); Phase 10 = 4 plans (this phase's plan count — in progress as 10-01/02/03/04 execute); Phase 11 = 3 plans (one per missing VERIFICATION.md — Phases 01, 08, 09); Phase 12 = 6 plans (one per missing VALIDATION.md).
- **Status vocabulary** per audit frontmatter line 11 (`compliant_phases: [04, 05, 06]`) and audit body's VERIFICATION.md inventory (Phases 02, 03, 04, 05, 06 have VERIFICATION.md; Phases 01, 07, 08, 09 do not):
  - Phases with VERIFICATION.md = `Complete (verified)` → Phases 2, 3, 4, 5, 6.
  - Phases with shipped code + green pipeline but no VERIFICATION.md = `Complete (evidence-only)` → Phases 1, 7, 8, 9.
  - Gap-closure phases not yet executed = `Planned (gap closure)`.
  - This phase (10) = `In Progress (gap closure)` since it is actively executing when this table is written.
- **Completion dates** are the ground-truth dates enumerated in the context section above.
- **Rollup caption** explicitly separates the v1-milestone rollup (9/9) from the v2.1 gap-closure rows (10-12) so that a reader reconciling ROADMAP against STATE.md's `total_phases: 9` scope choice sees the same split language on both surfaces.
  </action>
  <verify>
    <automated>grep -cE '\| Complete \((verified|evidence-only)\) \| 2026-04-' .planning/ROADMAP.md — expect `>= 9` (9 shipped phases). Also: grep -c 'Not started' .planning/ROADMAP.md — expect `0` (the stale "Not started" status for Phase 8 is gone). Also: grep -c '\*\*v1 milestone:\*\* 9/9 phases complete' .planning/ROADMAP.md — expect `1` (rollup-split caption landed). Also: grep -c '\*\*v2.1 gap-closure:\*\* Phases 10-12' .planning/ROADMAP.md — expect `1` (caption's gap-closure clause landed).</automated>
  </verify>
  <done>
Progress table reflects v2.1 ground-truth: 9 phases Complete with dates, Phase 10 In Progress, Phases 11/12 Planned. Rollup caption immediately after the table explicitly frames the 9-complete figure as v1-milestone scope, with Phases 10-12 called out as v2.1 gap-closure. No stale "Not started" / "In Progress (Wave 2 …)" / "Planned" rows remain for shipped phases.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update "Last updated" footer</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Edit the very last line of `.planning/ROADMAP.md` (currently line 278):

Replace the existing multi-line `*Last updated: 2026-04-20 (Phase 7 Plan 03 COMPLETE …` paragraph (entirely — from `*Last updated:` through the closing `*`) with this single-line replacement:

```
*Last updated: 2026-04-20 (Phase 10 gap-closure Plan 10-02 — ROADMAP resynced to v2.1 ground-truth: Phases 1/2/7/8/9 checkboxes flipped to `[x]` with completion dates; Phase 3 marker normalized to italicized `*(completed 2026-04-18, verified 2026-04-18)*`; Progress table rewritten with all 12 rows preserved + rollup-split caption separating v1-milestone 9/9 from v2.1 gap-closure Phases 10-12, matching STATE.md's `total_phases: 9` scope choice in Plan 10-03.)*
```

Do NOT modify any other line in the file.
  </action>
  <verify>
    <automated>grep -c 'Phase 10 gap-closure Plan 10-02' .planning/ROADMAP.md — expect at least `1`.</automated>
  </verify>
  <done>
Footer updated with 2026-04-20 resync note citing Phase 10 Plan 10-02 and the scope-split convention shared with Plan 10-03.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks land, verify the load-bearing grep invariants:

1. `grep -cE '^- \[ \] \*\*Phase [12789]:' .planning/ROADMAP.md` → `0` (stale Phase 1/2/7/8/9 checkboxes gone)
2. `grep -cE '^- \[x\] \*\*Phase [1-9]:' .planning/ROADMAP.md` → `9` (all 9 shipped phases checked)
3. `grep -c 'Not started' .planning/ROADMAP.md` → `0`
4. `grep -c 'Phase 10 gap-closure Plan 10-02' .planning/ROADMAP.md` → `>= 1`

Phase-3 marker normalization + italicized-pattern consistency:
5. `grep -c '\*(completed 2026' .planning/ROADMAP.md` → `>= 7` (Phases 1-7 inline markers use italicized `*(completed 2026-…)*`; Phases 8-9 also italicized makes total 9 — but Phase 8/9 landed via Task 1 above)
6. `grep -n '(completed 2026-04-18)' .planning/ROADMAP.md` (informational — every match should be wrapped inside `*...*`, i.e., part of the italicized pattern; no plain-parens standalone `(completed 2026-04-18)` should remain as the line's only marker)

Rollup-split caption + table additionally:
7. `grep -c 'Complete (verified)' .planning/ROADMAP.md` → `>= 5` (Phases 2, 3, 4, 5, 6)
8. `grep -c 'Complete (evidence-only)' .planning/ROADMAP.md` → `>= 4` (Phases 1, 7, 8, 9)
9. `grep -c '\*\*v1 milestone:\*\* 9/9 phases complete' .planning/ROADMAP.md` → `1` (caption landed)
10. `grep -c '\*\*v2.1 gap-closure:\*\* Phases 10-12' .planning/ROADMAP.md` → `1` (caption's gap-closure clause landed)
</verification>

<success_criteria>
- Phases 1, 2, 7, 8, 9 checkboxes flipped to `[x]` in `## Phases` (lines 14, 15, 21, 22; line 20 already correct).
- Phase 3 marker at line 16 normalized from `(completed 2026-04-18)` to `*(completed 2026-04-18, verified 2026-04-18)*` (matches Phase 4/5 style).
- `## Progress` table fully rewritten with ALL 12 rows preserved — 9 Complete with dates, 1 In Progress (this phase), 2 Planned. ROADMAP stays the authoritative project-wide phase list.
- Rollup-split caption immediately after the table explicitly frames the 9-complete figure as v1-milestone scope with Phases 10-12 called out as v2.1 gap-closure — internally consistent with Plan 10-03's STATE.md `total_phases: 9` scope choice.
- Footer updated to cite Plan 10-02 and note the scope-split convention.
- No other content modified (phase details bodies lines 31-238 stay verbatim).
- Commit atomic: `docs(10-02): resync ROADMAP.md checkboxes + Progress table to v2.1 ground-truth`.
</success_criteria>

<output>
After completion, create `.planning/phases/10-planning-doc-resync/10-02-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md` template. Include:
- Final grep counts as evidence for all 10 verification gates.
- Per-phase completion-date table (cross-references ROADMAP body markers + SUMMARY.md dates).
- Confirmation that Phase 3 marker normalization landed at line 16.
- Confirmation that Phases 10/11/12 checkboxes and their Progress rows stay unmodified as `[ ]` / Planned/In Progress — all 12 rows preserved in the table per ROADMAP-as-authoritative-project-list convention.
- Confirmation that the rollup-split caption is word-for-word matched with Plan 10-03's STATE.md Task 1 scope note.
</output>
</content>
</invoke>