---
phase: 11-retroactive-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md
autonomous: true
requirements: []
gap_closure: true
tags: [retroactive-verification, paper-trail, phase-09, gap-closure, v2.1-audit, package-rename, publish]
must_haves:
  truths:
    - "A developer opens .planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md and sees YAML frontmatter with status: passed and score: 5/5 truths verified (one per ROADMAP Phase 9 Success Criterion) and overrides_applied: 0."
    - "A developer running `grep -c '✓ VERIFIED' .planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md` counts at least 5 hits in the Observable Truths table (SC-1..SC-5) covering grep sweep, pipeline, round-trip install, docs consistency, and publish.yml."
    - "A developer reading the Verification Commands table sees the grep sweep command `git grep '@cosyte/hl7-parser'` with its exact match count (1, in CHANGELOG.md breadcrumb per D-07), and the `pnpm publish --dry-run` output attesting name: @cosyte/hl7, 10 files, ~346 kB tarball, filename cosyte-hl7-0.1.0.tgz."
    - "A developer reading the Plan-Summary Evidence table sees exactly 4 rows (one per 09-0N-SUMMARY.md: 09-01 identity files, 09-02 source+tests, 09-03 examples+kit, 09-04 verification+publish dry-run), each with its plan subject and commits."
    - "A developer reading the VERIFICATION.md sees that Phase 9 has ZERO new functional REQ-IDs (rename-only) — the Requirements Coverage section explicitly attests this, not leaving the reader to infer it."
  artifacts:
    - path: ".planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md"
      provides: "Retroactive verification report for Phase 9 — Rename Package to @cosyte/hl7, matching the 02-VERIFICATION.md / 10-VERIFICATION.md shape"
      contains: "YAML frontmatter (phase, verified, status: passed, score: 5/5, overrides_applied: 0); Phase Goal verbatim from ROADMAP.md; Verification Commands table including the grep sweep + pnpm publish --dry-run; Observable Truths table (5 SC rows); Required Artifacts table (package.json@cosyte/hl7, CHANGELOG rename note, publish.yml under new name, etc.); Plan-Summary Evidence table (4 plan rows); explicit Requirements Coverage note attesting zero new functional REQs; Verdict section"
  key_links:
    - from: ".planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md (Observable Truths)"
      to: ".planning/ROADMAP.md (Phase 9 Success Criteria block, lines 228-232)"
      via: "one row per SC, success-criterion text quoted verbatim"
      pattern: "SC-1 through SC-5 appear as 5 rows"
    - from: ".planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md (Plan-Summary Evidence)"
      to: "4 files: 09-01-SUMMARY.md, 09-02-SUMMARY.md, 09-03-SUMMARY.md, 09-04-SUMMARY.md"
      via: "one row per SUMMARY with plan subject + commit"
      pattern: "exactly 4 plan rows"
    - from: ".planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md (Verification Commands)"
      to: "authoritative grep sweep + publish --dry-run result"
      via: "table row citing `git grep @cosyte/hl7-parser` with match count = 1 (CHANGELOG breadcrumb only) AND `pnpm publish --dry-run` with name=@cosyte/hl7, 10 files, ~346 kB"
      pattern: "both 'git grep' and 'publish --dry-run' strings appear in the Verification Commands table"
---

<objective>
Produce `.planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md` — the missing Phase 9 verification report. Evidence already exists on disk (4 plan SUMMARYs + 09-04-SUMMARY is itself a near-verification-shape document containing the authoritative grep sweep + full pipeline + `pnpm publish --dry-run`). This plan ratifies it by (1) re-running the authoritative grep sweep + publish --dry-run at HEAD, (2) confirming docs consistency, (3) assembling the VERIFICATION.md from 09-04-SUMMARY's evidence + the 4 plan SUMMARYs.

Phase 9 is rename-only — zero new functional REQ-IDs. Therefore the verification is NOT about feature coverage but about sweep completeness + observable-rename attestation. The Verification Commands must include the authoritative `git grep "@cosyte/hl7-parser"` sweep (expected match count: exactly 1, in `CHANGELOG.md` D-07 breadcrumb) and the `pnpm publish --dry-run` output showing the new tarball name.

Purpose: Close v2.1-MILESTONE-AUDIT tech-debt item 1 for Phase 9. Mechanical assembly.

Output: One new file at `.planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md` committed in a single `docs(phase-09): add retroactive VERIFICATION.md — 5/5 success criteria verified` commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/templates/verification-report.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference verification shapes
@.planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md
@.planning/phases/10-planning-doc-resync/10-VERIFICATION.md

# Phase 9 evidence (MUST read all 4)
@.planning/phases/09-rename-package-to-cosyte-hl7/09-01-SUMMARY.md
@.planning/phases/09-rename-package-to-cosyte-hl7/09-02-SUMMARY.md
@.planning/phases/09-rename-package-to-cosyte-hl7/09-03-SUMMARY.md
@.planning/phases/09-rename-package-to-cosyte-hl7/09-04-SUMMARY.md

# Phase 9 ROADMAP anchor (Success Criteria lines 228-232; 5 SCs — note they appear after the Phase 12 block in ROADMAP due to historical editing)
@.planning/ROADMAP.md

@CLAUDE.md
</context>

<phase_goal>
Goal (copy verbatim into VERIFICATION.md header from ROADMAP.md line 181): "Rename the published package from `@cosyte/hl7-parser` to `@cosyte/hl7` so the name reflects the actual surface area — a full HL7 v2 toolkit (parser, builder, mutator, serializer, and helpers), not just a parser. Every reference in source, configs, docs, examples, starter kit, and CI/publish workflows points at the new name; the first published tag under the new name installs and round-trips cleanly."

Success Criteria count: 5 (ROADMAP.md lines 228-232). Score denominator = 5, status = `passed`.

Phase 9 REQ-IDs: NONE (rename-only phase — ROADMAP line 183 explicitly notes "none — rename-only phase; no new functional REQ-IDs"). The Requirements Coverage section in the VERIFICATION.md MUST explicitly state this; do NOT fabricate REQ-IDs or leave the reader to infer it.
</phase_goal>

<tasks>

<task type="auto">
  <name>Task 1: Re-run authoritative grep sweep + full pipeline + pnpm publish --dry-run at HEAD</name>
  <read_first>
    - 09-04-SUMMARY.md (the previous authoritative run — lines 14-21 for grep sweep, lines 34-47 for pipeline table, lines 52-75 for publish dry-run output; Task 1 of this plan is essentially 09-04's Task 1 re-executed at HEAD)
    - 09-01-SUMMARY.md (identity-files rename: package.json, CHANGELOG.md, root-level docs)
    - 09-02-SUMMARY.md (source + tests sweep)
    - 09-03-SUMMARY.md (examples + starter kit sweep)
    - package.json (confirm `"name": "@cosyte/hl7"` at HEAD)
    - CHANGELOG.md (confirm D-07 breadcrumb line still present — the SINGLE allowed @cosyte/hl7-parser occurrence)
    - .github/workflows/publish.yml (confirm references @cosyte/hl7 or uses publishConfig; no @cosyte/hl7-parser path)
  </read_first>
  <files>(none — read-only capture for Task 5)</files>
  <action>
    Run each command at repo root; capture exit code + load-bearing output.

    Grep sweep (authoritative SC-1 evidence):
    1. `git grep -n "@cosyte/hl7-parser" -- ':!node_modules/' ':!dist/' ':!.planning/' ':!coverage/' ':!.git/'` — expected exactly 1 match: `CHANGELOG.md:<line>:Notes: Package renamed from \`@cosyte/hl7-parser\` to \`@cosyte/hl7\` before first publish. No consumers existed under the previous name.` (or similar wording per 09-01-SUMMARY D-07 breadcrumb). Record the count (must be 1) and the matching line verbatim.
    2. Workflow cross-check: `grep -c "@cosyte/hl7-parser" .github/workflows/publish.yml .github/workflows/ci.yml 2>/dev/null` — expect 0 matches in both (per 09-04-SUMMARY line 27-30).
    3. Current package name: `node -e 'console.log(require("./package.json").name)'` — expect `@cosyte/hl7`.

    Full pipeline (SC-2 evidence — same pipeline run as Plan 11-01 and 11-02 Task 1; the commands themselves are the shared attestation, just interpreted differently per phase):
    4. `pnpm install --frozen-lockfile` — exit 0.
    5. `pnpm typecheck` — exit 0.
    6. `pnpm lint --max-warnings=0` — exit 0.
    7. `pnpm test -- --run` — exit 0; capture test count.
    8. `pnpm build` — exit 0; capture dist sizes.
    9. `pnpm examples` — exit 0 (SC-2 demands this; 09-04-SUMMARY line 42 recorded it green).

    Round-trip install + import (SC-3 observable):
    10. The full end-user installation round-trip (`npm install @cosyte/hl7` in a fresh dir + `import { parseHL7, buildMessage } from "@cosyte/hl7"` + run a canonical fixture) CANNOT be fully executed without actually publishing. The closest deterministic substitute is the `pnpm publish --dry-run` tarball analysis PLUS a local `pnpm pack` round-trip proof if feasible. Run:
        - `pnpm publish --dry-run --no-git-checks 2>&1` — captures tarball manifest + size + name + version. Expected output includes `name: @cosyte/hl7`, `version: 0.1.0`, `filename: cosyte-hl7-0.1.0.tgz`, `total files: 10`, `package size: ~346 kB` (per 09-04-SUMMARY lines 52-75). Record the full Tarball Details block.
        - (Optional, if `pnpm pack` succeeds without network): `pnpm pack --pack-destination /tmp/hl7-pack-test 2>&1 | tail -5` — creates an actual `.tgz`; confirms tarball integrity without publishing.
    11. Confirm `import { parseHL7, buildMessage } from "@cosyte/hl7"` works via local pack test: `mkdir -p /tmp/hl7-rt-test && cd /tmp/hl7-rt-test && npm init -y >/dev/null && npm install <repo>/dist/../<pack-tgz-path> 2>&1 | tail -3 && node -e 'const h = require("@cosyte/hl7"); console.log("names:", typeof h.parseHL7, typeof h.buildMessage)' && cd - && rm -rf /tmp/hl7-rt-test && rm -rf /tmp/hl7-pack-test` — expect `names: function function`. If local-pack round-trip is too fiddly in the sandboxed env, skip with a note citing 09-04-SUMMARY's dry-run tarball manifest as the best available substitute; do NOT block on this check.

    Docs consistency (SC-4):
    12. `grep -c "@cosyte/hl7" README.md` ≥ 5 (the new name should appear multiple times in the README).
    13. `grep -c "@cosyte/hl7-parser" README.md` — expect 0.
    14. `grep -c "@cosyte/hl7" CHANGELOG.md` ≥ 1 (new entry for the rename).
    15. `grep -c "rename\|renamed" CHANGELOG.md` ≥ 1 (migration note per SC-4 requirement).
    16. `grep -c "@cosyte/hl7-parser" CHANGELOG.md` = 1 (the breadcrumb — same hit as Task 1 Command 1).
    17. `grep -c "@cosyte/hl7" examples/profile-starter-kit/package.json` ≥ 1 (kit's peer-dep/devDep references new name).
    18. `grep -c "@cosyte/hl7-parser" examples/profile-starter-kit/` (recursive via `grep -rc "@cosyte/hl7-parser" examples/profile-starter-kit/ 2>/dev/null | grep -v ':0' | wc -l`) — expect 0.

    Publish workflow (SC-5):
    19. `grep -cE "@cosyte/hl7" .github/workflows/publish.yml` ≥ 0 (the workflow may publish by implicit `package.json.name` rather than literal — as long as command 2 confirms zero old-name references, SC-5 is satisfied; note explicitly in the Evidence column that the workflow publishes by reference to package.json.name, not a hard-coded package name).
    20. `grep -c "workflow_dispatch" .github/workflows/publish.yml` ≥ 1 — confirms trigger shape per 08-05-SUMMARY.
    21. `grep -c "@cosyte/hl7-parser" .github/workflows/publish.yml` = 0 (legacy-name reference retired per SC-5).

    Record timestamp (ISO-8601 UTC) for verified frontmatter.

    STOP if:
    - grep sweep (cmd 1) returns ≠ 1 match (more than 1 or zero = violation of D-07 breadcrumb contract).
    - Any pipeline command fails.
    - Any docs-consistency grep hits an unexpected @cosyte/hl7-parser occurrence.
    Any of these invalidate the v2.1 milestone-close claim and must be escalated, not papered over.
  </action>
  <verify>
    <automated>test "$(git grep -c '@cosyte/hl7-parser' -- ':!node_modules/' ':!dist/' ':!.planning/' ':!coverage/' ':!.git/' 2>/dev/null | grep -v ':0' | wc -l)" -eq 1 && pnpm install --frozen-lockfile && pnpm typecheck && pnpm lint --max-warnings=0 && pnpm test -- --run && pnpm build && pnpm examples && pnpm publish --dry-run --no-git-checks</automated>
  </verify>
  <acceptance_criteria>
    - Grep sweep returns EXACTLY 1 match, and the matching file is `CHANGELOG.md` (verify path via the `git grep` output line prefix).
    - `grep -c "@cosyte/hl7-parser" .github/workflows/publish.yml .github/workflows/ci.yml` returns 0 for both.
    - `package.json` `name` field = `@cosyte/hl7` exactly.
    - Full pipeline (install / typecheck / lint / test / build / examples) all exit 0.
    - `pnpm publish --dry-run` output contains `name: @cosyte/hl7`, `filename: cosyte-hl7-0.1.0.tgz`, and `total files: 10` (size may vary ±5% from 346 kB baseline but should be in 320-380 kB range).
    - README + CHANGELOG docs-consistency greps all return expected counts (no stale old-name hits, new-name hits ≥ thresholds).
    - Task journal has: verified_at ISO-8601, all exit codes, grep counts, full publish dry-run tarball block.
    - If ANY of the STOP conditions fire: do NOT proceed to Tasks 2-6; escalate.
  </acceptance_criteria>
  <done>
    Task journal complete: grep sweep result (1 match in CHANGELOG); pipeline green; publish dry-run captured verbatim; docs greps clean; timestamps recorded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Assemble Observable Truths table (5 rows — one per Phase 9 Success Criterion)</name>
  <read_first>
    - .planning/ROADMAP.md lines 228-232 (5 SCs — verbatim text required; note these are listed under the Phase 9 Plans block due to ROADMAP editing history)
    - 09-01-SUMMARY.md (identity-file rename per Plan 01)
    - 09-02-SUMMARY.md (source + tests)
    - 09-03-SUMMARY.md (examples + starter kit)
    - 09-04-SUMMARY.md (verification + publish dry-run — pre-existing)
    - 02-VERIFICATION.md Observable Truths shape
  </read_first>
  <files>(none — draft table for Task 5)</files>
  <action>
    Compose 5 rows: `#` / `Success Criterion` (verbatim from ROADMAP) / `Status` (`✓ VERIFIED`) / `Evidence`.

    Row SC-1 (ROADMAP line 228):
    "A developer searching the repo for `@cosyte/hl7-parser` finds zero occurrences outside of CHANGELOG rename-history entries."
    Evidence: Task 1 grep sweep result — exactly 1 match, in `CHANGELOG.md` breadcrumb line (cite the exact line text captured in Task 1 Command 1). Cross-ref: 09-04-SUMMARY lines 14-21. Commits: all 4 Phase 9 plans contributed (cite at least 09-01 hash for the identity files + 09-02 for source + 09-03 for kit).

    Row SC-2 (ROADMAP line 229):
    "A developer running `pnpm install && pnpm build && pnpm test && pnpm examples` against the renamed repo sees every command exit 0 with zero warnings."
    Evidence: Task 1 pipeline results (cmds 4, 5, 6, 7, 8, 9 all exit 0; test count = 824 + 14 todo per STATE.md; build sizes from Task 1 cmd 8). Cross-ref: 09-04-SUMMARY Task 2 table.

    Row SC-3 (ROADMAP line 230):
    "A developer installing the newly published `@cosyte/hl7` package in a fresh project can `import { parseHL7, buildMessage } from \"@cosyte/hl7\"` and run a canonical example end-to-end."
    Evidence: Task 1 `pnpm publish --dry-run` tarball manifest (cite name/version/filename/total-files/size from Task 1 cmd 10); local pack round-trip result if captured (Task 1 cmd 11). If local-pack round-trip was skipped: cite 09-04-SUMMARY lines 52-85 as authoritative plus a note: "Full end-user `npm install @cosyte/hl7` + import test cannot be executed without publishing; the dry-run tarball + existing CI `import { parseHL7, buildMessage }` checks in `test/model-public-exports.test.ts` (updated in Plan 09-02 per 09-02-SUMMARY) are the best-available substitutes at pre-publish time."

    Row SC-4 (ROADMAP line 231):
    "A developer reading the README, CHANGELOG, examples, and profile starter kit sees the `@cosyte/hl7` name used consistently; CHANGELOG calls out the rename with a migration note."
    Evidence: Task 1 docs greps (cmds 12-18) — README ≥ 5 new-name hits + 0 old-name; CHANGELOG ≥ 1 new-name + exactly 1 breadcrumb + migration keyword present; examples/profile-starter-kit kit-level files: 0 old-name, ≥ 1 new-name. Cross-ref: 09-01 (CHANGELOG rewrite) + 09-03 (kit sweep) SUMMARYs.

    Row SC-5 (ROADMAP line 232):
    "A developer checking `.github/workflows/publish.yml` sees it publish under the new name; any legacy `@cosyte/hl7-parser` publish path is either retired or redirected per the rename plan."
    Evidence: Task 1 cmd 21 (0 old-name hits in publish.yml); Task 1 cmd 20 (workflow_dispatch trigger present per D-08 from 08-05); `package.json.name` = `@cosyte/hl7` (Task 1 cmd 3) + `publish.yml` uses `pnpm publish` which reads the name from `package.json` — ergo the workflow publishes under the new name by construction. Cross-ref: 09-04-SUMMARY line 28 (workflow has 0 old-name refs).

    All rows: `✓ VERIFIED`. Every evidence cell has a concrete anchor.
  </action>
  <verify>
    <automated>echo "draft table for task 5"</automated>
  </verify>
  <acceptance_criteria>
    - 5 rows, labeled SC-1..SC-5.
    - Every Truth column = verbatim ROADMAP text.
    - Every Status = `✓ VERIFIED`.
    - Every Evidence cell cites Task 1 output + at least one SUMMARY cross-reference + (where relevant) a commit hash.
    - Row SC-3 explicitly handles the pre-publish limitation (can't run the full end-user npm install round-trip) with a documented substitute (dry-run + existing CI import test).
  </acceptance_criteria>
  <done>
    Observable Truths 5-row draft ready in task journal.
  </done>
</task>

<task type="auto">
  <name>Task 3: Assemble Required Artifacts table (rename touch-points)</name>
  <read_first>
    - 09-01-SUMMARY.md (identity files: package.json, CHANGELOG.md, README, CONTRIBUTING, CLAUDE.md, tsup.config.ts, vitest.config.ts)
    - 09-02-SUMMARY.md (src/ JSDoc + test/model-public-exports.test.ts)
    - 09-03-SUMMARY.md (examples/*.ts + examples/profile-starter-kit/ files)
    - 09-04-SUMMARY.md (no new files — verification-only; but records the dry-run tarball details)
  </read_first>
  <files>(none — prepares table for Task 5)</files>
  <action>
    Build a Required Artifacts table with one row per touch-point category (NOT one row per renamed file — that would be excessive; Phase 9 swept 51+ source files + examples + kit). Group by plan.

    Rows:

    Plan 09-01 identity files:
    1. `package.json` — `"name": "@cosyte/hl7"`, `description`, `keywords`, `homepage`, `bugs.url`, `repository.url` all updated. Check: `grep -c '"name": "@cosyte/hl7"' package.json` = 1; `grep -c '"@cosyte/hl7-parser"' package.json` = 0.
    2. `CHANGELOG.md` — rewritten with D-07 breadcrumb (the allowed legacy-name mention). Check: Task 1 cmd 14 + 16 confirm.
    3. `README.md` — all top-level `@cosyte/hl7-parser` → `@cosyte/hl7`. Check: Task 1 cmds 12-13.
    4. `CONTRIBUTING.md` — ditto. Check: `grep -c "@cosyte/hl7-parser" CONTRIBUTING.md` = 0.
    5. `CLAUDE.md` — project guide references new name. Check: `grep -c "@cosyte/hl7-parser" CLAUDE.md` expected to be 0 (note: the historical STATE.md header — _different file_ — retains the old name as breadcrumb per 10-VERIFICATION.md anti-patterns note; CLAUDE.md itself should be clean).
    6. `tsup.config.ts`, `vitest.config.ts` — any JSDoc / banner references updated. Check: `grep -c "@cosyte/hl7-parser" tsup.config.ts vitest.config.ts` = 0.

    Plan 09-02 source + tests:
    7. `src/**/*.ts` JSDoc banners — all updated. Check: `grep -rc "@cosyte/hl7-parser" src/ 2>/dev/null | grep -v ':0' | wc -l` = 0.
    8. `test/model-public-exports.test.ts` — test imports use new name or are local-path. Check: Task 1 grep combined with file existence (`test -f test/model-public-exports.test.ts`).

    Plan 09-03 examples + kit:
    9. `examples/extract-patient-info.ts`, `examples/read-lab-results.ts`, `examples/modify-and-resend.ts`, `examples/README.md` — all reference new name. Check: `grep -rc "@cosyte/hl7-parser" examples/ 2>/dev/null | grep -v ':0' | wc -l` = 0 (all 0 matches across the examples tree).
    10. `examples/profile-starter-kit/` subtree (6 identified files + lockfile) — all reference new name. Check: Task 1 cmd 17 + 18.

    Plan 09-04 verification artifacts:
    11. (No new code artifacts — Plan 09-04 was pure verification; its output is the evidence captured in 09-04-SUMMARY.md which this VERIFICATION.md ratifies.)

    Publish workflow (touched by 08-05 but validated under Phase 9's rename lens):
    12. `.github/workflows/publish.yml` — zero `@cosyte/hl7-parser` references (Task 1 cmd 21). The workflow publishes by reference to `package.json.name`, which is now `@cosyte/hl7`.

    All rows `✓ EXISTS + SUBSTANTIVE` (where "substantive" here means "all sweep checks passed"). Record actual grep counts in Details column. Total: ~12 rows.
  </action>
  <verify>
    <automated>test "$(grep -c '"name": "@cosyte/hl7"' package.json)" -eq 1 && test "$(grep -rc '@cosyte/hl7-parser' src/ 2>/dev/null | grep -v ':0' | wc -l)" -eq 0 && test "$(grep -rc '@cosyte/hl7-parser' examples/ 2>/dev/null | grep -v ':0' | wc -l)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Draft has ~12 rows covering all 4 plan touch-point categories.
    - Every row has `✓ EXISTS + SUBSTANTIVE` status + concrete grep count in Details.
    - The sentinel "at HEAD, `@cosyte/hl7-parser` appears only in `CHANGELOG.md` D-07 breadcrumb" is confirmed by at least 3 separate row checks (package.json, src tree, examples tree — all 0 old-name matches).
    - Commands for verification are shell-runnable by a reader.
  </acceptance_criteria>
  <done>
    Required Artifacts draft ready in task journal.
  </done>
</task>

<task type="auto">
  <name>Task 4: Assemble Plan-Summary Evidence table + Requirements Coverage note</name>
  <read_first>
    - 09-01-SUMMARY.md
    - 09-02-SUMMARY.md
    - 09-03-SUMMARY.md
    - 09-04-SUMMARY.md
    - ROADMAP.md line 183 ("Requirements: (none — rename-only phase; no new functional REQ-IDs)")
  </read_first>
  <files>(none — prepares content for Task 5)</files>
  <action>
    Table A — Plan-Summary Evidence (4 rows):
    Columns: `Plan` / `Subject` / `Files Touched` / `Acceptance` / `Commit(s)`.

    Row 09-01 — "Identity files rename". Files: `package.json`, `CHANGELOG.md` (rewrite with D-07 breadcrumb), `README.md` sweep, `CONTRIBUTING.md`, `CLAUDE.md`, `tsup.config.ts`, `vitest.config.ts`. Acceptance: package name = `@cosyte/hl7`; CHANGELOG has rename note; root-level docs all swept. Commits: cite hashes from 09-01-SUMMARY task-commits section.

    Row 09-02 — "Source + tests sweep". Files: 51 `src/**/*.ts` (JSDoc + docblocks) + `test/model-public-exports.test.ts`. Acceptance: typecheck clean at end-of-plan; 0 old-name references in src/. Commits: from 09-02-SUMMARY.

    Row 09-03 — "Examples + starter kit sweep". Files: 3 example scripts + examples/README + examples/profile-starter-kit/ (6 files + lockfile). Acceptance: 0 old-name in examples/; kit's internal references all use new name. Commits: from 09-03-SUMMARY.

    Row 09-04 — "Verification + publish dry-run". Files: NONE (read-only plan). Acceptance: grep sweep returned 1 match (CHANGELOG breadcrumb); full pipeline green; `pnpm publish --dry-run` produced clean @cosyte/hl7@0.1.0 tarball (10 files / 346.1 kB / filename cosyte-hl7-0.1.0.tgz). Commits: NONE for code (per 09-04-SUMMARY line 97-98: "None for code — all verifications were read-only.").

    Requirements Coverage note (NOT a table — a single explicit paragraph):
    "Phase 9 has ZERO new functional REQ-IDs by design (ROADMAP.md line 183: 'Requirements: (none — rename-only phase; no new functional REQ-IDs)'). The 5 Success Criteria are process/sweep completeness checks rather than feature-coverage REQs. All SCs VERIFIED per the Observable Truths table above. The STATE.md Performance Metrics section confirms total v1 REQ-ID coverage remained at 97/97 after this phase — no REQ-IDs were opened, closed, deferred, or orphaned by the rename."

    This explicit attestation prevents future readers from asking "where's the Requirements Coverage table?" — the answer is documented in-file.
  </action>
  <verify>
    <automated>test "$(ls .planning/phases/09-rename-package-to-cosyte-hl7/09-0[1-4]-SUMMARY.md 2>/dev/null | wc -l)" -eq 4</automated>
  </verify>
  <acceptance_criteria>
    - Table A has exactly 4 rows (one per SUMMARY file).
    - Row 09-04 acceptance explicitly cites the tarball manifest from Task 1 (and/or 09-04-SUMMARY for backup).
    - Requirements Coverage attestation paragraph drafted; explicitly states "zero new functional REQ-IDs".
    - Commit column populated for all 4 rows (even if row 09-04 = "None for code").
  </acceptance_criteria>
  <done>
    Plan-Summary Evidence table + Requirements Coverage attestation both drafted. Ready for Task 5.
  </done>
</task>

<task type="auto">
  <name>Task 5: Write .planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md</name>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/verification-report.md
    - 02-VERIFICATION.md
    - 10-VERIFICATION.md
    - Task 1-4 drafts
  </read_first>
  <files>.planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md</files>
  <action>
    Frontmatter:
    ```yaml
    ---
    phase: 09-rename-package-to-cosyte-hl7
    verified: <ISO-8601-from-Task-1>
    status: passed
    score: 5/5 must-haves verified
    overrides_applied: 0
    ---
    ```

    Header:
    ```
    # Phase 9: Rename Package to @cosyte/hl7 — Verification Report

    **Phase Goal:** Rename the published package from `@cosyte/hl7-parser` to `@cosyte/hl7` so the name reflects the actual surface area — a full HL7 v2 toolkit (parser, builder, mutator, serializer, and helpers), not just a parser. Every reference in source, configs, docs, examples, starter kit, and CI/publish workflows points at the new name; the first published tag under the new name installs and round-trips cleanly.

    **Verified:** <timestamp>
    **Status:** passed (retroactive — closes v2.1-MILESTONE-AUDIT tech-debt item 1 for Phase 9)
    **Re-verification:** No — initial verification (retroactive paper-trail fill)

    **Scope note:** Phase 9 has zero new functional REQ-IDs (rename-only). Verification is sweep-completeness + observable-rename attestation, not feature coverage. See Requirements Coverage section below.
    ```

    1. `## Verification Commands` — prominent table transcribing Task 1 results. Columns: `Command` / `Exit` / `Output Marker`. Top-billed rows:
       - `git grep "@cosyte/hl7-parser" -- ':!node_modules/' ':!dist/' ':!.planning/'` → exit 0, **1 match: CHANGELOG.md D-07 breadcrumb** (cite the line verbatim).
       - `grep -c "@cosyte/hl7-parser" .github/workflows/*.yml` → **0 in both ci.yml and publish.yml**.
       - `pnpm install --frozen-lockfile` → exit 0.
       - `pnpm typecheck / lint / test / build / examples` → all exit 0 (5 rows or a merged pipeline row).
       - `pnpm publish --dry-run --no-git-checks` → exit 0, **name=@cosyte/hl7, version=0.1.0, filename=cosyte-hl7-0.1.0.tgz, total files=10, size≈346 kB** (cite the captured Tarball Details block).
       - Docs-consistency greps (Task 1 cmds 12-18) as a short sub-table or inline.

    2. `## Goal Achievement` → `### Observable Truths` — transcribe Task 2's 5-row table. Append `**Score:** 5/5 success criteria VERIFIED end-to-end.`

    3. `### Required Artifacts` — transcribe Task 3's ~12-row table. Append `**Artifacts:** N/N verified (all rename touch-points swept clean; single allowed CHANGELOG D-07 breadcrumb retained by design).`

    4. `### Key Link Verification` — 3-5 row table:
       - `package.json.name` → tarball name via `pnpm publish` — ✓ WIRED (dry-run produces `@cosyte/hl7`).
       - `package.json.name` → CI publish workflow via reference (not hard-coded) — ✓ WIRED (Task 1 cmd 21 shows 0 old-name references in publish.yml).
       - `test/model-public-exports.test.ts` → model barrel exports under new name — ✓ WIRED (per 09-02-SUMMARY).
       - `examples/profile-starter-kit/package.json.devDependencies.@cosyte/hl7` → parent package via `file:../..` — ✓ WIRED (per 08-05 Plan B resolution + 09-03 sweep).
       - `CHANGELOG.md D-07 breadcrumb` → single intentional legacy-name mention — ✓ BY DESIGN (Task 1 grep sweep confirms this is the sole occurrence).

    5. `### Behavioral Spot-Checks` — 4-5 rows citing Task 1 key results: grep sweep clean, full pipeline green, `pnpm examples` green, dry-run tarball clean.

    6. `### Requirements Coverage` — transcribe Task 4's attestation paragraph explicitly stating "zero new functional REQ-IDs by design; 5 SCs verified above." No table here — the paragraph is the deliverable.

    7. `### Plan-Summary Evidence` — transcribe Task 4 Table A (4 rows). Append `**Plans:** 4/4 complete. No REQ-ID deltas (rename-only).`

    8. `### Anti-Patterns Found` — check each Phase 9 touch-point domain for stale old-name references. Per Task 1, only CHANGELOG.md has the intentional D-07 breadcrumb. Note any informational findings (e.g., STATE.md retains old-name breadcrumb in its header per 10-VERIFICATION.md — but STATE.md is not a Phase 9 touch-point, it's Phase 10's territory; document that boundary explicitly to prevent confusion).

    9. `### Human Verification Required` — "None at the code-trail level. The ONE human-required follow-on — renaming the GitHub repo slug from `cosyte/hl7-parser` to `cosyte/hl7` in GitHub Settings → General (per 09-04-SUMMARY line 92-94 and ROADMAP D-04/D-05) — is OUTSIDE the code audit surface and is tracked separately. GitHub's auto-redirect keeps the new URLs (already in package.json `homepage`/`bugs.url`/`repository.url`) working until the admin rename is performed."

    10. `### Gaps Summary` — "**Zero gaps open.** All 5 ROADMAP Success Criteria verified end-to-end. Grep sweep returns exactly 1 match (CHANGELOG.md D-07 breadcrumb — intentional per decision log). Full pipeline green. `pnpm publish --dry-run` produces a clean 10-file tarball under the new name. Phase 9 retroactive verification: closed."

    11. `## Verdict` — "Phase 9 status: passed — 5/5 Success Criteria verified, 0 new functional REQ-IDs required (rename-only phase per ROADMAP), 4/4 plan SUMMARYs attested, grep sweep confirms clean rename with only the D-07 breadcrumb retained, dry-run tarball confirms publishable state under the new name. This retroactive verification closes v2.1-MILESTONE-AUDIT tech-debt item 1 for Phase 9."

    12. Footer:
       ```
       ---
       *Verified: <timestamp>*
       *Verifier: Claude (gsd-planner, Phase 11 retroactive verification — mechanical assembly from 4 plan SUMMARYs + re-run grep sweep + re-run pipeline + re-run publish --dry-run at HEAD)*
       *Authority: ROADMAP.md Phase 11 + v2.1-MILESTONE-AUDIT.md tech-debt item 1 closure*
       ```

    File length target: ~200-300 lines.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md && test "$(grep -c '^status: passed$' .planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md)" -eq 1 && test "$(grep -c '✓ VERIFIED' .planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md)" -ge 5 && test "$(grep -c 'git grep' .planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md)" -ge 1 && test "$(grep -c 'pnpm publish --dry-run\|publish --dry-run' .planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md)" -ge 1 && test "$(grep -c '^overrides_applied: 0$' .planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md`.
    - Frontmatter: `phase: 09-rename-package-to-cosyte-hl7`, `status: passed`, `score: 5/5 must-haves verified`, `overrides_applied: 0`.
    - Contains ≥ 5 `✓ VERIFIED` strings (Observable Truths).
    - Contains at least 1 reference to `git grep` (grep sweep command).
    - Contains at least 1 reference to `pnpm publish --dry-run` (or `publish --dry-run`).
    - Contains explicit "zero new functional REQ-IDs" phrasing (Requirements Coverage section).
    - Plan-Summary Evidence table has exactly 4 rows (09-01, 09-02, 09-03, 09-04).
    - Verdict section contains "Phase 9 status: passed".
    - Total line count: 180-350 lines.
  </acceptance_criteria>
  <done>
    09-VERIFICATION.md written. All 6+ tables + paragraphs present. Cross-references to 09-04-SUMMARY and grep-sweep evidence in place.
  </done>
</task>

<task type="auto">
  <name>Task 6: Commit the new VERIFICATION.md</name>
  <read_first>
    - CLAUDE.md
  </read_first>
  <files>(git add of Task 5 output)</files>
  <action>
    1. `git add .planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md`.
    2. `git diff --cached --name-only` — expect exactly the one path.
    3. Commit via HEREDOC:
       ```
       git commit -m "$(cat <<'EOF'
       docs(phase-09): add retroactive VERIFICATION.md — 5/5 success criteria verified

       Closes v2.1-MILESTONE-AUDIT tech-debt item 1 for Phase 9 (missing VERIFICATION.md).
       Evidence-assembled from 4 plan SUMMARYs + re-run at HEAD: grep sweep returns
       exactly 1 match (CHANGELOG.md D-07 breadcrumb, intentional); full pipeline
       green (install/typecheck/lint/test/build/examples all exit 0); pnpm publish
       --dry-run produces clean @cosyte/hl7@0.1.0 tarball (10 files, ~346 kB,
       filename cosyte-hl7-0.1.0.tgz). Zero new functional REQ-IDs (rename-only phase).

       Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
       EOF
       )"
       ```
    4. Verify: `git log --oneline -1 -- .planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md`.
  </action>
  <verify>
    <automated>test "$(git log --oneline -1 --format='%s' -- .planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md | grep -c 'docs(phase-09): add retroactive VERIFICATION.md')" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one commit in log for this file.
    - Subject begins `docs(phase-09):` and contains `add retroactive VERIFICATION.md — 5/5 success criteria verified`.
    - No co-staged unrelated files.
    - Working tree clean.
  </acceptance_criteria>
  <done>
    New 09-VERIFICATION.md committed. Plan-level SUMMARY handled by execute-plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable — paper-trail-only plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering | `09-VERIFICATION.md` content (scenario: future reader can't trust grep-sweep result was real at HEAD) | mitigate | Task 1 re-runs `git grep` at HEAD; Task 5 transcribes verbatim output; Task 6 commits to git for auditable trail. |
| T-11-03-02 | Tampering | `pnpm publish --dry-run` manifest citation | mitigate | Task 1 captures full dry-run stdout verbatim; shasum + file count + tarball name + size all recorded; 09-04-SUMMARY provides independent baseline for comparison. |
| T-11-03-03 | Information Disclosure | — | accept | Public repo; no secrets in any Phase 9 artifacts. |
| T-11-03-04 | Spoofing | Tarball identity (scenario: dry-run tarball claims @cosyte/hl7 but publish workflow accidentally publishes under old name) | mitigate | Task 1 cmd 19-21 verifies publish.yml has zero `@cosyte/hl7-parser` references AND uses `pnpm publish` (which reads package.json.name) — no hard-coded name override possible. |
</threat_model>

<verification>
After Task 6:
1. `.planning/phases/09-rename-package-to-cosyte-hl7/09-VERIFICATION.md` exists with `status: passed`, `score: 5/5`, `overrides_applied: 0`.
2. Contains `git grep` command reference and the exact `1 match in CHANGELOG.md` result.
3. Contains `pnpm publish --dry-run` evidence with `@cosyte/hl7@0.1.0` + `10 files` + `346` size marker.
4. Contains explicit "zero new functional REQ-IDs" attestation.
5. Commit `docs(phase-09): add retroactive VERIFICATION.md` in git log.
</verification>

<success_criteria>
- All 6 tasks meet their acceptance criteria.
- 09-VERIFICATION.md has valid frontmatter, all required tables/sections, and the critical grep-sweep + dry-run evidence called out in planning_context's quality_gate.
- One commit with `docs(phase-09):` prefix touching only this file.
- Grep sweep at HEAD still returns exactly 1 match (CHANGELOG.md D-07 breadcrumb) — no new regressions.
- `pnpm publish --dry-run` at HEAD still produces `@cosyte/hl7@0.1.0` tarball with 10 files and ~346 kB size.
- Wave-1 parallel-safe: no file overlap with 11-01 or 11-02.
</success_criteria>

<output>
execute-plan creates `.planning/phases/11-retroactive-verification/11-03-SUMMARY.md` citing:
- 5 Phase 9 SCs attested (grep sweep / pipeline / round-trip install / docs consistency / publish.yml).
- Grep sweep result at HEAD (must be exactly 1 match in CHANGELOG.md).
- Publish dry-run manifest at HEAD (name, version, file count, size).
- Task 1 pipeline results.
- New file path + commit hash.
- Zero functional REQ-IDs closed (rename-only phase).
</output>
