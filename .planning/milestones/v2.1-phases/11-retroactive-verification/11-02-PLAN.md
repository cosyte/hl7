---
phase: 11-retroactive-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md
autonomous: true
requirements: []
gap_closure: true
tags: [retroactive-verification, paper-trail, phase-08, gap-closure, v2.1-audit, examples, starter-kit, documentation]
must_haves:
  truths:
    - "A developer opens .planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md and sees YAML frontmatter with status: passed and score: 5/5 truths verified (one per ROADMAP Phase 8 Success Criterion) and overrides_applied: 0."
    - "A developer running `grep -c '✓ VERIFIED' .planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md` counts at least 5 hits in the Observable Truths table (SC-1..SC-5) plus one per Required Artifact row."
    - "A developer reading the Verification Commands table sees every row with Exit=0 and every command matches one actually re-run at the verified_at timestamp — including the end-to-end pipeline, `pnpm examples`, and the starter-kit subtree pipeline (`pnpm install && pnpm typecheck && pnpm lint && pnpm test && pnpm build` from examples/profile-starter-kit/)."
    - "A developer reading the Plan-Summary Evidence table sees exactly 5 rows (one per 08-0N-SUMMARY.md: 08-01, 08-02, 08-03, 08-04, 08-05), each citing a commit hash and the specific plan subject."
    - "A developer searching the VERIFICATION.md finds an explicit cross-reference to 09-04-SUMMARY.md as authoritative evidence that the Phase 8 end-to-end pipeline (including `pnpm examples` and starter-kit subtree) was green at the v2.1 milestone close."
  artifacts:
    - path: ".planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md"
      provides: "Retroactive verification report for Phase 8 — Examples, Starter Kit & Documentation, matching the 02-VERIFICATION.md / 10-VERIFICATION.md shape"
      contains: "YAML frontmatter (phase, verified, status: passed, score: 5/5, overrides_applied: 0); Phase Goal verbatim from ROADMAP.md; Verification Commands table; Observable Truths table (5 SC rows); Required Artifacts table covering examples/ + profile-starter-kit/ + README/CHANGELOG/CONTRIBUTING/LICENSE + publish.yml; Plan-Summary Evidence table (5 plan rows); Requirements Coverage table (25 REQ-IDs: EX-01/02/03 + KIT-01..07 + DOC-01..15); cross-reference to 09-04-SUMMARY.md pipeline log; Verdict section"
  key_links:
    - from: ".planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md (Observable Truths table)"
      to: ".planning/ROADMAP.md (Phase 8 Success Criteria block, lines 165-170)"
      via: "one row per SC, success-criterion text quoted verbatim"
      pattern: "SC-1 through SC-5 appear as 5 rows, each labeled"
    - from: ".planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md (Plan-Summary Evidence table)"
      to: "5 files: 08-01-SUMMARY.md, 08-02-SUMMARY.md, 08-03-SUMMARY.md, 08-04-SUMMARY.md, 08-05-SUMMARY.md"
      via: "one row per SUMMARY with commit hash, artifacts, acceptance marker"
      pattern: "exactly 5 plan rows"
    - from: ".planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md (pipeline evidence section)"
      to: ".planning/phases/09-rename-package-to-cosyte-hl7/09-04-SUMMARY.md"
      via: "explicit text cross-reference — 09-04 is the authoritative v2.1-milestone pipeline log, attesting 824/14 test tally + `pnpm examples` green + starter-kit subtree green + tarball 10 files/346.2 kB"
      pattern: "At least one string match for '09-04-SUMMARY.md' OR '09-04-SUMMARY' in the VERIFICATION.md body"
---

<objective>
Produce `.planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md` — the missing Phase 8 verification report. Evidence already exists on disk (5 plan SUMMARYs + examples tree + profile-starter-kit subtree + comprehensive README + CHANGELOG + CONTRIBUTING + publish.yml + 09-04-SUMMARY's v2.1-milestone green pipeline log). This plan ratifies it by (1) re-running the Phase 8 deliverable-verifying commands at HEAD, (2) cross-referencing 09-04-SUMMARY for the authoritative end-to-end pipeline log, (3) assembling the VERIFICATION.md from that evidence.

Purpose: Close v2.1-MILESTONE-AUDIT tech-debt item 1 for Phase 8 (missing VERIFICATION.md). Mechanical assembly — NOT a fresh `/gsd-verify-work` run.

Output: One new file at `.planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md` committed in a single `docs(phase-08): add retroactive VERIFICATION.md — 5/5 success criteria verified` commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/templates/verification-report.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference verification shapes
@.planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md
@.planning/phases/10-planning-doc-resync/10-VERIFICATION.md

# Phase 8 evidence (MUST read all 5 — one row per SUMMARY in Plan-Summary Evidence)
@.planning/phases/08-examples-starter-kit-and-documentation/08-01-SUMMARY.md
@.planning/phases/08-examples-starter-kit-and-documentation/08-02-SUMMARY.md
@.planning/phases/08-examples-starter-kit-and-documentation/08-03-SUMMARY.md
@.planning/phases/08-examples-starter-kit-and-documentation/08-04-SUMMARY.md
@.planning/phases/08-examples-starter-kit-and-documentation/08-05-SUMMARY.md

# CRITICAL cross-reference: 09-04-SUMMARY is the authoritative v2.1-milestone pipeline log
# It attests the Phase 8 end-to-end pipeline (`pnpm examples` green, starter-kit subtree green,
# publish --dry-run clean at 10 files / 346.2 kB) is still valid at the post-rename state.
# Both 11-02 (this plan) AND 11-03 (Phase 9 plan) cite this SUMMARY for different reasons:
# - 11-02: attests Phase 8 deliverables (examples, kit, docs) still work end-to-end
# - 11-03: attests the rename didn't break the pipeline
@.planning/phases/09-rename-package-to-cosyte-hl7/09-04-SUMMARY.md

# Phase 8 ROADMAP anchor (Success Criteria are lines 165-170; 5 SCs); REQ-IDs on line 164
@.planning/ROADMAP.md

@CLAUDE.md
</context>

<phase_goal>
Goal (copy verbatim into the VERIFICATION.md header): "A developer landing on the README can go from zero to parsing a real message in under a minute, find a recipe for every common task, and copy the profile starter kit into a new directory to publish their own profile package in minutes."

Success Criteria count: 5 (ROADMAP.md lines 165-170). Score denominator = 5, status = `passed`.

Phase 8 REQ-IDs (25 total, per ROADMAP line 164): EX-01, EX-02, EX-03, KIT-01, KIT-02, KIT-03, KIT-04, KIT-05, KIT-06, KIT-07, DOC-01, DOC-02, DOC-03, DOC-04, DOC-05, DOC-06, DOC-07, DOC-08, DOC-09, DOC-10, DOC-11, DOC-12, DOC-13, DOC-14, DOC-15.
</phase_goal>

<tasks>

<task type="auto">
  <name>Task 1: Re-run Phase 8 deliverable commands at HEAD, capture exit codes + markers</name>
  <read_first>
    - .planning/phases/08-examples-starter-kit-and-documentation/08-05-SUMMARY.md (Plan 08-05 — capstone CI+publish wiring; the in-SUMMARY pipeline table on lines 80-92 is the shape we're reproducing)
    - .planning/phases/09-rename-package-to-cosyte-hl7/09-04-SUMMARY.md (authoritative v2.1 pipeline log — Task 2 block lines 34-47)
    - .github/workflows/ci.yml (confirm Plan 08-05's 3 matrix-gated steps — Examples smoke / Starter kit / kit-workflow actionlint — are still present)
    - .github/workflows/publish.yml (confirm workflow_dispatch-only + permissions + NPM_TOKEN wiring per 08-05)
    - scripts/run-examples.ts (confirms `pnpm examples` target exists)
    - examples/ tree (confirm 3 runnable scripts + 3 fixtures + README)
    - examples/profile-starter-kit/ tree (confirm kit subtree complete)
  </read_first>
  <files>(none — read-only capture; output goes into task journal for Task 5)</files>
  <action>
    Run each command below, capture exit code and load-bearing output. Record into task journal for transcription in Task 5.

    Pipeline at parent level:
    1. `pnpm install --frozen-lockfile` — exit 0 expected.
    2. `pnpm typecheck` — exit 0 expected.
    3. `pnpm lint --max-warnings=0` — exit 0 expected.
    4. `pnpm test -- --run` — exit 0 expected; capture the total test count (per 09-04: 824 passed + 14 todo; confirm at HEAD).
    5. `pnpm build` — exit 0 expected; capture dist sizes.

    Phase 8 specific (these are the ones that attest the Phase 8 deliverables specifically):
    6. `pnpm examples` — runs `tsx scripts/run-examples.ts`; exit 0 expected; stdout should reference all 3 example scripts by name (extract-patient-info, read-lab-results, modify-and-resend) — confirms EX-01, EX-02, EX-03 are live.
    7. Starter-kit subtree pipeline from `examples/profile-starter-kit/`:
       - `cd examples/profile-starter-kit && pnpm install --no-frozen-lockfile` — exit 0.
       - `pnpm typecheck` — exit 0.
       - `pnpm lint` — exit 0.
       - `pnpm test` — exit 0; capture test count (per 08-02-SUMMARY: 4/4 against ZAL sample fixture).
       - `pnpm build` — exit 0; verify `dist/` entries match `package.json` `exports` (KIT-02, KIT-04 evidence).
       - Return to repo root: `cd ../..`
    8. Validate kit workflows with actionlint (matches Plan 08-05's 3rd CI step):
       - `npx -y @rhysd/actionlint examples/profile-starter-kit/.github/workflows/ci.yml examples/profile-starter-kit/.github/workflows/publish.yml` — exit 0 expected; if `actionlint` binary not available locally, use the reviewdog action's equivalent or skip with a note citing 08-05-SUMMARY's confirmation that actionlint was clean.

    Documentation completeness checks (for DOC-01..DOC-15 attestation via file-content grep):
    9. README.md — confirm the 13 documented sections exist by grep (per Plan 08-03):
       - `grep -cE '^(##|###) ' README.md` ≥ 13 (at least 13 headings).
       - Specific required-section sentinels (one grep each, spot-check ~5 of the 13):
         - `grep -c '^# @cosyte/hl7' README.md` ≥ 1 (DOC-01 value-prop + H1).
         - `grep -c 'Quickstart\|quickstart' README.md` ≥ 1 (DOC-03).
         - `grep -c 'Cookbook\|cookbook' README.md` ≥ 1 (DOC-07).
         - `grep -c 'Profiles' README.md` ≥ 1 (DOC-09).
         - `grep -c 'Error Handling\|error handling' README.md` ≥ 1 (DOC-11).
         - `grep -c 'Built by Cosyte\|Cosyte' README.md` ≥ 1 (DOC-13 footer).
    10. CHANGELOG.md exists + `[Unreleased]` section + Keep-a-Changelog header (DOC-14):
        - `grep -c '^## \[Unreleased\]' CHANGELOG.md` ≥ 1.
        - `grep -c 'Keep a Changelog\|keepachangelog' CHANGELOG.md` ≥ 1.
    11. LICENSE exists + MIT: `grep -c 'MIT' LICENSE` ≥ 1 (DOC-15).
    12. CONTRIBUTING.md exists: `test -f CONTRIBUTING.md`.
    13. Publish workflow: `test -f .github/workflows/publish.yml && grep -c 'workflow_dispatch' .github/workflows/publish.yml` ≥ 1 (KIT-05-adjacent + release-surface).

    Record timestamp (ISO-8601 UTC) at verification-run start — becomes `verified:` in frontmatter.

    STOP if ANY of 1-8 fail: per critical_constraints, failure here invalidates v2.1 milestone close.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile && pnpm typecheck && pnpm lint --max-warnings=0 && pnpm test -- --run && pnpm build && pnpm examples && cd examples/profile-starter-kit && pnpm install --no-frozen-lockfile && pnpm typecheck && pnpm lint && pnpm test && pnpm build && cd ../..</automated>
  </verify>
  <acceptance_criteria>
    - Parent pipeline all 5 commands exit 0 (install / typecheck / lint / test / build).
    - `pnpm examples` exits 0 and stdout references all 3 example filenames.
    - Starter-kit subtree: all 5 commands (install / typecheck / lint / test / build) exit 0 from `examples/profile-starter-kit/` working directory.
    - Kit workflows validate via actionlint (OR: note inline that local actionlint unavailable, cite 08-05-SUMMARY's clean CI run on 2026-04-20).
    - All 5 documentation-existence greps return non-zero hits.
    - Task journal has: verified_at ISO-8601 timestamp, every command's exit code, test totals (parent + kit subtree), build sizes, each grep count.
    - If any command in 1-8 fails: STOP, escalate, do not proceed.
  </acceptance_criteria>
  <done>
    Task journal is complete: parent pipeline + pnpm examples + kit subtree all green; docs greps all hit; timestamps captured. Ready for Task 5.
  </done>
</task>

<task type="auto">
  <name>Task 2: Assemble Observable Truths table (5 rows — one per Phase 8 Success Criterion)</name>
  <read_first>
    - .planning/ROADMAP.md (lines 161-178 — Phase 8 block; SCs on lines 165-170; verbatim text required)
    - 08-01-SUMMARY.md (EX-01/02/03 + smoke runner evidence for SC-1)
    - 08-02-SUMMARY.md (profile-starter-kit subtree for SC-2)
    - 08-03-SUMMARY.md (comprehensive README.md for SC-3)
    - 08-04-SUMMARY.md (CHANGELOG + CONTRIBUTING + LICENSE for SC-4)
    - 08-05-SUMMARY.md (capstone integration for all SCs + pipeline glue)
    - 09-04-SUMMARY.md (cross-referenced v2.1-milestone pipeline log — cite lines 34-47 for "824 tests + pnpm examples green + kit subtree green + tarball 10 files/346.2kB")
    - 02-VERIFICATION.md Observable Truths table (shape model)
  </read_first>
  <files>(none — prepares table content for Task 5)</files>
  <action>
    Compose 5 rows, `#` / `Success Criterion` (verbatim quote) / `Status` (`✓ VERIFIED`) / `Evidence` columns.

    Row SC-1 (from ROADMAP line 166):
    "A developer running `tsx examples/extract-patient-info.ts`, `examples/read-lab-results.ts`, and `examples/modify-and-resend.ts` sees each example execute end-to-end and print the documented output, demonstrating helpers, observations/orders iteration, and mutation + round-trip respectively."
    Evidence: Task 1 `pnpm examples` stdout (cite the marker strings observed); `examples/extract-patient-info.ts`, `examples/read-lab-results.ts`, `examples/modify-and-resend.ts` all exist at HEAD (verify with `test -f`); `scripts/run-examples.ts` is the smoke runner (per 08-01-SUMMARY); cross-ref: `09-04-SUMMARY.md:42` recorded `pnpm examples` exit 0 at v2.1 close. Commits: cite 08-01 commit hash from 08-01-SUMMARY.

    Row SC-2 (from ROADMAP line 167):
    "A developer copying `examples/profile-starter-kit/` into a new directory can run `pnpm install && pnpm test && pnpm build` against the sample fixture with success; `dist/` entries match the `package.json` exports; CI and publish workflows validate with `actionlint`; `CUSTOMIZING.md` walks through rename → swap base → define Z-segments → fixtures → publish; placeholders (`{{YOUR_ORG}}`, `{{PROFILE_NAME}}`) appear consistently."
    Evidence: Task 1 kit-subtree pipeline (all 5 commands exit 0); Task 1 actionlint on `examples/profile-starter-kit/.github/workflows/*.yml`; `examples/profile-starter-kit/CUSTOMIZING.md` exists (verify + cite line count); placeholder grep: `grep -c '{{YOUR_ORG}}\|{{PROFILE_NAME}}' examples/profile-starter-kit/**/*.{json,md,ts,yml}` ≥ 1 (confirm). Commit: cite 08-02 commit hashes `43fccae` + `3c303c0` (from ROADMAP line 174 and 08-02-SUMMARY).

    Row SC-3 (from ROADMAP line 168):
    "A developer opening the README on GitHub or npm sees the one-sentence value prop as the first line, badges, a 30-second copy-pasteable quickstart, a 6–8-bullet feature list, an 'HL7 in 90 seconds' section, the three access patterns, the full cookbook (all recipes listed in the spec), a top-level Profiles section, a 4-tier tolerance section with table and runnable example, an Error Handling section, a Contributing section, and the 'Built by Cosyte' footer with license link."
    Evidence: Task 1 README.md grep hits (all 5 sentinel greps returned ≥1); README line count from `wc -l README.md`; section-heading count `grep -cE '^(##|###) ' README.md` ≥ 13. Commit: cite 08-03 commit hash from 08-03-SUMMARY.

    Row SC-4 (from ROADMAP line 169):
    "A developer looking for release history, license, or roadmap finds `CHANGELOG.md` in Keep-a-Changelog format with an `[Unreleased]` section, `LICENSE` (MIT) at the repo root, and a roadmap/stretch-goals section documenting the v2 deferrals (typed overlays, schema validation, streaming, JSON Schema/Zod, batch files, type-safe custom-segment fields)."
    Evidence: Task 1 CHANGELOG + LICENSE greps; `grep -c 'typed overlays\|schema validation\|streaming\|JSON Schema\|batch files\|type-safe custom-segment' README.md` or CHANGELOG.md — confirm at least 3 of the 6 v2 deferral keywords are present somewhere in docs; `[Unreleased]` header present. Commit: cite 08-04 commit hash from 08-04-SUMMARY.

    Row SC-5 (from ROADMAP line 170):
    "A developer reading the 'Publishing Your Profile' recipe is linked directly to `examples/profile-starter-kit/` and referenced to `CUSTOMIZING.md`."
    Evidence: `grep -cE 'examples/profile-starter-kit|CUSTOMIZING\.md' README.md` ≥ 1 (both tokens found); cross-reference from README cookbook to kit subtree explicit. Commit: 08-03 or 08-04 depending on where the cookbook section landed.

    Every Evidence cell cites at minimum one concrete anchor (command output, grep count, file path, or commit hash).
  </action>
  <verify>
    <automated>echo "draft table to be transcribed in task 5"</automated>
  </verify>
  <acceptance_criteria>
    - Draft has exactly 5 rows (labeled SC-1..SC-5).
    - Every Truth column quotes ROADMAP verbatim.
    - Every Status = `✓ VERIFIED` (5 hits total).
    - Every Evidence cell cites ≥ 1 concrete anchor.
    - At least one row (SC-1 and/or SC-2) explicitly cross-references 09-04-SUMMARY.md for the v2.1-milestone pipeline log.
  </acceptance_criteria>
  <done>
    5-row Observable Truths draft in task journal, ready for Task 5.
  </done>
</task>

<task type="auto">
  <name>Task 3: Assemble Required Artifacts table (examples + starter kit + docs)</name>
  <read_first>
    - 08-01-SUMMARY.md (examples/ deliverables)
    - 08-02-SUMMARY.md (profile-starter-kit deliverables — 15 files)
    - 08-03-SUMMARY.md (README.md)
    - 08-04-SUMMARY.md (CHANGELOG + CONTRIBUTING + LICENSE)
    - 08-05-SUMMARY.md (package.json bumps + publish.yml + CI additions)
  </read_first>
  <files>(none — prepares Required Artifacts table for Task 5)</files>
  <action>
    Build Required Artifacts table. One row per Phase 8 deliverable file; each row verifies existence + substantive content. Group rows by plan for readability.

    Expected artifacts (all rows `✓ EXISTS + SUBSTANTIVE`):

    Examples tree (Plan 08-01):
    1. `examples/extract-patient-info.ts` — existence + `grep -c 'msg.patient' examples/extract-patient-info.ts` ≥ 1 (helpers usage).
    2. `examples/read-lab-results.ts` — existence + `grep -c 'observations\|orders' examples/read-lab-results.ts` ≥ 1.
    3. `examples/modify-and-resend.ts` — existence + `grep -c 'setField\|buildMessage\|toString' examples/modify-and-resend.ts` ≥ 1 (mutation + round-trip).
    4. `examples/fixtures/` — 3 fixture files expected (one per example); `ls examples/fixtures/ | wc -l` ≥ 3.
    5. `examples/README.md` — existence + non-empty (`wc -l` ≥ 1).
    6. `scripts/run-examples.ts` — existence; referenced by `package.json` `scripts.examples`.

    Profile starter kit (Plan 08-02) — 15 files per SUMMARY:
    7. `examples/profile-starter-kit/package.json` — existence + `grep -c '"name"' examples/profile-starter-kit/package.json` ≥ 1.
    8. `examples/profile-starter-kit/tsconfig.json` — existence.
    9. `examples/profile-starter-kit/tsup.config.ts` — existence.
    10. `examples/profile-starter-kit/vitest.config.ts` — existence.
    11. `examples/profile-starter-kit/src/index.ts` (or sample profile file) — existence.
    12. `examples/profile-starter-kit/test/` — at least 1 test file.
    13. `examples/profile-starter-kit/fixtures/` — at least 1 sample fixture (the ZAL fixture per 08-02-SUMMARY).
    14. `examples/profile-starter-kit/.github/workflows/ci.yml` — existence (KIT-04).
    15. `examples/profile-starter-kit/.github/workflows/publish.yml` — existence (KIT-05-adjacent).
    16. `examples/profile-starter-kit/README.md` — existence.
    17. `examples/profile-starter-kit/CUSTOMIZING.md` — existence + contains rename/swap/define/fixtures/publish walkthrough (per 08-02-SUMMARY).
    18. `examples/profile-starter-kit/LICENSE` — MIT.

    Documentation (Plans 08-03, 08-04):
    19. `README.md` — Phase 8 comprehensive rewrite; `wc -l README.md` + section-heading count; sentinels from Task 1 grep Task 1.9.
    20. `CHANGELOG.md` — Keep-a-Changelog format + `[Unreleased]` section (Task 1 greps).
    21. `CONTRIBUTING.md` — existence.
    22. `LICENSE` — MIT at repo root (already verified in Plan 11-01 but re-confirm here for Phase 8 completeness — DOC-15).

    Release surface (Plan 08-05):
    23. `package.json` — `version: "0.1.0"` (per 08-05), `scripts.examples: "tsx scripts/run-examples.ts"`, `devDependencies.tsx` present. Check: `grep -cE '"version":\s*"0\.1\.0"' package.json` ≥ 1.
    24. `.github/workflows/publish.yml` — workflow_dispatch-only + `permissions.id-token: write` per 08-05. Task 1 grep 13 confirms.
    25. `.github/workflows/ci.yml` — now contains 3 new matrix-gated steps (Examples smoke / Starter kit / kit workflow actionlint), all gated `if: matrix.node == '20'` per 08-05. Check: `grep -c "matrix.node == '20'" .github/workflows/ci.yml` ≥ 3.

    Total: ~25 artifact rows. All `✓ EXISTS + SUBSTANTIVE`. Record actual `wc -l` + grep counts in Details column.
  </action>
  <verify>
    <automated>for f in examples/extract-patient-info.ts examples/read-lab-results.ts examples/modify-and-resend.ts scripts/run-examples.ts README.md CHANGELOG.md CONTRIBUTING.md LICENSE .github/workflows/publish.yml .github/workflows/ci.yml examples/profile-starter-kit/package.json examples/profile-starter-kit/README.md examples/profile-starter-kit/CUSTOMIZING.md examples/profile-starter-kit/LICENSE examples/profile-starter-kit/.github/workflows/ci.yml examples/profile-starter-kit/.github/workflows/publish.yml; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done; echo "ALL PHASE 8 KEY ARTIFACTS PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - All ≥16 key artifacts present (the loop above must exit 0; remaining artifacts are tree-level like `examples/fixtures/`, `examples/profile-starter-kit/test/`, checked separately via `ls | wc -l` ≥ 1).
    - Total row count in draft = 22-25 (groups: examples tree ~6, starter kit ~12, docs ~4, release ~3).
    - Every row has a concrete Details cell (line count, grep match count, or subtree file count).
    - No `✗` rows. If any key artifact is missing at HEAD, STOP and escalate (it's a regression; Phase 8 shipped these).
  </acceptance_criteria>
  <done>
    Required Artifacts table draft ready with ~22-25 rows, all `✓ EXISTS + SUBSTANTIVE`. Ready for Task 5.
  </done>
</task>

<task type="auto">
  <name>Task 4: Assemble Plan-Summary Evidence + Requirements Coverage tables</name>
  <read_first>
    - 08-01-SUMMARY.md (EX-01, EX-02, EX-03 closed)
    - 08-02-SUMMARY.md (KIT-01, KIT-02, KIT-03, KIT-04, KIT-05, KIT-06, KIT-07 closed)
    - 08-03-SUMMARY.md (DOC-01..DOC-13 closed)
    - 08-04-SUMMARY.md (DOC-14, DOC-15 closed)
    - 08-05-SUMMARY.md (capstone — wires all 25 REQs into CI+publish observability)
    - .planning/REQUIREMENTS.md (Phase 8 REQ-IDs: EX-01..03 + KIT-01..07 + DOC-01..15 = 25 total)
  </read_first>
  <files>(none — prepares two tables for Task 5)</files>
  <action>
    Build TWO draft tables.

    TABLE A — Plan-Summary Evidence (5 rows, one per 08-0N-SUMMARY.md):
    Columns: `Plan` / `Subject` / `REQ-IDs Closed` / `Artifacts` / `Acceptance` / `Commit(s)`.

    Row 08-01 — Subject="examples/ tree (3 scripts + 3 fixtures + README + smoke runner)", REQ-IDs=EX-01/02/03, Artifacts=`examples/extract-patient-info.ts`, `examples/read-lab-results.ts`, `examples/modify-and-resend.ts`, `examples/fixtures/`, `examples/README.md`, `scripts/run-examples.ts`. Acceptance=Plan 01 ran `tsx` against each + smoke runner hit marker strings. Commit=extract from 08-01-SUMMARY task-commits section.

    Row 08-02 — Subject="profile-starter-kit subtree (15 files incl. configs, sample profile, test, fixture, 2 workflows, CUSTOMIZING.md, LICENSE)", REQ-IDs=KIT-01..07 (7), Artifacts=15 files in `examples/profile-starter-kit/`. Acceptance=in-kit pipeline green (install+test+build) + actionlint clean. Commit=`43fccae` + `3c303c0` (per ROADMAP line 174).

    Row 08-03 — Subject="Comprehensive README.md replacement (13 sections)", REQ-IDs=DOC-01..DOC-13 (13), Artifacts=`README.md` (full rewrite). Acceptance=all 13 sections present per Task 1 grep sentinel sweep. Commit=from 08-03-SUMMARY.

    Row 08-04 — Subject="CHANGELOG.md (Keep-a-Changelog) + CONTRIBUTING.md + LICENSE verify", REQ-IDs=DOC-14, DOC-15, Artifacts=`CHANGELOG.md`, `CONTRIBUTING.md`, `LICENSE`. Acceptance=Keep-a-Changelog header + `[Unreleased]` section present; LICENSE=MIT. Commit=from 08-04-SUMMARY.

    Row 08-05 — Subject="Capstone: package.json@0.1.0 + tsx devDep + scripts.examples + 3 CI steps + publish.yml + kit peer-dep resolution", REQ-IDs=(none new — integration-only; wires observability into CI for all 25). Artifacts=`.github/workflows/publish.yml` (new), `.github/workflows/ci.yml` (3 added steps), `package.json` (bump + scripts + devDep). Acceptance=pnpm examples OK + kit subtree install+test+build green + publish --dry-run 10 files/346.2 kB. Commits=`f08b1dc` + `f9f2f6b` + `cfbac6f` (per ROADMAP line 177).

    TABLE B — Requirements Coverage (25 rows, one per Phase 8 REQ-ID):
    Columns: `Requirement` / `Description` (brief) / `Status` (`✓ SATISFIED`) / `Evidence (file + test/check)`.

    Rows (abbreviated descriptions acceptable — pull full text from REQUIREMENTS.md if at hand, otherwise use 1-line summaries):
    - EX-01 `examples/extract-patient-info.ts` runs and shows helpers extraction — Evidence: Task 1 pnpm examples + 08-01-SUMMARY.
    - EX-02 `examples/read-lab-results.ts` runs and shows observations/orders iteration — Evidence: same.
    - EX-03 `examples/modify-and-resend.ts` runs and shows mutation + round-trip — Evidence: same.
    - KIT-01 starter kit package.json declares proper name + exports — Evidence: 08-02-SUMMARY + kit package.json grep.
    - KIT-02 kit builds with `pnpm build` emitting dist/ matching exports — Evidence: Task 1 kit-subtree build.
    - KIT-03 kit ships at least one test with `pnpm test` green — Evidence: Task 1 kit-subtree test (4 tests per 08-02-SUMMARY).
    - KIT-04 kit ci.yml validates with actionlint — Evidence: Task 1 actionlint run.
    - KIT-05 kit publish.yml validates with actionlint — Evidence: Task 1 actionlint run.
    - KIT-06 kit CUSTOMIZING.md walks through the 5 steps — Evidence: file existence + content grep.
    - KIT-07 placeholders ({{YOUR_ORG}}, {{PROFILE_NAME}}) appear consistently — Evidence: Task 1 placeholder grep.
    - DOC-01..13 — each cites README.md + appropriate heading grep (use Task 1 sentinel results; for ones not individually grepped in Task 1, use `grep -cE '## ?<keyword>' README.md ≥ 1` pattern). If it's too unwieldy to grep each of the 13 DOC-01..13 individually, use an attestation shortcut: "Plan 08-03 closed all 13 DOC-01..13 per 08-03-SUMMARY's REQ-closure table; Task 1 sentinel grep confirms the 5 load-bearing section headings (H1, Quickstart, Cookbook, Profiles, Error Handling, Cosyte footer) present at HEAD."
    - DOC-14 CHANGELOG in Keep-a-Changelog format with [Unreleased] — Evidence: Task 1 CHANGELOG greps.
    - DOC-15 LICENSE at repo root MIT — Evidence: Task 1 LICENSE grep.

    Total: 25 rows. All `✓ SATISFIED`.
  </action>
  <verify>
    <automated>test "$(ls .planning/phases/08-examples-starter-kit-and-documentation/08-0[1-5]-SUMMARY.md 2>/dev/null | wc -l)" -eq 5</automated>
  </verify>
  <acceptance_criteria>
    - Table A draft has exactly 5 rows (one per SUMMARY).
    - Table B draft has exactly 25 rows (one per Phase 8 REQ-ID: 3 EX + 7 KIT + 15 DOC).
    - Every row in both tables has a concrete Evidence / Commits cell.
    - Table A aggregate: 25 REQ-IDs covered across the 5 plan rows (no gaps).
    - Cross-reference to 09-04-SUMMARY.md appears at least once (probably in Table A row 08-05 acceptance or in the overall prose framing to be added in Task 5).
  </acceptance_criteria>
  <done>
    Both tables (Plan-Summary Evidence: 5 rows; Requirements Coverage: 25 rows) drafted in task journal. Ready for Task 5.
  </done>
</task>

<task type="auto">
  <name>Task 5: Write .planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md</name>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/verification-report.md
    - .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md (shape reference — heavier)
    - .planning/phases/10-planning-doc-resync/10-VERIFICATION.md (shape reference — paper-trail precedent)
    - Tasks 1-4 drafts (in task journal)
  </read_first>
  <files>.planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md</files>
  <action>
    Assemble final VERIFICATION.md using this structure:

    Frontmatter:
    ```yaml
    ---
    phase: 08-examples-starter-kit-and-documentation
    verified: <ISO-8601-from-Task-1>
    status: passed
    score: 5/5 must-haves verified
    overrides_applied: 0
    ---
    ```

    Header (Phase Goal verbatim from ROADMAP.md line 162):
    ```
    # Phase 8: Examples, Starter Kit & Documentation — Verification Report

    **Phase Goal:** A developer landing on the README can go from zero to parsing a real message in under a minute, find a recipe for every common task, and copy the profile starter kit into a new directory to publish their own profile package in minutes.

    **Verified:** <timestamp>
    **Status:** passed (retroactive — closes v2.1-MILESTONE-AUDIT tech-debt item 1 for Phase 8)
    **Re-verification:** No — initial verification (retroactive paper-trail fill)

    **Pipeline evidence cross-reference:** The v2.1-milestone authoritative end-to-end pipeline log (824 tests + 14 it.todo, `pnpm examples` exit 0, starter-kit subtree install+typecheck+lint+test+build all exit 0, `pnpm publish --dry-run` produces clean 10-file/346.2 kB tarball) is recorded in `.planning/phases/09-rename-package-to-cosyte-hl7/09-04-SUMMARY.md` (Task 2 table, lines ~34-47). Phase 8 and Phase 9 share this single pipeline-run log because Phase 9 re-ran it immediately after the rename — the Phase 8 deliverables were the subject under test in that run. Task 1 of Plan 11-02 re-ran the same commands at HEAD for current-date attestation; results recorded in the Verification Commands table below.
    ```

    Then in order:

    1. `## Verification Commands` — transcribe Task 1 results as a table. Columns: `Command` / `Working Dir` / `Exit` / `Notes`. Include all 8+ commands (parent pipeline, `pnpm examples`, kit subtree, actionlint, docs greps).

    2. `## Goal Achievement` → `### Observable Truths` — transcribe Task 2's 5-row table. Append `**Score:** 5/5 success criteria VERIFIED end-to-end.`

    3. `### Required Artifacts` — transcribe Task 3's ~25-row table grouped by plan (examples tree → starter kit → docs → release surface). Append `**Artifacts:** N/N verified.`

    4. `### Key Link Verification` — 4-6 row table of critical wirings:
       - `package.json scripts.examples` → `scripts/run-examples.ts` — ✓ WIRED (Task 1 cmd 6).
       - `scripts/run-examples.ts` → `examples/*.ts` — ✓ WIRED (runner invokes all 3 scripts).
       - `.github/workflows/ci.yml` (3 matrix-gated steps) → Phase 8 deliverables — ✓ WIRED (Examples smoke + Starter kit + kit actionlint; per 08-05-SUMMARY lines 80-92).
       - `examples/profile-starter-kit/devDependencies.@cosyte/hl7` → parent package via `file:../..` — ✓ WIRED (Path B resolution per 08-05-SUMMARY decisions).
       - `README.md → examples/profile-starter-kit/` cross-link — ✓ WIRED (SC-5 evidence; Task 1 grep 9).
       - `.github/workflows/publish.yml` → 0.1.0 npm publish (workflow_dispatch-only) — ✓ WIRED (Task 1 cmd 13).

    5. `### Behavioral Spot-Checks` — 5-6 row table citing Task 1 key results: pipeline green, `pnpm examples` green, kit-subtree green, actionlint clean, README sentinels present, tarball shape (from 09-04-SUMMARY).

    6. `### Requirements Coverage` — transcribe Task 4 Table B (25 rows). Append `**Coverage:** 25/25 Phase 8 REQ-IDs satisfied.`

    7. `### Plan-Summary Evidence` — transcribe Task 4 Table A (5 rows). Append `**Plans:** 5/5 complete, 25/25 REQ-IDs closed.`

    8. `### Anti-Patterns Found` — scan for TODOs and note kit subtree is a template (placeholders `{{YOUR_ORG}}` etc. are INTENDED, not anti-patterns). Use either the (none) row style from 02-VERIFICATION.md, or an explicit one-row note: "Placeholders in `examples/profile-starter-kit/` (e.g., `{{YOUR_ORG}}`, `{{PROFILE_NAME}}`) are intentional per KIT-07 and the CUSTOMIZING.md rename walkthrough; these are NOT anti-patterns."

    9. `### Human Verification Required` — "None. All Phase 8 deliverables are verifiable programmatically: example scripts are runnable (via `pnpm examples`), starter kit subtree has its own pipeline, docs are file-content grep verifiable, and CI re-runs everything on Node 18/20/22 per commit."

    10. `### Gaps Summary` — "**Zero gaps open.** All 5 ROADMAP Success Criteria verified end-to-end. All 25 Phase 8 REQ-IDs (EX-01..03 + KIT-01..07 + DOC-01..15) closed per 5 plan SUMMARYs and re-confirmed at HEAD. The v2.1-milestone authoritative pipeline log in 09-04-SUMMARY.md attests the end-to-end green state; Task 1 re-ran the same commands at HEAD with equivalent results. Phase 8 retroactive verification: closed."

    11. `## Verdict` — "Phase 8 status: passed — 5/5 Success Criteria verified, 25/25 REQ-IDs satisfied, all deliverable artifacts present and substantive, 5/5 plan SUMMARYs attested. This retroactive verification closes v2.1-MILESTONE-AUDIT tech-debt item 1 for Phase 8."

    12. Footer:
       ```
       ---
       *Verified: <timestamp>*
       *Verifier: Claude (gsd-planner, Phase 11 retroactive verification — mechanical assembly from 5 plan SUMMARYs + 09-04-SUMMARY pipeline log + re-run pipeline at HEAD)*
       *Authority: ROADMAP.md Phase 11 + v2.1-MILESTONE-AUDIT.md tech-debt item 1 closure*
       ```

    File length target: ~300-400 lines (larger than Phase 1 because 25 REQ-IDs + 25 artifact rows vs Phase 1's 6 + 13).
  </action>
  <verify>
    <automated>test -f .planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md && test "$(grep -c '^status: passed$' .planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md)" -eq 1 && test "$(grep -c '✓ VERIFIED' .planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md)" -ge 5 && test "$(grep -c '09-04-SUMMARY' .planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md)" -ge 1 && test "$(grep -c '^overrides_applied: 0$' .planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md`.
    - Frontmatter has `phase: 08-examples-starter-kit-and-documentation`, `status: passed`, `score: 5/5 must-haves verified`, `overrides_applied: 0`.
    - Contains ≥ 5 `✓ VERIFIED` strings (Observable Truths).
    - Contains ≥ 20 `✓ EXISTS + SUBSTANTIVE` strings (Required Artifacts).
    - Contains exactly 25 rows in the Requirements Coverage table (one per EX/KIT/DOC REQ).
    - Contains exactly 5 rows in the Plan-Summary Evidence table.
    - Contains at least one reference to `09-04-SUMMARY` (string match) — attesting cross-phase pipeline evidence.
    - Contains a `## Verdict` with "Phase 8 status: passed".
    - Total line count: 250-500 lines.
  </acceptance_criteria>
  <done>
    08-VERIFICATION.md written with all 6 tables + Verdict + footer + cross-reference to 09-04-SUMMARY.md.
  </done>
</task>

<task type="auto">
  <name>Task 6: Commit the new VERIFICATION.md</name>
  <read_first>
    - CLAUDE.md
  </read_first>
  <files>(no new files; git add of Task 5 output)</files>
  <action>
    1. `git add .planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md`.
    2. `git diff --cached --name-only` — must output exactly that one path.
    3. Commit via HEREDOC:
       ```
       git commit -m "$(cat <<'EOF'
       docs(phase-08): add retroactive VERIFICATION.md — 5/5 success criteria verified

       Closes v2.1-MILESTONE-AUDIT tech-debt item 1 for Phase 8 (missing VERIFICATION.md).
       Evidence-assembled from 5 plan SUMMARYs (08-01 examples, 08-02 starter kit,
       08-03 README, 08-04 CHANGELOG/CONTRIBUTING, 08-05 capstone) + 09-04-SUMMARY
       (authoritative v2.1-milestone pipeline log: 824 tests + pnpm examples green
       + kit subtree green + publish --dry-run 10 files/346.2 kB) + re-run at HEAD.
       25/25 Phase 8 REQ-IDs confirmed (3 EX + 7 KIT + 15 DOC).

       Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
       EOF
       )"
       ```
    4. Verify: `git log --oneline -1 -- .planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md` returns exactly 1 commit.
  </action>
  <verify>
    <automated>test "$(git log --oneline -1 --format='%s' -- .planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md | grep -c 'docs(phase-08): add retroactive VERIFICATION.md')" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one commit touching `.planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md` in the log.
    - Commit subject begins with `docs(phase-08):` and contains `add retroactive VERIFICATION.md — 5/5 success criteria verified`.
    - No co-staged unrelated files.
    - Working tree clean.
  </acceptance_criteria>
  <done>
    New 08-VERIFICATION.md committed. Plan-level SUMMARY handled by execute-plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable — paper-trail-only plan. No new code surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | `08-VERIFICATION.md` content | mitigate | Task 1 re-runs pipeline at HEAD; timestamps + exit codes captured; Task 5 transcribes verbatim; Task 6 commits with auditable hash. |
| T-11-02-02 | Information Disclosure | File citations | accept | Public repo; no secrets in SUMMARYs or docs. |
| T-11-02-03 | Repudiation | Commit authorship | mitigate | Co-Authored-By trailer + project git config. |
| T-11-02-04 | Tampering | `publish.yml` reference (claimed to publish under @cosyte/hl7) | mitigate | Task 1 grep confirms workflow_dispatch + permissions; Task 5 records this as KIT-05-adjacent evidence. |
</threat_model>

<verification>
After Task 6:
1. `.planning/phases/08-examples-starter-kit-and-documentation/08-VERIFICATION.md` exists, `grep -c '^status: passed$'` = 1, `grep -c '✓ VERIFIED'` ≥ 5.
2. Requirements Coverage table has 25 rows (3 EX + 7 KIT + 15 DOC).
3. 09-04-SUMMARY cross-reference appears (grep ≥ 1).
4. Commit `docs(phase-08): add retroactive VERIFICATION.md` in git log.
</verification>

<success_criteria>
- All 6 tasks pass their acceptance criteria.
- 08-VERIFICATION.md exists, frontmatter valid, 6 tables populated (Verification Commands / Observable Truths / Required Artifacts / Key Link / Behavioral Spot-Checks / Requirements Coverage / Plan-Summary Evidence), Verdict present.
- One commit with `docs(phase-08):` prefix, touching only this file.
- Zero regressions in Phase 8 pipeline (parent + kit subtree both exit 0 for all pipeline commands).
- Wave-1 parallel-safe: no file overlap with 11-01 or 11-03.
</success_criteria>

<output>
execute-plan creates `.planning/phases/11-retroactive-verification/11-02-SUMMARY.md` citing:
- 5 Phase 8 SCs attested.
- 25 REQ-IDs closed (3 EX + 7 KIT + 15 DOC).
- Cross-reference to 09-04-SUMMARY.md for v2.1-milestone pipeline authority.
- Task 1 pipeline results (parent + kit subtree).
- New file path + commit hash.
</output>
