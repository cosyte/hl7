---
phase: 09-rename-package-to-cosyte-hl7
plan: 04
type: execute
wave: 4
depends_on:
  - 09-PLAN-01-identity-files
  - 09-PLAN-02-source-and-tests
  - 09-PLAN-03-examples-and-starter-kit
files_modified:
  - pnpm-lock.yaml
autonomous: true
requirements: []
must_haves:
  truths:
    - "A developer searching the repo for @cosyte/hl7-parser finds exactly one occurrence — the CHANGELOG Notes: breadcrumb line (ROADMAP success criterion #1 + D-08)"
    - "pnpm install && pnpm build && pnpm typecheck && pnpm lint && pnpm test && pnpm examples all exit 0 with zero warnings post-rename (ROADMAP #2)"
    - "pnpm publish --dry-run produces a tarball under the new name @cosyte/hl7 (ROADMAP #3 — proves installability)"
    - "README, CHANGELOG, examples, and starter kit consistently use @cosyte/hl7; CHANGELOG migration note is present verbatim per D-07 (ROADMAP #4)"
    - "publish.yml is name-agnostic and picks up the new name from package.json — no hardcoded legacy refs (ROADMAP #5)"
  artifacts:
    - path: "pnpm-lock.yaml"
      provides: "Root lockfile reconciled with renamed package.json (regenerated by pnpm install)"
      contains: "@cosyte/hl7"
    - path: ".planning/phases/09-rename-package-to-cosyte-hl7/09-04-SUMMARY.md"
      provides: "Evidence that the entire rename stack is green — grep sweep output + pipeline outputs + dry-run tarball summary"
  key_links:
    - from: "package.json.name (@cosyte/hl7)"
      to: "pnpm publish --dry-run tarball name"
      via: "pnpm reads name field"
      pattern: "@cosyte/hl7"
    - from: ".github/workflows/publish.yml"
      to: "package.json (indirectly, via pnpm publish)"
      via: "No hardcoded name — workflow derives from package.json"
      pattern: "pnpm publish"
---

<objective>
Final verification wave. After Plans 01, 02, 03 have landed their rename edits, this plan:
1. Runs a single authoritative `grep -rn "@cosyte/hl7-parser" . --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=.planning` sweep and asserts the output is exactly one line (the CHANGELOG `Notes:` breadcrumb per D-07/D-08).
2. Runs the full tooling pipeline: `pnpm install` (which regenerates the root `pnpm-lock.yaml` to reflect the renamed package.json), then `pnpm build && pnpm typecheck && pnpm lint && pnpm test && pnpm examples` — all must exit 0 with zero warnings (ROADMAP success criterion #2).
3. Audits `.github/workflows/publish.yml` to confirm it is name-agnostic (does not hardcode `@cosyte/hl7-parser` anywhere) per CONTEXT.md canonical_refs; if a hardcoded reference is found it must be rewritten.
4. Runs `pnpm publish --dry-run` to confirm the tarball publishes as `@cosyte/hl7` and passes provenance/access checks without errors (ROADMAP #3 — proves installability end-to-end).

Purpose: This is the "goal-backward truth" wave. The phase goal ("first published tag under the new name installs and round-trips cleanly") reduces to: pipeline green + dry-run green + grep-sweep clean. If any of those three fails, the rename has a leak somewhere in Waves 1–3.

Output: One committed file change (`pnpm-lock.yaml` regenerated by `pnpm install`) plus a SUMMARY.md that captures grep-sweep output, pipeline exit codes, and the dry-run tarball metadata. No source or doc changes — Waves 1–3 own those and this plan only verifies them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-rename-package-to-cosyte-hl7/09-CONTEXT.md
@CLAUDE.md
@.github/workflows/publish.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Authoritative grep sweep — confirm exactly one @cosyte/hl7-parser occurrence remains (CHANGELOG Notes: line)</name>
  <files>(none — this is a read-only verification task)</files>
  <read_first>
    - .planning/phases/09-rename-package-to-cosyte-hl7/09-CONTEXT.md (D-07 — exact breadcrumb text; D-08 — only CHANGELOG Notes: line may retain old name; CONTEXT.md specifies the sweep excludes `node_modules/`, `dist/`, and `.planning/phases/` — but this plan's sweep goes further and excludes all of `.planning/` since STATE.md and other planning artifacts may legitimately reference history)
    - CHANGELOG.md (confirm the Notes: breadcrumb is present from Plan 01 Task 2)
  </read_first>
  <action>
    Run the authoritative grep sweep that enforces ROADMAP success criterion #1:

    ```
    grep -rn "@cosyte/hl7-parser" . \
      --exclude-dir=node_modules \
      --exclude-dir=dist \
      --exclude-dir=.planning \
      --exclude-dir=coverage \
      --exclude-dir=.git
    ```

    **Expected output:** EXACTLY ONE matching line, in `./CHANGELOG.md`, containing the D-07 breadcrumb:
    `./CHANGELOG.md:<LINE>:Notes: Package renamed from \`@cosyte/hl7-parser\` to \`@cosyte/hl7\` before first publish. No consumers existed under the previous name.`

    Assertion logic:
    1. Capture the grep output to a variable or file.
    2. Assert the number of matching lines is exactly `1`.
    3. Assert the single matching line is in `./CHANGELOG.md` (file path prefix check).
    4. Assert the line contains the verbatim substring `Package renamed from` (sanity-check it's the D-07 line and not a stray straggler).

    If ANY of those three assertions fail:
    - Output the full grep result so the executor can identify which file still leaks the old name
    - STOP and do not proceed to Task 2 (the sweep is incomplete; something from Waves 1–3 failed or was missed)

    If all three pass, proceed to Task 2.

    Note on scope: CONTEXT.md specifics says exclude `node_modules/`, `dist/`, `.planning/phases/` — but we're excluding all of `.planning/` here. That's a strict-er sweep (captures STATE.md, ROADMAP.md, etc., which legitimately reference phase history). If any `.planning/*.md` file references the old name, that's fine — this sweep doesn't care. The sweep asks: "outside planning/build/install artifacts, is the new name the ONLY published-surface name?" Answer must be yes.

    Also briefly skim `.github/workflows/publish.yml` and `.github/workflows/ci.yml` for hardcoded package-name strings:
    ```
    grep -n "@cosyte/hl7-parser" .github/workflows/publish.yml .github/workflows/ci.yml
    ```
    Expected: no output (per CONTEXT.md canonical_refs — these workflows are name-agnostic). If output is present, it's a Wave 1 oversight that must be patched in this task (edit the workflow file, rerun grep). Document the patch in the SUMMARY.md.
  </action>
  <verify>
    <automated>test "$(grep -rn '@cosyte/hl7-parser' . --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=.planning --exclude-dir=coverage --exclude-dir=.git | wc -l)" = "1" && grep -rn "@cosyte/hl7-parser" . --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=.planning --exclude-dir=coverage --exclude-dir=.git | grep -q "^./CHANGELOG.md:.*Package renamed from" && test "$(grep -c '@cosyte/hl7-parser' .github/workflows/publish.yml .github/workflows/ci.yml 2>/dev/null | awk -F: '{s+=$2} END {print s}')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "@cosyte/hl7-parser" . --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=.planning --exclude-dir=coverage --exclude-dir=.git | wc -l` returns EXACTLY `1`
    - The one matching line starts with `./CHANGELOG.md:` (file-path anchor)
    - The one matching line contains the exact substring `Package renamed from `@cosyte/hl7-parser` to `@cosyte/hl7` before first publish.`
    - `grep -c "@cosyte/hl7-parser" .github/workflows/publish.yml` returns `0`
    - `grep -c "@cosyte/hl7-parser" .github/workflows/ci.yml` returns `0`
  </acceptance_criteria>
  <done>
    Exactly one `@cosyte/hl7-parser` occurrence remains in the repo — the D-07 breadcrumb in CHANGELOG.md. GitHub workflows are clean. ROADMAP success criterion #1 is verified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full pipeline — pnpm install + build + typecheck + lint + test + examples</name>
  <files>pnpm-lock.yaml (regenerated as a side-effect of pnpm install)</files>
  <read_first>
    - .planning/phases/09-rename-package-to-cosyte-hl7/09-CONTEXT.md (code_context — Plan 08-05 validated this exact pipeline as green; tarball dry-run 10 files 346.2kB baseline)
    - package.json (confirms scripts: build, typecheck, lint, test, examples, prepublishOnly)
  </read_first>
  <action>
    Run the ordered pipeline; EVERY step must exit 0 with zero warnings (ROADMAP success criterion #2). Capture stdout/stderr excerpts for the SUMMARY.md.

    ```
    # Step 1: regenerate root lockfile against the renamed package.json.
    # This updates pnpm-lock.yaml to reflect the renamed starter-kit peer/dev deps too.
    pnpm install --frozen-lockfile=false
    # Expected: exit 0. Lockfile may update its top-level package name entry.

    # Step 2: typecheck (no emit, strict mode per CLAUDE.md)
    pnpm typecheck
    # Expected: exit 0, zero errors

    # Step 3: lint with --max-warnings=0 (per package.json scripts)
    pnpm lint
    # Expected: exit 0, zero warnings (0 is hard-coded into the script)

    # Step 4: full test suite (should still be 824/824 + 14 todo per STATE.md baseline)
    pnpm test
    # Expected: exit 0; no test count regression vs. pre-rename baseline

    # Step 5: dual ESM+CJS build via tsup
    pnpm build
    # Expected: exit 0; dist/ populated with index.mjs, index.cjs, index.d.ts

    # Step 6: runnable examples smoke — tsx executes each example end-to-end
    pnpm examples
    # Expected: exit 0; each of the 3 examples prints its documented output
    ```

    Why this order:
    - `pnpm install` FIRST so node_modules reflects the renamed package.json (including the starter kit's file:../.. link resolving to `@cosyte/hl7`).
    - `typecheck` before `build` so TS errors surface before any emit work.
    - `test` and `lint` before `build` to fail fast on code quality issues.
    - `examples` LAST because it requires `dist/` to exist (tsx imports from the root package, which — via the `exports` map — points into `dist/`).

    Handling pnpm-lock.yaml drift: If `pnpm install` makes substantive changes to `pnpm-lock.yaml` (e.g., the top-level package name key moves from `@cosyte/hl7-parser` to `@cosyte/hl7`), that is EXPECTED and must be committed as part of this wave's output. Include the delta in the Wave 4 git commit.

    If ANY step fails:
    - Capture the failing output verbatim
    - Inspect whether the failure is a rename leak (e.g., a `src/**` JSDoc block that still references the old name and breaks an eslint-plugin-jsdoc rule)
    - Patch the leak IN-PLACE in this task (document in SUMMARY.md which file needed a touch-up), then re-run the full pipeline from scratch
    - Do NOT proceed to Task 3 until ALL six steps are green

    Two expected-safe deviations:
    - `pnpm install` may print progress lines about lockfile updates — that's not a warning.
    - `pnpm examples` output will have example-script stdout (printed fields, etc.) — that's normal, not a warning.
    Both are fine as long as exit codes are 0.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile=false && pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm examples</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` exits 0
    - `pnpm typecheck` exits 0 with zero errors
    - `pnpm lint` exits 0 with zero warnings (script enforces `--max-warnings=0`)
    - `pnpm test` exits 0; test count matches or exceeds STATE.md baseline (824/824 + 14 todo or newer if Phase 7 continued — do not assert a hard number, just non-regression)
    - `pnpm build` exits 0; `ls dist/` shows `index.mjs`, `index.cjs`, `index.d.ts`
    - `pnpm examples` exits 0; stdout includes at least the three example-output signatures (patient info extracted, lab results printed, modified message round-tripped)
    - Root `pnpm-lock.yaml`'s top-level package descriptor reflects `@cosyte/hl7` (e.g., `grep -c "@cosyte/hl7" pnpm-lock.yaml` > 0) — lockfile acknowledges the rename
    - `grep -c "@cosyte/hl7-parser" pnpm-lock.yaml` returns `0` after `pnpm install` reconciles the lockfile
  </acceptance_criteria>
  <done>
    The renamed repo produces a green pipeline end-to-end — install, typecheck, lint, test, build, examples. ROADMAP success criterion #2 is verified. The regenerated `pnpm-lock.yaml` is ready to commit.
  </done>
</task>

<task type="auto">
  <name>Task 3: pnpm publish --dry-run — confirm tarball publishes as @cosyte/hl7</name>
  <files>(none — read-only verification producing a dry-run report)</files>
  <read_first>
    - .planning/phases/09-rename-package-to-cosyte-hl7/09-CONTEXT.md (code_context reusable assets — Plan 08-05 validated the same pipeline with "tarball dry-run 10 files 346.2kB"; this task re-runs it post-rename)
    - package.json (confirm `publishConfig.access: public` and `publishConfig.provenance: true`, and that `files` array is `["dist", "README.md", "LICENSE", "CHANGELOG.md"]`)
  </read_first>
  <action>
    Run `pnpm publish --dry-run` from the repo root. This builds the tarball as if publishing, but does not upload. Capture the output verbatim.

    ```
    pnpm publish --dry-run
    ```

    Expected output shape (matching Plan 08-05's baseline + rename):
    ```
    Tarball Contents
    <tree listing of files to publish>

    Tarball Details
    name:          @cosyte/hl7              <-- NEW NAME, this is the critical line
    version:       0.1.0
    filename:      cosyte-hl7-0.1.0.tgz
    package size:  ~100 kB
    unpacked size: ~350 kB
    shasum:        <hash>
    integrity:     <hash>
    total files:   ~10
    ```

    Assertions:
    1. Exit code 0.
    2. `name:` line in the dry-run output shows `@cosyte/hl7` (NOT `@cosyte/hl7-parser`).
    3. `version:` line shows `0.1.0` (per D-01).
    4. `filename:` is `cosyte-hl7-0.1.0.tgz` or similar (name-derived filename reflects the new name — ALSO does not contain `hl7-parser`).
    5. Total file count is in the same order of magnitude as Plan 08-05's baseline (10 files per CONTEXT.md code_context).
    6. No errors about missing README, LICENSE, or CHANGELOG (those are in the `files` array per package.json).

    If the dry-run fails with an error like `package already exists` or `npm-registry-fetch: 404`, that's fine — `--dry-run` should not contact the registry. If it DOES contact the registry, check `publishConfig` in package.json to confirm `access: public`. pnpm publish --dry-run should be offline.

    If any assertion above fails:
    - Save the full dry-run output to `.planning/phases/09-rename-package-to-cosyte-hl7/.dry-run-output.txt` for debugging
    - Identify which rename leak caused it (most likely: `name:` field in package.json still wrong → Plan 01 Task 1 didn't land correctly, or version mismatch)
    - Fix the leak, re-run Task 2's full pipeline, then re-run Task 3

    On success:
    - Copy the key fields (name, version, filename, total files, tarball size) into `09-04-SUMMARY.md`
    - This satisfies ROADMAP success criterion #3 (a developer installing the newly published `@cosyte/hl7` package in a fresh project can import it — `--dry-run` proves the tarball is well-formed under the new name).
  </action>
  <verify>
    <automated>pnpm publish --dry-run 2>&1 | tee /tmp/phase9-dry-run.txt && grep -q "name:.*@cosyte/hl7\b" /tmp/phase9-dry-run.txt && grep -q "version:.*0.1.0" /tmp/phase9-dry-run.txt && ! grep -q "hl7-parser" /tmp/phase9-dry-run.txt</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm publish --dry-run` exits 0
    - Dry-run output contains the line `name: @cosyte/hl7` (or equivalent — the package name as pnpm sees it is the new one)
    - Dry-run output contains `version:` with `0.1.0` (D-01 — no version bump)
    - Dry-run output filename / tarball name contains `cosyte-hl7` and does NOT contain `hl7-parser`
    - Dry-run tarball file count is close to Plan 08-05's baseline (10 files ±1 is acceptable — CHANGELOG updates may shift the count by ≤ 1)
    - The dry-run produces no errors about provenance, access, or missing bundled files
  </acceptance_criteria>
  <done>
    `pnpm publish --dry-run` produces a tarball under `@cosyte/hl7@0.1.0`. ROADMAP success criterion #3 is verified. The rename is production-ready for a real `pnpm publish`.
  </done>
</task>

</tasks>

<verification>
Phase 9 is complete when ALL of the following hold simultaneously:

```
# (1) Grep sweep — exactly one old-name occurrence, in CHANGELOG Notes: line
grep -rn "@cosyte/hl7-parser" . --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=.planning --exclude-dir=coverage --exclude-dir=.git | wc -l
# Expected: 1

# (2) Full pipeline green
pnpm install --frozen-lockfile=false && pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm examples
# Expected: exit 0 at every step

# (3) Dry-run publishes under new name
pnpm publish --dry-run | grep -E "^name:\s+@cosyte/hl7\b"
# Expected: one matching line with exactly the new name

# (4) Workflows are name-agnostic
grep -c "@cosyte/hl7-parser" .github/workflows/*.yml
# Expected: 0 in every workflow file
```

All four invariants correspond directly to ROADMAP success criteria #1, #2, #3, #5 respectively. Criterion #4 (README/CHANGELOG/examples/starter-kit consistency) is verified implicitly by criterion #1 (grep-sweep zero + CHANGELOG breadcrumb present = content agreement).

Manual action (OUTSIDE this plan, per CONTEXT.md D-04/D-05): the GitHub repo rename (`cosyte/hl7-parser` → `cosyte/hl7`) is a user-side admin action performed around publish time. The code PR lands the new URLs speculatively; GitHub's auto-redirect covers the gap. Surface this clearly in the Wave 4 SUMMARY.md so the user knows to do the repo rename before the first real `pnpm publish` runs.
</verification>

<success_criteria>
- Task 1: grep sweep returns exactly one match, in `./CHANGELOG.md`, containing the D-07 breadcrumb
- Task 2: all six pipeline steps exit 0 with zero warnings; pnpm-lock.yaml is regenerated and committed
- Task 3: pnpm publish --dry-run exits 0 and reports the package name as `@cosyte/hl7` at version `0.1.0`
- `.github/workflows/publish.yml` and `ci.yml` contain zero occurrences of the old name
- SUMMARY.md captures: grep-sweep output (one line), pipeline exit codes (all 0), dry-run tarball metadata (name, version, filename, file count, total size)
</success_criteria>

<output>
After completion, create `.planning/phases/09-rename-package-to-cosyte-hl7/09-04-SUMMARY.md` with:
- The literal one-line output of the authoritative grep sweep from Task 1
- Per-step exit codes and tail output (last ~5 lines) for `pnpm install`, `typecheck`, `lint`, `test`, `build`, `examples` from Task 2
- `pnpm-lock.yaml` diff summary (what changed due to the rename)
- Key lines from `pnpm publish --dry-run` output from Task 3 (name, version, filename, total files, unpacked size)
- An explicit callout: **"Manual step remaining outside code: user must rename the GitHub repo `cosyte/hl7-parser` → `cosyte/hl7` in GitHub Settings around publish time (D-04/D-05). GitHub auto-redirects until then."**
- Any deviations from the plan
</output>
