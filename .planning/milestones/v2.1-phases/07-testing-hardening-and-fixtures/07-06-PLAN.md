---
phase: 07-testing-hardening-and-fixtures
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05, 07]
files_modified:
  - vitest.config.ts
  - .github/workflows/ci.yml
autonomous: false
requirements: [TEST-01]
user_setup: []
must_haves:
  truths:
    - "vitest.config.ts coverage.thresholds.branches across src/parser/**, src/model/**, src/helpers/** is 90 (bumped from 85 to match CLAUDE.md ≥90% guardrail)"
    - "Branch threshold for src/serialize/** and src/builder/** decided based on Plan 01 baseline data (KEEP at 90 if currently passing — recommendation; LOWER to 85 only if data shows an unrecoverable gap)"
    - ".github/workflows/ci.yml has a new step `Test (with coverage)` running `pnpm test:coverage` after the existing `Test` step (insert, not replace)"
    - "pnpm test:coverage exits 0 — coverage gate passes for src/parser/**, src/model/**, src/helpers/** at lines/branches/functions/statements ≥ 90%"
    - "If pnpm test:coverage exits non-zero against the tightened gate, gap-closure tasks added (write targeted tests for uncovered code paths) until the gate passes"
    - "CI workflow remains valid (actionlint or yamllint clean if available; otherwise visual review)"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Updated coverage thresholds — branches bumped to 90 across the three CLAUDE.md-mandated dirs"
      contains: "branches: 90"
    - path: ".github/workflows/ci.yml"
      provides: "CI gains a coverage step invoking pnpm test:coverage; matrix unchanged (node 18/20/22)"
      contains: "test:coverage"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json scripts.test:coverage"
      via: "pnpm test:coverage invocation"
      pattern: 'test:coverage'
    - from: "vitest.config.ts coverage block"
      to: "src/parser/**, src/model/**, src/helpers/**"
      via: "thresholds.{path}.branches ≥ 90"
      pattern: 'branches.*90'
---

<objective>
Wave 3 capstone plan. Tightens the existing `vitest.config.ts` coverage gate (branches 85 → 90 across the CLAUDE.md-mandated dirs), adds a `pnpm test:coverage` step to `.github/workflows/ci.yml` (the real Phase 7 CI gap per PATTERNS.md note 5), and confirms the full coverage gate passes with all Wave 1+2 fixtures and tests in place. If gaps surface, adds targeted unit tests until the gate passes. Closes TEST-01.

Purpose: PATTERNS.md reconciliation discovered that `package.json` test:coverage script + `@vitest/coverage-v8` devDep + `vitest.config.ts` coverage block were ALL landed by Phase 1 — the actual Phase 7 gaps are (a) branch threshold below CLAUDE.md's stated 90% bar and (b) CI workflow not running coverage. This plan closes those two gaps and verifies the gate empirically.

Output: 2 file edits (vitest.config.ts thresholds, ci.yml step), zero or more targeted test additions to close coverage gaps surfaced by the tightened gate, one human-verification checkpoint to inspect coverage output before CI lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-testing-hardening-and-fixtures/07-CONTEXT.md
@.planning/phases/07-testing-hardening-and-fixtures/07-PATTERNS.md
@.planning/phases/07-testing-hardening-and-fixtures/07-01-SUMMARY.md
@.planning/phases/07-testing-hardening-and-fixtures/07-02-SUMMARY.md
@.planning/phases/07-testing-hardening-and-fixtures/07-03-SUMMARY.md
@.planning/phases/07-testing-hardening-and-fixtures/07-04-SUMMARY.md
@.planning/phases/07-testing-hardening-and-fixtures/07-05-SUMMARY.md
@.planning/phases/07-testing-hardening-and-fixtures/07-07-SUMMARY.md
@CLAUDE.md
@vitest.config.ts
@.github/workflows/ci.yml
@package.json

<interfaces>
<!-- Existing config file shapes that this plan modifies. -->

vitest.config.ts current coverage.thresholds (lines 34–75):
```typescript
thresholds: {
  lines: 90,
  branches: 85,           // <-- bump candidate
  functions: 90,
  statements: 90,
  "src/parser/**":   { lines: 90, branches: 85, functions: 90, statements: 90 },
  "src/model/**":    { lines: 90, branches: 85, functions: 90, statements: 90 },
  "src/helpers/**":  { lines: 90, branches: 85, functions: 90, statements: 90 },
  "src/serialize/**":{ lines: 90, branches: 85, functions: 90, statements: 90 },
  "src/builder/**":  { lines: 90, branches: 85, functions: 90, statements: 90 },
},
```

.github/workflows/ci.yml insertion anchor (lines 49–56):
```yaml
- name: Format check
  run: pnpm format:check

- name: Test
  run: pnpm test

- name: Build
  run: pnpm build
```

Insert NEW step `- name: Test (with coverage)` between `Test` and `Build`.

package.json scripts (already in place from Phase 1 — DO NOT modify):
```json
"test": "vitest run",
"test:coverage": "vitest run --coverage",
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten vitest.config.ts coverage thresholds (branches 85 → 90)</name>
  <files>vitest.config.ts</files>
  <read_first>
    - vitest.config.ts (whole file — lines 34–75 are the threshold block)
    - .planning/phases/07-testing-hardening-and-fixtures/07-01-SUMMARY.md (CRITICAL — gives the pre-Phase-7 coverage baseline numbers from Plan 01 Task 1; whether the broader serialize/builder gates currently pass at 90/85/90/90 informs the keep-or-lower decision)
    - .planning/phases/07-testing-hardening-and-fixtures/07-PATTERNS.md (reconciliation note 2 + 3 — context on why this gate exists and the keep-or-lower options for serialize/builder)
    - CLAUDE.md (engineering guardrails — `Coverage target: ≥ 90% on src/parser/, src/model/, src/helpers/` — IS the source of truth for this bump)
  </read_first>
  <action>
    Read Plan 01's SUMMARY for the pre-Phase-7 baseline. Two scenarios:

    **Scenario A — Plan 01 baseline shows all 5 directories pass at lines/branches/functions/statements ≥ 90/85/90/90:**

    Edit `vitest.config.ts` lines 34–75 to set `branches: 90` everywhere (5 per-dir blocks + the global `branches: 85` line).

    Resulting block (replace verbatim):
    ```typescript
    thresholds: {
      lines: 90,
      branches: 90,
      functions: 90,
      statements: 90,
      "src/parser/**": {
        lines: 90,
        branches: 90,
        functions: 90,
        statements: 90,
      },
      "src/model/**": {
        lines: 90,
        branches: 90,
        functions: 90,
        statements: 90,
      },
      "src/helpers/**": {
        lines: 90,
        branches: 90,
        functions: 90,
        statements: 90,
      },
      // Phase 5 additions kept at the same bar — Phase 7 verified these
      // currently pass; tightening serialize/builder along with parser/
      // model/helpers prevents future regressions in the emit + outbound
      // pipelines.
      "src/serialize/**": {
        lines: 90,
        branches: 90,
        functions: 90,
        statements: 90,
      },
      "src/builder/**": {
        lines: 90,
        branches: 90,
        functions: 90,
        statements: 90,
      },
    },
    ```

    **Scenario B — Plan 01 baseline shows one or more directories below the 90 line/branch bar:**

    For directories that ARE below 90 branches but are within the CLAUDE.md scope (parser/model/helpers): bump to 90 anyway — the gap will be exposed by `pnpm test:coverage` and Task 2's gap-closure step will write the missing tests.

    For directories OUTSIDE the CLAUDE.md scope (serialize/builder): if Plan 01's baseline shows them currently passing at branches ≥ 90, bump to 90. If they're below 90, follow CONTEXT.md D-02's narrower scope — REMOVE the per-dir threshold blocks for `src/serialize/**` and `src/builder/**`, leaving them in `include` (still reported in console output) but ungated.

    Document the chosen scenario at the top of the modified `coverage` block as a brief comment.

    Do NOT modify any other part of vitest.config.ts. Do NOT change `include`, `exclude`, `reporter`, or `provider`.

    Run `pnpm test:coverage` after the edit. If it exits 0, proceed. If it exits non-zero with new failures introduced by the bump, Task 2 must close the gap. **Do not commit yet** — Task 2's checkpoint comes first.
  </action>
  <verify>
    <automated>grep -c 'branches: 90' vitest.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` has `branches: 90` for src/parser/**, src/model/**, src/helpers/** (`grep -A1 'src/parser/\*\*' vitest.config.ts | grep -c 'branches: 90'` ≥ 1; same for model and helpers)
    - The chosen scenario (A or B) is documented inline in the config or in the commit message
    - `pnpm test:coverage` runs to completion (exit 0 OR exits with documented coverage failures only — NOT a syntactic config error)
    - No other parts of vitest.config.ts changed (`git diff vitest.config.ts` shows changes confined to the thresholds block)
  </acceptance_criteria>
  <done>Branch threshold tightened per CLAUDE.md guardrail; serialize/builder scope decided based on baseline data.</done>
</task>

<task type="auto">
  <name>Task 2: Close any coverage gaps surfaced by the tightened gate</name>
  <files>varies — targeted test files in test/ for whichever src/ paths fall below threshold</files>
  <read_first>
    - Output of `pnpm test:coverage` from Task 1 (latest run — surfaces uncovered files + line numbers)
    - For each uncovered path: the source file in src/ AND any existing test file already touching that path (style anchor)
    - .planning/phases/07-testing-hardening-and-fixtures/07-PATTERNS.md (style anchors apply here — write tests in the same idiom as existing test/<area>-*.test.ts files)
  </read_first>
  <action>
    Run `pnpm test:coverage` and inspect the output. Three possible outcomes:

    **A — Coverage passes (exit 0):**
    No work in this task. Proceed to Task 3.

    **B — Coverage fails on lines/branches/functions/statements in src/parser/**, src/model/**, src/helpers/**:**
    For each failing path:
    1. Read the source file. Identify uncovered lines (vitest reports them in the text reporter).
    2. Decide whether the uncovered code is reachable:
       - Reachable but untested → write a targeted unit test in the matching `test/<area>-*.test.ts` file. Style: explicit `describe('<source path>', () => { it('covers <case>', () => { ... }) })`.
       - Unreachable / dead code → annotate with `/* c8 ignore start/end */` per @vitest/coverage-v8 syntax IF AND ONLY IF the code is provably dead (e.g., a default branch in an exhaustive switch). Do NOT use ignore comments to paper over real gaps.
    3. Re-run `pnpm test:coverage` and verify the threshold is now met.
    4. Iterate until all CLAUDE.md-scope directories pass.

    **C — Coverage fails on serialize/** or builder/** ONLY (CLAUDE.md says these are out of scope but PATTERNS.md note 3 recommends keeping the broader gate):**
    Either:
    - Write targeted tests to close the gap (preferred — these directories already had ≥90% coverage as of Phase 5/6 verifier; if Phase 7 fixtures changed something, fix it).
    - OR fall back to Scenario B from Task 1 (drop the per-dir threshold for the failing directory). Document the choice in the commit.

    Do NOT relax the parser/model/helpers thresholds. CLAUDE.md is non-negotiable for those three.

    Each new test file added in this task MUST:
    - Live under `test/`
    - Follow the existing per-area naming (`test/parser-<thing>.test.ts`, `test/model-<thing>.test.ts`, `test/helpers-<thing>.test.ts`)
    - Use `describe`/`it` with assertions that reference the specific source file's behavior
    - Pass under `pnpm test`

    After all gaps are closed, `pnpm test:coverage` MUST exit 0 with the tightened thresholds.
  </action>
  <verify>
    <automated>pnpm test:coverage 2>&1 | tail -20 | grep -E 'ALL files|All files|coverage|Tests' || pnpm test:coverage</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:coverage` exits 0 with the tightened thresholds active
    - If new test files were added, each follows the `test/<area>-*.test.ts` naming convention
    - If `c8 ignore` comments were added, each is justified by an inline comment explaining why the code is unreachable
    - `pnpm test` (without coverage) still exits 0
    - `pnpm typecheck` exits 0
    - `pnpm lint --max-warnings=0` exits 0
  </acceptance_criteria>
  <done>Coverage gate passes at the tightened thresholds across all gated directories; any gaps closed with targeted tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — coverage gate output before CI lands</name>
  <what-built>
    Tasks 1 + 2 tightened the vitest.config.ts coverage thresholds (branches 85 → 90 for parser/model/helpers per CLAUDE.md) and closed any coverage gaps surfaced by the bump. Before adding the CI step in Task 4 (which would fail every PR if the gate is broken), please visually confirm the gate actually passes at the tightened thresholds and review which directories ended up in scope.
  </what-built>
  <how-to-verify>
    1. Run `pnpm test:coverage` and observe the output. Expected: exit 0; the tail of the output should show a coverage table with each gated directory at ≥ 90% lines/branches/functions/statements.
    2. Inspect `vitest.config.ts` and confirm:
       - `branches: 90` appears for `src/parser/**`, `src/model/**`, `src/helpers/**` (CLAUDE.md non-negotiable)
       - The serialize/** and builder/** entries are EITHER also at 90 OR removed entirely (per Scenario A/B from Task 1)
    3. Confirm `pnpm test` (without coverage) still passes — coverage and tests are independently green.
    4. (Optional) Open the HTML report in `coverage/index.html` to spot-check uncovered lines that the text reporter may have summarized.

    Type `approved` to proceed to Task 4 (CI workflow edit). If anything looks off — tightened threshold doesn't actually pass, gaps closed by writing trivial tests rather than meaningful ones, or scope decision feels wrong — describe the issue.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Add coverage step to CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (whole file — lines 49–56 are the insertion anchor)
    - .planning/phases/07-testing-hardening-and-fixtures/07-CONTEXT.md (D-30 — coverage step recommendation)
    - .planning/phases/07-testing-hardening-and-fixtures/07-PATTERNS.md (§CI: .github/workflows/ci.yml — exact insertion shape)
    - package.json (confirm `test:coverage` script — already present)
  </read_first>
  <action>
    Insert a new step `Test (with coverage)` BETWEEN the existing `Test` step (line 52–53) and `Build` step (line 55–56). The new step runs after lint/format/test (fast feedback first) but before build (so a coverage failure stops a wasted build).

    The exact YAML to insert:
    ```yaml

          - name: Test (with coverage)
            run: pnpm test:coverage
    ```

    Note: 6-space indentation matches the existing step indentation (steps live under `jobs.verify.steps`, indented 6 spaces from the file root). Verify by visually comparing to the surrounding steps.

    Do NOT:
    - Remove or replace the existing `Test` step (keep both — fast `pnpm test` provides quick feedback even if coverage misbehaves)
    - Add any third-party action (no Codecov/Coveralls integration in v1 per CONTEXT.md deferred ideas)
    - Modify the matrix (Node 18/20/22 stays unchanged — coverage runs on all three)
    - Modify any other workflow file or add new workflow files

    After the edit, validate the YAML:
    - `cat .github/workflows/ci.yml` to visually confirm the new step
    - If `actionlint` is available locally (`actionlint --version`), run `actionlint .github/workflows/ci.yml`
    - Otherwise: a YAML parser smoke check with node — `node -e "console.log(require('js-yaml').load(require('fs').readFileSync('.github/workflows/ci.yml','utf8')).jobs.verify.steps.map(s=>s.name).join(','))"` (note: `js-yaml` is not in devDependencies; only run if available — otherwise visual review is sufficient since CI itself will fail on syntax errors at next push)

    Run `pnpm test:coverage` ONE MORE TIME locally after the workflow edit (sanity — the workflow change shouldn't affect local behavior, but verifies nothing else accidentally broke).
  </action>
  <verify>
    <automated>grep -A1 'Test (with coverage)' .github/workflows/ci.yml | grep -c 'pnpm test:coverage'</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a step named `Test (with coverage)` (`grep -c 'Test (with coverage)' .github/workflows/ci.yml` ≥ 1)
    - That step's run command is `pnpm test:coverage` (verified by the automated check above)
    - The existing `Test` step (`pnpm test`) is still present (`grep -A1 '^      - name: Test$' .github/workflows/ci.yml | grep -c 'pnpm test$'` ≥ 1)
    - The new step appears BETWEEN Test and Build (visual check via `grep -B1 -A1 'Test (with coverage)' .github/workflows/ci.yml` shows Test above and Build below)
    - The matrix block (`matrix.node: ["18", "20", "22"]`) is unchanged
    - No new actions added (`grep -c 'uses:' .github/workflows/ci.yml` returns the same count as before this task)
    - `pnpm test:coverage` still exits 0 locally (sanity)
    - `pnpm test`, `pnpm typecheck`, `pnpm lint` all exit 0
  </acceptance_criteria>
  <done>TEST-01 closed; CI workflow now enforces the coverage gate on every PR; the existing fast-feedback Test step is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → repo workflow | The CI workflow runs with repo permissions; this plan adds one step that invokes a local pnpm script. No external action; no new secrets read; no third-party integration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Tampering | CI workflow edit | mitigate | Edit confined to inserting one step; no `uses:` clauses added (no third-party action surface). Manual review via the human-verification checkpoint catches accidental regressions. |
| T-07-06-02 | Elevation of Privilege | pnpm test:coverage in CI | accept | Coverage runs the same vitest invocation as `pnpm test` plus an instrumented binary; same trust surface as the existing Test step. No new permissions required (`permissions: contents: read` already set in workflow line 14–15). |
| T-07-06-03 | Information Disclosure | Coverage HTML output in CI | accept | HTML report written to `./coverage/` which is gitignored (PATTERNS.md note 4). CI runner discards the workspace after the job. No upload to a third-party coverage service in v1 (deferred per CONTEXT.md). |
</threat_model>

<verification>
After Task 4, the following is end-to-end true (verifiable in a future PR or `act` run):
- A push to a PR triggers `verify (node-18)`, `verify (node-20)`, `verify (node-22)` — each runs install, typecheck, lint, format check, test, **test:coverage** (NEW), build, dist verify in order.
- A coverage regression below the tightened thresholds fails the build with the actual offending file + percentage in the log.

Local sanity: `pnpm test:coverage` exits 0; `cat coverage/coverage-summary.json` (if json reporter present) shows per-file stats.
</verification>

<success_criteria>
- vitest.config.ts coverage.thresholds.branches = 90 for src/parser/**, src/model/**, src/helpers/** (CLAUDE.md non-negotiable scope)
- vitest.config.ts serialize/** + builder/** thresholds either kept at 90 or removed (Scenario A/B per Task 1, documented)
- pnpm test:coverage exits 0 locally
- .github/workflows/ci.yml has a Test (with coverage) step running pnpm test:coverage between Test and Build
- pnpm test passes; pnpm typecheck passes; pnpm lint --max-warnings=0 passes
- TEST-01 closed
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing-hardening-and-fixtures/07-06-SUMMARY.md` summarizing:
- Branch threshold change applied (85 → 90) and final per-dir scope (which dirs ended up gated)
- Whether Scenario A or Scenario B was chosen and why (with the baseline data from Plan 01)
- Number of new test files added (Task 2) to close coverage gaps + their REQ-area
- CI workflow change (one new step inserted; matrix preserved)
- Final coverage numbers (lines/branches/functions/statements per gated directory) — pasted from the final `pnpm test:coverage` run
- TEST-01 closed
</output>
