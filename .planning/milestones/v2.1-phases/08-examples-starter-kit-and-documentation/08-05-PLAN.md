---
phase: 08-examples-starter-kit-and-documentation
plan: 05
type: execute
wave: 2
depends_on: [08-01, 08-02, 08-03, 08-04]
files_modified:
  - package.json
  - .github/workflows/ci.yml
  - .github/workflows/publish.yml
  - pnpm-workspace.yaml
  - .npmrc
autonomous: true
requirements: []
tags: [integration, capstone, publish-workflow, ci-wiring, version-bump]

must_haves:
  truths:
    - "A developer inspecting `package.json` sees version `0.1.0` (bumped from `0.0.0` per CONTEXT D-20)."
    - "A developer inspecting `package.json` sees `scripts.examples = \"tsx scripts/run-examples.ts\"` (wires the Plan 08-01 smoke runner)."
    - "A developer inspecting `package.json` sees `tsx` under `devDependencies` (D-01 caret pin e.g. `^4.x`) and NO new entry under `dependencies` (zero runtime deps preserved)."
    - "A developer opening `.github/workflows/ci.yml` sees three new steps after the existing `Test (with coverage)` step: `Examples (smoke)`, `Starter kit (install + test + build)`, `Validate kit workflows (actionlint)` — gated to a single matrix slot (`if: matrix.node == '20'`) to keep CI time additive-not-multiplicative per CONTEXT D-11."
    - "A developer opening the NEW `.github/workflows/publish.yml` sees `on: workflow_dispatch` ONLY (no push, no tag triggers) and `NODE_AUTH_TOKEN` scoped to the single `pnpm publish` step's env (never job-level, never logged)."
    - "A developer running `pnpm install && pnpm build && pnpm typecheck && pnpm lint && pnpm test && pnpm examples` from a clean clone sees every command exit 0 — the end-to-end Phase 8 pipeline."
    - "A developer running `pnpm publish --dry-run --access public` from repo root sees a tarball preview containing `dist/`, `README.md`, `LICENSE`, `CHANGELOG.md` (matching the existing `files` allow-list in package.json) — VERIFICATION ONLY, does NOT publish (CONTEXT D-25)."
  artifacts:
    - path: "package.json"
      provides: "Version 0.1.0, scripts.examples wired, tsx devDep added"
      contains: "\"version\": \"0.1.0\""
    - path: ".github/workflows/ci.yml"
      provides: "3 new CI steps wiring examples smoke + kit smoke + kit actionlint"
      contains: "Examples (smoke)"
    - path: ".github/workflows/publish.yml"
      provides: "workflow_dispatch-only publish workflow with NPM_TOKEN secret injection"
      contains: "workflow_dispatch"
    - path: "pnpm-workspace.yaml"
      provides: "(CONDITIONAL — see task 3) Workspace declaration that makes `@cosyte/hl7-parser` peer-resolvable from examples/profile-starter-kit"
    - path: ".npmrc"
      provides: "(CONDITIONAL — see task 3) Adds link-workspace-packages=true if workspace path chosen"
  key_links:
    - from: ".github/workflows/ci.yml Examples (smoke) step"
      to: "scripts/run-examples.ts"
      via: "pnpm examples -> runs Plan 08-01's smoke script"
      pattern: "pnpm examples"
    - from: ".github/workflows/ci.yml Starter kit step"
      to: "examples/profile-starter-kit/"
      via: "working-directory: examples/profile-starter-kit"
      pattern: "working-directory:.*profile-starter-kit"
    - from: ".github/workflows/publish.yml"
      to: "secrets.NPM_TOKEN"
      via: "NODE_AUTH_TOKEN env scoped to single publish step"
      pattern: "NODE_AUTH_TOKEN.*secrets\\.NPM_TOKEN"
    - from: "package.json scripts.examples"
      to: "scripts/run-examples.ts"
      via: "tsx scripts/run-examples.ts"
      pattern: "tsx scripts/run-examples\\.ts"
---

<objective>
Wave-2 capstone plan. Wires the four Wave-1 deliverables (examples, starter kit, README, ancillary docs) into the parent repo's CI + publish + version-bump surface — everything that MUST be serialized because it touches `package.json` or `.github/workflows/ci.yml`.

**Purpose:** After Wave 1, four disjoint subtrees exist in isolation. This plan turns them into a coherent release:
- Adds `tsx` devDep so `pnpm examples` works.
- Bumps version `0.0.0 → 0.1.0` (CONTEXT D-20).
- Wires `Examples (smoke)` + `Starter kit (install + test + build)` + `Validate kit workflows (actionlint)` into `.github/workflows/ci.yml` — gated to a single matrix slot (CONTEXT D-11 "Adds ~30s to CI" suggests not running 3×).
- Creates new `.github/workflows/publish.yml` (workflow_dispatch-only, CONTEXT D-24).
- Resolves the peer-dep pattern for the starter kit (CONTEXT D-11 — pnpm-workspace.yaml + `.npmrc`, OR `--filter` if workspace adds friction; planner-confirmed at execute time).
- Runs the end-to-end Phase 8 pipeline locally as a correctness gate before commit.
- Optionally runs `pnpm publish --dry-run` (CONTEXT D-25) to confirm tarball contents — does NOT publish.

**Output:** 2 new workflow files (publish.yml, possibly pnpm-workspace.yaml) + 2 modified files (package.json, ci.yml) + 1 conditionally modified file (.npmrc) + an end-to-end smoke verification.

**Scope boundary:** This plan does NOT add new REQs (hence empty `requirements` field — the REQs it enables were assigned to Plans 08-01..04). It is the integration glue that makes those REQs observable in CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-examples-starter-kit-and-documentation/08-CONTEXT.md
@.planning/phases/08-examples-starter-kit-and-documentation/08-PATTERNS.md
@CLAUDE.md

@package.json
@.github/workflows/ci.yml
@.npmrc

@examples/extract-patient-info.ts
@examples/read-lab-results.ts
@examples/modify-and-resend.ts
@scripts/run-examples.ts

@examples/profile-starter-kit/package.json
@examples/profile-starter-kit/.github/workflows/ci.yml
@examples/profile-starter-kit/.github/workflows/publish.yml

@README.md
@CHANGELOG.md
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version to 0.1.0 + add scripts.examples + add tsx devDep in package.json</name>
  <read_first>
    - package.json (current state — confirm version is still 0.0.0; confirm scripts + devDependencies layout)
    - .planning/phases/08-examples-starter-kit-and-documentation/08-CONTEXT.md (D-01, D-04, D-20, D-21)
    - .planning/phases/08-examples-starter-kit-and-documentation/08-PATTERNS.md (section `package.json` — lines 769-796)
    - scripts/run-examples.ts (produced by Plan 08-01 — confirm path + filename)
  </read_first>
  <action>
Edit `package.json` with three surgical changes — do NOT touch any other field:

### Change 1 — Version bump
`"version": "0.0.0"` → `"version": "0.1.0"` (CONTEXT D-20 "first release = 0.1.0"; matches kit's `peerDependencies: ">=0.1.0"` from Plan 08-02).

### Change 2 — Add scripts.examples
Insert `"examples": "tsx scripts/run-examples.ts",` into the `scripts` block. Place it alphabetically adjacent — recommended slot between `"clean"` and `"format"` for readability (or anywhere in the block; order is not semantically meaningful to pnpm).

The final `scripts` block should contain (new entry in **bold**):
```json
"scripts": {
  "build": "tsup",
  "typecheck": "tsc --noEmit",
  "lint": "eslint \"src/**/*.ts\" \"test/**/*.ts\" --max-warnings=0",
  "lint:fix": "eslint \"src/**/*.ts\" \"test/**/*.ts\" --fix",
  "format": "prettier --write \"src/**/*.{ts,md}\" \"test/**/*.ts\" \"*.{json,md,yml}\"",
  "format:check": "prettier --check \"src/**/*.{ts,md}\" \"test/**/*.ts\" \"*.{json,md,yml}\"",
  "test": "vitest run",
  "test:watch": "vitest",
  "test:coverage": "vitest run --coverage",
  "clean": "rm -rf dist coverage",
  "examples": "tsx scripts/run-examples.ts",
  "prepublishOnly": "pnpm clean && pnpm typecheck && pnpm lint && pnpm test && pnpm build"
},
```

### Change 3 — Add tsx devDep
Insert `"tsx": "^4.0.0",` into the `devDependencies` block. Place alphabetically (after `"tsup"`):
```json
"devDependencies": {
  "@types/node": "^20.11.0",
  "@typescript-eslint/eslint-plugin": "^7.0.0",
  "@typescript-eslint/parser": "^7.0.0",
  "@vitest/coverage-v8": "^1.2.0",
  "eslint": "^8.57.0",
  "eslint-config-prettier": "^9.1.0",
  "eslint-plugin-jsdoc": "^48.0.0",
  "prettier": "^3.2.0",
  "tsup": "^8.0.0",
  "tsx": "^4.0.0",
  "typescript": "^5.3.0",
  "vitest": "^1.2.0"
},
```

Use `^4.0.0` (CONTEXT "Claude's Discretion" line 333: "caret pin to current, e.g., `^4.x` — matches parent devDep style"; concrete current: `^4.0.0`).

### Non-changes to confirm
- `"dependencies": {}` REMAINS empty — zero runtime deps preserved (PROJECT.md constraint + CLAUDE.md guardrail).
- `"files"` array unchanged: `["dist", "README.md", "LICENSE", "CHANGELOG.md"]` (matches the new CHANGELOG.md created by Plan 08-04).
- `"publishConfig"` unchanged.
- `"prettier"` format globs in `scripts.format` / `scripts.format:check` NOT widened to include `examples/` or `scripts/` — CONTEXT PATTERNS.md line 795-796 notes this is intentional (examples have their own prose conventions; the kit has its own prettier config).

After editing, run `pnpm install --no-frozen-lockfile` to refresh `pnpm-lock.yaml` with the new `tsx` entry. Commit the updated lockfile with this change.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if (p.version!=='0.1.0') throw new Error('version: '+p.version); if (!p.scripts.examples || !p.scripts.examples.includes('scripts/run-examples.ts')) throw new Error('scripts.examples missing'); if (!p.devDependencies.tsx) throw new Error('tsx devDep missing'); if (Object.keys(p.dependencies||{}).length!==0) throw new Error('runtime deps leaked: '+JSON.stringify(p.dependencies)); console.log('package.json OK: version='+p.version+' tsx='+p.devDependencies.tsx);" && pnpm install --no-frozen-lockfile && pnpm examples</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `.version` equals `"0.1.0"` (was `"0.0.0"`).
    - `package.json` `.scripts.examples` equals `"tsx scripts/run-examples.ts"`.
    - `package.json` `.devDependencies.tsx` matches `^4.` (e.g., `^4.0.0`).
    - `package.json` `.dependencies` is `{}` — no runtime deps added.
    - `package.json` `.files` is unchanged: `["dist", "README.md", "LICENSE", "CHANGELOG.md"]`.
    - `pnpm install --no-frozen-lockfile` exits 0.
    - `pnpm examples` exits 0 and prints three `OK   <file>` lines (one per example).
    - `pnpm test`, `pnpm typecheck`, `pnpm lint`, `pnpm build` all still pass.
  </acceptance_criteria>
  <done>Parent `package.json` is at 0.1.0 with `tsx` installed and `pnpm examples` wired + green.</done>
</task>

<task type="auto">
  <name>Task 2: Add 3 new steps to .github/workflows/ci.yml — Examples + Starter kit + actionlint</name>
  <read_first>
    - .github/workflows/ci.yml (current state — confirm existing step layout; the new steps go AFTER "Test (with coverage)")
    - examples/profile-starter-kit/.github/workflows/ci.yml (kit workflow that actionlint will validate)
    - examples/profile-starter-kit/.github/workflows/publish.yml (kit workflow that actionlint will validate)
    - scripts/run-examples.ts (the smoke runner invoked by `pnpm examples`)
    - .planning/phases/08-examples-starter-kit-and-documentation/08-CONTEXT.md (D-04, D-11)
    - .planning/phases/08-examples-starter-kit-and-documentation/08-PATTERNS.md (section `.github/workflows/ci.yml` — lines 718-751)
  </read_first>
  <action>
Edit `.github/workflows/ci.yml`. Insert three new steps AFTER the existing `Test (with coverage)` step and BEFORE the `Build` step. All three new steps are gated to a single matrix slot via `if: matrix.node == '20'` (CONTEXT D-11 "Adds ~30s to CI" — running 3× the matrix triples the additive time for no real benefit; examples and kit are node-version-agnostic).

Insert exactly this block (YAML indentation preserved — 6 spaces matches existing step indentation):

```yaml
      - name: Examples (smoke)
        if: matrix.node == '20'
        run: pnpm examples

      - name: Starter kit (install + test + build)
        if: matrix.node == '20'
        working-directory: examples/profile-starter-kit
        run: |
          pnpm install --no-frozen-lockfile
          pnpm typecheck
          pnpm lint
          pnpm test
          pnpm build

      - name: Validate kit workflows (actionlint)
        if: matrix.node == '20'
        uses: reviewdog/action-actionlint@v1
        with:
          actionlint_flags: -color=never examples/profile-starter-kit/.github/workflows/ci.yml examples/profile-starter-kit/.github/workflows/publish.yml
```

Notes on the actionlint invocation:
- `reviewdog/action-actionlint@v1` is the official marketplace action (CONTEXT "Claude's Discretion" line 322: "Recommendation: the official `reviewdog/action-actionlint@v1` for low setup cost").
- Passing the two specific kit workflow files via `actionlint_flags` keeps the linter focused on the Phase-8 deliverables (the main ci.yml and parent publish.yml are covered by GitHub's own syntax validation at push time).
- `-color=never` keeps the CI log clean.

Do NOT modify:
- The existing `push` / `pull_request` triggers.
- The `concurrency` block.
- The `permissions` block.
- The `verify` job's matrix definition (`node: ["18", "20", "22"]` stays).
- Any existing step (Checkout, Setup pnpm, Setup Node.js, Install, Typecheck, Lint, Format check, Test, Test (with coverage), Build, Verify dual-module build artifacts).

Alternative placement: if the YAML structure causes a conflict with the "Verify dual-module build artifacts" step (which runs `node -e "import(...)"` on the built dist/), keep the three new steps in this order and position — they fit cleanly between `Test (with coverage)` and `Build`. The kit's `Starter kit (install + test + build)` step includes its OWN build inside the kit directory; it does not replace the parent's build.

After editing, run `actionlint .github/workflows/ci.yml` locally (if actionlint is available) to confirm the YAML is still valid. If actionlint is unavailable, `pnpm install` followed by a `git status` check confirms no syntax errors in the manifest.
  </action>
  <verify>
    <automated>grep -q "Examples (smoke)" .github/workflows/ci.yml && grep -q "Starter kit (install + test + build)" .github/workflows/ci.yml && grep -q "Validate kit workflows (actionlint)" .github/workflows/ci.yml && grep -q "working-directory: examples/profile-starter-kit" .github/workflows/ci.yml && grep -q "reviewdog/action-actionlint" .github/workflows/ci.yml && grep -q "if: matrix.node == '20'" .github/workflows/ci.yml; echo "ci.yml additions OK"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains the step name `Examples (smoke)`.
    - `.github/workflows/ci.yml` contains the step name `Starter kit (install + test + build)` with `working-directory: examples/profile-starter-kit`.
    - `.github/workflows/ci.yml` contains the step name `Validate kit workflows (actionlint)` using `reviewdog/action-actionlint@v1`.
    - All three new steps include `if: matrix.node == '20'` (single-matrix-slot gating per D-11).
    - Existing steps (`Checkout`, `Setup pnpm`, `Setup Node.js ${{ matrix.node }}`, `Install dependencies`, `Typecheck`, `Lint`, `Format check`, `Test`, `Test (with coverage)`, `Build`, `Verify dual-module build artifacts`) are all still present and unmodified.
    - Matrix `node: ["18", "20", "22"]` is unchanged.
    - `actionlint .github/workflows/ci.yml` exits 0 (if actionlint available locally).
  </acceptance_criteria>
  <done>Parent CI now gates examples smoke + starter-kit build + kit workflow actionlint on every PR — KIT-02/03/04 + EX-01/02/03 become enforceable green-or-red signals in CI.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve the kit's @cosyte/hl7-parser peer-dep via pnpm workspace OR --filter</name>
  <read_first>
    - .npmrc (current state — confirm `auto-install-peers=true`, `strict-peer-dependencies=false`)
    - examples/profile-starter-kit/package.json (the kit's `devDependencies."@cosyte/hl7-parser": "workspace:*"` from Plan 08-02)
    - .planning/phases/08-examples-starter-kit-and-documentation/08-CONTEXT.md (D-11)
    - .planning/phases/08-examples-starter-kit-and-documentation/08-PATTERNS.md (sections `pnpm-workspace.yaml` + `.npmrc`)
  </read_first>
  <action>
Decide between two paths based on how `pnpm install` behaves in the repo root after Plan 08-02's smoke test:

### Path A — Workspace (preferred if Plan 08-02 Task 3 Option A succeeded)

Create `pnpm-workspace.yaml` at repo root:

```yaml
packages:
  - "examples/profile-starter-kit"
```

Append to `.npmrc`:
```
link-workspace-packages=true
```

Verify:
1. `pnpm install --no-frozen-lockfile` at repo root STILL succeeds (no regression on the parent install).
2. `cd examples/profile-starter-kit && pnpm install --no-frozen-lockfile` resolves `@cosyte/hl7-parser` via workspace link — no external npm registry hit for the peer.
3. `cd examples/profile-starter-kit && pnpm test && pnpm build` green.

### Path B — Per-job `--filter` (fallback if Path A breaks parent install)

Do NOT create `pnpm-workspace.yaml`. Do NOT modify `.npmrc`.

Instead, modify the `Starter kit (install + test + build)` step from Task 2 to pre-build the parent and pin the peer via a tarball-install:

```yaml
      - name: Starter kit (install + test + build)
        if: matrix.node == '20'
        run: |
          # Build parent first so the kit can install it as a tarball
          pnpm build
          PARENT_TGZ="$(pnpm pack | tail -1)"
          cd examples/profile-starter-kit
          # Swap peer to the just-built parent tarball
          pnpm add --save-dev "../../${PARENT_TGZ}"
          pnpm typecheck
          pnpm lint
          pnpm test
          pnpm build
```

Note: Path B is acceptable per CONTEXT D-11 "the kit job uses a `pnpm install --filter` invocation instead" — the tarball variant above is cleaner than `--filter` and also works when the kit's `workspace:*` dep has been swapped to a version range.

### Decision heuristic

- If Plan 08-02 Task 3 reported Option A worked → choose Path A here.
- If Plan 08-02 Task 3 reported Option B worked (tarball / file: override) → choose Path B here.
- If unclear → attempt Path A; if `pnpm install` at repo root fails with a workspace-related error, revert and use Path B.

Record which path was taken in the SUMMARY.

### Path-independent safety check

Regardless of path, verify AFTER implementing:
- `git status` shows no accidental `node_modules/` or `*.tgz` files staged.
- The kit's `devDependencies."@cosyte/hl7-parser"` value is still `"workspace:*"` (Path A) OR was temporarily swapped and then REVERTED for commit (Path B — CI handles the swap at run time, not at rest).
- `pnpm test` at repo root STILL passes (no regression on the parent's own test suite from workspace setup).
  </action>
  <verify>
    <automated>( test -f pnpm-workspace.yaml && grep -q "examples/profile-starter-kit" pnpm-workspace.yaml && grep -q "link-workspace-packages" .npmrc ) || grep -q "pnpm pack" .github/workflows/ci.yml; pnpm install --no-frozen-lockfile && pnpm test --run; echo "peer-dep resolution OK"</automated>
  </verify>
  <acceptance_criteria>
    - Either Path A artifacts exist (`pnpm-workspace.yaml` listing the kit path AND `.npmrc` containing `link-workspace-packages=true`) OR Path B artifacts exist (parent ci.yml's Starter kit step uses `pnpm pack` and tarball install).
    - `pnpm install --no-frozen-lockfile` at repo root exits 0 (no regression on parent install).
    - `pnpm test --run` at repo root exits 0 (no regression on parent tests).
    - Kit install resolves the peer without hitting npm registry (Path A — workspace link) OR CI successfully swaps to a local tarball (Path B).
    - `examples/profile-starter-kit/package.json` `.devDependencies["@cosyte/hl7-parser"]` is unchanged at rest: `"workspace:*"` (Path A's canonical shape; Path B's CI-time swap does not persist).
    - SUMMARY records which path was chosen and why.
  </acceptance_criteria>
  <done>Starter kit's peer dependency is resolvable both locally and in parent CI — KIT-02 is reachable in both environments.</done>
</task>

<task type="auto">
  <name>Task 4: Create .github/workflows/publish.yml (parent — D-24 workflow_dispatch-only)</name>
  <read_first>
    - .github/workflows/ci.yml (existing format — same `actions/setup-node@v4` pattern, `pnpm/action-setup@v4`)
    - examples/profile-starter-kit/.github/workflows/publish.yml (sibling twin from Plan 08-02)
    - package.json (confirm `publishConfig.access: "public"` and `publishConfig.provenance: true` are set)
    - .planning/phases/08-examples-starter-kit-and-documentation/08-CONTEXT.md (D-24, D-25)
    - .planning/phases/08-examples-starter-kit-and-documentation/08-PATTERNS.md (section `.github/workflows/publish.yml`)
  </read_first>
  <action>
Create `.github/workflows/publish.yml` at repo root. `workflow_dispatch`-only (D-24). The parent publish workflow differs from the kit's by:
- `pnpm install --frozen-lockfile` (parent has a real lockfile — matches parent ci.yml line 41).
- `publishConfig.provenance: true` is set in `package.json` so `pnpm publish` automatically emits a signed provenance record. No `--provenance` CLI flag required (the config is authoritative).

```yaml
name: Publish

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Test
        run: pnpm test

      - name: Build
        run: pnpm build

      - name: Publish to npm
        run: pnpm publish --access public --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
```

Threat-model notes enforced here (T-08-19/20/21):
- `on: workflow_dispatch` ONLY — zero accidental publish surface (no `push`, no `tag`, no `release`, no `schedule`).
- `NODE_AUTH_TOKEN` set on the publish step's `env` (not job-level). GitHub's standard secret masking in logs applies; the token never appears in plaintext.
- `permissions: { contents: read, id-token: write }` — `id-token: write` is required for `publishConfig.provenance: true` to emit a signed attestation.

After creating the file, verify with `actionlint .github/workflows/publish.yml` (if available locally).

Optionally (CONTEXT D-25 "Verifier MAY run `pnpm publish --dry-run` to confirm tarball contents"), run:
```bash
pnpm publish --dry-run --access public
```
from repo root. This prints the tarball contents (should list `dist/index.mjs`, `dist/index.cjs`, `dist/index.d.ts`, `README.md`, `LICENSE`, `CHANGELOG.md`, `package.json`). Does NOT publish. Record the tarball file list in the SUMMARY.

**Do NOT** actually publish (`pnpm publish` without `--dry-run`) — D-25 explicitly defers the publish act to a post-phase manual step.
  </action>
  <verify>
    <automated>test -f .github/workflows/publish.yml && grep -q "workflow_dispatch" .github/workflows/publish.yml && grep -q "secrets.NPM_TOKEN" .github/workflows/publish.yml && grep -q "pnpm publish --access public" .github/workflows/publish.yml && ! grep -qE "on:\\s*\\n\\s*push|on:\\s*\\n\\s*tags|on:\\s*\\n\\s*release" .github/workflows/publish.yml && grep -q "permissions:" .github/workflows/publish.yml && grep -q "id-token: write" .github/workflows/publish.yml; echo "publish.yml OK"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/publish.yml` exists at repo root.
    - File contains `on:` block with ONLY `workflow_dispatch:` (no `push`, `tags`, `release`, `schedule`).
    - File contains `NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}` scoped to the single `Publish to npm` step's `env` (not job-level).
    - File contains `permissions: contents: read` AND `permissions: id-token: write`.
    - File uses `pnpm/action-setup@v4` + `actions/setup-node@v4` matching parent ci.yml style.
    - File uses `pnpm install --frozen-lockfile` (parent has a lockfile; differs from kit's `--no-frozen-lockfile`).
    - `actionlint .github/workflows/publish.yml` exits 0 (if available locally).
    - If `pnpm publish --dry-run --access public` was run, SUMMARY records the tarball file list.
  </acceptance_criteria>
  <done>Parent publish.yml is workflow_dispatch-only, secrets are step-scoped, and the publishable tarball shape is confirmed (via optional dry-run).</done>
</task>

<task type="auto">
  <name>Task 5: End-to-end capstone smoke — full Phase 8 pipeline green on a clean install</name>
  <read_first>
    - All files produced by Plans 08-01 through 08-04 (examples/, starter kit, README.md, CHANGELOG.md, CONTRIBUTING.md)
    - All files modified by Tasks 1-4 of this plan (package.json, ci.yml, publish.yml, possibly pnpm-workspace.yaml + .npmrc)
    - .planning/phases/08-examples-starter-kit-and-documentation/08-CONTEXT.md (D-25 — publish dry-run optional)
  </read_first>
  <action>
Run the full Phase 8 pipeline from repo root. Every command must exit 0. This is a CORRECTNESS gate before committing — it simulates what the parent CI will run on every PR.

```bash
# Clean slate (does not delete node_modules — pnpm is fast on warm cache)
rm -rf dist coverage
pnpm install --no-frozen-lockfile
pnpm typecheck
pnpm lint
pnpm test
pnpm build
pnpm examples
```

Optionally, for KIT-02/03/04 local coverage (simulates Tasks 2's "Starter kit (install + test + build)" step):

```bash
cd examples/profile-starter-kit
pnpm install --no-frozen-lockfile
pnpm typecheck
pnpm lint
pnpm test
pnpm build
cd ../..
```

Optionally (D-25 — tarball preview, NOT a publish):

```bash
pnpm publish --dry-run --access public
```

Record the tarball file list in the SUMMARY. Confirm it contains:
- `dist/index.mjs`
- `dist/index.cjs`
- `dist/index.d.ts`
- `README.md`
- `LICENSE`
- `CHANGELOG.md`
- `package.json`

If any of these are missing, investigate the `files` allow-list in `package.json` and/or the `tsup` build output.

**Explicitly DO NOT** run `pnpm publish` without `--dry-run`. CONTEXT D-25 defers the publish act to a post-phase manual step outside the planning loop.

After all pipeline commands pass, commit all Phase 8 changes. Plan 08-05 is the final plan in Phase 8 — after commit, `/gsd-verify-work 8` is the next workflow step.
  </action>
  <verify>
    <automated>pnpm install --no-frozen-lockfile && pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm examples && echo "Phase 8 capstone smoke GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install --no-frozen-lockfile` exits 0.
    - `pnpm typecheck` exits 0.
    - `pnpm lint` exits 0 with `--max-warnings=0`.
    - `pnpm test` exits 0 — all existing tests (824+) still pass; no regressions from Phase 8 wiring.
    - `pnpm build` exits 0 and produces `dist/index.mjs`, `dist/index.cjs`, `dist/index.d.ts`.
    - `pnpm examples` exits 0 with three `OK   <file>` lines.
    - Kit smoke (if run): `cd examples/profile-starter-kit && pnpm install --no-frozen-lockfile && pnpm test && pnpm build` exits 0.
    - (Optional) `pnpm publish --dry-run --access public` exits 0; tarball file list recorded in SUMMARY.
    - No accidental `node_modules/` or build artifacts staged in git (`git status` clean of those paths).
  </acceptance_criteria>
  <done>Phase 8 end-to-end pipeline green. Ready for `/gsd-verify-work 8`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary                                | Description                                                                                                                                                           |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `publish.yml` <-> npm registry          | Publishes the tarball using `NODE_AUTH_TOKEN` from `secrets.NPM_TOKEN`. Only triggered by human via `workflow_dispatch`.                                              |
| CI workflow <-> kit workflow actionlint | `reviewdog/action-actionlint@v1` runs on kit workflow files — unprivileged; no write access beyond CI log.                                                            |
| `pnpm examples` CI step <-> smoke runner | CI invokes `pnpm examples` which runs `scripts/run-examples.ts` which `spawnSync`s each example (per Plan 08-01 T-08-01 mitigation — preserved here).                |
| `pnpm install` + tsx devDep             | `tsx` (^4.0.0) is a well-known maintained ESM-loader package; supply-chain risk mitigated by lockfile once install completes.                                        |

## STRIDE Threat Register

| Threat ID | Category | Component                           | Disposition | Mitigation Plan                                                                                                                                                                                        |
| --------- | -------- | ----------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-08-19   | E        | Parent `publish.yml` trigger        | mitigate    | `on: workflow_dispatch` ONLY — no `push`, `tag`, `release`, `schedule`. Prevents accidental publish from any auto-triggered event. CONTEXT D-24.                                                      |
| T-08-20   | I        | `NPM_TOKEN` secret                  | mitigate    | `NODE_AUTH_TOKEN` env scoped to single `Publish to npm` step. Not job-level. GitHub masks secrets in logs automatically. Never committed to code.                                                     |
| T-08-21   | T        | Provenance attestation              | mitigate    | `publishConfig.provenance: true` in package.json + `permissions: { id-token: write }` in publish.yml — pnpm automatically emits signed provenance on publish.                                           |
| T-08-22   | T        | New `tsx` devDep supply chain       | accept      | `tsx` is an established package (esbuild-based ESM loader); caret-pinned to `^4.x`. Lockfile pins exact transitive tree on commit. Post-1.0 hardening may add dependabot/renovate.                   |
| T-08-23   | I        | CI log secret leakage               | mitigate    | `reviewdog/action-actionlint@v1` + all custom `run:` blocks do not echo `NODE_AUTH_TOKEN` or any secret. GitHub masks anyway. Bash set -x is not enabled.                                             |
| T-08-24   | E        | `pnpm publish --dry-run` by verifier | accept      | Dry-run is read-only — prints tarball manifest; no registry write. Safe to include in verification. Does NOT require NPM_TOKEN.                                                                        |
| T-08-25   | T        | Kit workflow validation gap          | mitigate    | `reviewdog/action-actionlint@v1` step specifically lints both kit workflows on every PR. YAML errors in the kit surface in parent CI immediately.                                                      |
</threat_model>

<verification>
Phase-level verification for Plan 08-05:

1. Parent `package.json` at 0.1.0 with tsx + scripts.examples wired.
2. Parent `.github/workflows/ci.yml` has all three new steps with `if: matrix.node == '20'` gating.
3. Parent `.github/workflows/publish.yml` exists and is workflow_dispatch-only with provenance-ready permissions.
4. Starter kit peer-dep path chosen (workspace or tarball) and both local + CI paths verified.
5. End-to-end `pnpm install && pnpm build && pnpm typecheck && pnpm lint && pnpm test && pnpm examples` green.
6. Optional `pnpm publish --dry-run` confirms tarball shape (dist/ + README + LICENSE + CHANGELOG + package.json).
7. No new runtime dep entries in `package.json` `.dependencies` (still `{}`).
8. Threat T-08-19/20/21 mitigations in place: workflow_dispatch-only, step-scoped secret, id-token write permission.
</verification>

<success_criteria>
- Version bumped: `0.0.0` -> `0.1.0` matching CHANGELOG `[0.1.0]` and kit's `peerDependencies` minimum.
- `pnpm examples` is a real CI-wired script backed by `scripts/run-examples.ts` and the `tsx` devDep.
- Parent CI gates all Phase-8 deliverables: examples smoke + kit install/test/build + kit workflow lint.
- Parent publish.yml exists + workflow_dispatch-only + secrets scoped to single step.
- Kit peer resolution works both locally and in CI.
- End-to-end pipeline green from a clean install.
- Tarball dry-run (optional) confirms shape of the forthcoming 0.1.0 package.
</success_criteria>

<output>
After completion, create `.planning/phases/08-examples-starter-kit-and-documentation/08-05-SUMMARY.md` recording:
- `package.json` diff: exact fields changed (version, scripts.examples, devDependencies.tsx), confirmation `dependencies: {}` remains.
- ci.yml new steps summary (step names + gating + actionlint target paths).
- publish.yml summary (trigger, permissions, step-scoped secret injection).
- Peer-dep resolution path chosen (Path A workspace OR Path B tarball) + rationale.
- End-to-end pipeline pass/fail matrix for the 6 commands.
- `pnpm publish --dry-run` output tarball file list (if run).
- Any lockfile churn (expected: `pnpm-lock.yaml` gains tsx entry).
- Readiness signal for `/gsd-verify-work 8`.
</output>
