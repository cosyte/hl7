---
phase: 12-retroactive-nyquist-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md
autonomous: true
requirements: []
requirements_addressed: []
threat_model_severity: none
gap_closure: true
tags: [retroactive-validation, nyquist, paper-trail, phase-02, gap-closure, v2.1-audit]
must_haves:
  truths:
    - "A developer opens .planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md and sees YAML frontmatter with status: approved, nyquist_compliant: true, wave_0_complete: true, reconstructed_from: artifacts."
    - "A developer reading the Per-Task Verification Map sees at least 7 rows (one per plan 02-01..02-07) each with an Automated Command column entry that is a real pnpm test invocation."
    - "A developer reading the Requirement → Test Cross-Reference sees all 19 Phase 2 REQ-IDs (PARSE-01..09, TOL-01..10) classified as COVERED with dedicated test-file evidence."
    - "A developer running `grep -E 'TODO|TBD|XXX|FIXME' .planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md` sees zero placeholder markers."
    - "A developer running `pnpm format:check .planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md` sees Prettier exit 0."
    - "Every test/parser-*.test.ts and test/types-shared.test.ts file cited in the Automated Command column exists on disk at HEAD."
  artifacts:
    - path: ".planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md"
      provides: "Retroactive Nyquist validation report for Phase 2 — Core Parser & Tolerance, matching the 04/05/06-VALIDATION.md template shape"
      contains: "YAML frontmatter (phase, slug, status, nyquist_compliant, wave_0_complete, created, reconstructed_from); Test Infrastructure table; Sampling Rate block; Per-Task Verification Map (≥7 plan rows); Requirement → Test Cross-Reference (19 REQ rows: 9 PARSE + 10 TOL); Wave 0 Requirements section; Manual-Only Verifications section; Validation Sign-Off checklist; Validation Audit table with verdict"
  key_links:
    - from: ".planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md (Per-Task Verification Map)"
      to: ".planning/phases/02-core-parser-and-tolerance/02-0N-SUMMARY.md (7 files)"
      via: "one row per plan, transcribing REQ-IDs closed and test file invoked"
      pattern: "7 plan rows: 02-01, 02-02, 02-03, 02-04, 02-05, 02-06, 02-07 (gap-closure charset)"
    - from: ".planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md (Requirement → Test Cross-Reference)"
      to: "test/parser-*.test.ts (~13 test files)"
      via: "one row per REQ-ID citing the primary test file(s)"
      pattern: "19 REQ rows: PARSE-01..09 + TOL-01..10, all COVERED"
---

<objective>
Produce `.planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md` — the missing Phase 2 Nyquist validation report. Phase 2 is the largest test-surface phase in v1: 19 REQ-IDs (9 PARSE + 10 TOL) spread across 7 plans with dedicated test files (parser-delimiters, parser-tokenize, parser-escapes, parser-dates, parser-normalize, parser-mllp, parser-warnings, parser-errors, parser-public, parser-segments, parser-strict-mode-sweep, parser-malformed-sweep, parser-edge-cases, types-shared). This plan mechanically reconstructs the validation report from 7 SUMMARYs + `02-VERIFICATION.md` (which already contains REQ-ID traceability) and commits the result.

Purpose: Close v2.1-MILESTONE-AUDIT tech-debt item 2 for Phase 2 (missing VALIDATION.md). State B reconstruction — not a fresh `/gsd-validate-phase` run. Rich per-REQ-ID mapping is expected given the dedicated test files already shipped in Phase 2.

Output: One new file at `.planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md`, ~200-350 lines (largest of the 6), committed in a single `docs(phase-02): add retroactive VALIDATION.md — Nyquist-compliant, 19/19 PARSE+TOL REQs covered` commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference VALIDATION.md shapes — 04 is the canonical detailed shape, 06 is the richest REQ-ID table
@.planning/phases/04-named-helpers/04-VALIDATION.md
@.planning/phases/05-serialization-and-round-trip/05-VALIDATION.md
@.planning/phases/06-profile-system-and-built-ins/06-VALIDATION.md

# Phase 2 evidence (MUST read all 7 SUMMARYs + the ratified VERIFICATION.md which already has REQ traceability)
@.planning/phases/02-core-parser-and-tolerance/02-01-SUMMARY.md
@.planning/phases/02-core-parser-and-tolerance/02-02-SUMMARY.md
@.planning/phases/02-core-parser-and-tolerance/02-03-SUMMARY.md
@.planning/phases/02-core-parser-and-tolerance/02-04-SUMMARY.md
@.planning/phases/02-core-parser-and-tolerance/02-05-SUMMARY.md
@.planning/phases/02-core-parser-and-tolerance/02-06-SUMMARY.md
@.planning/phases/02-core-parser-and-tolerance/02-07-SUMMARY.md
@.planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md

# REQ-ID traceability (confirm PARSE-01..09 + TOL-01..10 all mapped to Phase 2)
@.planning/REQUIREMENTS.md

# Project conventions
@CLAUDE.md
</context>

<phase_scope>
Phase 2 REQ-IDs (19 total):
- PARSE-01, PARSE-02, PARSE-03, PARSE-04, PARSE-05, PARSE-06, PARSE-07, PARSE-08, PARSE-09 (9)
- TOL-01, TOL-02, TOL-03, TOL-04, TOL-05, TOL-06, TOL-07, TOL-08, TOL-09, TOL-10 (10)

Phase 2 plans: 7 (02-01 warnings-errors-shell, 02-02 normalization-mllp-charset, 02-03 segments-delimiters-tokenize, 02-04 escape-sequences, 02-05 dateformats-plumbing, 02-06 public-parsehl7-strict-mode, 02-07 gap-closure-charset).

Expected test files at HEAD (verify each exists):
`test/parser-delimiters.test.ts`, `test/parser-tokenize.test.ts`, `test/parser-escapes.test.ts`, `test/parser-dates.test.ts`, `test/parser-normalize.test.ts`, `test/parser-mllp.test.ts`, `test/parser-warnings.test.ts`, `test/parser-errors.test.ts`, `test/parser-public.test.ts`, `test/parser-segments.test.ts`, `test/parser-strict-mode-sweep.test.ts`, `test/parser-malformed-sweep.test.ts`, `test/parser-edge-cases.test.ts`, `test/types-shared.test.ts`.
</phase_scope>

<tasks>

<task type="auto">
  <name>Task 1: Read Phase 2 source artifacts + extract per-plan and per-REQ evidence</name>
  <read_first>
    - .planning/phases/02-core-parser-and-tolerance/02-01-SUMMARY.md (Plan 01: warnings registry + errors classes + Hl7Message shell)
    - .planning/phases/02-core-parser-and-tolerance/02-02-SUMMARY.md (Plan 02: normalize + normalizeBuffer + stripMllp — TOL-05, TOL-07, TOL-09)
    - .planning/phases/02-core-parser-and-tolerance/02-03-SUMMARY.md (Plan 03: splitSegments + readDelimiters + tokenize — PARSE-01..04, PARSE-06, TOL-01, TOL-02)
    - .planning/phases/02-core-parser-and-tolerance/02-04-SUMMARY.md (Plan 04: unescape + reescape — PARSE-05, TOL-06)
    - .planning/phases/02-core-parser-and-tolerance/02-05-SUMMARY.md (Plan 05: parseHl7Timestamp cascade — PARSE-07, TOL-08)
    - .planning/phases/02-core-parser-and-tolerance/02-06-SUMMARY.md (Plan 06: parseHL7 public + strict-mode escalation — PARSE-08, PARSE-09, TOL-03, TOL-04)
    - .planning/phases/02-core-parser-and-tolerance/02-07-SUMMARY.md (Plan 07: gap-closure charset — if TOL-10 was closed here)
    - .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md (already-ratified REQ traceability table — extract per-REQ test file + commit refs)
    - .planning/phases/04-named-helpers/04-VALIDATION.md (canonical template)
    - .planning/phases/06-profile-system-and-built-ins/06-VALIDATION.md (richest REQ table shape reference)
  </read_first>
  <files>(none — prepares draft content for Task 2)</files>
  <action>
    Extract per-plan rows (7 total — fills Per-Task Verification Map). Columns: `Task ID | Plan | Wave | Requirement(s) | Test File(s) | Test Type | Automated Command | File Exists | Status`.

    Template for each row (use the REQ column from 02-VERIFICATION.md's traceability table as the source of truth; if 02-VERIFICATION.md is ambiguous, fall back to each 02-0N-SUMMARY.md's REQ-ID closure section):
    - `02-01` / `01 warnings-errors-and-message-shell` / Wave 1 / (foundation — no direct REQ closure; supports PARSE-08, PARSE-09 error surface) / `test/parser-warnings.test.ts`, `test/parser-errors.test.ts` / unit / `pnpm test -- parser-warnings parser-errors` / ✅ / ✅ green.
    - `02-02` / `02 input-normalization-mllp-and-charset` / Wave 2 / TOL-05 (MLLP), TOL-07 (BOM), TOL-09 (line endings); partial TOL-10 (charset, closed in 02-07) / `test/parser-normalize.test.ts`, `test/parser-mllp.test.ts` / unit / `pnpm test -- parser-normalize parser-mllp` / ✅ / ✅ green.
    - `02-03` / `03 segments-delimiters-and-tokenize` / Wave 2 / PARSE-01, PARSE-02, PARSE-03, PARSE-04, PARSE-06, TOL-01, TOL-02 / `test/parser-delimiters.test.ts`, `test/parser-tokenize.test.ts`, `test/parser-segments.test.ts` / unit / `pnpm test -- parser-delimiters parser-tokenize parser-segments` / ✅ / ✅ green.
    - `02-04` / `04 escape-sequences` / Wave 2 / PARSE-05, TOL-06 / `test/parser-escapes.test.ts` / unit / `pnpm test -- parser-escapes` / ✅ / ✅ green.
    - `02-05` / `05 dateformats-plumbing` / Wave 2 / PARSE-07 (timestamp cascade), TOL-08 (user dateFormats fallback — note: 02-VERIFICATION.md TOL-08 deferred block was retired in Phase 10 per STATE.md; mention this in the row's Notes if 02-VERIFICATION.md still shows the deferred marker) / `test/parser-dates.test.ts`, `test/types-shared.test.ts` / unit / `pnpm test -- parser-dates types-shared` / ✅ / ✅ green.
    - `02-06` / `06 public-parsehl7-and-strict-mode` / Wave 3 / PARSE-08 (Hl7ParseError), PARSE-09 (stable error codes), TOL-03 (warnings), TOL-04 (strict mode escalation) / `test/parser-public.test.ts`, `test/parser-strict-mode-sweep.test.ts`, `test/parser-malformed-sweep.test.ts`, `test/parser-edge-cases.test.ts` / unit + integration / `pnpm test -- parser-public parser-strict-mode-sweep parser-malformed-sweep parser-edge-cases` / ✅ / ✅ green.
    - `02-07` / `07 gap-closure-charset` / Wave 3 (gap closure wave) / TOL-10 (MSH-18 charset) / `test/parser-normalize.test.ts` (charset cases added here) / unit / `pnpm test -- parser-normalize` / ✅ / ✅ green.

    Extract per-REQ rows (19 total — fills Requirement → Test Cross-Reference). Columns: `REQ-ID | Source Plan | Test Evidence | Status`. Status all `COVERED`.
    - PARSE-01 (MSH-1 delimiter detection) — Plan 02-03 — `test/parser-delimiters.test.ts` — COVERED
    - PARSE-02 (encoding chars from MSH-2) — 02-03 — `test/parser-delimiters.test.ts` — COVERED
    - PARSE-03 (segment splitting) — 02-03 — `test/parser-segments.test.ts` — COVERED
    - PARSE-04 (field/rep/comp/sub tokenization) — 02-03 — `test/parser-tokenize.test.ts` — COVERED
    - PARSE-05 (escape sequences — 8 forms) — 02-04 — `test/parser-escapes.test.ts` — COVERED
    - PARSE-06 (repetitions/components/subs) — 02-03 — `test/parser-tokenize.test.ts` — COVERED
    - PARSE-07 (timestamp parsing HL7 TS/DTM → Date) — 02-05 — `test/parser-dates.test.ts` + `test/types-shared.test.ts` — COVERED
    - PARSE-08 (Hl7ParseError class + code/position/snippet) — 02-06 — `test/parser-errors.test.ts` + `test/parser-malformed-sweep.test.ts` — COVERED
    - PARSE-09 (stable error codes — 4 fatal codes) — 02-06 — `test/parser-malformed-sweep.test.ts` — COVERED
    - TOL-01 (vendor-quirky lenient parse) — 02-03/02-06 — `test/parser-edge-cases.test.ts` + `test/parser-warnings.test.ts` — COVERED
    - TOL-02 (whitespace-trim warning) — 02-03 — `test/parser-tokenize.test.ts` — COVERED
    - TOL-03 (warnings registry + onWarning callback) — 02-01/02-06 — `test/parser-warnings.test.ts` — COVERED
    - TOL-04 (strict-mode escalation) — 02-06 — `test/parser-strict-mode-sweep.test.ts` — COVERED
    - TOL-05 (MLLP framing strip) — 02-02 — `test/parser-mllp.test.ts` — COVERED
    - TOL-06 (unknown escape → warning) — 02-04 — `test/parser-escapes.test.ts` — COVERED
    - TOL-07 (UTF-8 BOM handling) — 02-02 — `test/parser-normalize.test.ts` — COVERED
    - TOL-08 (dateFormats fallback cascade) — 02-05 — `test/parser-dates.test.ts` — COVERED (note: previously deferred; Phase 10 Plan 10-04 retired the deferred block — reflect this in Notes column if space permits)
    - TOL-09 (mixed line endings) — 02-02 — `test/parser-normalize.test.ts` — COVERED
    - TOL-10 (MSH-18 charset via Buffer input) — 02-07 — `test/parser-normalize.test.ts` (charset-specific cases) — COVERED

    Extract test infrastructure facts: Framework (Vitest 1.2.x), Config file (vitest.config.ts), Quick run (`pnpm test -- parser`), Full suite (`pnpm test`), Coverage (`pnpm test:coverage` — enforces ≥90% branches on src/parser/**), Estimated runtime (~10-14s full suite).

    Verify at HEAD every test-file path above exists via `ls test/parser-*.test.ts test/types-shared.test.ts`. If any is missing, STOP — that is a regression predating this plan.

    All content goes into the journal; no file written in this task.
  </action>
  <verify>
    <automated>for f in .planning/phases/02-core-parser-and-tolerance/02-0{1,2,3,4,5,6,7}-SUMMARY.md .planning/phases/02-core-parser-and-tolerance/02-VERIFICATION.md; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done && for t in test/parser-delimiters.test.ts test/parser-tokenize.test.ts test/parser-escapes.test.ts test/parser-dates.test.ts test/parser-normalize.test.ts test/parser-mllp.test.ts test/parser-warnings.test.ts test/parser-errors.test.ts test/parser-public.test.ts test/parser-segments.test.ts test/parser-strict-mode-sweep.test.ts test/parser-malformed-sweep.test.ts test/parser-edge-cases.test.ts test/types-shared.test.ts; do test -f "$t" || { echo "MISSING: $t"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - 7 per-plan rows drafted with all 9 columns populated.
    - 19 per-REQ rows drafted (9 PARSE + 10 TOL), each with Status=COVERED and at least one concrete test-file anchor.
    - Test Infrastructure facts captured.
    - Every SUMMARY file (02-01..02-07) + 02-VERIFICATION.md present on disk.
    - Every test/*.test.ts path cited in the journal exists at HEAD.
  </acceptance_criteria>
  <done>
    Draft ready: 7 per-plan rows, 19 per-REQ rows, Test Infrastructure block. Ready for Task 2 transcription.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write .planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md</name>
  <read_first>
    - Task 1 journal drafts
    - .planning/phases/04-named-helpers/04-VALIDATION.md (section order reference)
    - .planning/phases/06-profile-system-and-built-ins/06-VALIDATION.md (REQ table shape reference for the large 15+ row case)
  </read_first>
  <files>.planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md</files>
  <action>
    Write the file top-to-bottom. Sections (exact order):

    Frontmatter:
    ```yaml
    ---
    phase: 02
    slug: core-parser-and-tolerance
    status: approved
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-04-21
    reconstructed_from: artifacts (State B — Phase 12 retroactive Nyquist validation; no prior VALIDATION.md existed)
    ---
    ```

    Header:
    ```
    # Phase 2 — Validation Strategy

    > Retroactive Nyquist validation audit. Phase 2 is the largest test-surface phase in v1 — 19 REQ-IDs (9 PARSE + 10 TOL) closed across 7 plans with 14 dedicated test files under `test/parser-*.test.ts` and `test/types-shared.test.ts`. All 19 REQ-IDs have dedicated unit + integration tests enforced by the ≥90% branch-coverage gate on `src/parser/**` (tightened in Phase 7).
    ```

    1. `## Test Infrastructure` — table rows: Framework (Vitest 1.2.x), Config file (`vitest.config.ts`), Quick run (`pnpm test -- parser`), Full suite (`pnpm test`), Coverage (`pnpm test:coverage` — enforces ≥90% branches on `src/parser/**`), Estimated runtime (~14s full suite; ~3s parser-scoped).

    2. `## Sampling Rate` — bullets:
       - After every task commit: `pnpm test -- parser-<surface>` (scoped to the file under active change)
       - After every plan wave: `pnpm test -- parser` (all 13 parser test files + types-shared, ~3s)
       - Before `/gsd-verify-work 2`: `pnpm test && pnpm typecheck && pnpm lint --max-warnings=0 && pnpm build`
       - Max feedback latency: ~3s scoped, ~14s full

    3. `## Per-Task Verification Map` — transcribe Task 1's 7 per-plan rows into a markdown table with columns: `Task ID | Plan | Wave | Requirement(s) | Test File(s) | Test Type | Automated Command | File Exists | Status`. Include legend `*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*` below. Add `**Total Phase-2 tests:** see `02-VERIFICATION.md` for exact case counts. **Test files:** 14 dedicated (`test/parser-*.test.ts` × 13 + `test/types-shared.test.ts`).`

    4. `## Requirement → Test Cross-Reference` — transcribe Task 1's 19 per-REQ rows. Columns: `REQ-ID | Source Plan | Primary Test File(s) | Evidence | Status`. Include a trailing Notes column only for TOL-08 (to document the retired Phase-10 deferred-block note). Below the table: `**Gap summary: 0 MISSING · 0 PARTIAL · 19 COVERED.**`

    5. `## Wave 0 Requirements` — paragraph: "*None.* Vitest 1.2.x + `vitest.config.ts` were installed in Phase 1 (SETUP-06). Phase 2 added 14 new test files on top of existing infrastructure — no framework, config, or scaffolding gaps required. Coverage threshold tightening from 85% → 90% branches on `src/parser/**` happened in Phase 7 Plan 06 (post-Phase-2); Phase 2 shipped under the initial 85% gate and subsequently met the tighter bar without backfill."

    6. `## Manual-Only Verifications` — paragraph: "*None.* Every PARSE-01..09 and TOL-01..10 behavior has at least one automated unit or integration test. The parser-malformed-sweep and parser-strict-mode-sweep files exercise every FATAL_CODES and WARNING_CODES entry programmatically."

    7. `## Validation Sign-Off` — checklist (all `[x]`):
       - [x] All tasks have automated verify (`pnpm test -- parser*`)
       - [x] Sampling continuity: every plan has dedicated test file(s) committed in its wave
       - [x] Wave 0 covers all MISSING references (none — infrastructure pre-existed)
       - [x] No watch-mode flags
       - [x] Feedback latency < 15s full suite
       - [x] `nyquist_compliant: true` set in frontmatter
       - [x] Verifier PASS 2026-04-18 per `02-VERIFICATION.md`

       `**Approval:** approved 2026-04-21 (Nyquist audit — State B reconstruction from 7 SUMMARYs + 02-VERIFICATION.md).`

    8. `## Validation Audit 2026-04-21` — metric table:
       | Input state | B (reconstructed from 7 SUMMARYs + 02-VERIFICATION.md) |
       | REQ-IDs audited | 19 (9 PARSE + 10 TOL) |
       | Plans mapped | 7 (02-01..02-07) |
       | Test files mapped | 14 |
       | Gaps found | 0 |
       | Resolved | 0 |
       | Escalated | 0 |
       | Auditor spawn needed | No |
       | Coverage gate | ≥90% branches on `src/parser/**` enforced via `pnpm test:coverage` (tightened Phase 7 Plan 06) |

       Verdict paragraph: "Phase 2 is Nyquist-compliant. Every PARSE-01..09 and TOL-01..10 REQ maps 1:1 to at least one dedicated test file with runtime-enforced branch coverage ≥90%. The Phase 10 retired TOL-08 deferred block is reflected here as COVERED (closed in Phase 2 Plan 02-05). TOL-10 closed in gap-closure Plan 02-07. Closes v2.1-MILESTONE-AUDIT tech-debt item 2 for Phase 2."

    Target length: 200-320 lines. No TODO/TBD/XXX/FIXME markers.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md && grep -q '^nyquist_compliant: true$' .planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md && grep -q '^status: approved$' .planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md && test "$(grep -cE '^\| (PARSE-0[1-9]|TOL-0[1-9]|TOL-10) \|' .planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md)" -eq 19 && test "$(grep -cE '^\| 02-0[1-7] \|' .planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md)" -ge 7 && ! grep -qE '\b(TODO|TBD|XXX|FIXME)\b' .planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md`.
    - Frontmatter has `phase: 02`, `slug: core-parser-and-tolerance`, `status: approved`, `nyquist_compliant: true`, `wave_0_complete: true`, `created: 2026-04-21`.
    - Per-Task Verification Map has ≥7 rows matching `| 02-0[1-7] |`.
    - Requirement → Test Cross-Reference has exactly 19 rows matching `| (PARSE-0[1-9]|TOL-0[1-9]|TOL-10) |`, all with Status=COVERED.
    - Zero TODO/TBD/XXX/FIXME placeholder markers.
    - All 8 sections present in the required order.
  </acceptance_criteria>
  <done>
    File written with all 8 sections, 7+ plan rows, 19 REQ rows, zero placeholders.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify format, path integrity, and commit</name>
  <read_first>
    - The file written in Task 2
    - CLAUDE.md (commit message convention)
  </read_first>
  <files>(no new files; `git add` of the file from Task 2)</files>
  <action>
    1. `pnpm format:check .planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md` — expect exit 0; if fails, run `pnpm format` on the file and re-check.
    2. `grep -E 'TODO|TBD|XXX|FIXME|placeholder' .planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md` — expect zero matches.
    3. Extract every test-path reference and verify it exists: `grep -oE 'test/[a-zA-Z0-9_-]+\.test\.ts' .planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md | sort -u | while read f; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done`.
    4. `git add .planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md`.
    5. Verify only one path is staged: `git diff --cached --name-only`.
    6. Commit via HEREDOC:
       ```
       git commit -m "$(cat <<'EOF'
       docs(phase-02): add retroactive VALIDATION.md — Nyquist-compliant, 19/19 PARSE+TOL REQs covered

       Closes v2.1-MILESTONE-AUDIT tech-debt item 2 for Phase 2 (missing VALIDATION.md).
       State B reconstruction from 7 plan SUMMARYs + 02-VERIFICATION.md. All 19 REQ-IDs
       (9 PARSE + 10 TOL) classified COVERED via 14 dedicated test files under
       test/parser-*.test.ts + test/types-shared.test.ts. TOL-08 shows as COVERED
       (Phase 10 retired the deferred block). TOL-10 closed in gap-closure plan 02-07.
       Coverage gate (≥90% branches on src/parser/**) enforced via pnpm test:coverage.

       Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
       EOF
       )"
       ```
    7. Confirm single commit: `git log --oneline -1 -- .planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md`.
  </action>
  <verify>
    <automated>pnpm format:check .planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md && ! grep -qE 'TODO|TBD|XXX|FIXME|placeholder' .planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md && test "$(git log --oneline -1 --format='%s' -- .planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md | grep -c 'docs(phase-02): add retroactive VALIDATION.md')" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm format:check` passes for the new file.
    - Zero placeholder markers.
    - Every referenced `test/*.test.ts` exists on disk.
    - Exactly one commit touches the new VALIDATION.md.
    - Commit message begins with `docs(phase-02):` and contains `add retroactive VALIDATION.md`.
    - `git status` clean afterward.
  </acceptance_criteria>
  <done>
    File committed; working tree clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced. Writes a single Markdown file under `.planning/phases/02-core-parser-and-tolerance/`.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Tampering | `02-VALIDATION.md` content | mitigate | Transcribed from SUMMARY + VERIFICATION.md (authoritative commit-trail sources); git commit preserves auditable trail. |
| T-12-02-02 | Information Disclosure | validation-report citations | accept | Repo public (MIT); no secrets in SUMMARYs/tests. |
</threat_model>

<verification>
After Task 3:
1. `.planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md` exists.
2. `grep -c '^nyquist_compliant: true$'` = 1.
3. `grep -cE '^\| (PARSE-0[1-9]|TOL-0[1-9]|TOL-10) \|'` = 19.
4. `grep -cE '^\| 02-0[1-7] \|'` ≥ 7.
5. `git log` records a `docs(phase-02): add retroactive VALIDATION.md` commit.
</verification>

<success_criteria>
- All 3 tasks complete with acceptance_criteria satisfied.
- `.planning/phases/02-core-parser-and-tolerance/02-VALIDATION.md` exists with all 8 sections.
- One new commit with `docs(phase-02):` prefix touching only the new file.
- Zero format failures, zero placeholder markers, every test-path resolved.
- Wave 1 parallel-safe: no file overlap with 12-01, 12-03..12-06.
</success_criteria>

<output>
After completion, execute-plan creates `.planning/phases/12-retroactive-nyquist-validation/12-02-SUMMARY.md` citing:
- 19 REQ-IDs classified (9 PARSE + 10 TOL, all COVERED).
- 7 plan rows transcribed (02-01..02-07).
- New file path + commit hash.
- Plan-level REQ-IDs closed: 0 (paper-trail).
</output>
