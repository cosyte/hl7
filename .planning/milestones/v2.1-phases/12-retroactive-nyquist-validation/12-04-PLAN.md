---
phase: 12-retroactive-nyquist-validation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md
autonomous: true
requirements: []
requirements_addressed: []
threat_model_severity: none
gap_closure: true
tags: [retroactive-validation, nyquist, paper-trail, phase-07, gap-closure, thin-by-design, v2.1-audit]
must_haves:
  truths:
    - "A developer opens .planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md and sees YAML frontmatter with status: approved, nyquist_compliant: true, wave_0_complete: true, reconstructed_from: artifacts."
    - "A developer reading the Per-Task Verification Map sees exactly 7 rows (one per plan 07-01..07-07)."
    - "A developer reading the Requirement → Test Cross-Reference sees all 8 Phase 7 REQ-IDs (TEST-01..08) classified as COVERED — TEST-01 coverage gate enforced by vitest.config.ts, TEST-02..06 by fixture sweeps, TEST-07+08 by attribution+audit."
    - "A developer sees a prominent `## Thin-by-Design Callout` section explaining that Phase 7 is a meta-phase (tests-for-tests) and the canonical coverage gate — ≥90% branches on 5 src subdirectories — is itself the runtime invariant."
    - "A developer running `grep -E 'TODO|TBD|XXX|FIXME' .planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md` sees zero placeholder markers."
    - "A developer running `pnpm format:check .planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md` sees Prettier exit 0."
  artifacts:
    - path: ".planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md"
      provides: "Retroactive Nyquist validation report for Phase 7 — Testing Hardening & Fixtures. Explicitly thin-by-design: Phase 7's own validation surface is meta (the coverage gate enforces the gate)."
      contains: "YAML frontmatter; Test Infrastructure; Sampling Rate; Per-Task Verification Map (7 plan rows); Requirement → Test Cross-Reference (8 TEST rows); Coverage Gate Enforcement callout; Thin-by-Design Callout (prominent); Fixture Inventory Note; Wave 0 Requirements; Manual-Only Verifications; Validation Sign-Off; Validation Audit with verdict"
  key_links:
    - from: ".planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md (Coverage Gate Enforcement callout)"
      to: "vitest.config.ts (per-directory branches: 90 on parser/model/helpers/serialize/builder) + .github/workflows/ci.yml (Test with coverage step across Node 18/20/22)"
      via: "documents the runtime invariant that TEST-01 closes — coverage gate IS the test"
      pattern: "TEST-01 enforcement is config-level, not test-case-level"
    - from: ".planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md (Per-Task Verification Map)"
      to: ".planning/phases/07-testing-hardening-and-fixtures/07-0N-SUMMARY.md (7 files)"
      via: "one row per plan, transcribing REQ-IDs closed and fixture/test work"
      pattern: "7 plan rows: 07-01..07-07"
---

<objective>
Produce `.planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md` — the missing Phase 7 Nyquist validation report. Phase 7 is a **thin-by-design meta-phase**: its "tests" are partly fixtures and partly the coverage-gate configuration itself. Authoring validation for a test-hardening phase recurses (tests-for-tests), so this plan documents coverage at the gate level (vitest.config.ts per-dir branch thresholds) and at the fixture-sweep level (parser-malformed-sweep, parser-strict-mode-sweep, canonical-messages, parser-edge-cases, profiles-builtins fixture-parity).

Purpose: Close v2.1-MILESTONE-AUDIT tech-debt item 2 for Phase 7 (missing VALIDATION.md). State B mechanical reconstruction from 7 SUMMARYs + `07-VERIFICATION.md` + `TEST-08-AUDIT.md`. Must prominently note thin-by-design per ROADMAP Phase 12 SC-2.

Output: One new file at `.planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md`, ~180-280 lines, committed in a single `docs(phase-07): add retroactive VALIDATION.md — Nyquist-compliant, 8/8 TEST REQs covered (thin-by-design)` commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference VALIDATION.md templates
@.planning/phases/04-named-helpers/04-VALIDATION.md
@.planning/phases/05-serialization-and-round-trip/05-VALIDATION.md
@.planning/phases/06-profile-system-and-built-ins/06-VALIDATION.md

# Phase 7 evidence (7 SUMMARYs + VERIFICATION + TEST-08-AUDIT)
@.planning/phases/07-testing-hardening-and-fixtures/07-01-SUMMARY.md
@.planning/phases/07-testing-hardening-and-fixtures/07-02-SUMMARY.md
@.planning/phases/07-testing-hardening-and-fixtures/07-03-SUMMARY.md
@.planning/phases/07-testing-hardening-and-fixtures/07-04-SUMMARY.md
@.planning/phases/07-testing-hardening-and-fixtures/07-05-SUMMARY.md
@.planning/phases/07-testing-hardening-and-fixtures/07-06-SUMMARY.md
@.planning/phases/07-testing-hardening-and-fixtures/07-07-SUMMARY.md
@.planning/phases/07-testing-hardening-and-fixtures/07-VERIFICATION.md
@.planning/phases/07-testing-hardening-and-fixtures/TEST-08-AUDIT.md

# Coverage-gate config (runtime invariant for TEST-01)
@vitest.config.ts
@.github/workflows/ci.yml

# REQ-ID traceability
@.planning/REQUIREMENTS.md

# Project conventions
@CLAUDE.md
</context>

<phase_scope>
Phase 7 REQ-IDs (8 total): TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06, TEST-07, TEST-08.

Phase 7 plans: 7 (07-01 fixture-tree-migration + coverage-baseline, 07-02 canonical-fixtures, 07-03 edge-case-fixtures, 07-04 vendor-quirks+strict-mode-sweep, 07-05 malformed-fixtures+parser-malformed-sweep, 07-06 coverage-gate-tighten+ci, 07-07 TEST-08-audit + TEST-07-confirmation).

Key thin-by-design observations:
- TEST-01 (≥90% coverage gate) is enforced by `vitest.config.ts` per-dir branches config + CI "Test (with coverage)" step — the gate IS the invariant.
- TEST-02..06 map to fixture sweep test files (canonical-messages, parser-edge-cases, parser-malformed-sweep, parser-strict-mode-sweep).
- TEST-07 (profile-authoring test surface) was confirmed closed by Phase 6 BIP-06 tests (per Plan 07-07).
- TEST-08 (audit completeness) was verified via a dedicated TEST-08-AUDIT.md with zero code deltas (per Plan 07-07 SUMMARY).
- 07-01 is fixture-tree migration infrastructure — no direct REQ closure, but supports TEST-02..06 fixture layout.
</phase_scope>

<tasks>

<task type="auto">
  <name>Task 1: Read Phase 7 source artifacts + extract per-plan and per-REQ evidence</name>
  <read_first>
    - .planning/phases/07-testing-hardening-and-fixtures/07-01-SUMMARY.md (fixture tree migration + coverage baseline capture — infra for TEST-02..06)
    - .planning/phases/07-testing-hardening-and-fixtures/07-02-SUMMARY.md (7 canonical fixtures + canonical-messages.test.ts — TEST-02)
    - .planning/phases/07-testing-hardening-and-fixtures/07-03-SUMMARY.md (11 edge-case fixtures + parser-edge-cases.test.ts — TEST-03)
    - .planning/phases/07-testing-hardening-and-fixtures/07-04-SUMMARY.md (13 vendor-quirks fixtures + parser-strict-mode-sweep.test.ts — TEST-05 + TEST-06)
    - .planning/phases/07-testing-hardening-and-fixtures/07-05-SUMMARY.md (4 malformed fixtures + parser-malformed-sweep.test.ts — TEST-04)
    - .planning/phases/07-testing-hardening-and-fixtures/07-06-SUMMARY.md (vitest.config.ts branches 85→90 on 5 dirs + CI coverage step — TEST-01)
    - .planning/phases/07-testing-hardening-and-fixtures/07-07-SUMMARY.md (TEST-08 audit + TEST-07 confirmation — 0 test-file deltas)
    - .planning/phases/07-testing-hardening-and-fixtures/07-VERIFICATION.md (ratified REQ traceability)
    - .planning/phases/07-testing-hardening-and-fixtures/TEST-08-AUDIT.md (audit document cited by 07-07)
    - vitest.config.ts (confirm `branches: 90` on src/parser/**, src/model/**, src/helpers/**, src/serialize/**, src/builder/** at HEAD)
  </read_first>
  <files>(none — prepares draft for Task 2)</files>
  <action>
    Extract per-plan rows (7 total). Columns: `Task ID | Plan | Wave | Requirement(s) | Test File / Fixture Set | Test Type | Automated Command | File Exists | Status`.

    - `07-01` / `01 fixture-tree-migration + coverage baseline` / Wave 1 / (infrastructure; supports TEST-02..06) / `test/fixtures/canonical/`, `test/fixtures/edge-cases/`, `test/_helpers/` / infrastructure / `pnpm test` (regression suite green post-migration — 747/747) / ✅ / ✅ green.
    - `07-02` / `02 canonical-fixtures` / Wave 2 / TEST-02 / `test/canonical-messages.test.ts` + 7 fixtures under `test/fixtures/canonical/` / integration (fixture sweep) / `pnpm test -- canonical-messages` / ✅ / ✅ green (774/774).
    - `07-03` / `03 edge-case-fixtures` / Wave 2 / TEST-03 / `test/parser-edge-cases.test.ts` + 11 fixtures under `test/fixtures/edge-cases/` / integration / `pnpm test -- parser-edge-cases` / ✅ / ✅ green (789/789).
    - `07-04` / `04 vendor-quirks + strict-mode sweep` / Wave 2 / TEST-05, TEST-06 / `test/parser-strict-mode-sweep.test.ts` + 13 fixtures under `test/fixtures/vendor-quirks/` (one per WARNING_CODES entry) / integration / `pnpm test -- parser-strict-mode-sweep` / ✅ / ✅ green (815/815 + 14 todo — 6 codes emit today, 7 have factories tracked via it.todo).
    - `07-05` / `05 malformed-fixtures + malformed sweep` / Wave 2 / TEST-04 / `test/parser-malformed-sweep.test.ts` + 4 fixtures under `test/fixtures/malformed/` (one per FATAL_CODES entry) / integration / `pnpm test -- parser-malformed-sweep` / ✅ / ✅ green (824/824 + 14 todo).
    - `07-06` / `06 coverage gate tighten + CI` / Wave 3 (capstone) / TEST-01 / `vitest.config.ts` + `.github/workflows/ci.yml` / infrastructure (config-level) / `pnpm test:coverage` (enforces ≥90% branches on src/parser/**, src/model/**, src/helpers/**, src/serialize/**, src/builder/**) / ✅ / ✅ green (CI `Test (with coverage)` step added across Node 18/20/22).
    - `07-07` / `07 TEST-08 audit + TEST-07 confirmation` / Wave 3 / TEST-07, TEST-08 / existing `test/profiles-*.test.ts` (no new files — audit confirmed 8/8 TEST-08 cases mapped to existing Phase 6 tests with 0 gaps; TEST-07 confirmed closed by Phase 6 BIP-06) / audit (documentary) / `pnpm test -- profiles` / ✅ / ✅ green (824/824 + 14 todo — zero test-file deltas; see `TEST-08-AUDIT.md`).

    Extract per-REQ rows (8 total). All Status=COVERED.
    - TEST-01 (≥90% branch coverage on parser/model/helpers/serialize/builder) — 07-06 — `vitest.config.ts` per-dir thresholds + CI coverage step across Node 18/20/22 — COVERED (config-level; the gate IS the invariant)
    - TEST-02 (canonical-fixture round-trip for ADT^A01/A04/A08 + ORU^R01 + ORM^O01 + SIU^S12 + MDM^T02 + Z-segment + nested-subcomponent) — 07-02 — `test/canonical-messages.test.ts` + 7 fixtures — COVERED
    - TEST-03 (edge-case fixtures: repeating fields, nested subs, whitespace, empties, unusual delimiters) — 07-03 — `test/parser-edge-cases.test.ts` + 11 fixtures — COVERED
    - TEST-04 (malformed messages throw Hl7ParseError with position + snippet) — 07-05 — `test/parser-malformed-sweep.test.ts` + 4 fatal-code fixtures — COVERED
    - TEST-05 (vendor-quirk fixtures per Tier-2 scenario — one per WARNING_CODES entry) — 07-04 — `test/parser-strict-mode-sweep.test.ts` + 13 vendor-quirks fixtures — COVERED
    - TEST-06 (strict-mode escalation sweep across Tier-2 scenarios) — 07-04 — `test/parser-strict-mode-sweep.test.ts` describe-block strict branch — COVERED
    - TEST-07 (profile authoring test surface — defineProfile validation + extends + describe + attribution + round-trip) — 07-07 confirms closure by 06-06 — `test/profiles-define.test.ts` + `test/profiles-extends.test.ts` + `test/profiles-custom-segments.test.ts` + `test/profiles-builtins.test.ts` — COVERED (Phase 6 BIP-06 closure ratified here)
    - TEST-08 (audit: every WARNING_CODES entry has at least one test case) — 07-07 — `TEST-08-AUDIT.md` (0 gaps, all 8 cases map to existing tests) — COVERED (documentary; no new code)

    Test Infrastructure: Framework (Vitest 1.2.x), Config file (vitest.config.ts with per-dir branch thresholds tightened by 07-06), Quick run (`pnpm test -- <surface>`), Full suite (`pnpm test`), Coverage (`pnpm test:coverage` — the PRIMARY invariant), Estimated runtime (~14s full suite at Phase 7 close; 824/824 + 14 todo).

    Verify all referenced files/fixtures exist: test/canonical-messages.test.ts, test/parser-edge-cases.test.ts, test/parser-strict-mode-sweep.test.ts, test/parser-malformed-sweep.test.ts, vitest.config.ts, .github/workflows/ci.yml, .planning/phases/07-testing-hardening-and-fixtures/TEST-08-AUDIT.md. Also spot-check fixture directories exist: test/fixtures/canonical/, test/fixtures/edge-cases/, test/fixtures/vendor-quirks/, test/fixtures/malformed/.
  </action>
  <verify>
    <automated>for f in .planning/phases/07-testing-hardening-and-fixtures/07-0{1,2,3,4,5,6,7}-SUMMARY.md .planning/phases/07-testing-hardening-and-fixtures/07-VERIFICATION.md .planning/phases/07-testing-hardening-and-fixtures/TEST-08-AUDIT.md; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done && for t in test/canonical-messages.test.ts test/parser-edge-cases.test.ts test/parser-strict-mode-sweep.test.ts test/parser-malformed-sweep.test.ts vitest.config.ts .github/workflows/ci.yml; do test -f "$t" || { echo "MISSING: $t"; exit 1; }; done && for d in test/fixtures/canonical test/fixtures/edge-cases test/fixtures/vendor-quirks test/fixtures/malformed; do test -d "$d" || { echo "MISSING dir: $d"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - 7 per-plan rows drafted with all 9 columns populated.
    - 8 per-REQ rows drafted (TEST-01..08), all COVERED.
    - Coverage-gate callout facts captured (5 per-dir thresholds @ 90% branches + CI step + Node matrix).
    - Test Infrastructure block captured.
    - All 7 SUMMARYs + VERIFICATION + TEST-08-AUDIT + vitest.config.ts + ci.yml present.
    - All 4 fixture directories present.
  </acceptance_criteria>
  <done>
    Draft ready: 7 per-plan rows, 8 per-REQ rows, gate callout facts, infra block.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write .planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md</name>
  <read_first>
    - Task 1 journal drafts
    - .planning/phases/04-named-helpers/04-VALIDATION.md (section order)
    - .planning/phases/06-profile-system-and-built-ins/06-VALIDATION.md (large table reference)
  </read_first>
  <files>.planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md</files>
  <action>
    Write top-to-bottom. Exact section order:

    Frontmatter:
    ```yaml
    ---
    phase: 07
    slug: testing-hardening-and-fixtures
    status: approved
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-04-21
    reconstructed_from: artifacts (State B — Phase 12 retroactive Nyquist validation; no prior VALIDATION.md existed)
    thin_by_design: true
    ---
    ```

    Header:
    ```
    # Phase 7 — Validation Strategy

    > Retroactive Nyquist validation audit. **Phase 7 is a thin-by-design meta-phase** — its job is to harden the v1 test surface (coverage gate + canonical + edge-case + vendor-quirk + malformed + profile-authoring fixtures). Validation for such a phase is inherently recursive (tests-for-tests). We resolve this by treating the coverage-gate configuration itself as the runtime invariant: `vitest.config.ts` per-directory branch thresholds (≥90% on parser/model/helpers/serialize/builder) plus the CI "Test (with coverage)" step across Node 18/20/22 constitute TEST-01's enforcement. The remaining 7 TEST REQ-IDs map to fixture-sweep test files that the coverage gate also enforces.
    ```

    1. `## Test Infrastructure` — table: Framework (Vitest 1.2.x), Config file (`vitest.config.ts` — per-dir branches tightened 85→90 by Plan 07-06), Quick run (`pnpm test -- <surface>`), Full suite (`pnpm test`), Coverage (`pnpm test:coverage` — **the primary invariant for TEST-01**), CI (`.github/workflows/ci.yml` "Test (with coverage)" step across Node 18/20/22), Estimated runtime (~14s full; 824/824 + 14 todo at Phase 7 close).

    2. `## Coverage Gate Enforcement` (Phase-7-specific section, placed early) — paragraph + sub-table. Paragraph: "TEST-01 is enforced at config level, not test-case level. `vitest.config.ts` declares per-directory branch thresholds at 90% for 5 critical src subdirectories; any drop below the threshold fails `pnpm test:coverage`. The CI workflow runs `pnpm test:coverage` between `pnpm test` and `pnpm build` across the Node 18/20/22 matrix, so regressions are blocked at PR-time."

       Sub-table:
       | Directory | Branch Threshold | Enforcement |
       |-----------|------------------|-------------|
       | `src/parser/**` | ≥90% | `vitest.config.ts` + CI |
       | `src/model/**` | ≥90% | `vitest.config.ts` + CI |
       | `src/helpers/**` | ≥90% | `vitest.config.ts` + CI |
       | `src/serialize/**` | ≥90% | `vitest.config.ts` + CI |
       | `src/builder/**` | ≥90% | `vitest.config.ts` + CI |
       | (global) | ≥85% | kept lower to avoid implicitly gating ungated `src/profiles/**` |

    3. `## Sampling Rate`:
       - After every task commit: `pnpm test -- <surface>` scoped to fixtures/files under change
       - After every plan wave: `pnpm test` full suite
       - Before `/gsd-verify-work 7`: `pnpm test:coverage` + typecheck + lint + build
       - Max feedback latency: ~14s full suite; ~30s with coverage

    4. `## Per-Task Verification Map` — 7-row table from Task 1 (all 9 columns). Legend below.

    5. `## Requirement → Test Cross-Reference` — 8-row table (TEST-01..TEST-08). Columns: `REQ-ID | Source Plan | Primary Test File / Artifact | Evidence | Status`. Below: `**Gap summary: 0 MISSING · 0 PARTIAL · 8 COVERED.**`

    6. `## Thin-by-Design Callout` (prominent; required per Phase 12 ROADMAP SC-2) — paragraph: "Phase 7 is a meta-phase: its deliverables are fixtures and a coverage gate, not new library functionality. Authoring formal Nyquist validation for this phase recurses — you end up writing tests for the tests-for-tests. We resolve the recursion at two points: (1) TEST-01's coverage gate is config-level and self-validating (the gate either fails CI or it doesn't — no per-case assertion needed); (2) TEST-02..06 each ship a dedicated fixture-sweep test file that the coverage gate already guards. TEST-07 and TEST-08 are documentary closures — TEST-07 was ratified by Phase 6 BIP-06 tests (no new Phase 7 tests); TEST-08 was closed by an audit document (`TEST-08-AUDIT.md`) with zero test-file deltas. This thin validation surface is EXPECTED for a hardening/meta-phase and does not constitute a Nyquist gap."

    7. `## Fixture Inventory Note` — short paragraph + optional compact table: "Phase 7 contributed 35 fixtures across 4 categories: 7 canonical, 11 edge-case, 13 vendor-quirk, 4 malformed. Each category's corresponding test file sweeps every fixture programmatically; adding a new fixture adds a test case automatically via the sweep pattern. This is why TEST-02..06 map cleanly to single test files each."

    8. `## Wave 0 Requirements` — "*None.* Vitest + initial `vitest.config.ts` were installed in Phase 1. Phase 7's Plan 07-06 tightened the per-directory branch threshold from the initial 85% to 90% on 5 src subdirectories — this is a config evolution, not a Wave 0 dependency. `wave_0_complete: true` reflects that all necessary framework and gate infrastructure was in place (via Phase 1 + Plan 07-06) before any downstream phase consumer depended on it."

    9. `## Manual-Only Verifications` — "*None with gating authority.* The coverage gate runs programmatically; fixture sweeps are programmatic; the TEST-08 audit document is a deliverable with zero runtime dependency. A human spot-check of `TEST-08-AUDIT.md` confirms it lists 8/8 WARNING_CODES entries mapped to existing tests, but this is documentation review, not a gating manual step."

    10. `## Validation Sign-Off` — all `[x]`:
        - [x] All tasks have automated verify (fixture sweeps + coverage gate + audit doc)
        - [x] Sampling continuity: 6 of 7 plans ship dedicated test files (07-07 is the documentary exception, explicitly noted)
        - [x] Wave 0 covers all MISSING references (none — gate tightening is a config evolution, not a Wave 0 dependency)
        - [x] No watch-mode flags
        - [x] Feedback latency < 30s with coverage
        - [x] `nyquist_compliant: true` set
        - [x] Verifier PASS 2026-04-19 per `07-VERIFICATION.md`

        `**Approval:** approved 2026-04-21 (Nyquist audit — State B reconstruction from 7 SUMMARYs + 07-VERIFICATION.md + TEST-08-AUDIT.md).`

    11. `## Validation Audit 2026-04-21`:
        | Input state | B (7 SUMMARYs + 07-VERIFICATION.md + TEST-08-AUDIT.md) |
        | REQ-IDs audited | 8 (TEST-01..08) |
        | Plans mapped | 7 (07-01..07-07) |
        | Test files mapped | 4 dedicated sweep files (canonical-messages, parser-edge-cases, parser-strict-mode-sweep, parser-malformed-sweep) + 1 config file (vitest.config.ts) + 1 audit doc (TEST-08-AUDIT.md) |
        | Fixtures mapped | 35 (7 canonical + 11 edge-case + 13 vendor-quirk + 4 malformed) |
        | Gaps found | 0 |
        | Resolved | 0 |
        | Escalated | 0 |
        | Coverage gate | ≥90% branches on 5 src subdirectories (Plan 07-06); global ≥85% kept deliberately lower to avoid implicit gating of ungated src/profiles/** |
        | Thin-by-design | true (meta-phase; coverage gate IS the primary invariant) |

        Verdict paragraph: "Phase 7 is Nyquist-compliant as a thin-by-design meta-phase. TEST-01 closure is config-level (coverage gate); TEST-02..06 closure is fixture-sweep test files guarded by the gate; TEST-07+08 closure is documentary (Phase 6 BIP-06 attribution + audit document with zero code deltas). The 90%-branches coverage gate on 5 src subdirectories is the strongest runtime invariant in the v1 test suite and is enforced on every PR via CI across Node 18/20/22. Closes v2.1-MILESTONE-AUDIT tech-debt item 2 for Phase 7."

    Target length: 180-280 lines. No placeholder markers.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md && grep -q '^nyquist_compliant: true$' .planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md && grep -q '^thin_by_design: true$' .planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md && grep -q '## Thin-by-Design Callout' .planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md && test "$(grep -cE '^\| TEST-0[1-8] \|' .planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md)" -eq 8 && test "$(grep -cE '^\| 07-0[1-7] \|' .planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md)" -eq 7 && ! grep -qE '\b(TODO|TBD|XXX|FIXME)\b' .planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md`.
    - Frontmatter has `phase: 07`, `slug: testing-hardening-and-fixtures`, `status: approved`, `nyquist_compliant: true`, `wave_0_complete: true`, `thin_by_design: true`.
    - Contains a `## Thin-by-Design Callout` section explicitly.
    - Contains a `## Coverage Gate Enforcement` section with the 5-dir threshold sub-table.
    - Per-Task Verification Map has exactly 7 rows (07-01..07-07).
    - Requirement → Test Cross-Reference has exactly 8 rows (TEST-01..TEST-08), all COVERED.
    - Zero placeholder markers.
    - All 11 sections present in the required order.
  </acceptance_criteria>
  <done>
    File written with all 11 sections, 7 plan rows, 8 REQ rows, explicit thin-by-design + coverage-gate callouts, zero placeholders.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify format, path integrity, and commit</name>
  <read_first>
    - The file written in Task 2
    - CLAUDE.md
  </read_first>
  <files>(no new files; `git add` of Task-2 output)</files>
  <action>
    1. `pnpm format:check .planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md` — expect exit 0.
    2. Grep for placeholders — expect zero matches.
    3. Verify every `test/*.test.ts` path cited exists on disk; verify `vitest.config.ts` + `.github/workflows/ci.yml` + `TEST-08-AUDIT.md` exist.
    4. `git add .planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md`.
    5. Confirm single staged path.
    6. Commit via HEREDOC:
       ```
       git commit -m "$(cat <<'EOF'
       docs(phase-07): add retroactive VALIDATION.md — Nyquist-compliant, 8/8 TEST REQs covered (thin-by-design)

       Closes v2.1-MILESTONE-AUDIT tech-debt item 2 for Phase 7 (missing VALIDATION.md).
       State B reconstruction from 7 plan SUMMARYs + 07-VERIFICATION.md + TEST-08-AUDIT.md.
       All 8 TEST REQ-IDs classified COVERED: TEST-01 at config level (vitest.config.ts
       per-dir branches @ 90% on 5 src subdirs + CI coverage step across Node 18/20/22);
       TEST-02..06 via 4 dedicated sweep files (canonical-messages, parser-edge-cases,
       parser-strict-mode-sweep, parser-malformed-sweep) + 35 fixtures; TEST-07 ratified
       by Phase 6 BIP-06; TEST-08 by audit doc with 0 test-file deltas. Phase 7 is
       thin-by-design as a meta-phase — frontmatter flag `thin_by_design: true` set.

       Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
       EOF
       )"
       ```
    7. Confirm single commit.
  </action>
  <verify>
    <automated>pnpm format:check .planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md && ! grep -qE 'TODO|TBD|XXX|FIXME|placeholder' .planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md && test "$(git log --oneline -1 --format='%s' -- .planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md | grep -c 'docs(phase-07): add retroactive VALIDATION.md')" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm format:check` passes for the new file.
    - Zero placeholder markers.
    - Every referenced `test/*.test.ts` exists on disk; `vitest.config.ts` + `.github/workflows/ci.yml` + `TEST-08-AUDIT.md` all exist.
    - Exactly one commit touches `.planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md`.
    - Commit message begins with `docs(phase-07):` and contains `add retroactive VALIDATION.md`.
    - `git status` clean afterward.
  </acceptance_criteria>
  <done>
    File committed; working tree clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced. Writes a single Markdown file under `.planning/phases/07-testing-hardening-and-fixtures/`.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Tampering | `07-VALIDATION.md` content | mitigate | Transcribed from 7 SUMMARYs + 07-VERIFICATION.md + TEST-08-AUDIT.md (authoritative sources); git commit preserves audit trail. |
| T-12-04-02 | Information Disclosure | citations | accept | Public repo; no secrets. |
</threat_model>

<verification>
After Task 3:
1. File exists at `.planning/phases/07-testing-hardening-and-fixtures/07-VALIDATION.md`.
2. `grep -c '^nyquist_compliant: true$'` = 1.
3. `grep -c '^thin_by_design: true$'` = 1.
4. `grep -c '## Thin-by-Design Callout'` >= 1.
5. `grep -c '## Coverage Gate Enforcement'` >= 1.
6. `grep -cE '^\| TEST-0[1-8] \|'` = 8.
7. `grep -cE '^\| 07-0[1-7] \|'` = 7.
8. `git log` records a `docs(phase-07): add retroactive VALIDATION.md` commit.
</verification>

<success_criteria>
- All 3 tasks complete with acceptance_criteria satisfied.
- File exists with all 11 sections including Coverage Gate Enforcement + Thin-by-Design Callout + Fixture Inventory Note.
- One new commit with `docs(phase-07):` prefix touching only the new file.
- Zero format failures, zero placeholder markers, every test-path + config-path resolved.
- Wave 1 parallel-safe: zero overlap with 12-01, 12-02, 12-03, 12-05, 12-06.
</success_criteria>

<output>
After completion, execute-plan creates `.planning/phases/12-retroactive-nyquist-validation/12-04-SUMMARY.md` citing:
- 8 TEST REQ-IDs classified (all COVERED; TEST-01 at config level, TEST-02..06 via 4 sweep files + 35 fixtures, TEST-07 ratified by Phase 6 BIP-06, TEST-08 via audit doc with 0 test-file deltas).
- 7 plan rows transcribed (07-01..07-07).
- Coverage Gate Enforcement callout (5-dir 90%-branches table) + Thin-by-Design rationale (meta-phase recursion resolved at gate + fixture-sweep layers).
- New file path + commit hash.
- Plan-level REQ-IDs closed: 0 (paper-trail).
</output>
