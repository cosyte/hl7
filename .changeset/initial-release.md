---
"@cosyte/hl7": patch
---

Initial pre-alpha release of `@cosyte/hl7` — the complete v1 HL7 v2 toolkit (parser, structural
model, named helpers, serializer, message builder, and the profile system). See `CHANGELOG.md` for
the full surface.
